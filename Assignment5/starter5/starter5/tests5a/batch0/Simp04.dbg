 .data # Program at 1.0
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3264:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3284:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3316:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3320:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 1.6
  .byte 205 # 'M' with high bit set # ClassDecl at 1.6
  .byte 97 # 'a' # ClassDecl at 1.6
  .byte 105 # 'i' # ClassDecl at 1.6
  .byte 110 # 'n' # ClassDecl at 1.6
3368:CLASS_Main: # ClassDecl at 1.6
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 2.15
3416:END_CLASS_Main: # ClassDecl at 1.6
3416:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3424:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3436:END_CLASS_RunMain: # ClassDecl at 0.0
3436:dataArrayVTableStart: # IntType at 0.0
3436:END_CLASS_Object: # ClassDecl at 0.0
  .byte 72 # StringLit at 3.22
  .byte 101 # StringLit at 3.22
  .byte 108 # StringLit at 3.22
  .byte 108 # StringLit at 3.22
  .byte 111 # StringLit at 3.22
  .byte 46 # StringLit at 3.22
  .byte 10 # StringLit at 3.22
  .byte 0 # StringLit at 3.22
  .word CLASS_String # StringLit at 3.22
  .word 3 # StringLit at 3.22
  .word -7 # StringLit at 3.22
3456:strLit_0: # StringLit at 3.22
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp,$sp,4
28#  move $s2, $s7
32#  subu $sp, $sp, 4
36#  sw $s2, ($sp)
40#  jal mth_main_Main
44#  addu $sp, $sp, 4
48#  li $v0, 10
52#  syscall
.globl mth_main_Main
56:mth_main_Main:
56# subu $sp, $sp, 4
60# sw $ra, ($sp)
64# lw $s2, 4($sp)
# begin: CallStmt at 3.13; stackHeight = 0
# begin: Call at 3.13; stackHeight = 0
# begin: Super at 3.13; stackHeight = 0
68# subu $sp, $sp, 4
72# sw $s2, ($sp)
# end: Super at 3.13; stackHeight = 4
# begin: StringLit at 3.22; stackHeight = 4
76# la $t0, strLit_0
80# subu $sp, $sp, 4
84# sw $t0, ($sp)
# end: StringLit at 3.22; stackHeight = 8
88# jal mth_printStr_Lib
# end: Call at 3.13; stackHeight = 8
92# lw $t0, ($sp)
96# addu $sp, $sp, 4
# end: CallStmt at 3.13; stackHeight = 4
100# addu $sp, $sp, 4
104# lw $ra, ($sp)
108# addu $sp, $sp, 4
112# jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
116:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
116#    lw $t0,-8($s2) # get word with hash bits from this
120#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
124#    addu $t0,$t1

128#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
132:mth_equals_Object:
132#    move $t0,$s2 # first pointer (this)
136#    lw $t1,($sp) # second pointer
140#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
141##...subu $at,$t0,$t1
145##...sltiu $t0,$at,1

148#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
152:mth_hashCode_String:
152#    move $t0,$s2 # put this pointer in t0

156#    li $t2,0 # current hash value
160#    lw $t1,-4($t0) # negative of number of characters in string
164#    beq $t1,$zero,done_hashCode_String # if zero length => done
168#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
172#    and $t0,0xfffffffc # adjust for padding
173##...lui $at,65535
177##...ori $at,$at,0xfffc
181##...and $t0,$t0,$at
184#    subu $t1,$t0,$t1 # address at end of string +12
188:loop_hashCode_String:    
188#    sll $t4,$t2,5 # multiply by 32
192#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
196#    lbu $t3,-12($t0) # current character
200#    addu $t2,$t3 # add in current character
204#    addu $t0,1 # go to next character's address
208#    bne $t0,$t1,loop_hashCode_String # loop back if more
212:done_hashCode_String:    

    # push int on stack
212#    move $t0, $t2 # put $t2 in return slot
216#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
220:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
220#    subu $sp,4
224#    sw $ra,($sp)
    
    # get hash value
228#    lw $t2,-12($s2) # vtable address
232#    lw $t0,($t2) # method address
236#    jalr $t0
240#    subu $sp,8 # push result (int in $t0) on stack
244#    sw $s5,4($sp)
248#    sw $t0,($sp)

    # compute print-size of hash value
252#    li $t0, 1
256#    lw $t1, ($sp)
260#    srl $t1,$t1,4
264#    beq $t1,$zero,toString_skip_loop
268:toString_loop_top:
268#    addu $t0,1
272#    srl $t1,$t1,4
276#    bne $t1,$zero,toString_loop_top
280:toString_skip_loop:
280#    subu $sp,8
284#    sw $s5,4($sp)
288#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
292#    lw $t2,-12($s2) # vtable address
296#    move $t0,$t2
300:toString_checkTagBit:
300#    subu $t0,1
304#    lb $t1,($t0)
308#    and $t1,0x80
309##...lui $at,0
313##...ori $at,$at,0x80
317##...and $t1,$t1,$at
320#    beq $t1,$zero,toString_checkTagBit
324#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
328#    subu $sp,8
332#    sw $t1,($sp)
336#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
340#    move $s7,$zero # -4 offset header word
344#    lw $t4,8($sp) # bytes for hash value
348#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
352#    addu $t4,8
356#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
360#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

364#    li $t0,CLASS_String # store string vtable address into ...
368#    sw $t0,-12($s7) # ... -12 offset in object header
372#    lw $t2,-12($s2) # our object's vtable address
376#    lw $t5,4($sp) # number characters in prefix
380#    lw $t1,12($sp) # number of characters in hashcode
384#    addu $t3,$t1,$t5 # number of characters in string ...
388#    addu $t3,1 # ... including the '@' character
392#    subu $t0,$zero,$t3 # store negated length into ...
396#    sw $t0,-4($s7) # ... -4 offset header word
400#    lw $t0,-8($s7) # get -8 offset header-word
404#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
405##...slt $at,$t0,$zero
409##...beq $at,$zero,428
412#    and $t0,0xff # mask off object's hash value ...
413##...lui $at,0
417##...ori $at,$at,0xff
421##...and $t0,$t0,$at
424#    sw $t0,-8($s7) # ... and store back into -8 header-word
428:skipMask_toString:
428#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
432#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
436#    subu $t4,$t2,$t5
440:toString_copyLoop:    
440#    lb $t3,($t4)
444#    and $t3,0x7f # mask off sign bit (in case first word)
445##...lui $at,0
449##...ori $at,$at,0x7f
453##...and $t3,$t3,$at
456#    sb $t3,-8($t0)
460#    addu $t4,1
464#    addu $t0,1
468#    blt $t4,$t2,toString_copyLoop
469##...slt $at,$t4,$t2
473##...bne $at,$zero,440

    # copy the '@' sign
476#    li $t3,'@'
480#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
484#    addu $t2,$t0,$t1 # address of last byte in string to store +8
488#    lw $t3,20($sp) # hash code to print
492:toString_hashCopyLoop:
492#    and $t4,$t3,0xf
496#    srl $t3,4
500#    lb $t4,hexChars($t4)
504#    sb $t4,-8($t2)
508#    subu $t2,$t2,1
512#    bgt $t2,$t0,toString_hashCopyLoop
513##...slt $at,$t0,$t2
517##...bne $at,$zero,492

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
520#    addu $sp,28
524#    lw $ra,($sp)
528#    addu $sp,4
532#    move $t0, $s7 # return string we created
536#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
540:mth_equals_String:
    
    # quick test for obvious false
540#    lw $t0,($sp) # second parameter
544#    beq $t0,$zero,goEsFalse # go return false if null
548#    lw $t1,-12($t0) # vtable pointer
552#    la $t2,CLASS_String # string vtable pointer
556#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
560#    subu $sp,12
564#    sw $ra,8($sp)
568#    sw $s2,4($sp)
572#    sw $t0,($sp)
576#    jal mth_compareTo_String
580#    lw $s2, 4($sp) #restore this pointer and pop parameters
584#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
588#    seq $t0,$t0,$zero
589##...subu $at,$t0,$zero
593##...sltiu $t0,$at,1

    # pop return address and return $t0
596#    lw $ra,($sp)
600#    addu $sp,4

604#    jr $ra # return
    
608:goEsFalse:
    # put 0 in $t0 and return
608#    li $t0, 0
612#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
616:mth_toString_String:
    #we are a string, so return this.
616#    move $t0, $s2
620#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
624:mth_readLine_Lib:
    
624#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
628#    sw $ra, 4($sp) # save $ra
632#    move $t1,$sp # save "original" sp

636:  doRead:

    # read the character
636#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
640#    subu $t2,$v0,13
644#    bne $t2,$zero,notReturnRL
648#    jal readLogicalChar
652#    subu $t2,$v0,10 # check for newline
656#    beq $t2,$zero,foundNewLine
660#    sw $v0,lastCharRead # push back character into queue
664#    j foundNewLine # go process the line

668:  notReturnRL:

    # at this point, $v0 has our character

668#    subu $t0,$v0,10
672#    beq $t0,$zero,foundNewLine
676#    blt $v0,$zero,foundEof
677##...slt $at,$v0,$zero
681##...bne $at,$zero,696

    # we have a character, so push it onto stack
684#    subu $sp,$sp,4
688#    sw $v0,($sp)

    # loop back up to get next character
692#    j doRead

696:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
696#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
700#    li $s7, 0 # return-value: null
704#    j rlReturn # return

708:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
708#    subu $t0,$t1,$sp # number of chars we read (times 4)
712#    srl $s6,$t0,2 # number of words on stack with chars
716#    subu $sp,$sp,4 # push char-count ...
720#    sw $s6,($sp) # ... onto stack
724#    addu $t2,$t0,5 # GC tag, incl. for count-word
728#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
732#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
736#    srl $s6,$s6,2  # data words in object
740#    move $s7,$zero # object words in object
744#    jal newObject  # allocate space
    
    # store header words
748#    la $t0,CLASS_String
752#    sw $t0,-12($s7)# store class tag (String) into object
756#    lw $t2,4($sp) # char-count
760#    sll $t1,$t2,2 # 4 times number of chars
764#    subu $t2,$zero,$t2 # negative of char-count
768#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
772#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
776#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
777##...slt $at,$t0,$zero
781##...beq $at,$zero,800
784#    and $t0,0xff # mask off object's hash value ...
785##...lui $at,0
789##...ori $at,$at,0xff
793##...and $t0,$t0,$at
796#    sw $t0,-8($s7) # ... and store back into -8 header-word
800:  skipMask_readLine:

800#    sll $t0,$t0,2 # data bytes in object
804#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
808#    subu $t0,$t0,$t2 # place to store last character (plus 9)
812#    addu $sp,$sp,8 # pop redundant object-pointer and count
816#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

820#    beq $sp,$t1,rlReturn
824:  charCopyLoop:
824#    lw $t2,($sp)
828#    sb $t2,-9($t0)
832#    addu $sp,$sp,4
836#    subu $t0,$t0,1
840#    bne $sp,$t1,charCopyLoop
844:  rlReturn:

844#    lw $ra, 4($sp)
848#    addu $sp, 8   # pop $ra and pointless word off stack
852#    move $t0, $s7 # return string we allocated in $s7
856#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
860:mth_readInt_Lib:

    # save $ra by pushing onto stack
860#    subu $sp,$sp,4
864#    sw $ra,($sp)

868:  riSkipWhiteLoop:
    # read a character
868#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
872#    subu $t0,$v0,32
876#    bgt $t0,$zero,nonWhite
877##...slt $at,$zero,$t0
881##...bne $at,$zero,924
884#    beq $t0,$zero,riSkipWhiteLoop # start over if space
888#    subu $t0,$v0,10
892#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
896#    subu $t0,$v0,9
900#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
904#    subu $t0,$v0,13
908#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
912#    subu $t0,$v0,12
916#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
920#    j badIntegerFormat # illegal integer char: abort program

924:  nonWhite:
924#    subu $t0,$v0,'-'
928#    li $t4,1 # final multiplier
932#    bne $t0,$zero,helpReadInt # go read
936#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
940#    jal readLogicalChar

944:  helpReadInt:
944#    li $t2,10
948#    subu $t1,$v0,'0' # convert digit to 0-9 value
952#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
953##...sltu $at,$t1,$t2
957##...beq $at,$zero,3240
960#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

964:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

964#    jal readLogicalChar # read next character
968#    subu $t1,$v0,'0' # convert digit to 0-9 value
972#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
973##...sltu $at,$t1,$t2
977##...beq $at,$zero,996

980#    mul $t3,$t3,$t2 # multiply old value by 10
981##...mult $t3,$t2
985##...mflo $t3
988#    addu $t3,$t3,$t1 # add in value of new digit
992#    j digitLoop

996:  doneDigitLoop:

    # "push back" unused character into queue
996#    sw $v0,lastCharRead

    # restore return address
1000#    lw $ra,($sp)
1004#    addu $sp, 4

    # multiply by sign and put return value in $t0
1008#    mult $t3,$t4 # multiply to account for poss. minus sign
1012#    mflo $t3
1016#    move $t0, $t3
    
    # return
1020#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1024:mth_readChar_Lib:

    # save $ra by pushing onto stack
1024#    subu $sp,$sp,4
1028#    sw $ra,($sp)

      # read the character
1032#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1036#    lw $ra,($sp)
1040#    addu $sp, 4
1044#    move $t0, $v0

    # return
1048#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1052:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1052#    lw $t0,($sp)
1056#    bne $t0,$zero,psNotNull

    # print "(null)"
1060#    la $a0,nullStr
1064#    li $v0,4
1068#    syscall
1072#    j donePrintStr
    
1076:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1076#    subu $t0,$t0,8
1080#    lw $t1,($t0) # number of data words, but needs modification
1084#    blt $t1,$zero,skipPrintStr1
1085##...slt $at,$t1,$zero
1089##...bne $at,$zero,1108
1092#    and $t1,0xff # mask out all but low byte
1093##...lui $at,0
1097##...ori $at,$at,0xff
1101##...and $t1,$t1,$at
1104#    b skipPrintStr2
1108:skipPrintStr1:
1108#    and $t1,0x7ffffff
1109##...lui $at,2047
1113##...ori $at,$at,0xffff
1117##...and $t1,$t1,$at
1120:skipPrintStr2:    
1120#    sll $t1,$t1,2
1124#    subu $t1,$t0,$t1 # first word in string
1128#    lw $t0,4($t0) # negative of string-length
1132#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1136#    beq $t0,$t1,donePrintStr
1140:  psLoop:
1140#    lb $a0,($t1) # next byte
1144#    li $v0,11 # code for printing char
1148#        syscall # print the char
1152#    addu $t1,$t1,1 # go to next char
1156#    blt $t1,$t0,psLoop
1157##...slt $at,$t1,$t0
1161##...bne $at,$zero,1140

1164:  donePrintStr:
1164#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1168:mth_printInt_Lib:
1168#    lw $a0,($sp)

    # print it
1172#    li $v0,1 # code for print-int
1176#    syscall

    # return
1180#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1184:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1184#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1188#    la $a0,falseString
1192#    beq $t0,$zero,skipPB
1196#    la $a0,trueString
1200:  skipPB:
1200#    li $v0,4 # code for print-string
1204#    syscall

    # return
1208#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1212:mth_substring_String:

    # save $ra by pushing onto stack
1212#    subu $sp,$sp,4
1216#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1220#    move $t0,$s2
1224#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1228#    lw $t0,-4($t0) # negative of string length
1232#    subu $t0,$zero,$t0 # string length
1236#    lw $t1,4($sp) # k
1240#    lw $t2,12($sp) # n
1244#    bgt $zero,$t2,strIndexOutOfBounds
1245##...slt $at,$t2,$zero
1249##...bne $at,$zero,3208
1252#    bgt $t2,$t1,strIndexOutOfBounds
1253##...slt $at,$t1,$t2
1257##...bne $at,$zero,3208
1260#    bgt $t1,$t0,strIndexOutOfBounds
1261##...slt $at,$t0,$t1
1265##...bne $at,$zero,3208

    # allocate memory
1268#    subu $s6,$t1,$t2 # # chars in target-string
1272#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1276#    srl $s6,$s6,2 # convert bytes-count to word-count
1280#    move $s7,$zero # (no object-bytes in string)
1284#    jal newObject

    # store "String" tag in object-type field
1288#    la $t0,CLASS_String
1292#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1296#    lw $t4,-8($s7) # data words in source string
1300#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1301##...slt $at,$t4,$zero
1305##...beq $at,$zero,1324
1308#    and $t4,0xff # mask off object's hash value ...
1309##...lui $at,0
1313##...ori $at,$at,0xff
1317##...and $t4,$t4,$at
1320#    sw $t4,-8($s7) # ... and store back into -8 header-word
1324:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1324#    lw $t1,8($sp) # k
1328#    lw $t2,16($sp) # n
1332#    subu $t0,$t2,$t1 # value to store
1336#    sw $t0,-4($s7) # store value
    
1340#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1344#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1348#    lw $t4,-8($t3) # data words in source string
1352#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1356#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1360#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1364#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1368#    lw $t2,-8($s7)   # data words in target string
1372#    sll $t2,$t2,2    # data bytes in target string
1376#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1380:  subCopyLoopzz:
1380#    lb $t4,-8($t3)
1384#    sb $t4,-8($t2)
1388#    addu $t2,$t2,1
1392#    addu $t3,$t3,1
1396#    blt $t3,$t1,subCopyLoopzz
1397##...slt $at,$t3,$t1
1401##...bne $at,$zero,1380
    
1404:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1404#    lw $ra,4($sp) # restore $ra
1408#    addu $sp,$sp,8 # pop ra, and allocated newObject
1412#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1416#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1420:mth_length_String:

    # the length is the negative of 4($s2)
1420#    lw $t0,-4($s2) # -length
1424#    subu $t0,$zero,$t0

    # return
1428#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1432:mth_concat_String:
    # save $ra by pushing onto stack
1432#    subu $sp,$sp,4
1436#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1440#    lw $t0,4($sp)
1444#    beq $t0,$zero,nullPtrException
1448#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1452#    lw $t0,-4($t0) # negative size of second object
1456#    lw $t1,-4($t1) # negative size of first object
1460#    addu $s6,$t0,$t1 # sum of negative sizes
1464#    sra $s6,$s6,2 # negative word-size of char part
1468#    subu $s6,$zero,$s6 # word size of char part
1472#    addu $s6,$s6,1 # data word size, including v-table word
1476#    move $s7,$zero
1480#    jal newObject

    # store String vtable address in vtable address field
1484#    la $t0,CLASS_String
1488#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1492#    lw $t4,-8($s7) # # data words in source string
1496#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1497##...slt $at,$t4,$zero
1501##...beq $at,$zero,1520
1504#    and $t4,0xff # mask off object's hash value ...
1505##...lui $at,0
1509##...ori $at,$at,0xff
1513##...and $t4,$t4,$at
1516#    sw $t4,-8($s7) # ... and store back into -8 header-word
1520:skipMask_concat:

1520#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1524#    move $t1,$s2   # first object
1528#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1532#    lw $t2,-4($t0) # negative length of second object
1536#    lw $t3,-4($t1) # negative length of first object
1540#    addu $t4,$t2,$t3 # sum of negative lengths
1544#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1548#    lw $t4,-8($t1) # # data words in first string
1552#    sll $t4,$t4,2 # # data bytes in first string
1556#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1560#    lw $t4,-8($s7) # # data words in new string
1564#    sll $t4,$t4,2 # # data bytes in new string
1568#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1572#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1576#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1580:  concatLoop1zz:
1580#    lb $t4,-8($t1)
1584#    sb $t4,-8($s7)
1588#    addu $s7,$s7,1
1592#    addu $t1,$t1,1
1596#    blt $t1,$t3,concatLoop1zz
1597##...slt $at,$t1,$t3
1601##...bne $at,$zero,1580
1604:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1604#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1608#    lw $t4,-8($t0) # # data words in second string
1612#    sll $t4,$t4,2 # # data bytes in second string
1616#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1620#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1624:  concatLoop2zz:
1624#    lb $t4,-8($t1)
1628#    sb $t4,-8($s7)
1632#    addu $s7,$s7,1
1636#    addu $t1,$t1,1
1640#    blt $t1,$t3,concatLoop2zz
1641##...slt $at,$t1,$t3
1645##...bne $at,$zero,1624
1648:  doneConcatLoop2zz:
 
1648:  concatRtnzz:
    # return
1648#    move $t0, $t9 # put saved return string in $t0
1652#    lw $ra, 4($sp)
1656#    addu $sp, 8 # pop ra and allocated newObject
1660#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1664:mth_charAt_String:
    # get string
1664#    move $t0,$s2

    # check that index is in bounds
1668#    lw $t1,-4($t0) # negative of # data words in string
1672#    subu $t3,$zero,$t1 # # chars in string
1676#    lw $t2,($sp) # index
1680#    bgeu $t2,$t3,strIndexOutOfBounds
1681##...sltu $at,$t2,$t3
1685##...beq $at,$zero,3208

    # access element
1688#    lw $t1,-8($t0) # # data words in object
1692#    sll $t1,$t1,2 # - byte-offset from end of chars
1696#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1700#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1704#    lb $t0,-8($t0) # our char

    # return
1708#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1712:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1712#    subu $sp,$sp,8
1716#    sw $ra,4($sp)

    # save current sp
1720#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1724#    li $t3,10

    # get argument, negate if negative
1728#    lw $t1,8($sp)
1732#    bge $t1,$zero,itsNonNegLoop
1733##...slt $at,$t1,$zero
1737##...beq $at,$zero,1744
1740#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1744:  itsNonNegLoop:
1744#    divu $t1,$t3
1748#    mflo $t1 # quotient
1752#    mfhi $t4 # remainder
1756#    addu $t4,$t4,'0' # turn remainder into digit
1760#    subu $sp,$sp,4
1764#    sw $t4,($sp) # push digit onto stack
1768#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1772#    lw $t4,8($t0)
1776#    bge $t4,$zero,itsSkipNeg
1777##...slt $at,$t4,$zero
1781##...beq $at,$zero,1796
1784#    li $t4,'-'
1788#    subu $sp,$sp,4
1792#    sw $t4,($sp)
1796:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
1796#    subu $s6,$t0,$sp
1800#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
1804#    sw $t3,($t0)
1808#    srl $s6,$s6,2
1812#    subu $sp,$sp,4
1816#    sw $s6,($sp)
    
    # allocate memory
1820#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1824#    srl $s6,$s6,2
1828#    move $s7,$zero # no "object" words in object
1832#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
1836#    lw $t0,4($sp)
1840#    addu $sp,$sp,8

    # store "String" tag into class field
1844#    subu $s7,$s7,8 # address of header-1 word
1848#    la $t1,CLASS_String
1852#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
1856#    lw $t4,($s7) # # data words in source string
1860#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
1861##...slt $at,$t4,$zero
1865##...beq $at,$zero,1884
1868#    and $t4,0xff # mask off object's hash value ...
1869##...lui $at,0
1873##...ori $at,$at,0xff
1877##...and $t4,$t4,$at
1880#    sw $t4,($s7) # ... and store back into -8 header-word
1884:skipMask_intToString:
    
    # store negative of char-count into header-2 word
1884#    subu $t0,$zero,$t0
1888#    sw $t0,4($s7)

1892#    lw $t1,($s7) # number of data words in string
1896#    sll $t1,$t1,2 # number data bytes in string
1900#    subu $t1,$s7,$t1 # first location to store chars in string
1904#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
1908:  itsLoop:
1908#    lw $t2,($sp)
1912#    addu $sp,$sp,4
1916#    sb $t2,($t1)
1920#    addu $t1,$t1,1
1924#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
1928#    lw $ra,4($sp)
1932#    addu $sp,$sp,8
1936#    addu $t0,$t0,15
1940#    and $t0,$t0,0xfffffffc
1941##...lui $at,65535
1945##...ori $at,$at,0xfffc
1949##...and $t0,$t0,$at
1952#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
1956:mth_intToChar_Lib:
    # save return address
1956#    subu $sp,$sp,4
1960#    sw $ra,($sp)

    # allocate object
1964#    li $s6,2
1968#    move $s7,$zero
1972#    jal newObject

    # get n from string.
1976#    lw $t1,8($sp)

    # store "String" tag into class field
1980#    la $t0,CLASS_String
1984#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1988#    lw $t4,-8($s7) # # data words in source string
1992#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
1993##...slt $at,$t4,$zero
1997##...beq $at,$zero,2016
2000#    and $t4,0xff # mask off object's hash value ...
2001##...lui $at,0
2005##...ori $at,$at,0xff
2009##...and $t4,$t4,$at
2012#    sw $t4,-8($s7) # ... and store back into -8 header-word
2016:  skipMask_intToChar:

    # store data in string
2016#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2020#    li $t0,-1
2024#    sw $t0,-4($s7)

    # we're returning $s7
2028#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2032#    lw $ra,4($sp)
2036#    addu $sp,$sp,8

    # return
2040#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2044:mth_compareTo_String:
    # get string pointers and check parameter for null
2044#    lw $t0,($sp) # second string
2048#    beq $t0,$zero,nullPtrException
2052#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2056#    lw $t2,-4($t0) # negative length of second string
2060#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2064#    slt $t4,$t3,$t2
2068#    slt $t5,$t2,$t3
2072#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2076#    lw $t2,-8($t0) # # data words in second string
2080#    lw $t3,-8($t1) # # data words in first string
2084#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2088#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2092#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2096#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2100#    beq $zero,$t5,skipStrCmpLenzz
2104#    move $t2,$t3
2108:  skipStrCmpLenzz:
2108#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2112:  cmpLoopzz:
2112#    lw $t3,-8($t1) # word from str2
2116#    lw $t4,-8($t0) # word from str1
2120#    bne $t3,$t4,cmpNotEqualzz
2124#    addu $t1,$t1,4
2128#    addu $t0,$t0,4
2132#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2136#    j cmpHitLimitzz

2140:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2140#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2144#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2148#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2152:  cmpHitLimitzz:
2152#    move $t0, $t5 # store return value
2156#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2160:readLogicalChar:

    # check if we already have a character
2160#    lw $v0,lastCharRead
2164#    li $t0,-2
2168#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2172#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2176#    jr $ra # return
    
2180:  doReadCh:
    # we need to actually read a character: read and return
2180#    li $v0,12 # use system call to read a character
2184#    syscall
2188#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2192:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2192#    blt $s7,$zero arraySizeOutOfBounds
2193##...slt $at,$s7,$zero
2197##...bne $at,$zero,3224

    # mark the fact that we have not yet GC'd for this allocation
2200#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2204:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2204#    addu $t0,$s6,$s7
2208#    sll $t0,$t0,2
2212#    addu $t0,$t0,8 # amount of memory needed
2216#    addu $t1,$t0,$s3
2220#    bgtu $t1,$s4,doGC
2221##...sltu $at,$s4,$t1
2225##...bne $at,$zero,2348

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2228#    move $t2,$s3
2232:  zeroObjLoop:
2232#    sw $zero,($s3)
2236#    lw $zero,($s3)

2240#    addu $s3,4
2244#    bltu $s3,$t1,zeroObjLoop
2245##...sltu $at,$s3,$t1
2249##...bne $at,$zero,2232

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2252#    sll $t0,$s7,2    # number of post-header bytes
2256#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2260#    li $t2,255
2264#    bgeu $s6,$t2 firstHeaderLarge
2265##...sltu $at,$s6,$t2
2269##...beq $at,$zero,2316
2272#    lw $t1,prevHashConstant
2276#    addu $t1,0x12b68fd2
2277##...lui $at,4791
2281##...addiu $at,$at,-28718
2285##...addu $t1,$at,$t1
2288#    sw $t1,prevHashConstant # update hash constant
2292#    or $t1,0x80000000 # set top bit
2293##...lui $at,32768
2297##...or $t1,$at,$t1
2300#    and $t1,0xffffff00 # clear low bit
2301##...lui $at,65535
2305##...ori $at,$at,0xff00
2309##...and $t1,$t1,$at
2312#    or $s6,$t1
2316:firstHeaderLarge:
2316#    sw $s6,-8($t0) # store header-1 value
2320#    lw $zero,-8($t0)

    # store second header-value
2324#    sw $s7,-4($t0)   # second header-word
2328#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2332#    move $s7,$t0
2336#    subu $sp,$sp,4
2340#    sw $t0,($sp)

2344#    jr $ra
    
2348:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2348#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2352#    lw $t5,gcCount
2356#    addu $t5,1
2360#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2364#    subu $sp,$sp,4
2368#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2372#    lw $t0,cur_seg
2376#    move $t7,$s4
2380#    la $t6,seg1_start
2381##...lui $t6,16
2385##...addiu $t6,$t6,3828
2388#    la $s3,seg0_start
2392#    la $s4,seg0_end
2393##...lui $s4,16
2397##...addiu $s4,$s4,3828
2400#    sw $zero,cur_seg
2404#    bne $t0,$zero,skipGc1
2408#    la $t6,seg0_start
2412#    la $s3,seg1_start
2413##...lui $s3,16
2417##...addiu $s3,$s3,3828
2420#    la $s4,seg1_end
2421##...lui $s4,32
2425##...addiu $s4,$s4,3828
2428#    sw $s5,cur_seg
2432:  skipGc1:
2432#    li $t5,0x80000000
2436#    lw $t0,stack_bottom
2440#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2444:  gcStackCopyLoop:
2444#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2448#    sll $t2,$t1,31 # zero iff low bit was zero
2452#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2456#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2460#    bleu $t1,$t6,gcTestIterDone1
2461##...sltu $at,$t6,$t1
2465##...beq $at,$zero,2628
2468#    bgtu $t1,$t7,gcTestIterDone1
2469##...sltu $at,$t7,$t1
2473##...bne $at,$zero,2628

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2476#    lw $t2,-8($t1) # possible forwarding tag
2480#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2484#    lw $t2,-4($t1) # forwarding pointer: object's new address
2488#    sw $t2,4($t0) # update stack value
2492#    j gcTestIterDone1 # do next iteration

2496:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2496#    bge $t2,$zero,gcSkip2
2497##...slt $at,$t2,$zero
2501##...beq $at,$zero,2516
2504#    and $t2,0xff #### mask off hash value
2505##...lui $at,0
2509##...ori $at,$at,0xff
2513##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2516:  gcSkip2:
2516#    sll $t2,$t2,2
2520#    addu $t2,$t2,8
2524#    subu $t4,$t1,$t2 # address of first word of source
2528#    lw $t3,-4($t1) # number of object words (negative means zero)
2532#    bge $t3,$zero,gcH2Neg1
2533##...slt $at,$t3,$zero
2537##...beq $at,$zero,2544
2540#    move $t3,$zero
2544:  gcH2Neg1:
2544#    sll $t3,$t3,2
2548#    addu $t3,$t3,$t1 # address one word past last word of source

2552#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2556#    lw $s0,($t4)
2560#    lw $s1,-8($t1)
2564#    sw $s1,($t4)
2568#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2572:  gCinnerCopy1:
2572#    lw $s0,($t4)
2576#    sw $s0,($s3)
2580#    addu $t4,$t4,4
2584#    addu $s3,$s3,4
2588#    bltu $t4,$t3,gCinnerCopy1
2589##...sltu $at,$t4,$t3
2593##...bne $at,$zero,2572

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2596#    sw $zero,-8($t1) # 0 in first header-slot
2600#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2604#    sw $t2,4($t0)
2608#    lw $zero,-8($t2)
2612#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2616#    j gcTestIterDone1

2620:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2620#    subu $t0,$t0,$t1
2624#    subu $t0,$t0,3

2628:  gcTestIterDone1:
2628#    bgeu $t0,$sp,gcStackCopyLoop
2629##...sltu $at,$t0,$sp
2633##...beq $at,$zero,2444

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2636#    lw $t1,cur_seg
2640#    la $t0,seg0_start
2644#    beq $t1,$zero,gcSkip4
2648#    la $t0,seg1_start
2649##...lui $t0,16
2653##...addiu $t0,$t0,3828


2656:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2656#    bgeu $t0,$s3,gcFinishUp
2657##...sltu $at,$t0,$s3
2661##...beq $at,$zero,2952

    ###### begin outer loop to copy all non-stack-direct objects ######

2664:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2664#    lw $t1,($t0) # (what will be) first header word for current object
2668#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2672#    bge $t2,$zero,gcSkip4a # are there hash bits?
2673##...slt $at,$t2,$zero
2677##...beq $at,$zero,2692
2680#    and $t2,0xff # mask off hash bits
2681##...lui $at,0
2685##...ori $at,$at,0xff
2689##...and $t2,$t2,$at
2692:gcSkip4a:    
2692#    sll $t2,$t2,2 # # data bytes
2696#    addu $t2,$t2,8 # to skip header words
2700#    addu $t2,$t0,$t2 # pointer to new object
2704#    lw $t3,-8($t2) # word to swap
2708#    sw $t1,-8($t2) # store header word
2712#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2716#    lw $t1,-12($t2) # vtable pointer
2720#    li $t0,dataArrayVTableStart # address of vtable for first data array
2724#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2725##...slt $at,$t1,$t0
2729##...bne $at,$zero,2748

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2732:  skipToNextObj:
2732#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2736#    sll $t1,$t1,2 # convert to byte-count
2740#    addu $t0,$t2,$t1 # skip over object
2744#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2748:  gcNotDataArray:
2748#    lw $t3,-4($t2) # # object words (negative treated as zero)
2752#    bge $t3,$zero,gcH2Neg2
2753##...slt $at,$t3,$zero
2757##...beq $at,$zero,2764
2760#    move $t3,$zero
2764:  gcH2Neg2:
2764#    sll $t3,$t3,2 # # object bytes
2768#    move $t0,$t2 # address of first pointer in object
2772#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2776#    beq $t0,$s1,gcTestIterDone2
    
2780:  gcNextPointerInObject:
    # get pointer from object
2780#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2784#    bleu $t1,$t6,gcGoNextPointerSlot
2785##...sltu $at,$t6,$t1
2789##...beq $at,$zero,2932
2792#    bgtu $t1,$t7,gcGoNextPointerSlot
2793##...sltu $at,$t7,$t1
2797##...bne $at,$zero,2932

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2800#    lw $t8,-8($t1) # possible forwarding tag
2804#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
2808#    lw $t8,-4($t1) # forwarding pointer: object's new address
2812#    sw $t8,($t0) # update pointer in object
2816#    j gcGoNextPointerSlot # do next iteration

2820:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
2820#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
2821##...slt $at,$t8,$zero
2825##...beq $at,$zero,2840
2828#    and $t8,0xff # mask off hash bits
2829##...lui $at,0
2833##...ori $at,$at,0xff
2837##...and $t8,$t8,$at

2840:  gcSkip5:
2840#    sll $t8,$t8,2 # bytes in negative-offset portion ...
2844#    addu $t8,$t8,8 # ... add in bytes for header-words
2848#    subu $t4,$t1,$t8 # address of first word of source object
2852#    lw $t3,-4($t1) # # words in positive-offset portion
2856#    bge $t3,$zero,gcNoNeg
2857##...slt $at,$t3,$zero
2861##...beq $at,$zero,2868
2864#    move $t3,$zero # negative treated as zero
2868:  gcNoNeg:
2868#    sll $t3,$t3,2 # bytes in negative-offset portion
2872#    addu $t3,$t3,$t1 # address one word past last word of source

2876#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
2880#    lw $s0,($t4)
2884#    lw $t9,-8($t1)
2888#    sw $t9,($t4)
2892#    sw $s0,-8($t1)

    # copy all source bytes to the target
2896:  gCinnerCopy2:
2896#    lw $s0,($t4)
2900#    sw $s0,($s3)
2904#    addu $t4,$t4,4
2908#    addu $s3,$s3,4
2912#    bltu $t4,$t3,gCinnerCopy2
2913##...sltu $at,$t4,$t3
2917##...bne $at,$zero,2896

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2920#    sw $zero,-8($t1) # 0 in first header-slot
2924#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
2928#    sw $t8,($t0)

2932:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
2932#    addu $t0,$t0,4
2936#    bltu $t0,$s1,gcNextPointerInObject
2937##...sltu $at,$t0,$s1
2941##...bne $at,$zero,2780

2944:  gcTestIterDone2:
2944#    bltu $t0,$s3,gcHeapCopyLoop
2945##...sltu $at,$t0,$s3
2949##...bne $at,$zero,2664

    ###### end outer loop to copy all non-stack-direct objects ######

2952:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
2952#    lw $s2,($sp)
2956#    addu $sp,$sp,4

    # mark us as having already GC'd
2960#    move $t5,$s5

    # go try and allocate again
2964#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
2968:vm_init:

    # mark bottom of stack
2968#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
2972#    move $s2,$zero

    #set up the "five" register
2976#    li $s5,5

2980#    la $s3,seg0_start
2984#    la $s4,seg0_end
2985##...lui $s4,16
2989##...addiu $s4,$s4,3828
    
    # return
2992#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2996:divide:
    # get parameters; abort if divisor zero
2996#    lw $t0,($sp)
3000#    lw $t1,8($sp)
3004#    beq $t0,$zero,divByZeroError
    
    # perform division
3008#    div $t1,$t0
3012#    mflo $t0

    # store result, adjust stack and return
3016#    addu $sp,$sp,8 # adjust stack
3020#    sw $t0,($sp) # store result
3024#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3028:remainder:
    # get parameters; abort if divisor zero
3028#    lw $t0,($sp)
3032#    lw $t1,8($sp)
3036#    beq $t0,$zero,divByZeroError
    
    # perform division
3040#    div $t1,$t0
3044#    mfhi $t0

    # store result, adjust stack and return
3048#    addu $sp,$sp,8 # adjust stack
3052#    sw $t0,($sp) # store result
3056#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3060:checkCast:
    # get object, allow immediately if null
3060#    lw $t2,($sp)
3064#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3068#    lw $t2,-12($t2) # vtable address
3072#    bge $t2,$t1,castException
3073##...slt $at,$t2,$t1
3077##...beq $at,$zero,3248
3080#    blt $t2,$t0,castException
3081##...slt $at,$t2,$t0
3085##...bne $at,$zero,3248
    
    # return, leaving object unchanged on stack
3088:  checkCastReturn:    
3088#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3092:instanceOf:
    # get object, go return false if null
3092#    lw $t2,($sp)
3096#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3100#    lw $t2,-12($t2) # vtable address
3104#    sge $t0,$t2,$t0 # are we >= the lower limit?
3105##...slt $t0,$t2,$t0
3109##...xori $t0,$t0,0x1
3112#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3116#    and $t2,$t0,$t1
3120#    sw $t2,($sp)
3124:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3124#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3128:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3128#    li $v0,4 # syscall-code for print-string
3132#    syscall
3136:exit:
    # print termination message
3136#    li $v0,4 # syscall-code for print-string
3140#    la $a0,exitString
3144#    syscall

    # print number of garbage collections
3148#    li $v0,4 # syscall-code for print-string
3152#    la $a0,gcCountMsg
3156#    syscall # print "Number of ..."
3160#    lw $a0,gcCount # number of GC's
3164#    li $v0,1 # code for print-int
3168#    syscall
3172#    li $v0,4 # syscall-code for print-string
3176#    la $a0,newlineString
3180#    syscall # print newline

    # terminate execution
3184#    li $v0,10 #syscall-code for "exit"
3188#    syscall

3192:outOfHeapMemory:
3192#    la $a0,heapMemoryMsg
3196#    j exitError
3200:divByZeroError:
3200#    la $a0,divByZeroMsg
3204#    j exitError
3208:strIndexOutOfBounds:
3208#    la $a0,strIndexOutOfBoundsMsg
3212#    j exitError
3216:arrayIndexOutOfBounds:
3216#    la $a0,arrayIndexOutOfBoundsMsg
3220#    j exitError
3224:arraySizeOutOfBounds:
3224#    la $a0,arraySizeOutOfBoundsMsg
3228#    j exitError
3232:nullPtrException:
3232#    la $a0,nullPtrExceptionMsg
3236#    j exitError
3240:badIntegerFormat:
3240#    la $a0,badIntegerFormatMsg
3244#    j exitError
3248:castException:
3248#    la $a0,castExceptionMsg
3252#    j exitError
############## data section ################
    .data
3456:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3472:cur_seg:
    .word 0

3476:lastCharRead:
    .word -2 # -2 => no buffered character
3480:prevHashConstant:
    .word 0x2785173c
3484:gcCount:
    .word 0

3488:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3515:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3538:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3573:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3607:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3640:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3671:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3719:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3740:gcMsg: 
      .asciiz "\nGC!\n"
3746:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3778:nullStr:
    .asciiz "null"
3783:trueString:
    .asciiz "true"
3788:falseString:
    .asciiz "false"
3794:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
3822:newlineString:
    .asciiz "\n"
    
    .align 2
3824:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

3828:seg0_start:
    .space 0x100000
1052404:seg0_end:
1052404:seg1_start:
    .space 0x100000
2100980:seg1_end:
