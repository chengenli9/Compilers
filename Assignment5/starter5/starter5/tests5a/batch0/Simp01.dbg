 .data # Program at 1.0
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3232:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3252:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3284:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3288:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
3332:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3340:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3352:END_CLASS_RunMain: # ClassDecl at 0.0
# ****** class Main ****** # ClassDecl at 1.6
  .byte 205 # 'M' with high bit set # ClassDecl at 1.6
  .byte 97 # 'a' # ClassDecl at 1.6
  .byte 105 # 'i' # ClassDecl at 1.6
  .byte 110 # 'n' # ClassDecl at 1.6
3356:CLASS_Main: # ClassDecl at 1.6
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 2.15
3372:END_CLASS_Main: # ClassDecl at 1.6
3372:dataArrayVTableStart: # IntType at 0.0
3372:END_CLASS_Object: # ClassDecl at 0.0
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp,$sp,4
28#  move $s2, $s7
32#  subu $sp, $sp, 4
36#  sw $s2, ($sp)
40#  jal mth_main_Main
44#  addu $sp, $sp, 4
48#  li $v0, 10
52#  syscall
.globl mth_main_Main
56:mth_main_Main:
56# subu $sp, $sp, 4
60# sw $ra, ($sp)
64# lw $s2, 4($sp)
68# addu $sp, $sp, 0
72# lw $ra, ($sp)
76# addu $sp, $sp, 4
80# jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
84:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
84#    lw $t0,-8($s2) # get word with hash bits from this
88#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
92#    addu $t0,$t1

96#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
100:mth_equals_Object:
100#    move $t0,$s2 # first pointer (this)
104#    lw $t1,($sp) # second pointer
108#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
109##...subu $at,$t0,$t1
113##...sltiu $t0,$at,1

116#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
120:mth_hashCode_String:
120#    move $t0,$s2 # put this pointer in t0

124#    li $t2,0 # current hash value
128#    lw $t1,-4($t0) # negative of number of characters in string
132#    beq $t1,$zero,done_hashCode_String # if zero length => done
136#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
140#    and $t0,0xfffffffc # adjust for padding
141##...lui $at,65535
145##...ori $at,$at,0xfffc
149##...and $t0,$t0,$at
152#    subu $t1,$t0,$t1 # address at end of string +12
156:loop_hashCode_String:    
156#    sll $t4,$t2,5 # multiply by 32
160#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
164#    lbu $t3,-12($t0) # current character
168#    addu $t2,$t3 # add in current character
172#    addu $t0,1 # go to next character's address
176#    bne $t0,$t1,loop_hashCode_String # loop back if more
180:done_hashCode_String:    

    # push int on stack
180#    move $t0, $t2 # put $t2 in return slot
184#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
188:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
188#    subu $sp,4
192#    sw $ra,($sp)
    
    # get hash value
196#    lw $t2,-12($s2) # vtable address
200#    lw $t0,($t2) # method address
204#    jalr $t0
208#    subu $sp,8 # push result (int in $t0) on stack
212#    sw $s5,4($sp)
216#    sw $t0,($sp)

    # compute print-size of hash value
220#    li $t0, 1
224#    lw $t1, ($sp)
228#    srl $t1,$t1,4
232#    beq $t1,$zero,toString_skip_loop
236:toString_loop_top:
236#    addu $t0,1
240#    srl $t1,$t1,4
244#    bne $t1,$zero,toString_loop_top
248:toString_skip_loop:
248#    subu $sp,8
252#    sw $s5,4($sp)
256#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
260#    lw $t2,-12($s2) # vtable address
264#    move $t0,$t2
268:toString_checkTagBit:
268#    subu $t0,1
272#    lb $t1,($t0)
276#    and $t1,0x80
277##...lui $at,0
281##...ori $at,$at,0x80
285##...and $t1,$t1,$at
288#    beq $t1,$zero,toString_checkTagBit
292#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
296#    subu $sp,8
300#    sw $t1,($sp)
304#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
308#    move $s7,$zero # -4 offset header word
312#    lw $t4,8($sp) # bytes for hash value
316#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
320#    addu $t4,8
324#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
328#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

332#    li $t0,CLASS_String # store string vtable address into ...
336#    sw $t0,-12($s7) # ... -12 offset in object header
340#    lw $t2,-12($s2) # our object's vtable address
344#    lw $t5,4($sp) # number characters in prefix
348#    lw $t1,12($sp) # number of characters in hashcode
352#    addu $t3,$t1,$t5 # number of characters in string ...
356#    addu $t3,1 # ... including the '@' character
360#    subu $t0,$zero,$t3 # store negated length into ...
364#    sw $t0,-4($s7) # ... -4 offset header word
368#    lw $t0,-8($s7) # get -8 offset header-word
372#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
373##...slt $at,$t0,$zero
377##...beq $at,$zero,396
380#    and $t0,0xff # mask off object's hash value ...
381##...lui $at,0
385##...ori $at,$at,0xff
389##...and $t0,$t0,$at
392#    sw $t0,-8($s7) # ... and store back into -8 header-word
396:skipMask_toString:
396#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
400#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
404#    subu $t4,$t2,$t5
408:toString_copyLoop:    
408#    lb $t3,($t4)
412#    and $t3,0x7f # mask off sign bit (in case first word)
413##...lui $at,0
417##...ori $at,$at,0x7f
421##...and $t3,$t3,$at
424#    sb $t3,-8($t0)
428#    addu $t4,1
432#    addu $t0,1
436#    blt $t4,$t2,toString_copyLoop
437##...slt $at,$t4,$t2
441##...bne $at,$zero,408

    # copy the '@' sign
444#    li $t3,'@'
448#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
452#    addu $t2,$t0,$t1 # address of last byte in string to store +8
456#    lw $t3,20($sp) # hash code to print
460:toString_hashCopyLoop:
460#    and $t4,$t3,0xf
464#    srl $t3,4
468#    lb $t4,hexChars($t4)
472#    sb $t4,-8($t2)
476#    subu $t2,$t2,1
480#    bgt $t2,$t0,toString_hashCopyLoop
481##...slt $at,$t0,$t2
485##...bne $at,$zero,460

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
488#    addu $sp,28
492#    lw $ra,($sp)
496#    addu $sp,4
500#    move $t0, $s7 # return string we created
504#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
508:mth_equals_String:
    
    # quick test for obvious false
508#    lw $t0,($sp) # second parameter
512#    beq $t0,$zero,goEsFalse # go return false if null
516#    lw $t1,-12($t0) # vtable pointer
520#    la $t2,CLASS_String # string vtable pointer
524#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
528#    subu $sp,12
532#    sw $ra,8($sp)
536#    sw $s2,4($sp)
540#    sw $t0,($sp)
544#    jal mth_compareTo_String
548#    lw $s2, 4($sp) #restore this pointer and pop parameters
552#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
556#    seq $t0,$t0,$zero
557##...subu $at,$t0,$zero
561##...sltiu $t0,$at,1

    # pop return address and return $t0
564#    lw $ra,($sp)
568#    addu $sp,4

572#    jr $ra # return
    
576:goEsFalse:
    # put 0 in $t0 and return
576#    li $t0, 0
580#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
584:mth_toString_String:
    #we are a string, so return this.
584#    move $t0, $s2
588#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
592:mth_readLine_Lib:
    
592#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
596#    sw $ra, 4($sp) # save $ra
600#    move $t1,$sp # save "original" sp

604:  doRead:

    # read the character
604#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
608#    subu $t2,$v0,13
612#    bne $t2,$zero,notReturnRL
616#    jal readLogicalChar
620#    subu $t2,$v0,10 # check for newline
624#    beq $t2,$zero,foundNewLine
628#    sw $v0,lastCharRead # push back character into queue
632#    j foundNewLine # go process the line

636:  notReturnRL:

    # at this point, $v0 has our character

636#    subu $t0,$v0,10
640#    beq $t0,$zero,foundNewLine
644#    blt $v0,$zero,foundEof
645##...slt $at,$v0,$zero
649##...bne $at,$zero,664

    # we have a character, so push it onto stack
652#    subu $sp,$sp,4
656#    sw $v0,($sp)

    # loop back up to get next character
660#    j doRead

664:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
664#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
668#    li $s7, 0 # return-value: null
672#    j rlReturn # return

676:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
676#    subu $t0,$t1,$sp # number of chars we read (times 4)
680#    srl $s6,$t0,2 # number of words on stack with chars
684#    subu $sp,$sp,4 # push char-count ...
688#    sw $s6,($sp) # ... onto stack
692#    addu $t2,$t0,5 # GC tag, incl. for count-word
696#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
700#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
704#    srl $s6,$s6,2  # data words in object
708#    move $s7,$zero # object words in object
712#    jal newObject  # allocate space
    
    # store header words
716#    la $t0,CLASS_String
720#    sw $t0,-12($s7)# store class tag (String) into object
724#    lw $t2,4($sp) # char-count
728#    sll $t1,$t2,2 # 4 times number of chars
732#    subu $t2,$zero,$t2 # negative of char-count
736#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
740#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
744#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
745##...slt $at,$t0,$zero
749##...beq $at,$zero,768
752#    and $t0,0xff # mask off object's hash value ...
753##...lui $at,0
757##...ori $at,$at,0xff
761##...and $t0,$t0,$at
764#    sw $t0,-8($s7) # ... and store back into -8 header-word
768:  skipMask_readLine:

768#    sll $t0,$t0,2 # data bytes in object
772#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
776#    subu $t0,$t0,$t2 # place to store last character (plus 9)
780#    addu $sp,$sp,8 # pop redundant object-pointer and count
784#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

788#    beq $sp,$t1,rlReturn
792:  charCopyLoop:
792#    lw $t2,($sp)
796#    sb $t2,-9($t0)
800#    addu $sp,$sp,4
804#    subu $t0,$t0,1
808#    bne $sp,$t1,charCopyLoop
812:  rlReturn:

812#    lw $ra, 4($sp)
816#    addu $sp, 8   # pop $ra and pointless word off stack
820#    move $t0, $s7 # return string we allocated in $s7
824#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
828:mth_readInt_Lib:

    # save $ra by pushing onto stack
828#    subu $sp,$sp,4
832#    sw $ra,($sp)

836:  riSkipWhiteLoop:
    # read a character
836#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
840#    subu $t0,$v0,32
844#    bgt $t0,$zero,nonWhite
845##...slt $at,$zero,$t0
849##...bne $at,$zero,892
852#    beq $t0,$zero,riSkipWhiteLoop # start over if space
856#    subu $t0,$v0,10
860#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
864#    subu $t0,$v0,9
868#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
872#    subu $t0,$v0,13
876#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
880#    subu $t0,$v0,12
884#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
888#    j badIntegerFormat # illegal integer char: abort program

892:  nonWhite:
892#    subu $t0,$v0,'-'
896#    li $t4,1 # final multiplier
900#    bne $t0,$zero,helpReadInt # go read
904#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
908#    jal readLogicalChar

912:  helpReadInt:
912#    li $t2,10
916#    subu $t1,$v0,'0' # convert digit to 0-9 value
920#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
921##...sltu $at,$t1,$t2
925##...beq $at,$zero,3208
928#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

932:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

932#    jal readLogicalChar # read next character
936#    subu $t1,$v0,'0' # convert digit to 0-9 value
940#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
941##...sltu $at,$t1,$t2
945##...beq $at,$zero,964

948#    mul $t3,$t3,$t2 # multiply old value by 10
949##...mult $t3,$t2
953##...mflo $t3
956#    addu $t3,$t3,$t1 # add in value of new digit
960#    j digitLoop

964:  doneDigitLoop:

    # "push back" unused character into queue
964#    sw $v0,lastCharRead

    # restore return address
968#    lw $ra,($sp)
972#    addu $sp, 4

    # multiply by sign and put return value in $t0
976#    mult $t3,$t4 # multiply to account for poss. minus sign
980#    mflo $t3
984#    move $t0, $t3
    
    # return
988#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
992:mth_readChar_Lib:

    # save $ra by pushing onto stack
992#    subu $sp,$sp,4
996#    sw $ra,($sp)

      # read the character
1000#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1004#    lw $ra,($sp)
1008#    addu $sp, 4
1012#    move $t0, $v0

    # return
1016#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1020:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1020#    lw $t0,($sp)
1024#    bne $t0,$zero,psNotNull

    # print "(null)"
1028#    la $a0,nullStr
1032#    li $v0,4
1036#    syscall
1040#    j donePrintStr
    
1044:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1044#    subu $t0,$t0,8
1048#    lw $t1,($t0) # number of data words, but needs modification
1052#    blt $t1,$zero,skipPrintStr1
1053##...slt $at,$t1,$zero
1057##...bne $at,$zero,1076
1060#    and $t1,0xff # mask out all but low byte
1061##...lui $at,0
1065##...ori $at,$at,0xff
1069##...and $t1,$t1,$at
1072#    b skipPrintStr2
1076:skipPrintStr1:
1076#    and $t1,0x7ffffff
1077##...lui $at,2047
1081##...ori $at,$at,0xffff
1085##...and $t1,$t1,$at
1088:skipPrintStr2:    
1088#    sll $t1,$t1,2
1092#    subu $t1,$t0,$t1 # first word in string
1096#    lw $t0,4($t0) # negative of string-length
1100#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1104#    beq $t0,$t1,donePrintStr
1108:  psLoop:
1108#    lb $a0,($t1) # next byte
1112#    li $v0,11 # code for printing char
1116#        syscall # print the char
1120#    addu $t1,$t1,1 # go to next char
1124#    blt $t1,$t0,psLoop
1125##...slt $at,$t1,$t0
1129##...bne $at,$zero,1108

1132:  donePrintStr:
1132#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1136:mth_printInt_Lib:
1136#    lw $a0,($sp)

    # print it
1140#    li $v0,1 # code for print-int
1144#    syscall

    # return
1148#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1152:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1152#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1156#    la $a0,falseString
1160#    beq $t0,$zero,skipPB
1164#    la $a0,trueString
1168:  skipPB:
1168#    li $v0,4 # code for print-string
1172#    syscall

    # return
1176#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1180:mth_substring_String:

    # save $ra by pushing onto stack
1180#    subu $sp,$sp,4
1184#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1188#    move $t0,$s2
1192#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1196#    lw $t0,-4($t0) # negative of string length
1200#    subu $t0,$zero,$t0 # string length
1204#    lw $t1,4($sp) # k
1208#    lw $t2,12($sp) # n
1212#    bgt $zero,$t2,strIndexOutOfBounds
1213##...slt $at,$t2,$zero
1217##...bne $at,$zero,3176
1220#    bgt $t2,$t1,strIndexOutOfBounds
1221##...slt $at,$t1,$t2
1225##...bne $at,$zero,3176
1228#    bgt $t1,$t0,strIndexOutOfBounds
1229##...slt $at,$t0,$t1
1233##...bne $at,$zero,3176

    # allocate memory
1236#    subu $s6,$t1,$t2 # # chars in target-string
1240#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1244#    srl $s6,$s6,2 # convert bytes-count to word-count
1248#    move $s7,$zero # (no object-bytes in string)
1252#    jal newObject

    # store "String" tag in object-type field
1256#    la $t0,CLASS_String
1260#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1264#    lw $t4,-8($s7) # data words in source string
1268#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1269##...slt $at,$t4,$zero
1273##...beq $at,$zero,1292
1276#    and $t4,0xff # mask off object's hash value ...
1277##...lui $at,0
1281##...ori $at,$at,0xff
1285##...and $t4,$t4,$at
1288#    sw $t4,-8($s7) # ... and store back into -8 header-word
1292:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1292#    lw $t1,8($sp) # k
1296#    lw $t2,16($sp) # n
1300#    subu $t0,$t2,$t1 # value to store
1304#    sw $t0,-4($s7) # store value
    
1308#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1312#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1316#    lw $t4,-8($t3) # data words in source string
1320#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1324#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1328#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1332#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1336#    lw $t2,-8($s7)   # data words in target string
1340#    sll $t2,$t2,2    # data bytes in target string
1344#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1348:  subCopyLoopzz:
1348#    lb $t4,-8($t3)
1352#    sb $t4,-8($t2)
1356#    addu $t2,$t2,1
1360#    addu $t3,$t3,1
1364#    blt $t3,$t1,subCopyLoopzz
1365##...slt $at,$t3,$t1
1369##...bne $at,$zero,1348
    
1372:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1372#    lw $ra,4($sp) # restore $ra
1376#    addu $sp,$sp,8 # pop ra, and allocated newObject
1380#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1384#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1388:mth_length_String:

    # the length is the negative of 4($s2)
1388#    lw $t0,-4($s2) # -length
1392#    subu $t0,$zero,$t0

    # return
1396#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1400:mth_concat_String:
    # save $ra by pushing onto stack
1400#    subu $sp,$sp,4
1404#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1408#    lw $t0,4($sp)
1412#    beq $t0,$zero,nullPtrException
1416#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1420#    lw $t0,-4($t0) # negative size of second object
1424#    lw $t1,-4($t1) # negative size of first object
1428#    addu $s6,$t0,$t1 # sum of negative sizes
1432#    sra $s6,$s6,2 # negative word-size of char part
1436#    subu $s6,$zero,$s6 # word size of char part
1440#    addu $s6,$s6,1 # data word size, including v-table word
1444#    move $s7,$zero
1448#    jal newObject

    # store String vtable address in vtable address field
1452#    la $t0,CLASS_String
1456#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1460#    lw $t4,-8($s7) # # data words in source string
1464#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1465##...slt $at,$t4,$zero
1469##...beq $at,$zero,1488
1472#    and $t4,0xff # mask off object's hash value ...
1473##...lui $at,0
1477##...ori $at,$at,0xff
1481##...and $t4,$t4,$at
1484#    sw $t4,-8($s7) # ... and store back into -8 header-word
1488:skipMask_concat:

1488#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1492#    move $t1,$s2   # first object
1496#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1500#    lw $t2,-4($t0) # negative length of second object
1504#    lw $t3,-4($t1) # negative length of first object
1508#    addu $t4,$t2,$t3 # sum of negative lengths
1512#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1516#    lw $t4,-8($t1) # # data words in first string
1520#    sll $t4,$t4,2 # # data bytes in first string
1524#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1528#    lw $t4,-8($s7) # # data words in new string
1532#    sll $t4,$t4,2 # # data bytes in new string
1536#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1540#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1544#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1548:  concatLoop1zz:
1548#    lb $t4,-8($t1)
1552#    sb $t4,-8($s7)
1556#    addu $s7,$s7,1
1560#    addu $t1,$t1,1
1564#    blt $t1,$t3,concatLoop1zz
1565##...slt $at,$t1,$t3
1569##...bne $at,$zero,1548
1572:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1572#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1576#    lw $t4,-8($t0) # # data words in second string
1580#    sll $t4,$t4,2 # # data bytes in second string
1584#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1588#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1592:  concatLoop2zz:
1592#    lb $t4,-8($t1)
1596#    sb $t4,-8($s7)
1600#    addu $s7,$s7,1
1604#    addu $t1,$t1,1
1608#    blt $t1,$t3,concatLoop2zz
1609##...slt $at,$t1,$t3
1613##...bne $at,$zero,1592
1616:  doneConcatLoop2zz:
 
1616:  concatRtnzz:
    # return
1616#    move $t0, $t9 # put saved return string in $t0
1620#    lw $ra, 4($sp)
1624#    addu $sp, 8 # pop ra and allocated newObject
1628#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1632:mth_charAt_String:
    # get string
1632#    move $t0,$s2

    # check that index is in bounds
1636#    lw $t1,-4($t0) # negative of # data words in string
1640#    subu $t3,$zero,$t1 # # chars in string
1644#    lw $t2,($sp) # index
1648#    bgeu $t2,$t3,strIndexOutOfBounds
1649##...sltu $at,$t2,$t3
1653##...beq $at,$zero,3176

    # access element
1656#    lw $t1,-8($t0) # # data words in object
1660#    sll $t1,$t1,2 # - byte-offset from end of chars
1664#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1668#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1672#    lb $t0,-8($t0) # our char

    # return
1676#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1680:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1680#    subu $sp,$sp,8
1684#    sw $ra,4($sp)

    # save current sp
1688#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1692#    li $t3,10

    # get argument, negate if negative
1696#    lw $t1,8($sp)
1700#    bge $t1,$zero,itsNonNegLoop
1701##...slt $at,$t1,$zero
1705##...beq $at,$zero,1712
1708#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1712:  itsNonNegLoop:
1712#    divu $t1,$t3
1716#    mflo $t1 # quotient
1720#    mfhi $t4 # remainder
1724#    addu $t4,$t4,'0' # turn remainder into digit
1728#    subu $sp,$sp,4
1732#    sw $t4,($sp) # push digit onto stack
1736#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1740#    lw $t4,8($t0)
1744#    bge $t4,$zero,itsSkipNeg
1745##...slt $at,$t4,$zero
1749##...beq $at,$zero,1764
1752#    li $t4,'-'
1756#    subu $sp,$sp,4
1760#    sw $t4,($sp)
1764:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
1764#    subu $s6,$t0,$sp
1768#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
1772#    sw $t3,($t0)
1776#    srl $s6,$s6,2
1780#    subu $sp,$sp,4
1784#    sw $s6,($sp)
    
    # allocate memory
1788#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1792#    srl $s6,$s6,2
1796#    move $s7,$zero # no "object" words in object
1800#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
1804#    lw $t0,4($sp)
1808#    addu $sp,$sp,8

    # store "String" tag into class field
1812#    subu $s7,$s7,8 # address of header-1 word
1816#    la $t1,CLASS_String
1820#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
1824#    lw $t4,($s7) # # data words in source string
1828#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
1829##...slt $at,$t4,$zero
1833##...beq $at,$zero,1852
1836#    and $t4,0xff # mask off object's hash value ...
1837##...lui $at,0
1841##...ori $at,$at,0xff
1845##...and $t4,$t4,$at
1848#    sw $t4,($s7) # ... and store back into -8 header-word
1852:skipMask_intToString:
    
    # store negative of char-count into header-2 word
1852#    subu $t0,$zero,$t0
1856#    sw $t0,4($s7)

1860#    lw $t1,($s7) # number of data words in string
1864#    sll $t1,$t1,2 # number data bytes in string
1868#    subu $t1,$s7,$t1 # first location to store chars in string
1872#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
1876:  itsLoop:
1876#    lw $t2,($sp)
1880#    addu $sp,$sp,4
1884#    sb $t2,($t1)
1888#    addu $t1,$t1,1
1892#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
1896#    lw $ra,4($sp)
1900#    addu $sp,$sp,8
1904#    addu $t0,$t0,15
1908#    and $t0,$t0,0xfffffffc
1909##...lui $at,65535
1913##...ori $at,$at,0xfffc
1917##...and $t0,$t0,$at
1920#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
1924:mth_intToChar_Lib:
    # save return address
1924#    subu $sp,$sp,4
1928#    sw $ra,($sp)

    # allocate object
1932#    li $s6,2
1936#    move $s7,$zero
1940#    jal newObject

    # get n from string.
1944#    lw $t1,8($sp)

    # store "String" tag into class field
1948#    la $t0,CLASS_String
1952#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1956#    lw $t4,-8($s7) # # data words in source string
1960#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
1961##...slt $at,$t4,$zero
1965##...beq $at,$zero,1984
1968#    and $t4,0xff # mask off object's hash value ...
1969##...lui $at,0
1973##...ori $at,$at,0xff
1977##...and $t4,$t4,$at
1980#    sw $t4,-8($s7) # ... and store back into -8 header-word
1984:  skipMask_intToChar:

    # store data in string
1984#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
1988#    li $t0,-1
1992#    sw $t0,-4($s7)

    # we're returning $s7
1996#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2000#    lw $ra,4($sp)
2004#    addu $sp,$sp,8

    # return
2008#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2012:mth_compareTo_String:
    # get string pointers and check parameter for null
2012#    lw $t0,($sp) # second string
2016#    beq $t0,$zero,nullPtrException
2020#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2024#    lw $t2,-4($t0) # negative length of second string
2028#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2032#    slt $t4,$t3,$t2
2036#    slt $t5,$t2,$t3
2040#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2044#    lw $t2,-8($t0) # # data words in second string
2048#    lw $t3,-8($t1) # # data words in first string
2052#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2056#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2060#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2064#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2068#    beq $zero,$t5,skipStrCmpLenzz
2072#    move $t2,$t3
2076:  skipStrCmpLenzz:
2076#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2080:  cmpLoopzz:
2080#    lw $t3,-8($t1) # word from str2
2084#    lw $t4,-8($t0) # word from str1
2088#    bne $t3,$t4,cmpNotEqualzz
2092#    addu $t1,$t1,4
2096#    addu $t0,$t0,4
2100#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2104#    j cmpHitLimitzz

2108:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2108#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2112#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2116#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2120:  cmpHitLimitzz:
2120#    move $t0, $t5 # store return value
2124#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2128:readLogicalChar:

    # check if we already have a character
2128#    lw $v0,lastCharRead
2132#    li $t0,-2
2136#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2140#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2144#    jr $ra # return
    
2148:  doReadCh:
    # we need to actually read a character: read and return
2148#    li $v0,12 # use system call to read a character
2152#    syscall
2156#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2160:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2160#    blt $s7,$zero arraySizeOutOfBounds
2161##...slt $at,$s7,$zero
2165##...bne $at,$zero,3192

    # mark the fact that we have not yet GC'd for this allocation
2168#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2172:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2172#    addu $t0,$s6,$s7
2176#    sll $t0,$t0,2
2180#    addu $t0,$t0,8 # amount of memory needed
2184#    addu $t1,$t0,$s3
2188#    bgtu $t1,$s4,doGC
2189##...sltu $at,$s4,$t1
2193##...bne $at,$zero,2316

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2196#    move $t2,$s3
2200:  zeroObjLoop:
2200#    sw $zero,($s3)
2204#    lw $zero,($s3)

2208#    addu $s3,4
2212#    bltu $s3,$t1,zeroObjLoop
2213##...sltu $at,$s3,$t1
2217##...bne $at,$zero,2200

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2220#    sll $t0,$s7,2    # number of post-header bytes
2224#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2228#    li $t2,255
2232#    bgeu $s6,$t2 firstHeaderLarge
2233##...sltu $at,$s6,$t2
2237##...beq $at,$zero,2284
2240#    lw $t1,prevHashConstant
2244#    addu $t1,0x12b68fd2
2245##...lui $at,4791
2249##...addiu $at,$at,-28718
2253##...addu $t1,$at,$t1
2256#    sw $t1,prevHashConstant # update hash constant
2260#    or $t1,0x80000000 # set top bit
2261##...lui $at,32768
2265##...or $t1,$at,$t1
2268#    and $t1,0xffffff00 # clear low bit
2269##...lui $at,65535
2273##...ori $at,$at,0xff00
2277##...and $t1,$t1,$at
2280#    or $s6,$t1
2284:firstHeaderLarge:
2284#    sw $s6,-8($t0) # store header-1 value
2288#    lw $zero,-8($t0)

    # store second header-value
2292#    sw $s7,-4($t0)   # second header-word
2296#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2300#    move $s7,$t0
2304#    subu $sp,$sp,4
2308#    sw $t0,($sp)

2312#    jr $ra
    
2316:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2316#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2320#    lw $t5,gcCount
2324#    addu $t5,1
2328#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2332#    subu $sp,$sp,4
2336#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2340#    lw $t0,cur_seg
2344#    move $t7,$s4
2348#    la $t6,seg1_start
2349##...lui $t6,16
2353##...addiu $t6,$t6,3744
2356#    la $s3,seg0_start
2360#    la $s4,seg0_end
2361##...lui $s4,16
2365##...addiu $s4,$s4,3744
2368#    sw $zero,cur_seg
2372#    bne $t0,$zero,skipGc1
2376#    la $t6,seg0_start
2380#    la $s3,seg1_start
2381##...lui $s3,16
2385##...addiu $s3,$s3,3744
2388#    la $s4,seg1_end
2389##...lui $s4,32
2393##...addiu $s4,$s4,3744
2396#    sw $s5,cur_seg
2400:  skipGc1:
2400#    li $t5,0x80000000
2404#    lw $t0,stack_bottom
2408#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2412:  gcStackCopyLoop:
2412#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2416#    sll $t2,$t1,31 # zero iff low bit was zero
2420#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2424#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2428#    bleu $t1,$t6,gcTestIterDone1
2429##...sltu $at,$t6,$t1
2433##...beq $at,$zero,2596
2436#    bgtu $t1,$t7,gcTestIterDone1
2437##...sltu $at,$t7,$t1
2441##...bne $at,$zero,2596

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2444#    lw $t2,-8($t1) # possible forwarding tag
2448#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2452#    lw $t2,-4($t1) # forwarding pointer: object's new address
2456#    sw $t2,4($t0) # update stack value
2460#    j gcTestIterDone1 # do next iteration

2464:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2464#    bge $t2,$zero,gcSkip2
2465##...slt $at,$t2,$zero
2469##...beq $at,$zero,2484
2472#    and $t2,0xff #### mask off hash value
2473##...lui $at,0
2477##...ori $at,$at,0xff
2481##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2484:  gcSkip2:
2484#    sll $t2,$t2,2
2488#    addu $t2,$t2,8
2492#    subu $t4,$t1,$t2 # address of first word of source
2496#    lw $t3,-4($t1) # number of object words (negative means zero)
2500#    bge $t3,$zero,gcH2Neg1
2501##...slt $at,$t3,$zero
2505##...beq $at,$zero,2512
2508#    move $t3,$zero
2512:  gcH2Neg1:
2512#    sll $t3,$t3,2
2516#    addu $t3,$t3,$t1 # address one word past last word of source

2520#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2524#    lw $s0,($t4)
2528#    lw $s1,-8($t1)
2532#    sw $s1,($t4)
2536#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2540:  gCinnerCopy1:
2540#    lw $s0,($t4)
2544#    sw $s0,($s3)
2548#    addu $t4,$t4,4
2552#    addu $s3,$s3,4
2556#    bltu $t4,$t3,gCinnerCopy1
2557##...sltu $at,$t4,$t3
2561##...bne $at,$zero,2540

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2564#    sw $zero,-8($t1) # 0 in first header-slot
2568#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2572#    sw $t2,4($t0)
2576#    lw $zero,-8($t2)
2580#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2584#    j gcTestIterDone1

2588:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2588#    subu $t0,$t0,$t1
2592#    subu $t0,$t0,3

2596:  gcTestIterDone1:
2596#    bgeu $t0,$sp,gcStackCopyLoop
2597##...sltu $at,$t0,$sp
2601##...beq $at,$zero,2412

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2604#    lw $t1,cur_seg
2608#    la $t0,seg0_start
2612#    beq $t1,$zero,gcSkip4
2616#    la $t0,seg1_start
2617##...lui $t0,16
2621##...addiu $t0,$t0,3744


2624:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2624#    bgeu $t0,$s3,gcFinishUp
2625##...sltu $at,$t0,$s3
2629##...beq $at,$zero,2920

    ###### begin outer loop to copy all non-stack-direct objects ######

2632:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2632#    lw $t1,($t0) # (what will be) first header word for current object
2636#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2640#    bge $t2,$zero,gcSkip4a # are there hash bits?
2641##...slt $at,$t2,$zero
2645##...beq $at,$zero,2660
2648#    and $t2,0xff # mask off hash bits
2649##...lui $at,0
2653##...ori $at,$at,0xff
2657##...and $t2,$t2,$at
2660:gcSkip4a:    
2660#    sll $t2,$t2,2 # # data bytes
2664#    addu $t2,$t2,8 # to skip header words
2668#    addu $t2,$t0,$t2 # pointer to new object
2672#    lw $t3,-8($t2) # word to swap
2676#    sw $t1,-8($t2) # store header word
2680#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2684#    lw $t1,-12($t2) # vtable pointer
2688#    li $t0,dataArrayVTableStart # address of vtable for first data array
2692#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2693##...slt $at,$t1,$t0
2697##...bne $at,$zero,2716

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2700:  skipToNextObj:
2700#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2704#    sll $t1,$t1,2 # convert to byte-count
2708#    addu $t0,$t2,$t1 # skip over object
2712#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2716:  gcNotDataArray:
2716#    lw $t3,-4($t2) # # object words (negative treated as zero)
2720#    bge $t3,$zero,gcH2Neg2
2721##...slt $at,$t3,$zero
2725##...beq $at,$zero,2732
2728#    move $t3,$zero
2732:  gcH2Neg2:
2732#    sll $t3,$t3,2 # # object bytes
2736#    move $t0,$t2 # address of first pointer in object
2740#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2744#    beq $t0,$s1,gcTestIterDone2
    
2748:  gcNextPointerInObject:
    # get pointer from object
2748#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2752#    bleu $t1,$t6,gcGoNextPointerSlot
2753##...sltu $at,$t6,$t1
2757##...beq $at,$zero,2900
2760#    bgtu $t1,$t7,gcGoNextPointerSlot
2761##...sltu $at,$t7,$t1
2765##...bne $at,$zero,2900

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2768#    lw $t8,-8($t1) # possible forwarding tag
2772#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
2776#    lw $t8,-4($t1) # forwarding pointer: object's new address
2780#    sw $t8,($t0) # update pointer in object
2784#    j gcGoNextPointerSlot # do next iteration

2788:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
2788#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
2789##...slt $at,$t8,$zero
2793##...beq $at,$zero,2808
2796#    and $t8,0xff # mask off hash bits
2797##...lui $at,0
2801##...ori $at,$at,0xff
2805##...and $t8,$t8,$at

2808:  gcSkip5:
2808#    sll $t8,$t8,2 # bytes in negative-offset portion ...
2812#    addu $t8,$t8,8 # ... add in bytes for header-words
2816#    subu $t4,$t1,$t8 # address of first word of source object
2820#    lw $t3,-4($t1) # # words in positive-offset portion
2824#    bge $t3,$zero,gcNoNeg
2825##...slt $at,$t3,$zero
2829##...beq $at,$zero,2836
2832#    move $t3,$zero # negative treated as zero
2836:  gcNoNeg:
2836#    sll $t3,$t3,2 # bytes in negative-offset portion
2840#    addu $t3,$t3,$t1 # address one word past last word of source

2844#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
2848#    lw $s0,($t4)
2852#    lw $t9,-8($t1)
2856#    sw $t9,($t4)
2860#    sw $s0,-8($t1)

    # copy all source bytes to the target
2864:  gCinnerCopy2:
2864#    lw $s0,($t4)
2868#    sw $s0,($s3)
2872#    addu $t4,$t4,4
2876#    addu $s3,$s3,4
2880#    bltu $t4,$t3,gCinnerCopy2
2881##...sltu $at,$t4,$t3
2885##...bne $at,$zero,2864

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2888#    sw $zero,-8($t1) # 0 in first header-slot
2892#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
2896#    sw $t8,($t0)

2900:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
2900#    addu $t0,$t0,4
2904#    bltu $t0,$s1,gcNextPointerInObject
2905##...sltu $at,$t0,$s1
2909##...bne $at,$zero,2748

2912:  gcTestIterDone2:
2912#    bltu $t0,$s3,gcHeapCopyLoop
2913##...sltu $at,$t0,$s3
2917##...bne $at,$zero,2632

    ###### end outer loop to copy all non-stack-direct objects ######

2920:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
2920#    lw $s2,($sp)
2924#    addu $sp,$sp,4

    # mark us as having already GC'd
2928#    move $t5,$s5

    # go try and allocate again
2932#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
2936:vm_init:

    # mark bottom of stack
2936#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
2940#    move $s2,$zero

    #set up the "five" register
2944#    li $s5,5

2948#    la $s3,seg0_start
2952#    la $s4,seg0_end
2953##...lui $s4,16
2957##...addiu $s4,$s4,3744
    
    # return
2960#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2964:divide:
    # get parameters; abort if divisor zero
2964#    lw $t0,($sp)
2968#    lw $t1,8($sp)
2972#    beq $t0,$zero,divByZeroError
    
    # perform division
2976#    div $t1,$t0
2980#    mflo $t0

    # store result, adjust stack and return
2984#    addu $sp,$sp,8 # adjust stack
2988#    sw $t0,($sp) # store result
2992#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2996:remainder:
    # get parameters; abort if divisor zero
2996#    lw $t0,($sp)
3000#    lw $t1,8($sp)
3004#    beq $t0,$zero,divByZeroError
    
    # perform division
3008#    div $t1,$t0
3012#    mfhi $t0

    # store result, adjust stack and return
3016#    addu $sp,$sp,8 # adjust stack
3020#    sw $t0,($sp) # store result
3024#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3028:checkCast:
    # get object, allow immediately if null
3028#    lw $t2,($sp)
3032#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3036#    lw $t2,-12($t2) # vtable address
3040#    bge $t2,$t1,castException
3041##...slt $at,$t2,$t1
3045##...beq $at,$zero,3216
3048#    blt $t2,$t0,castException
3049##...slt $at,$t2,$t0
3053##...bne $at,$zero,3216
    
    # return, leaving object unchanged on stack
3056:  checkCastReturn:    
3056#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3060:instanceOf:
    # get object, go return false if null
3060#    lw $t2,($sp)
3064#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3068#    lw $t2,-12($t2) # vtable address
3072#    sge $t0,$t2,$t0 # are we >= the lower limit?
3073##...slt $t0,$t2,$t0
3077##...xori $t0,$t0,0x1
3080#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3084#    and $t2,$t0,$t1
3088#    sw $t2,($sp)
3092:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3092#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3096:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3096#    li $v0,4 # syscall-code for print-string
3100#    syscall
3104:exit:
    # print termination message
3104#    li $v0,4 # syscall-code for print-string
3108#    la $a0,exitString
3112#    syscall

    # print number of garbage collections
3116#    li $v0,4 # syscall-code for print-string
3120#    la $a0,gcCountMsg
3124#    syscall # print "Number of ..."
3128#    lw $a0,gcCount # number of GC's
3132#    li $v0,1 # code for print-int
3136#    syscall
3140#    li $v0,4 # syscall-code for print-string
3144#    la $a0,newlineString
3148#    syscall # print newline

    # terminate execution
3152#    li $v0,10 #syscall-code for "exit"
3156#    syscall

3160:outOfHeapMemory:
3160#    la $a0,heapMemoryMsg
3164#    j exitError
3168:divByZeroError:
3168#    la $a0,divByZeroMsg
3172#    j exitError
3176:strIndexOutOfBounds:
3176#    la $a0,strIndexOutOfBoundsMsg
3180#    j exitError
3184:arrayIndexOutOfBounds:
3184#    la $a0,arrayIndexOutOfBoundsMsg
3188#    j exitError
3192:arraySizeOutOfBounds:
3192#    la $a0,arraySizeOutOfBoundsMsg
3196#    j exitError
3200:nullPtrException:
3200#    la $a0,nullPtrExceptionMsg
3204#    j exitError
3208:badIntegerFormat:
3208#    la $a0,badIntegerFormatMsg
3212#    j exitError
3216:castException:
3216#    la $a0,castExceptionMsg
3220#    j exitError
############## data section ################
    .data
3372:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3388:cur_seg:
    .word 0

3392:lastCharRead:
    .word -2 # -2 => no buffered character
3396:prevHashConstant:
    .word 0x2785173c
3400:gcCount:
    .word 0

3404:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3431:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3454:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3489:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3523:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3556:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3587:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3635:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3656:gcMsg: 
      .asciiz "\nGC!\n"
3662:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3694:nullStr:
    .asciiz "null"
3699:trueString:
    .asciiz "true"
3704:falseString:
    .asciiz "false"
3710:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
3738:newlineString:
    .asciiz "\n"
    
    .align 2
3740:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

3744:seg0_start:
    .space 0x100000
1052320:seg0_end:
1052320:seg1_start:
    .space 0x100000
2100896:seg1_end:
