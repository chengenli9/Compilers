 .data # Program at 3.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3692:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3712:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3744:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3748:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 3.7
  .byte 205 # 'M' with high bit set # ClassDecl at 3.7
  .byte 97 # 'a' # ClassDecl at 3.7
  .byte 105 # 'i' # ClassDecl at 3.7
  .byte 110 # 'n' # ClassDecl at 3.7
3796:CLASS_Main: # ClassDecl at 3.7
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 4.15
3844:END_CLASS_Main: # ClassDecl at 3.7
3844:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3852:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3864:END_CLASS_RunMain: # ClassDecl at 0.0
3864:dataArrayVTableStart: # IntType at 0.0
3864:END_CLASS_Object: # ClassDecl at 0.0
  .byte 32 # StringLit at 8.41
  .byte 0 # StringLit at 8.41
  .byte 0 # StringLit at 8.41
  .byte 0 # StringLit at 8.41
  .word CLASS_String # StringLit at 8.41
  .word 2 # StringLit at 8.41
  .word -1 # StringLit at 8.41
3880:strLit_27: # StringLit at 8.41
  .byte 10 # StringLit at 10.20
  .byte 0 # StringLit at 10.20
  .byte 0 # StringLit at 10.20
  .byte 0 # StringLit at 10.20
  .word CLASS_String # StringLit at 10.20
  .word 2 # StringLit at 10.20
  .word -1 # StringLit at 10.20
3896:strLit_35: # StringLit at 10.20
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp, $sp, 4
28#  move $s2, $s7
32#  jal mth_main_Main
36#  li $v0, 10
40#  syscall
.globl mth_main_Main
44:mth_main_Main:
44#  subu $sp, $sp, 4
48#  sw $ra, ($sp)
52#  li $t0, 20
56#  subu $sp, $sp, 8
60#  sw $zero, 4($sp)
64#  sw $t0, ($sp)
68#  li $t0, 50
72#  subu $sp, $sp, 8
76#  sw $zero, 4($sp)
80#  sw $t0, ($sp)
84:while_cond_33:
84#  lw $t0, 0($sp)
88#  subu $sp, $sp, 8
92#  sw $zero, 4($sp)
96#  sw $t0, ($sp)
100#  li $t0, 100
104#  subu $sp, $sp, 8
108#  sw $zero, 4($sp)
112#  sw $t0, ($sp)
116#  lw $t2, ($sp)
120#  addu $sp, $sp, 8
124#  lw $t1, ($sp)
128#  addu $sp, $sp, 8
132#  sgt $t0, $t1, $t2
136#  subu $sp, $sp, 4
140#  sw $t0, ($sp)
144#  lw $t0, ($sp)
148#  xor $t0, $t0, 1
152#  sw $t0, ($sp)
156#  lw $t0, ($sp)
160#  addu $sp, $sp, 4
164#  beq $t0, $zero, break_target_33
168#  lw $t0, 8($sp)
172#  subu $sp, $sp, 8
176#  sw $zero, 4($sp)
180#  sw $t0, ($sp)
184#  li $t0, 25
188#  subu $sp, $sp, 8
192#  sw $zero, 4($sp)
196#  sw $t0, ($sp)
200#  lw $t2, ($sp)
204#  addu $sp, $sp, 8
208#  lw $t1, ($sp)
212#  addu $sp, $sp, 8
216#  sgt $t0, $t1, $t2
220#  subu $sp, $sp, 4
224#  sw $t0, ($sp)
228#  lw $t0, ($sp)
232#  addu $sp, $sp, 4
236#  beq $t0, $zero, if_else_22
240#  j break_target_33
244#  j if_done_22
248:if_else_22:
248:if_done_22:
248#  subu $sp, $sp, 4
252#  sw $s2, ($sp)
256#  lw $t0, 4($sp)
260#  subu $sp, $sp, 8
264#  sw $zero, 4($sp)
268#  sw $t0, ($sp)
272#  lw $t0, 8($sp)
276#  sw $s2, 8($sp)
280#  move $s2, $t0
284#  jal mth_printInt_Lib
288#  addu $sp, $sp, 8
292#  lw $s2, ($sp)
296#  addu $sp, $sp, 4
300#  subu $sp, $sp, 4
304#  sw $t0, ($sp)
308#  lw $t0, ($sp)
312#  addu $sp, $sp, 4
316#  subu $sp, $sp, 4
320#  sw $s2, ($sp)
324#  la $t0, strLit_27
328#  subu $sp, $sp, 4
332#  sw $t0, ($sp)
336#  lw $t0, 4($sp)
340#  sw $s2, 4($sp)
344#  move $s2, $t0
348#  jal mth_printStr_Lib
352#  addu $sp, $sp, 4
356#  lw $s2, ($sp)
360#  addu $sp, $sp, 4
364#  subu $sp, $sp, 4
368#  sw $t0, ($sp)
372#  lw $t0, ($sp)
376#  addu $sp, $sp, 4
380#  lw $t0, 0($sp)
384#  subu $sp, $sp, 8
388#  sw $zero, 4($sp)
392#  sw $t0, ($sp)
396#  li $t0, 1
400#  subu $sp, $sp, 8
404#  sw $zero, 4($sp)
408#  sw $t0, ($sp)
412#  lw $t2, ($sp)
416#  addu $sp, $sp, 8
420#  lw $t1, ($sp)
424#  addu $sp, $sp, 8
428#  addu $t0, $t1, $t2
432#  subu $sp, $sp, 8
436#  sw $zero, 4($sp)
440#  sw $t0, ($sp)
444#  lw $t0, ($sp)
448#  addu $sp, $sp, 8
452#  sw $t0, 0($sp)
456#  j while_cond_33
460:break_target_33:
460#  addu $sp, $sp, 8
464#  subu $sp, $sp, 4
468#  sw $s2, ($sp)
472#  la $t0, strLit_35
476#  subu $sp, $sp, 4
480#  sw $t0, ($sp)
484#  lw $t0, 4($sp)
488#  sw $s2, 4($sp)
492#  move $s2, $t0
496#  jal mth_printStr_Lib
500#  addu $sp, $sp, 4
504#  lw $s2, ($sp)
508#  addu $sp, $sp, 4
512#  subu $sp, $sp, 4
516#  sw $t0, ($sp)
520#  lw $t0, ($sp)
524#  addu $sp, $sp, 4
528#  addu $sp, $sp, 8
532#  lw $ra, ($sp)
536#  addu $sp, $sp, 4
540#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
544:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
544#    lw $t0,-8($s2) # get word with hash bits from this
548#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
552#    addu $t0,$t1

556#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
560:mth_equals_Object:
560#    move $t0,$s2 # first pointer (this)
564#    lw $t1,($sp) # second pointer
568#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
569##...subu $at,$t0,$t1
573##...sltiu $t0,$at,1

576#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
580:mth_hashCode_String:
580#    move $t0,$s2 # put this pointer in t0

584#    li $t2,0 # current hash value
588#    lw $t1,-4($t0) # negative of number of characters in string
592#    beq $t1,$zero,done_hashCode_String # if zero length => done
596#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
600#    and $t0,0xfffffffc # adjust for padding
601##...lui $at,65535
605##...ori $at,$at,0xfffc
609##...and $t0,$t0,$at
612#    subu $t1,$t0,$t1 # address at end of string +12
616:loop_hashCode_String:    
616#    sll $t4,$t2,5 # multiply by 32
620#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
624#    lbu $t3,-12($t0) # current character
628#    addu $t2,$t3 # add in current character
632#    addu $t0,1 # go to next character's address
636#    bne $t0,$t1,loop_hashCode_String # loop back if more
640:done_hashCode_String:    

    # push int on stack
640#    move $t0, $t2 # put $t2 in return slot
644#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
648:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
648#    subu $sp,4
652#    sw $ra,($sp)
    
    # get hash value
656#    lw $t2,-12($s2) # vtable address
660#    lw $t0,($t2) # method address
664#    jalr $t0
668#    subu $sp,8 # push result (int in $t0) on stack
672#    sw $s5,4($sp)
676#    sw $t0,($sp)

    # compute print-size of hash value
680#    li $t0, 1
684#    lw $t1, ($sp)
688#    srl $t1,$t1,4
692#    beq $t1,$zero,toString_skip_loop
696:toString_loop_top:
696#    addu $t0,1
700#    srl $t1,$t1,4
704#    bne $t1,$zero,toString_loop_top
708:toString_skip_loop:
708#    subu $sp,8
712#    sw $s5,4($sp)
716#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
720#    lw $t2,-12($s2) # vtable address
724#    move $t0,$t2
728:toString_checkTagBit:
728#    subu $t0,1
732#    lb $t1,($t0)
736#    and $t1,0x80
737##...lui $at,0
741##...ori $at,$at,0x80
745##...and $t1,$t1,$at
748#    beq $t1,$zero,toString_checkTagBit
752#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
756#    subu $sp,8
760#    sw $t1,($sp)
764#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
768#    move $s7,$zero # -4 offset header word
772#    lw $t4,8($sp) # bytes for hash value
776#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
780#    addu $t4,8
784#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
788#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

792#    li $t0,CLASS_String # store string vtable address into ...
796#    sw $t0,-12($s7) # ... -12 offset in object header
800#    lw $t2,-12($s2) # our object's vtable address
804#    lw $t5,4($sp) # number characters in prefix
808#    lw $t1,12($sp) # number of characters in hashcode
812#    addu $t3,$t1,$t5 # number of characters in string ...
816#    addu $t3,1 # ... including the '@' character
820#    subu $t0,$zero,$t3 # store negated length into ...
824#    sw $t0,-4($s7) # ... -4 offset header word
828#    lw $t0,-8($s7) # get -8 offset header-word
832#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
833##...slt $at,$t0,$zero
837##...beq $at,$zero,856
840#    and $t0,0xff # mask off object's hash value ...
841##...lui $at,0
845##...ori $at,$at,0xff
849##...and $t0,$t0,$at
852#    sw $t0,-8($s7) # ... and store back into -8 header-word
856:skipMask_toString:
856#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
860#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
864#    subu $t4,$t2,$t5
868:toString_copyLoop:    
868#    lb $t3,($t4)
872#    and $t3,0x7f # mask off sign bit (in case first word)
873##...lui $at,0
877##...ori $at,$at,0x7f
881##...and $t3,$t3,$at
884#    sb $t3,-8($t0)
888#    addu $t4,1
892#    addu $t0,1
896#    blt $t4,$t2,toString_copyLoop
897##...slt $at,$t4,$t2
901##...bne $at,$zero,868

    # copy the '@' sign
904#    li $t3,'@'
908#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
912#    addu $t2,$t0,$t1 # address of last byte in string to store +8
916#    lw $t3,20($sp) # hash code to print
920:toString_hashCopyLoop:
920#    and $t4,$t3,0xf
924#    srl $t3,4
928#    lb $t4,hexChars($t4)
932#    sb $t4,-8($t2)
936#    subu $t2,$t2,1
940#    bgt $t2,$t0,toString_hashCopyLoop
941##...slt $at,$t0,$t2
945##...bne $at,$zero,920

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
948#    addu $sp,28
952#    lw $ra,($sp)
956#    addu $sp,4
960#    move $t0, $s7 # return string we created
964#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
968:mth_equals_String:
    
    # quick test for obvious false
968#    lw $t0,($sp) # second parameter
972#    beq $t0,$zero,goEsFalse # go return false if null
976#    lw $t1,-12($t0) # vtable pointer
980#    la $t2,CLASS_String # string vtable pointer
984#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
988#    subu $sp,12
992#    sw $ra,8($sp)
996#    sw $s2,4($sp)
1000#    sw $t0,($sp)
1004#    jal mth_compareTo_String
1008#    lw $s2, 4($sp) #restore this pointer and pop parameters
1012#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
1016#    seq $t0,$t0,$zero
1017##...subu $at,$t0,$zero
1021##...sltiu $t0,$at,1

    # pop return address and return $t0
1024#    lw $ra,($sp)
1028#    addu $sp,4

1032#    jr $ra # return
    
1036:goEsFalse:
    # put 0 in $t0 and return
1036#    li $t0, 0
1040#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
1044:mth_toString_String:
    #we are a string, so return this.
1044#    move $t0, $s2
1048#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
1052:mth_readLine_Lib:
    
1052#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
1056#    sw $ra, 4($sp) # save $ra
1060#    move $t1,$sp # save "original" sp

1064:  doRead:

    # read the character
1064#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
1068#    subu $t2,$v0,13
1072#    bne $t2,$zero,notReturnRL
1076#    jal readLogicalChar
1080#    subu $t2,$v0,10 # check for newline
1084#    beq $t2,$zero,foundNewLine
1088#    sw $v0,lastCharRead # push back character into queue
1092#    j foundNewLine # go process the line

1096:  notReturnRL:

    # at this point, $v0 has our character

1096#    subu $t0,$v0,10
1100#    beq $t0,$zero,foundNewLine
1104#    blt $v0,$zero,foundEof
1105##...slt $at,$v0,$zero
1109##...bne $at,$zero,1124

    # we have a character, so push it onto stack
1112#    subu $sp,$sp,4
1116#    sw $v0,($sp)

    # loop back up to get next character
1120#    j doRead

1124:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
1124#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
1128#    li $s7, 0 # return-value: null
1132#    j rlReturn # return

1136:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
1136#    subu $t0,$t1,$sp # number of chars we read (times 4)
1140#    srl $s6,$t0,2 # number of words on stack with chars
1144#    subu $sp,$sp,4 # push char-count ...
1148#    sw $s6,($sp) # ... onto stack
1152#    addu $t2,$t0,5 # GC tag, incl. for count-word
1156#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
1160#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
1164#    srl $s6,$s6,2  # data words in object
1168#    move $s7,$zero # object words in object
1172#    jal newObject  # allocate space
    
    # store header words
1176#    la $t0,CLASS_String
1180#    sw $t0,-12($s7)# store class tag (String) into object
1184#    lw $t2,4($sp) # char-count
1188#    sll $t1,$t2,2 # 4 times number of chars
1192#    subu $t2,$zero,$t2 # negative of char-count
1196#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
1200#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
1204#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
1205##...slt $at,$t0,$zero
1209##...beq $at,$zero,1228
1212#    and $t0,0xff # mask off object's hash value ...
1213##...lui $at,0
1217##...ori $at,$at,0xff
1221##...and $t0,$t0,$at
1224#    sw $t0,-8($s7) # ... and store back into -8 header-word
1228:  skipMask_readLine:

1228#    sll $t0,$t0,2 # data bytes in object
1232#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
1236#    subu $t0,$t0,$t2 # place to store last character (plus 9)
1240#    addu $sp,$sp,8 # pop redundant object-pointer and count
1244#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

1248#    beq $sp,$t1,rlReturn
1252:  charCopyLoop:
1252#    lw $t2,($sp)
1256#    sb $t2,-9($t0)
1260#    addu $sp,$sp,4
1264#    subu $t0,$t0,1
1268#    bne $sp,$t1,charCopyLoop
1272:  rlReturn:

1272#    lw $ra, 4($sp)
1276#    addu $sp, 8   # pop $ra and pointless word off stack
1280#    move $t0, $s7 # return string we allocated in $s7
1284#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1288:mth_readInt_Lib:

    # save $ra by pushing onto stack
1288#    subu $sp,$sp,4
1292#    sw $ra,($sp)

1296:  riSkipWhiteLoop:
    # read a character
1296#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
1300#    subu $t0,$v0,32
1304#    bgt $t0,$zero,nonWhite
1305##...slt $at,$zero,$t0
1309##...bne $at,$zero,1352
1312#    beq $t0,$zero,riSkipWhiteLoop # start over if space
1316#    subu $t0,$v0,10
1320#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
1324#    subu $t0,$v0,9
1328#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
1332#    subu $t0,$v0,13
1336#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1340#    subu $t0,$v0,12
1344#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1348#    j badIntegerFormat # illegal integer char: abort program

1352:  nonWhite:
1352#    subu $t0,$v0,'-'
1356#    li $t4,1 # final multiplier
1360#    bne $t0,$zero,helpReadInt # go read
1364#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1368#    jal readLogicalChar

1372:  helpReadInt:
1372#    li $t2,10
1376#    subu $t1,$v0,'0' # convert digit to 0-9 value
1380#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1381##...sltu $at,$t1,$t2
1385##...beq $at,$zero,3668
1388#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1392:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1392#    jal readLogicalChar # read next character
1396#    subu $t1,$v0,'0' # convert digit to 0-9 value
1400#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1401##...sltu $at,$t1,$t2
1405##...beq $at,$zero,1424

1408#    mul $t3,$t3,$t2 # multiply old value by 10
1409##...mult $t3,$t2
1413##...mflo $t3
1416#    addu $t3,$t3,$t1 # add in value of new digit
1420#    j digitLoop

1424:  doneDigitLoop:

    # "push back" unused character into queue
1424#    sw $v0,lastCharRead

    # restore return address
1428#    lw $ra,($sp)
1432#    addu $sp, 4

    # multiply by sign and put return value in $t0
1436#    mult $t3,$t4 # multiply to account for poss. minus sign
1440#    mflo $t3
1444#    move $t0, $t3
    
    # return
1448#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1452:mth_readChar_Lib:

    # save $ra by pushing onto stack
1452#    subu $sp,$sp,4
1456#    sw $ra,($sp)

      # read the character
1460#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1464#    lw $ra,($sp)
1468#    addu $sp, 4
1472#    move $t0, $v0

    # return
1476#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1480:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1480#    lw $t0,($sp)
1484#    bne $t0,$zero,psNotNull

    # print "(null)"
1488#    la $a0,nullStr
1492#    li $v0,4
1496#    syscall
1500#    j donePrintStr
    
1504:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1504#    subu $t0,$t0,8
1508#    lw $t1,($t0) # number of data words, but needs modification
1512#    blt $t1,$zero,skipPrintStr1
1513##...slt $at,$t1,$zero
1517##...bne $at,$zero,1536
1520#    and $t1,0xff # mask out all but low byte
1521##...lui $at,0
1525##...ori $at,$at,0xff
1529##...and $t1,$t1,$at
1532#    b skipPrintStr2
1536:skipPrintStr1:
1536#    and $t1,0x7ffffff
1537##...lui $at,2047
1541##...ori $at,$at,0xffff
1545##...and $t1,$t1,$at
1548:skipPrintStr2:    
1548#    sll $t1,$t1,2
1552#    subu $t1,$t0,$t1 # first word in string
1556#    lw $t0,4($t0) # negative of string-length
1560#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1564#    beq $t0,$t1,donePrintStr
1568:  psLoop:
1568#    lb $a0,($t1) # next byte
1572#    li $v0,11 # code for printing char
1576#        syscall # print the char
1580#    addu $t1,$t1,1 # go to next char
1584#    blt $t1,$t0,psLoop
1585##...slt $at,$t1,$t0
1589##...bne $at,$zero,1568

1592:  donePrintStr:
1592#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1596:mth_printInt_Lib:
1596#    lw $a0,($sp)

    # print it
1600#    li $v0,1 # code for print-int
1604#    syscall

    # return
1608#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1612:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1612#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1616#    la $a0,falseString
1620#    beq $t0,$zero,skipPB
1624#    la $a0,trueString
1628:  skipPB:
1628#    li $v0,4 # code for print-string
1632#    syscall

    # return
1636#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1640:mth_substring_String:

    # save $ra by pushing onto stack
1640#    subu $sp,$sp,4
1644#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1648#    move $t0,$s2
1652#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1656#    lw $t0,-4($t0) # negative of string length
1660#    subu $t0,$zero,$t0 # string length
1664#    lw $t1,4($sp) # k
1668#    lw $t2,12($sp) # n
1672#    bgt $zero,$t2,strIndexOutOfBounds
1673##...slt $at,$t2,$zero
1677##...bne $at,$zero,3636
1680#    bgt $t2,$t1,strIndexOutOfBounds
1681##...slt $at,$t1,$t2
1685##...bne $at,$zero,3636
1688#    bgt $t1,$t0,strIndexOutOfBounds
1689##...slt $at,$t0,$t1
1693##...bne $at,$zero,3636

    # allocate memory
1696#    subu $s6,$t1,$t2 # # chars in target-string
1700#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1704#    srl $s6,$s6,2 # convert bytes-count to word-count
1708#    move $s7,$zero # (no object-bytes in string)
1712#    jal newObject

    # store "String" tag in object-type field
1716#    la $t0,CLASS_String
1720#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1724#    lw $t4,-8($s7) # data words in source string
1728#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1729##...slt $at,$t4,$zero
1733##...beq $at,$zero,1752
1736#    and $t4,0xff # mask off object's hash value ...
1737##...lui $at,0
1741##...ori $at,$at,0xff
1745##...and $t4,$t4,$at
1748#    sw $t4,-8($s7) # ... and store back into -8 header-word
1752:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1752#    lw $t1,8($sp) # k
1756#    lw $t2,16($sp) # n
1760#    subu $t0,$t2,$t1 # value to store
1764#    sw $t0,-4($s7) # store value
    
1768#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1772#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1776#    lw $t4,-8($t3) # data words in source string
1780#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1784#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1788#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1792#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1796#    lw $t2,-8($s7)   # data words in target string
1800#    sll $t2,$t2,2    # data bytes in target string
1804#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1808:  subCopyLoopzz:
1808#    lb $t4,-8($t3)
1812#    sb $t4,-8($t2)
1816#    addu $t2,$t2,1
1820#    addu $t3,$t3,1
1824#    blt $t3,$t1,subCopyLoopzz
1825##...slt $at,$t3,$t1
1829##...bne $at,$zero,1808
    
1832:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1832#    lw $ra,4($sp) # restore $ra
1836#    addu $sp,$sp,8 # pop ra, and allocated newObject
1840#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1844#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1848:mth_length_String:

    # the length is the negative of 4($s2)
1848#    lw $t0,-4($s2) # -length
1852#    subu $t0,$zero,$t0

    # return
1856#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1860:mth_concat_String:
    # save $ra by pushing onto stack
1860#    subu $sp,$sp,4
1864#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1868#    lw $t0,4($sp)
1872#    beq $t0,$zero,nullPtrException
1876#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1880#    lw $t0,-4($t0) # negative size of second object
1884#    lw $t1,-4($t1) # negative size of first object
1888#    addu $s6,$t0,$t1 # sum of negative sizes
1892#    sra $s6,$s6,2 # negative word-size of char part
1896#    subu $s6,$zero,$s6 # word size of char part
1900#    addu $s6,$s6,1 # data word size, including v-table word
1904#    move $s7,$zero
1908#    jal newObject

    # store String vtable address in vtable address field
1912#    la $t0,CLASS_String
1916#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1920#    lw $t4,-8($s7) # # data words in source string
1924#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1925##...slt $at,$t4,$zero
1929##...beq $at,$zero,1948
1932#    and $t4,0xff # mask off object's hash value ...
1933##...lui $at,0
1937##...ori $at,$at,0xff
1941##...and $t4,$t4,$at
1944#    sw $t4,-8($s7) # ... and store back into -8 header-word
1948:skipMask_concat:

1948#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1952#    move $t1,$s2   # first object
1956#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1960#    lw $t2,-4($t0) # negative length of second object
1964#    lw $t3,-4($t1) # negative length of first object
1968#    addu $t4,$t2,$t3 # sum of negative lengths
1972#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1976#    lw $t4,-8($t1) # # data words in first string
1980#    sll $t4,$t4,2 # # data bytes in first string
1984#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1988#    lw $t4,-8($s7) # # data words in new string
1992#    sll $t4,$t4,2 # # data bytes in new string
1996#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
2000#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
2004#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
2008:  concatLoop1zz:
2008#    lb $t4,-8($t1)
2012#    sb $t4,-8($s7)
2016#    addu $s7,$s7,1
2020#    addu $t1,$t1,1
2024#    blt $t1,$t3,concatLoop1zz
2025##...slt $at,$t1,$t3
2029##...bne $at,$zero,2008
2032:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
2032#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
2036#    lw $t4,-8($t0) # # data words in second string
2040#    sll $t4,$t4,2 # # data bytes in second string
2044#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
2048#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
2052:  concatLoop2zz:
2052#    lb $t4,-8($t1)
2056#    sb $t4,-8($s7)
2060#    addu $s7,$s7,1
2064#    addu $t1,$t1,1
2068#    blt $t1,$t3,concatLoop2zz
2069##...slt $at,$t1,$t3
2073##...bne $at,$zero,2052
2076:  doneConcatLoop2zz:
 
2076:  concatRtnzz:
    # return
2076#    move $t0, $t9 # put saved return string in $t0
2080#    lw $ra, 4($sp)
2084#    addu $sp, 8 # pop ra and allocated newObject
2088#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
2092:mth_charAt_String:
    # get string
2092#    move $t0,$s2

    # check that index is in bounds
2096#    lw $t1,-4($t0) # negative of # data words in string
2100#    subu $t3,$zero,$t1 # # chars in string
2104#    lw $t2,($sp) # index
2108#    bgeu $t2,$t3,strIndexOutOfBounds
2109##...sltu $at,$t2,$t3
2113##...beq $at,$zero,3636

    # access element
2116#    lw $t1,-8($t0) # # data words in object
2120#    sll $t1,$t1,2 # - byte-offset from end of chars
2124#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
2128#    addu $t0,$t0,$t1 # - address of our char, offset by 8
2132#    lb $t0,-8($t0) # our char

    # return
2136#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
2140:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
2140#    subu $sp,$sp,8
2144#    sw $ra,4($sp)

    # save current sp
2148#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
2152#    li $t3,10

    # get argument, negate if negative
2156#    lw $t1,8($sp)
2160#    bge $t1,$zero,itsNonNegLoop
2161##...slt $at,$t1,$zero
2165##...beq $at,$zero,2172
2168#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
2172:  itsNonNegLoop:
2172#    divu $t1,$t3
2176#    mflo $t1 # quotient
2180#    mfhi $t4 # remainder
2184#    addu $t4,$t4,'0' # turn remainder into digit
2188#    subu $sp,$sp,4
2192#    sw $t4,($sp) # push digit onto stack
2196#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
2200#    lw $t4,8($t0)
2204#    bge $t4,$zero,itsSkipNeg
2205##...slt $at,$t4,$zero
2209##...beq $at,$zero,2224
2212#    li $t4,'-'
2216#    subu $sp,$sp,4
2220#    sw $t4,($sp)
2224:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
2224#    subu $s6,$t0,$sp
2228#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
2232#    sw $t3,($t0)
2236#    srl $s6,$s6,2
2240#    subu $sp,$sp,4
2244#    sw $s6,($sp)
    
    # allocate memory
2248#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
2252#    srl $s6,$s6,2
2256#    move $s7,$zero # no "object" words in object
2260#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
2264#    lw $t0,4($sp)
2268#    addu $sp,$sp,8

    # store "String" tag into class field
2272#    subu $s7,$s7,8 # address of header-1 word
2276#    la $t1,CLASS_String
2280#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
2284#    lw $t4,($s7) # # data words in source string
2288#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
2289##...slt $at,$t4,$zero
2293##...beq $at,$zero,2312
2296#    and $t4,0xff # mask off object's hash value ...
2297##...lui $at,0
2301##...ori $at,$at,0xff
2305##...and $t4,$t4,$at
2308#    sw $t4,($s7) # ... and store back into -8 header-word
2312:skipMask_intToString:
    
    # store negative of char-count into header-2 word
2312#    subu $t0,$zero,$t0
2316#    sw $t0,4($s7)

2320#    lw $t1,($s7) # number of data words in string
2324#    sll $t1,$t1,2 # number data bytes in string
2328#    subu $t1,$s7,$t1 # first location to store chars in string
2332#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2336:  itsLoop:
2336#    lw $t2,($sp)
2340#    addu $sp,$sp,4
2344#    sb $t2,($t1)
2348#    addu $t1,$t1,1
2352#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2356#    lw $ra,4($sp)
2360#    addu $sp,$sp,8
2364#    addu $t0,$t0,15
2368#    and $t0,$t0,0xfffffffc
2369##...lui $at,65535
2373##...ori $at,$at,0xfffc
2377##...and $t0,$t0,$at
2380#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2384:mth_intToChar_Lib:
    # save return address
2384#    subu $sp,$sp,4
2388#    sw $ra,($sp)

    # allocate object
2392#    li $s6,2
2396#    move $s7,$zero
2400#    jal newObject

    # get n from string.
2404#    lw $t1,8($sp)

    # store "String" tag into class field
2408#    la $t0,CLASS_String
2412#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2416#    lw $t4,-8($s7) # # data words in source string
2420#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2421##...slt $at,$t4,$zero
2425##...beq $at,$zero,2444
2428#    and $t4,0xff # mask off object's hash value ...
2429##...lui $at,0
2433##...ori $at,$at,0xff
2437##...and $t4,$t4,$at
2440#    sw $t4,-8($s7) # ... and store back into -8 header-word
2444:  skipMask_intToChar:

    # store data in string
2444#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2448#    li $t0,-1
2452#    sw $t0,-4($s7)

    # we're returning $s7
2456#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2460#    lw $ra,4($sp)
2464#    addu $sp,$sp,8

    # return
2468#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2472:mth_compareTo_String:
    # get string pointers and check parameter for null
2472#    lw $t0,($sp) # second string
2476#    beq $t0,$zero,nullPtrException
2480#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2484#    lw $t2,-4($t0) # negative length of second string
2488#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2492#    slt $t4,$t3,$t2
2496#    slt $t5,$t2,$t3
2500#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2504#    lw $t2,-8($t0) # # data words in second string
2508#    lw $t3,-8($t1) # # data words in first string
2512#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2516#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2520#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2524#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2528#    beq $zero,$t5,skipStrCmpLenzz
2532#    move $t2,$t3
2536:  skipStrCmpLenzz:
2536#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2540:  cmpLoopzz:
2540#    lw $t3,-8($t1) # word from str2
2544#    lw $t4,-8($t0) # word from str1
2548#    bne $t3,$t4,cmpNotEqualzz
2552#    addu $t1,$t1,4
2556#    addu $t0,$t0,4
2560#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2564#    j cmpHitLimitzz

2568:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2568#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2572#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2576#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2580:  cmpHitLimitzz:
2580#    move $t0, $t5 # store return value
2584#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2588:readLogicalChar:

    # check if we already have a character
2588#    lw $v0,lastCharRead
2592#    li $t0,-2
2596#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2600#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2604#    jr $ra # return
    
2608:  doReadCh:
    # we need to actually read a character: read and return
2608#    li $v0,12 # use system call to read a character
2612#    syscall
2616#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2620:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2620#    blt $s7,$zero arraySizeOutOfBounds
2621##...slt $at,$s7,$zero
2625##...bne $at,$zero,3652

    # mark the fact that we have not yet GC'd for this allocation
2628#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2632:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2632#    addu $t0,$s6,$s7
2636#    sll $t0,$t0,2
2640#    addu $t0,$t0,8 # amount of memory needed
2644#    addu $t1,$t0,$s3
2648#    bgtu $t1,$s4,doGC
2649##...sltu $at,$s4,$t1
2653##...bne $at,$zero,2776

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2656#    move $t2,$s3
2660:  zeroObjLoop:
2660#    sw $zero,($s3)
2664#    lw $zero,($s3)

2668#    addu $s3,4
2672#    bltu $s3,$t1,zeroObjLoop
2673##...sltu $at,$s3,$t1
2677##...bne $at,$zero,2660

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2680#    sll $t0,$s7,2    # number of post-header bytes
2684#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2688#    li $t2,255
2692#    bgeu $s6,$t2 firstHeaderLarge
2693##...sltu $at,$s6,$t2
2697##...beq $at,$zero,2744
2700#    lw $t1,prevHashConstant
2704#    addu $t1,0x12b68fd2
2705##...lui $at,4791
2709##...addiu $at,$at,-28718
2713##...addu $t1,$at,$t1
2716#    sw $t1,prevHashConstant # update hash constant
2720#    or $t1,0x80000000 # set top bit
2721##...lui $at,32768
2725##...or $t1,$at,$t1
2728#    and $t1,0xffffff00 # clear low bit
2729##...lui $at,65535
2733##...ori $at,$at,0xff00
2737##...and $t1,$t1,$at
2740#    or $s6,$t1
2744:firstHeaderLarge:
2744#    sw $s6,-8($t0) # store header-1 value
2748#    lw $zero,-8($t0)

    # store second header-value
2752#    sw $s7,-4($t0)   # second header-word
2756#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2760#    move $s7,$t0
2764#    subu $sp,$sp,4
2768#    sw $t0,($sp)

2772#    jr $ra
    
2776:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2776#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2780#    lw $t5,gcCount
2784#    addu $t5,1
2788#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2792#    subu $sp,$sp,4
2796#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2800#    lw $t0,cur_seg
2804#    move $t7,$s4
2808#    la $t6,seg1_start
2809##...lui $t6,16
2813##...addiu $t6,$t6,4268
2816#    la $s3,seg0_start
2820#    la $s4,seg0_end
2821##...lui $s4,16
2825##...addiu $s4,$s4,4268
2828#    sw $zero,cur_seg
2832#    bne $t0,$zero,skipGc1
2836#    la $t6,seg0_start
2840#    la $s3,seg1_start
2841##...lui $s3,16
2845##...addiu $s3,$s3,4268
2848#    la $s4,seg1_end
2849##...lui $s4,32
2853##...addiu $s4,$s4,4268
2856#    sw $s5,cur_seg
2860:  skipGc1:
2860#    li $t5,0x80000000
2864#    lw $t0,stack_bottom
2868#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2872:  gcStackCopyLoop:
2872#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2876#    sll $t2,$t1,31 # zero iff low bit was zero
2880#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2884#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2888#    bleu $t1,$t6,gcTestIterDone1
2889##...sltu $at,$t6,$t1
2893##...beq $at,$zero,3056
2896#    bgtu $t1,$t7,gcTestIterDone1
2897##...sltu $at,$t7,$t1
2901##...bne $at,$zero,3056

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2904#    lw $t2,-8($t1) # possible forwarding tag
2908#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2912#    lw $t2,-4($t1) # forwarding pointer: object's new address
2916#    sw $t2,4($t0) # update stack value
2920#    j gcTestIterDone1 # do next iteration

2924:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2924#    bge $t2,$zero,gcSkip2
2925##...slt $at,$t2,$zero
2929##...beq $at,$zero,2944
2932#    and $t2,0xff #### mask off hash value
2933##...lui $at,0
2937##...ori $at,$at,0xff
2941##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2944:  gcSkip2:
2944#    sll $t2,$t2,2
2948#    addu $t2,$t2,8
2952#    subu $t4,$t1,$t2 # address of first word of source
2956#    lw $t3,-4($t1) # number of object words (negative means zero)
2960#    bge $t3,$zero,gcH2Neg1
2961##...slt $at,$t3,$zero
2965##...beq $at,$zero,2972
2968#    move $t3,$zero
2972:  gcH2Neg1:
2972#    sll $t3,$t3,2
2976#    addu $t3,$t3,$t1 # address one word past last word of source

2980#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2984#    lw $s0,($t4)
2988#    lw $s1,-8($t1)
2992#    sw $s1,($t4)
2996#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
3000:  gCinnerCopy1:
3000#    lw $s0,($t4)
3004#    sw $s0,($s3)
3008#    addu $t4,$t4,4
3012#    addu $s3,$s3,4
3016#    bltu $t4,$t3,gCinnerCopy1
3017##...sltu $at,$t4,$t3
3021##...bne $at,$zero,3000

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3024#    sw $zero,-8($t1) # 0 in first header-slot
3028#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
3032#    sw $t2,4($t0)
3036#    lw $zero,-8($t2)
3040#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
3044#    j gcTestIterDone1

3048:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
3048#    subu $t0,$t0,$t1
3052#    subu $t0,$t0,3

3056:  gcTestIterDone1:
3056#    bgeu $t0,$sp,gcStackCopyLoop
3057##...sltu $at,$t0,$sp
3061##...beq $at,$zero,2872

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
3064#    lw $t1,cur_seg
3068#    la $t0,seg0_start
3072#    beq $t1,$zero,gcSkip4
3076#    la $t0,seg1_start
3077##...lui $t0,16
3081##...addiu $t0,$t0,4268


3084:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
3084#    bgeu $t0,$s3,gcFinishUp
3085##...sltu $at,$t0,$s3
3089##...beq $at,$zero,3380

    ###### begin outer loop to copy all non-stack-direct objects ######

3092:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
3092#    lw $t1,($t0) # (what will be) first header word for current object
3096#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
3100#    bge $t2,$zero,gcSkip4a # are there hash bits?
3101##...slt $at,$t2,$zero
3105##...beq $at,$zero,3120
3108#    and $t2,0xff # mask off hash bits
3109##...lui $at,0
3113##...ori $at,$at,0xff
3117##...and $t2,$t2,$at
3120:gcSkip4a:    
3120#    sll $t2,$t2,2 # # data bytes
3124#    addu $t2,$t2,8 # to skip header words
3128#    addu $t2,$t0,$t2 # pointer to new object
3132#    lw $t3,-8($t2) # word to swap
3136#    sw $t1,-8($t2) # store header word
3140#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
3144#    lw $t1,-12($t2) # vtable pointer
3148#    li $t0,dataArrayVTableStart # address of vtable for first data array
3152#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
3153##...slt $at,$t1,$t0
3157##...bne $at,$zero,3176

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
3160:  skipToNextObj:
3160#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
3164#    sll $t1,$t1,2 # convert to byte-count
3168#    addu $t0,$t2,$t1 # skip over object
3172#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
3176:  gcNotDataArray:
3176#    lw $t3,-4($t2) # # object words (negative treated as zero)
3180#    bge $t3,$zero,gcH2Neg2
3181##...slt $at,$t3,$zero
3185##...beq $at,$zero,3192
3188#    move $t3,$zero
3192:  gcH2Neg2:
3192#    sll $t3,$t3,2 # # object bytes
3196#    move $t0,$t2 # address of first pointer in object
3200#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
3204#    beq $t0,$s1,gcTestIterDone2
    
3208:  gcNextPointerInObject:
    # get pointer from object
3208#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
3212#    bleu $t1,$t6,gcGoNextPointerSlot
3213##...sltu $at,$t6,$t1
3217##...beq $at,$zero,3360
3220#    bgtu $t1,$t7,gcGoNextPointerSlot
3221##...sltu $at,$t7,$t1
3225##...bne $at,$zero,3360

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3228#    lw $t8,-8($t1) # possible forwarding tag
3232#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
3236#    lw $t8,-4($t1) # forwarding pointer: object's new address
3240#    sw $t8,($t0) # update pointer in object
3244#    j gcGoNextPointerSlot # do next iteration

3248:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
3248#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
3249##...slt $at,$t8,$zero
3253##...beq $at,$zero,3268
3256#    and $t8,0xff # mask off hash bits
3257##...lui $at,0
3261##...ori $at,$at,0xff
3265##...and $t8,$t8,$at

3268:  gcSkip5:
3268#    sll $t8,$t8,2 # bytes in negative-offset portion ...
3272#    addu $t8,$t8,8 # ... add in bytes for header-words
3276#    subu $t4,$t1,$t8 # address of first word of source object
3280#    lw $t3,-4($t1) # # words in positive-offset portion
3284#    bge $t3,$zero,gcNoNeg
3285##...slt $at,$t3,$zero
3289##...beq $at,$zero,3296
3292#    move $t3,$zero # negative treated as zero
3296:  gcNoNeg:
3296#    sll $t3,$t3,2 # bytes in negative-offset portion
3300#    addu $t3,$t3,$t1 # address one word past last word of source

3304#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
3308#    lw $s0,($t4)
3312#    lw $t9,-8($t1)
3316#    sw $t9,($t4)
3320#    sw $s0,-8($t1)

    # copy all source bytes to the target
3324:  gCinnerCopy2:
3324#    lw $s0,($t4)
3328#    sw $s0,($s3)
3332#    addu $t4,$t4,4
3336#    addu $s3,$s3,4
3340#    bltu $t4,$t3,gCinnerCopy2
3341##...sltu $at,$t4,$t3
3345##...bne $at,$zero,3324

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3348#    sw $zero,-8($t1) # 0 in first header-slot
3352#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3356#    sw $t8,($t0)

3360:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3360#    addu $t0,$t0,4
3364#    bltu $t0,$s1,gcNextPointerInObject
3365##...sltu $at,$t0,$s1
3369##...bne $at,$zero,3208

3372:  gcTestIterDone2:
3372#    bltu $t0,$s3,gcHeapCopyLoop
3373##...sltu $at,$t0,$s3
3377##...bne $at,$zero,3092

    ###### end outer loop to copy all non-stack-direct objects ######

3380:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3380#    lw $s2,($sp)
3384#    addu $sp,$sp,4

    # mark us as having already GC'd
3388#    move $t5,$s5

    # go try and allocate again
3392#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3396:vm_init:

    # mark bottom of stack
3396#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3400#    move $s2,$zero

    #set up the "five" register
3404#    li $s5,5

3408#    la $s3,seg0_start
3412#    la $s4,seg0_end
3413##...lui $s4,16
3417##...addiu $s4,$s4,4268
    
    # return
3420#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3424:divide:
    # get parameters; abort if divisor zero
3424#    lw $t0,($sp)
3428#    lw $t1,8($sp)
3432#    beq $t0,$zero,divByZeroError
    
    # perform division
3436#    div $t1,$t0
3440#    mflo $t0

    # store result, adjust stack and return
3444#    addu $sp,$sp,8 # adjust stack
3448#    sw $t0,($sp) # store result
3452#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3456:remainder:
    # get parameters; abort if divisor zero
3456#    lw $t0,($sp)
3460#    lw $t1,8($sp)
3464#    beq $t0,$zero,divByZeroError
    
    # perform division
3468#    div $t1,$t0
3472#    mfhi $t0

    # store result, adjust stack and return
3476#    addu $sp,$sp,8 # adjust stack
3480#    sw $t0,($sp) # store result
3484#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3488:checkCast:
    # get object, allow immediately if null
3488#    lw $t2,($sp)
3492#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3496#    lw $t2,-12($t2) # vtable address
3500#    bge $t2,$t1,castException
3501##...slt $at,$t2,$t1
3505##...beq $at,$zero,3676
3508#    blt $t2,$t0,castException
3509##...slt $at,$t2,$t0
3513##...bne $at,$zero,3676
    
    # return, leaving object unchanged on stack
3516:  checkCastReturn:    
3516#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3520:instanceOf:
    # get object, go return false if null
3520#    lw $t2,($sp)
3524#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3528#    lw $t2,-12($t2) # vtable address
3532#    sge $t0,$t2,$t0 # are we >= the lower limit?
3533##...slt $t0,$t2,$t0
3537##...xori $t0,$t0,0x1
3540#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3544#    and $t2,$t0,$t1
3548#    sw $t2,($sp)
3552:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3552#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3556:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3556#    li $v0,4 # syscall-code for print-string
3560#    syscall
3564:exit:
    # print termination message
3564#    li $v0,4 # syscall-code for print-string
3568#    la $a0,exitString
3572#    syscall

    # print number of garbage collections
3576#    li $v0,4 # syscall-code for print-string
3580#    la $a0,gcCountMsg
3584#    syscall # print "Number of ..."
3588#    lw $a0,gcCount # number of GC's
3592#    li $v0,1 # code for print-int
3596#    syscall
3600#    li $v0,4 # syscall-code for print-string
3604#    la $a0,newlineString
3608#    syscall # print newline

    # terminate execution
3612#    li $v0,10 #syscall-code for "exit"
3616#    syscall

3620:outOfHeapMemory:
3620#    la $a0,heapMemoryMsg
3624#    j exitError
3628:divByZeroError:
3628#    la $a0,divByZeroMsg
3632#    j exitError
3636:strIndexOutOfBounds:
3636#    la $a0,strIndexOutOfBoundsMsg
3640#    j exitError
3644:arrayIndexOutOfBounds:
3644#    la $a0,arrayIndexOutOfBoundsMsg
3648#    j exitError
3652:arraySizeOutOfBounds:
3652#    la $a0,arraySizeOutOfBoundsMsg
3656#    j exitError
3660:nullPtrException:
3660#    la $a0,nullPtrExceptionMsg
3664#    j exitError
3668:badIntegerFormat:
3668#    la $a0,badIntegerFormatMsg
3672#    j exitError
3676:castException:
3676#    la $a0,castExceptionMsg
3680#    j exitError
############## data section ################
    .data
3896:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3912:cur_seg:
    .word 0

3916:lastCharRead:
    .word -2 # -2 => no buffered character
3920:prevHashConstant:
    .word 0x2785173c
3924:gcCount:
    .word 0

3928:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3955:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3978:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
4013:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
4047:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
4080:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
4111:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
4159:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
4180:gcMsg: 
      .asciiz "\nGC!\n"
4186:gcCountMsg: 
      .asciiz "Number of garbage collections: "

4218:nullStr:
    .asciiz "null"
4223:trueString:
    .asciiz "true"
4228:falseString:
    .asciiz "false"
4234:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
4262:newlineString:
    .asciiz "\n"
    
    .align 2
4264:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

4268:seg0_start:
    .space 0x100000
1052844:seg0_end:
1052844:seg1_start:
    .space 0x100000
2101420:seg1_end:
