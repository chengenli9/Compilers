 .data # Program at 3.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3620:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3640:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3672:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3676:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 3.7
  .byte 205 # 'M' with high bit set # ClassDecl at 3.7
  .byte 97 # 'a' # ClassDecl at 3.7
  .byte 105 # 'i' # ClassDecl at 3.7
  .byte 110 # 'n' # ClassDecl at 3.7
3724:CLASS_Main: # ClassDecl at 3.7
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 4.17
3772:END_CLASS_Main: # ClassDecl at 3.7
3772:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3780:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3792:END_CLASS_RunMain: # ClassDecl at 0.0
3792:dataArrayVTableStart: # IntType at 0.0
  .byte 0 # ArrayType at 5.14
  .byte 0 # ArrayType at 5.14
  .byte 219 # '[' with high bit set # ArrayType at 5.14
  .byte 73 # 'I' # ArrayType at 5.14
3796:CLASS_ARRAY_INT: # ArrayType at 5.14
  .word mth_hashCode_Object # ArrayType at 5.14
  .word mth_equals_Object # ArrayType at 5.14
  .word mth_toString_Object # ArrayType at 5.14
3808:END_CLASS_ARRAY_INT: # ArrayType at 5.14
3808:END_CLASS_Object: # ClassDecl at 0.0
  .byte 10 # StringLit at 8.17
  .byte 0 # StringLit at 8.17
  .byte 0 # StringLit at 8.17
  .byte 0 # StringLit at 8.17
  .word CLASS_String # StringLit at 8.17
  .word 2 # StringLit at 8.17
  .word -1 # StringLit at 8.17
3824:strLit_17: # StringLit at 8.17
  .byte 83 # StringLit at 10.17
  .byte 104 # StringLit at 10.17
  .byte 111 # StringLit at 10.17
  .byte 117 # StringLit at 10.17
  .byte 108 # StringLit at 10.17
  .byte 100 # StringLit at 10.17
  .byte 32 # StringLit at 10.17
  .byte 110 # StringLit at 10.17
  .byte 111 # StringLit at 10.17
  .byte 116 # StringLit at 10.17
  .byte 32 # StringLit at 10.17
  .byte 103 # StringLit at 10.17
  .byte 101 # StringLit at 10.17
  .byte 116 # StringLit at 10.17
  .byte 32 # StringLit at 10.17
  .byte 104 # StringLit at 10.17
  .byte 101 # StringLit at 10.17
  .byte 114 # StringLit at 10.17
  .byte 101 # StringLit at 10.17
  .byte 46 # StringLit at 10.17
  .byte 10 # StringLit at 10.17
  .byte 0 # StringLit at 10.17
  .byte 0 # StringLit at 10.17
  .byte 0 # StringLit at 10.17
  .word CLASS_String # StringLit at 10.17
  .word 7 # StringLit at 10.17
  .word -21 # StringLit at 10.17
3860:strLit_27: # StringLit at 10.17
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp, $sp, 4
28#  move $s2, $s7
32#  jal mth_main_Main
36#  li $v0, 10
40#  syscall
.globl mth_main_Main
44:mth_main_Main:
44#  subu $sp, $sp, 4
48#  sw $ra, ($sp)
52#  li $t0, 54
56#  subu $sp, $sp, 8
60#  sw $zero, 4($sp)
64#  sw $t0, ($sp)
68#  li $s6, 1
72#  lw $s7, ($sp)
76#  addu $sp, $sp, 8
80#  jal newObject
84#  la $t0, CLASS_ARRAY_INT
88#  sw $t0, -12($s7)
92#  li $t0, 53
96#  subu $sp, $sp, 8
100#  sw $zero, 4($sp)
104#  sw $t0, ($sp)
108#  lw $t0, 8($sp)
112#  subu $sp, $sp, 4
116#  sw $t0, ($sp)
120#  lw $t0, ($sp)
124#  addu $sp, $sp, 4
128#  lw $t1, ($sp)
132#  addu $sp, $sp, 8
136#  beq $t0, $zero, nullPtrException
140#  lw $t3, -4($t0)
144#  bgeu $t1, $t3, arrayIndexOutOfBounds
145##...sltu $at,$t1,$t3
149##...beq $at,$zero,3572
152#  sll $t1, $t1, 2
156#  addu $t1, $t1, $t0
160#  lw $t0, ($t1)
164#  subu $sp, $sp, 8
168#  sw $zero, 4($sp)
172#  sw $t0, ($sp)
176#  subu $sp, $sp, 4
180#  sw $s2, ($sp)
184#  lw $t0, 4($sp)
188#  subu $sp, $sp, 8
192#  sw $zero, 4($sp)
196#  sw $t0, ($sp)
200#  lw $t0, 8($sp)
204#  sw $s2, 8($sp)
208#  move $s2, $t0
212#  jal mth_printInt_Lib
216#  addu $sp, $sp, 8
220#  lw $s2, ($sp)
224#  addu $sp, $sp, 4
228#  subu $sp, $sp, 4
232#  sw $t0, ($sp)
236#  lw $t0, ($sp)
240#  addu $sp, $sp, 4
244#  subu $sp, $sp, 4
248#  sw $s2, ($sp)
252#  la $t0, strLit_17
256#  subu $sp, $sp, 4
260#  sw $t0, ($sp)
264#  lw $t0, 4($sp)
268#  sw $s2, 4($sp)
272#  move $s2, $t0
276#  jal mth_printStr_Lib
280#  addu $sp, $sp, 4
284#  lw $s2, ($sp)
288#  addu $sp, $sp, 4
292#  subu $sp, $sp, 4
296#  sw $t0, ($sp)
300#  lw $t0, ($sp)
304#  addu $sp, $sp, 4
308#  li $t0, 54
312#  subu $sp, $sp, 8
316#  sw $zero, 4($sp)
320#  sw $t0, ($sp)
324#  lw $t0, 16($sp)
328#  subu $sp, $sp, 4
332#  sw $t0, ($sp)
336#  lw $t0, ($sp)
340#  addu $sp, $sp, 4
344#  lw $t1, ($sp)
348#  addu $sp, $sp, 8
352#  beq $t0, $zero, nullPtrException
356#  lw $t3, -4($t0)
360#  bgeu $t1, $t3, arrayIndexOutOfBounds
361##...sltu $at,$t1,$t3
365##...beq $at,$zero,3572
368#  sll $t1, $t1, 2
372#  addu $t1, $t1, $t0
376#  lw $t0, ($t1)
380#  subu $sp, $sp, 8
384#  sw $zero, 4($sp)
388#  sw $t0, ($sp)
392#  subu $sp, $sp, 4
396#  sw $s2, ($sp)
400#  la $t0, strLit_27
404#  subu $sp, $sp, 4
408#  sw $t0, ($sp)
412#  lw $t0, 4($sp)
416#  sw $s2, 4($sp)
420#  move $s2, $t0
424#  jal mth_printStr_Lib
428#  addu $sp, $sp, 4
432#  lw $s2, ($sp)
436#  addu $sp, $sp, 4
440#  subu $sp, $sp, 4
444#  sw $t0, ($sp)
448#  lw $t0, ($sp)
452#  addu $sp, $sp, 4
456#  addu $sp, $sp, 20
460#  lw $ra, ($sp)
464#  addu $sp, $sp, 4
468#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
472:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
472#    lw $t0,-8($s2) # get word with hash bits from this
476#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
480#    addu $t0,$t1

484#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
488:mth_equals_Object:
488#    move $t0,$s2 # first pointer (this)
492#    lw $t1,($sp) # second pointer
496#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
497##...subu $at,$t0,$t1
501##...sltiu $t0,$at,1

504#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
508:mth_hashCode_String:
508#    move $t0,$s2 # put this pointer in t0

512#    li $t2,0 # current hash value
516#    lw $t1,-4($t0) # negative of number of characters in string
520#    beq $t1,$zero,done_hashCode_String # if zero length => done
524#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
528#    and $t0,0xfffffffc # adjust for padding
529##...lui $at,65535
533##...ori $at,$at,0xfffc
537##...and $t0,$t0,$at
540#    subu $t1,$t0,$t1 # address at end of string +12
544:loop_hashCode_String:    
544#    sll $t4,$t2,5 # multiply by 32
548#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
552#    lbu $t3,-12($t0) # current character
556#    addu $t2,$t3 # add in current character
560#    addu $t0,1 # go to next character's address
564#    bne $t0,$t1,loop_hashCode_String # loop back if more
568:done_hashCode_String:    

    # push int on stack
568#    move $t0, $t2 # put $t2 in return slot
572#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
576:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
576#    subu $sp,4
580#    sw $ra,($sp)
    
    # get hash value
584#    lw $t2,-12($s2) # vtable address
588#    lw $t0,($t2) # method address
592#    jalr $t0
596#    subu $sp,8 # push result (int in $t0) on stack
600#    sw $s5,4($sp)
604#    sw $t0,($sp)

    # compute print-size of hash value
608#    li $t0, 1
612#    lw $t1, ($sp)
616#    srl $t1,$t1,4
620#    beq $t1,$zero,toString_skip_loop
624:toString_loop_top:
624#    addu $t0,1
628#    srl $t1,$t1,4
632#    bne $t1,$zero,toString_loop_top
636:toString_skip_loop:
636#    subu $sp,8
640#    sw $s5,4($sp)
644#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
648#    lw $t2,-12($s2) # vtable address
652#    move $t0,$t2
656:toString_checkTagBit:
656#    subu $t0,1
660#    lb $t1,($t0)
664#    and $t1,0x80
665##...lui $at,0
669##...ori $at,$at,0x80
673##...and $t1,$t1,$at
676#    beq $t1,$zero,toString_checkTagBit
680#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
684#    subu $sp,8
688#    sw $t1,($sp)
692#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
696#    move $s7,$zero # -4 offset header word
700#    lw $t4,8($sp) # bytes for hash value
704#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
708#    addu $t4,8
712#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
716#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

720#    li $t0,CLASS_String # store string vtable address into ...
724#    sw $t0,-12($s7) # ... -12 offset in object header
728#    lw $t2,-12($s2) # our object's vtable address
732#    lw $t5,4($sp) # number characters in prefix
736#    lw $t1,12($sp) # number of characters in hashcode
740#    addu $t3,$t1,$t5 # number of characters in string ...
744#    addu $t3,1 # ... including the '@' character
748#    subu $t0,$zero,$t3 # store negated length into ...
752#    sw $t0,-4($s7) # ... -4 offset header word
756#    lw $t0,-8($s7) # get -8 offset header-word
760#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
761##...slt $at,$t0,$zero
765##...beq $at,$zero,784
768#    and $t0,0xff # mask off object's hash value ...
769##...lui $at,0
773##...ori $at,$at,0xff
777##...and $t0,$t0,$at
780#    sw $t0,-8($s7) # ... and store back into -8 header-word
784:skipMask_toString:
784#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
788#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
792#    subu $t4,$t2,$t5
796:toString_copyLoop:    
796#    lb $t3,($t4)
800#    and $t3,0x7f # mask off sign bit (in case first word)
801##...lui $at,0
805##...ori $at,$at,0x7f
809##...and $t3,$t3,$at
812#    sb $t3,-8($t0)
816#    addu $t4,1
820#    addu $t0,1
824#    blt $t4,$t2,toString_copyLoop
825##...slt $at,$t4,$t2
829##...bne $at,$zero,796

    # copy the '@' sign
832#    li $t3,'@'
836#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
840#    addu $t2,$t0,$t1 # address of last byte in string to store +8
844#    lw $t3,20($sp) # hash code to print
848:toString_hashCopyLoop:
848#    and $t4,$t3,0xf
852#    srl $t3,4
856#    lb $t4,hexChars($t4)
860#    sb $t4,-8($t2)
864#    subu $t2,$t2,1
868#    bgt $t2,$t0,toString_hashCopyLoop
869##...slt $at,$t0,$t2
873##...bne $at,$zero,848

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
876#    addu $sp,28
880#    lw $ra,($sp)
884#    addu $sp,4
888#    move $t0, $s7 # return string we created
892#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
896:mth_equals_String:
    
    # quick test for obvious false
896#    lw $t0,($sp) # second parameter
900#    beq $t0,$zero,goEsFalse # go return false if null
904#    lw $t1,-12($t0) # vtable pointer
908#    la $t2,CLASS_String # string vtable pointer
912#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
916#    subu $sp,12
920#    sw $ra,8($sp)
924#    sw $s2,4($sp)
928#    sw $t0,($sp)
932#    jal mth_compareTo_String
936#    lw $s2, 4($sp) #restore this pointer and pop parameters
940#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
944#    seq $t0,$t0,$zero
945##...subu $at,$t0,$zero
949##...sltiu $t0,$at,1

    # pop return address and return $t0
952#    lw $ra,($sp)
956#    addu $sp,4

960#    jr $ra # return
    
964:goEsFalse:
    # put 0 in $t0 and return
964#    li $t0, 0
968#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
972:mth_toString_String:
    #we are a string, so return this.
972#    move $t0, $s2
976#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
980:mth_readLine_Lib:
    
980#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
984#    sw $ra, 4($sp) # save $ra
988#    move $t1,$sp # save "original" sp

992:  doRead:

    # read the character
992#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
996#    subu $t2,$v0,13
1000#    bne $t2,$zero,notReturnRL
1004#    jal readLogicalChar
1008#    subu $t2,$v0,10 # check for newline
1012#    beq $t2,$zero,foundNewLine
1016#    sw $v0,lastCharRead # push back character into queue
1020#    j foundNewLine # go process the line

1024:  notReturnRL:

    # at this point, $v0 has our character

1024#    subu $t0,$v0,10
1028#    beq $t0,$zero,foundNewLine
1032#    blt $v0,$zero,foundEof
1033##...slt $at,$v0,$zero
1037##...bne $at,$zero,1052

    # we have a character, so push it onto stack
1040#    subu $sp,$sp,4
1044#    sw $v0,($sp)

    # loop back up to get next character
1048#    j doRead

1052:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
1052#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
1056#    li $s7, 0 # return-value: null
1060#    j rlReturn # return

1064:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
1064#    subu $t0,$t1,$sp # number of chars we read (times 4)
1068#    srl $s6,$t0,2 # number of words on stack with chars
1072#    subu $sp,$sp,4 # push char-count ...
1076#    sw $s6,($sp) # ... onto stack
1080#    addu $t2,$t0,5 # GC tag, incl. for count-word
1084#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
1088#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
1092#    srl $s6,$s6,2  # data words in object
1096#    move $s7,$zero # object words in object
1100#    jal newObject  # allocate space
    
    # store header words
1104#    la $t0,CLASS_String
1108#    sw $t0,-12($s7)# store class tag (String) into object
1112#    lw $t2,4($sp) # char-count
1116#    sll $t1,$t2,2 # 4 times number of chars
1120#    subu $t2,$zero,$t2 # negative of char-count
1124#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
1128#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
1132#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
1133##...slt $at,$t0,$zero
1137##...beq $at,$zero,1156
1140#    and $t0,0xff # mask off object's hash value ...
1141##...lui $at,0
1145##...ori $at,$at,0xff
1149##...and $t0,$t0,$at
1152#    sw $t0,-8($s7) # ... and store back into -8 header-word
1156:  skipMask_readLine:

1156#    sll $t0,$t0,2 # data bytes in object
1160#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
1164#    subu $t0,$t0,$t2 # place to store last character (plus 9)
1168#    addu $sp,$sp,8 # pop redundant object-pointer and count
1172#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

1176#    beq $sp,$t1,rlReturn
1180:  charCopyLoop:
1180#    lw $t2,($sp)
1184#    sb $t2,-9($t0)
1188#    addu $sp,$sp,4
1192#    subu $t0,$t0,1
1196#    bne $sp,$t1,charCopyLoop
1200:  rlReturn:

1200#    lw $ra, 4($sp)
1204#    addu $sp, 8   # pop $ra and pointless word off stack
1208#    move $t0, $s7 # return string we allocated in $s7
1212#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1216:mth_readInt_Lib:

    # save $ra by pushing onto stack
1216#    subu $sp,$sp,4
1220#    sw $ra,($sp)

1224:  riSkipWhiteLoop:
    # read a character
1224#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
1228#    subu $t0,$v0,32
1232#    bgt $t0,$zero,nonWhite
1233##...slt $at,$zero,$t0
1237##...bne $at,$zero,1280
1240#    beq $t0,$zero,riSkipWhiteLoop # start over if space
1244#    subu $t0,$v0,10
1248#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
1252#    subu $t0,$v0,9
1256#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
1260#    subu $t0,$v0,13
1264#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1268#    subu $t0,$v0,12
1272#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1276#    j badIntegerFormat # illegal integer char: abort program

1280:  nonWhite:
1280#    subu $t0,$v0,'-'
1284#    li $t4,1 # final multiplier
1288#    bne $t0,$zero,helpReadInt # go read
1292#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1296#    jal readLogicalChar

1300:  helpReadInt:
1300#    li $t2,10
1304#    subu $t1,$v0,'0' # convert digit to 0-9 value
1308#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1309##...sltu $at,$t1,$t2
1313##...beq $at,$zero,3596
1316#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1320:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1320#    jal readLogicalChar # read next character
1324#    subu $t1,$v0,'0' # convert digit to 0-9 value
1328#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1329##...sltu $at,$t1,$t2
1333##...beq $at,$zero,1352

1336#    mul $t3,$t3,$t2 # multiply old value by 10
1337##...mult $t3,$t2
1341##...mflo $t3
1344#    addu $t3,$t3,$t1 # add in value of new digit
1348#    j digitLoop

1352:  doneDigitLoop:

    # "push back" unused character into queue
1352#    sw $v0,lastCharRead

    # restore return address
1356#    lw $ra,($sp)
1360#    addu $sp, 4

    # multiply by sign and put return value in $t0
1364#    mult $t3,$t4 # multiply to account for poss. minus sign
1368#    mflo $t3
1372#    move $t0, $t3
    
    # return
1376#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1380:mth_readChar_Lib:

    # save $ra by pushing onto stack
1380#    subu $sp,$sp,4
1384#    sw $ra,($sp)

      # read the character
1388#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1392#    lw $ra,($sp)
1396#    addu $sp, 4
1400#    move $t0, $v0

    # return
1404#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1408:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1408#    lw $t0,($sp)
1412#    bne $t0,$zero,psNotNull

    # print "(null)"
1416#    la $a0,nullStr
1420#    li $v0,4
1424#    syscall
1428#    j donePrintStr
    
1432:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1432#    subu $t0,$t0,8
1436#    lw $t1,($t0) # number of data words, but needs modification
1440#    blt $t1,$zero,skipPrintStr1
1441##...slt $at,$t1,$zero
1445##...bne $at,$zero,1464
1448#    and $t1,0xff # mask out all but low byte
1449##...lui $at,0
1453##...ori $at,$at,0xff
1457##...and $t1,$t1,$at
1460#    b skipPrintStr2
1464:skipPrintStr1:
1464#    and $t1,0x7ffffff
1465##...lui $at,2047
1469##...ori $at,$at,0xffff
1473##...and $t1,$t1,$at
1476:skipPrintStr2:    
1476#    sll $t1,$t1,2
1480#    subu $t1,$t0,$t1 # first word in string
1484#    lw $t0,4($t0) # negative of string-length
1488#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1492#    beq $t0,$t1,donePrintStr
1496:  psLoop:
1496#    lb $a0,($t1) # next byte
1500#    li $v0,11 # code for printing char
1504#        syscall # print the char
1508#    addu $t1,$t1,1 # go to next char
1512#    blt $t1,$t0,psLoop
1513##...slt $at,$t1,$t0
1517##...bne $at,$zero,1496

1520:  donePrintStr:
1520#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1524:mth_printInt_Lib:
1524#    lw $a0,($sp)

    # print it
1528#    li $v0,1 # code for print-int
1532#    syscall

    # return
1536#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1540:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1540#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1544#    la $a0,falseString
1548#    beq $t0,$zero,skipPB
1552#    la $a0,trueString
1556:  skipPB:
1556#    li $v0,4 # code for print-string
1560#    syscall

    # return
1564#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1568:mth_substring_String:

    # save $ra by pushing onto stack
1568#    subu $sp,$sp,4
1572#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1576#    move $t0,$s2
1580#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1584#    lw $t0,-4($t0) # negative of string length
1588#    subu $t0,$zero,$t0 # string length
1592#    lw $t1,4($sp) # k
1596#    lw $t2,12($sp) # n
1600#    bgt $zero,$t2,strIndexOutOfBounds
1601##...slt $at,$t2,$zero
1605##...bne $at,$zero,3564
1608#    bgt $t2,$t1,strIndexOutOfBounds
1609##...slt $at,$t1,$t2
1613##...bne $at,$zero,3564
1616#    bgt $t1,$t0,strIndexOutOfBounds
1617##...slt $at,$t0,$t1
1621##...bne $at,$zero,3564

    # allocate memory
1624#    subu $s6,$t1,$t2 # # chars in target-string
1628#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1632#    srl $s6,$s6,2 # convert bytes-count to word-count
1636#    move $s7,$zero # (no object-bytes in string)
1640#    jal newObject

    # store "String" tag in object-type field
1644#    la $t0,CLASS_String
1648#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1652#    lw $t4,-8($s7) # data words in source string
1656#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1657##...slt $at,$t4,$zero
1661##...beq $at,$zero,1680
1664#    and $t4,0xff # mask off object's hash value ...
1665##...lui $at,0
1669##...ori $at,$at,0xff
1673##...and $t4,$t4,$at
1676#    sw $t4,-8($s7) # ... and store back into -8 header-word
1680:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1680#    lw $t1,8($sp) # k
1684#    lw $t2,16($sp) # n
1688#    subu $t0,$t2,$t1 # value to store
1692#    sw $t0,-4($s7) # store value
    
1696#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1700#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1704#    lw $t4,-8($t3) # data words in source string
1708#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1712#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1716#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1720#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1724#    lw $t2,-8($s7)   # data words in target string
1728#    sll $t2,$t2,2    # data bytes in target string
1732#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1736:  subCopyLoopzz:
1736#    lb $t4,-8($t3)
1740#    sb $t4,-8($t2)
1744#    addu $t2,$t2,1
1748#    addu $t3,$t3,1
1752#    blt $t3,$t1,subCopyLoopzz
1753##...slt $at,$t3,$t1
1757##...bne $at,$zero,1736
    
1760:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1760#    lw $ra,4($sp) # restore $ra
1764#    addu $sp,$sp,8 # pop ra, and allocated newObject
1768#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1772#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1776:mth_length_String:

    # the length is the negative of 4($s2)
1776#    lw $t0,-4($s2) # -length
1780#    subu $t0,$zero,$t0

    # return
1784#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1788:mth_concat_String:
    # save $ra by pushing onto stack
1788#    subu $sp,$sp,4
1792#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1796#    lw $t0,4($sp)
1800#    beq $t0,$zero,nullPtrException
1804#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1808#    lw $t0,-4($t0) # negative size of second object
1812#    lw $t1,-4($t1) # negative size of first object
1816#    addu $s6,$t0,$t1 # sum of negative sizes
1820#    sra $s6,$s6,2 # negative word-size of char part
1824#    subu $s6,$zero,$s6 # word size of char part
1828#    addu $s6,$s6,1 # data word size, including v-table word
1832#    move $s7,$zero
1836#    jal newObject

    # store String vtable address in vtable address field
1840#    la $t0,CLASS_String
1844#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1848#    lw $t4,-8($s7) # # data words in source string
1852#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1853##...slt $at,$t4,$zero
1857##...beq $at,$zero,1876
1860#    and $t4,0xff # mask off object's hash value ...
1861##...lui $at,0
1865##...ori $at,$at,0xff
1869##...and $t4,$t4,$at
1872#    sw $t4,-8($s7) # ... and store back into -8 header-word
1876:skipMask_concat:

1876#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1880#    move $t1,$s2   # first object
1884#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1888#    lw $t2,-4($t0) # negative length of second object
1892#    lw $t3,-4($t1) # negative length of first object
1896#    addu $t4,$t2,$t3 # sum of negative lengths
1900#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1904#    lw $t4,-8($t1) # # data words in first string
1908#    sll $t4,$t4,2 # # data bytes in first string
1912#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1916#    lw $t4,-8($s7) # # data words in new string
1920#    sll $t4,$t4,2 # # data bytes in new string
1924#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1928#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1932#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1936:  concatLoop1zz:
1936#    lb $t4,-8($t1)
1940#    sb $t4,-8($s7)
1944#    addu $s7,$s7,1
1948#    addu $t1,$t1,1
1952#    blt $t1,$t3,concatLoop1zz
1953##...slt $at,$t1,$t3
1957##...bne $at,$zero,1936
1960:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1960#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1964#    lw $t4,-8($t0) # # data words in second string
1968#    sll $t4,$t4,2 # # data bytes in second string
1972#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1976#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1980:  concatLoop2zz:
1980#    lb $t4,-8($t1)
1984#    sb $t4,-8($s7)
1988#    addu $s7,$s7,1
1992#    addu $t1,$t1,1
1996#    blt $t1,$t3,concatLoop2zz
1997##...slt $at,$t1,$t3
2001##...bne $at,$zero,1980
2004:  doneConcatLoop2zz:
 
2004:  concatRtnzz:
    # return
2004#    move $t0, $t9 # put saved return string in $t0
2008#    lw $ra, 4($sp)
2012#    addu $sp, 8 # pop ra and allocated newObject
2016#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
2020:mth_charAt_String:
    # get string
2020#    move $t0,$s2

    # check that index is in bounds
2024#    lw $t1,-4($t0) # negative of # data words in string
2028#    subu $t3,$zero,$t1 # # chars in string
2032#    lw $t2,($sp) # index
2036#    bgeu $t2,$t3,strIndexOutOfBounds
2037##...sltu $at,$t2,$t3
2041##...beq $at,$zero,3564

    # access element
2044#    lw $t1,-8($t0) # # data words in object
2048#    sll $t1,$t1,2 # - byte-offset from end of chars
2052#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
2056#    addu $t0,$t0,$t1 # - address of our char, offset by 8
2060#    lb $t0,-8($t0) # our char

    # return
2064#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
2068:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
2068#    subu $sp,$sp,8
2072#    sw $ra,4($sp)

    # save current sp
2076#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
2080#    li $t3,10

    # get argument, negate if negative
2084#    lw $t1,8($sp)
2088#    bge $t1,$zero,itsNonNegLoop
2089##...slt $at,$t1,$zero
2093##...beq $at,$zero,2100
2096#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
2100:  itsNonNegLoop:
2100#    divu $t1,$t3
2104#    mflo $t1 # quotient
2108#    mfhi $t4 # remainder
2112#    addu $t4,$t4,'0' # turn remainder into digit
2116#    subu $sp,$sp,4
2120#    sw $t4,($sp) # push digit onto stack
2124#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
2128#    lw $t4,8($t0)
2132#    bge $t4,$zero,itsSkipNeg
2133##...slt $at,$t4,$zero
2137##...beq $at,$zero,2152
2140#    li $t4,'-'
2144#    subu $sp,$sp,4
2148#    sw $t4,($sp)
2152:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
2152#    subu $s6,$t0,$sp
2156#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
2160#    sw $t3,($t0)
2164#    srl $s6,$s6,2
2168#    subu $sp,$sp,4
2172#    sw $s6,($sp)
    
    # allocate memory
2176#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
2180#    srl $s6,$s6,2
2184#    move $s7,$zero # no "object" words in object
2188#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
2192#    lw $t0,4($sp)
2196#    addu $sp,$sp,8

    # store "String" tag into class field
2200#    subu $s7,$s7,8 # address of header-1 word
2204#    la $t1,CLASS_String
2208#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
2212#    lw $t4,($s7) # # data words in source string
2216#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
2217##...slt $at,$t4,$zero
2221##...beq $at,$zero,2240
2224#    and $t4,0xff # mask off object's hash value ...
2225##...lui $at,0
2229##...ori $at,$at,0xff
2233##...and $t4,$t4,$at
2236#    sw $t4,($s7) # ... and store back into -8 header-word
2240:skipMask_intToString:
    
    # store negative of char-count into header-2 word
2240#    subu $t0,$zero,$t0
2244#    sw $t0,4($s7)

2248#    lw $t1,($s7) # number of data words in string
2252#    sll $t1,$t1,2 # number data bytes in string
2256#    subu $t1,$s7,$t1 # first location to store chars in string
2260#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2264:  itsLoop:
2264#    lw $t2,($sp)
2268#    addu $sp,$sp,4
2272#    sb $t2,($t1)
2276#    addu $t1,$t1,1
2280#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2284#    lw $ra,4($sp)
2288#    addu $sp,$sp,8
2292#    addu $t0,$t0,15
2296#    and $t0,$t0,0xfffffffc
2297##...lui $at,65535
2301##...ori $at,$at,0xfffc
2305##...and $t0,$t0,$at
2308#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2312:mth_intToChar_Lib:
    # save return address
2312#    subu $sp,$sp,4
2316#    sw $ra,($sp)

    # allocate object
2320#    li $s6,2
2324#    move $s7,$zero
2328#    jal newObject

    # get n from string.
2332#    lw $t1,8($sp)

    # store "String" tag into class field
2336#    la $t0,CLASS_String
2340#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2344#    lw $t4,-8($s7) # # data words in source string
2348#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2349##...slt $at,$t4,$zero
2353##...beq $at,$zero,2372
2356#    and $t4,0xff # mask off object's hash value ...
2357##...lui $at,0
2361##...ori $at,$at,0xff
2365##...and $t4,$t4,$at
2368#    sw $t4,-8($s7) # ... and store back into -8 header-word
2372:  skipMask_intToChar:

    # store data in string
2372#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2376#    li $t0,-1
2380#    sw $t0,-4($s7)

    # we're returning $s7
2384#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2388#    lw $ra,4($sp)
2392#    addu $sp,$sp,8

    # return
2396#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2400:mth_compareTo_String:
    # get string pointers and check parameter for null
2400#    lw $t0,($sp) # second string
2404#    beq $t0,$zero,nullPtrException
2408#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2412#    lw $t2,-4($t0) # negative length of second string
2416#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2420#    slt $t4,$t3,$t2
2424#    slt $t5,$t2,$t3
2428#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2432#    lw $t2,-8($t0) # # data words in second string
2436#    lw $t3,-8($t1) # # data words in first string
2440#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2444#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2448#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2452#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2456#    beq $zero,$t5,skipStrCmpLenzz
2460#    move $t2,$t3
2464:  skipStrCmpLenzz:
2464#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2468:  cmpLoopzz:
2468#    lw $t3,-8($t1) # word from str2
2472#    lw $t4,-8($t0) # word from str1
2476#    bne $t3,$t4,cmpNotEqualzz
2480#    addu $t1,$t1,4
2484#    addu $t0,$t0,4
2488#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2492#    j cmpHitLimitzz

2496:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2496#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2500#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2504#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2508:  cmpHitLimitzz:
2508#    move $t0, $t5 # store return value
2512#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2516:readLogicalChar:

    # check if we already have a character
2516#    lw $v0,lastCharRead
2520#    li $t0,-2
2524#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2528#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2532#    jr $ra # return
    
2536:  doReadCh:
    # we need to actually read a character: read and return
2536#    li $v0,12 # use system call to read a character
2540#    syscall
2544#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2548:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2548#    blt $s7,$zero arraySizeOutOfBounds
2549##...slt $at,$s7,$zero
2553##...bne $at,$zero,3580

    # mark the fact that we have not yet GC'd for this allocation
2556#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2560:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2560#    addu $t0,$s6,$s7
2564#    sll $t0,$t0,2
2568#    addu $t0,$t0,8 # amount of memory needed
2572#    addu $t1,$t0,$s3
2576#    bgtu $t1,$s4,doGC
2577##...sltu $at,$s4,$t1
2581##...bne $at,$zero,2704

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2584#    move $t2,$s3
2588:  zeroObjLoop:
2588#    sw $zero,($s3)
2592#    lw $zero,($s3)

2596#    addu $s3,4
2600#    bltu $s3,$t1,zeroObjLoop
2601##...sltu $at,$s3,$t1
2605##...bne $at,$zero,2588

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2608#    sll $t0,$s7,2    # number of post-header bytes
2612#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2616#    li $t2,255
2620#    bgeu $s6,$t2 firstHeaderLarge
2621##...sltu $at,$s6,$t2
2625##...beq $at,$zero,2672
2628#    lw $t1,prevHashConstant
2632#    addu $t1,0x12b68fd2
2633##...lui $at,4791
2637##...addiu $at,$at,-28718
2641##...addu $t1,$at,$t1
2644#    sw $t1,prevHashConstant # update hash constant
2648#    or $t1,0x80000000 # set top bit
2649##...lui $at,32768
2653##...or $t1,$at,$t1
2656#    and $t1,0xffffff00 # clear low bit
2657##...lui $at,65535
2661##...ori $at,$at,0xff00
2665##...and $t1,$t1,$at
2668#    or $s6,$t1
2672:firstHeaderLarge:
2672#    sw $s6,-8($t0) # store header-1 value
2676#    lw $zero,-8($t0)

    # store second header-value
2680#    sw $s7,-4($t0)   # second header-word
2684#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2688#    move $s7,$t0
2692#    subu $sp,$sp,4
2696#    sw $t0,($sp)

2700#    jr $ra
    
2704:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2704#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2708#    lw $t5,gcCount
2712#    addu $t5,1
2716#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2720#    subu $sp,$sp,4
2724#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2728#    lw $t0,cur_seg
2732#    move $t7,$s4
2736#    la $t6,seg1_start
2737##...lui $t6,16
2741##...addiu $t6,$t6,4232
2744#    la $s3,seg0_start
2748#    la $s4,seg0_end
2749##...lui $s4,16
2753##...addiu $s4,$s4,4232
2756#    sw $zero,cur_seg
2760#    bne $t0,$zero,skipGc1
2764#    la $t6,seg0_start
2768#    la $s3,seg1_start
2769##...lui $s3,16
2773##...addiu $s3,$s3,4232
2776#    la $s4,seg1_end
2777##...lui $s4,32
2781##...addiu $s4,$s4,4232
2784#    sw $s5,cur_seg
2788:  skipGc1:
2788#    li $t5,0x80000000
2792#    lw $t0,stack_bottom
2796#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2800:  gcStackCopyLoop:
2800#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2804#    sll $t2,$t1,31 # zero iff low bit was zero
2808#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2812#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2816#    bleu $t1,$t6,gcTestIterDone1
2817##...sltu $at,$t6,$t1
2821##...beq $at,$zero,2984
2824#    bgtu $t1,$t7,gcTestIterDone1
2825##...sltu $at,$t7,$t1
2829##...bne $at,$zero,2984

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2832#    lw $t2,-8($t1) # possible forwarding tag
2836#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2840#    lw $t2,-4($t1) # forwarding pointer: object's new address
2844#    sw $t2,4($t0) # update stack value
2848#    j gcTestIterDone1 # do next iteration

2852:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2852#    bge $t2,$zero,gcSkip2
2853##...slt $at,$t2,$zero
2857##...beq $at,$zero,2872
2860#    and $t2,0xff #### mask off hash value
2861##...lui $at,0
2865##...ori $at,$at,0xff
2869##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2872:  gcSkip2:
2872#    sll $t2,$t2,2
2876#    addu $t2,$t2,8
2880#    subu $t4,$t1,$t2 # address of first word of source
2884#    lw $t3,-4($t1) # number of object words (negative means zero)
2888#    bge $t3,$zero,gcH2Neg1
2889##...slt $at,$t3,$zero
2893##...beq $at,$zero,2900
2896#    move $t3,$zero
2900:  gcH2Neg1:
2900#    sll $t3,$t3,2
2904#    addu $t3,$t3,$t1 # address one word past last word of source

2908#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2912#    lw $s0,($t4)
2916#    lw $s1,-8($t1)
2920#    sw $s1,($t4)
2924#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2928:  gCinnerCopy1:
2928#    lw $s0,($t4)
2932#    sw $s0,($s3)
2936#    addu $t4,$t4,4
2940#    addu $s3,$s3,4
2944#    bltu $t4,$t3,gCinnerCopy1
2945##...sltu $at,$t4,$t3
2949##...bne $at,$zero,2928

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2952#    sw $zero,-8($t1) # 0 in first header-slot
2956#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2960#    sw $t2,4($t0)
2964#    lw $zero,-8($t2)
2968#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2972#    j gcTestIterDone1

2976:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2976#    subu $t0,$t0,$t1
2980#    subu $t0,$t0,3

2984:  gcTestIterDone1:
2984#    bgeu $t0,$sp,gcStackCopyLoop
2985##...sltu $at,$t0,$sp
2989##...beq $at,$zero,2800

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2992#    lw $t1,cur_seg
2996#    la $t0,seg0_start
3000#    beq $t1,$zero,gcSkip4
3004#    la $t0,seg1_start
3005##...lui $t0,16
3009##...addiu $t0,$t0,4232


3012:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
3012#    bgeu $t0,$s3,gcFinishUp
3013##...sltu $at,$t0,$s3
3017##...beq $at,$zero,3308

    ###### begin outer loop to copy all non-stack-direct objects ######

3020:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
3020#    lw $t1,($t0) # (what will be) first header word for current object
3024#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
3028#    bge $t2,$zero,gcSkip4a # are there hash bits?
3029##...slt $at,$t2,$zero
3033##...beq $at,$zero,3048
3036#    and $t2,0xff # mask off hash bits
3037##...lui $at,0
3041##...ori $at,$at,0xff
3045##...and $t2,$t2,$at
3048:gcSkip4a:    
3048#    sll $t2,$t2,2 # # data bytes
3052#    addu $t2,$t2,8 # to skip header words
3056#    addu $t2,$t0,$t2 # pointer to new object
3060#    lw $t3,-8($t2) # word to swap
3064#    sw $t1,-8($t2) # store header word
3068#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
3072#    lw $t1,-12($t2) # vtable pointer
3076#    li $t0,dataArrayVTableStart # address of vtable for first data array
3080#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
3081##...slt $at,$t1,$t0
3085##...bne $at,$zero,3104

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
3088:  skipToNextObj:
3088#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
3092#    sll $t1,$t1,2 # convert to byte-count
3096#    addu $t0,$t2,$t1 # skip over object
3100#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
3104:  gcNotDataArray:
3104#    lw $t3,-4($t2) # # object words (negative treated as zero)
3108#    bge $t3,$zero,gcH2Neg2
3109##...slt $at,$t3,$zero
3113##...beq $at,$zero,3120
3116#    move $t3,$zero
3120:  gcH2Neg2:
3120#    sll $t3,$t3,2 # # object bytes
3124#    move $t0,$t2 # address of first pointer in object
3128#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
3132#    beq $t0,$s1,gcTestIterDone2
    
3136:  gcNextPointerInObject:
    # get pointer from object
3136#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
3140#    bleu $t1,$t6,gcGoNextPointerSlot
3141##...sltu $at,$t6,$t1
3145##...beq $at,$zero,3288
3148#    bgtu $t1,$t7,gcGoNextPointerSlot
3149##...sltu $at,$t7,$t1
3153##...bne $at,$zero,3288

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3156#    lw $t8,-8($t1) # possible forwarding tag
3160#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
3164#    lw $t8,-4($t1) # forwarding pointer: object's new address
3168#    sw $t8,($t0) # update pointer in object
3172#    j gcGoNextPointerSlot # do next iteration

3176:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
3176#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
3177##...slt $at,$t8,$zero
3181##...beq $at,$zero,3196
3184#    and $t8,0xff # mask off hash bits
3185##...lui $at,0
3189##...ori $at,$at,0xff
3193##...and $t8,$t8,$at

3196:  gcSkip5:
3196#    sll $t8,$t8,2 # bytes in negative-offset portion ...
3200#    addu $t8,$t8,8 # ... add in bytes for header-words
3204#    subu $t4,$t1,$t8 # address of first word of source object
3208#    lw $t3,-4($t1) # # words in positive-offset portion
3212#    bge $t3,$zero,gcNoNeg
3213##...slt $at,$t3,$zero
3217##...beq $at,$zero,3224
3220#    move $t3,$zero # negative treated as zero
3224:  gcNoNeg:
3224#    sll $t3,$t3,2 # bytes in negative-offset portion
3228#    addu $t3,$t3,$t1 # address one word past last word of source

3232#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
3236#    lw $s0,($t4)
3240#    lw $t9,-8($t1)
3244#    sw $t9,($t4)
3248#    sw $s0,-8($t1)

    # copy all source bytes to the target
3252:  gCinnerCopy2:
3252#    lw $s0,($t4)
3256#    sw $s0,($s3)
3260#    addu $t4,$t4,4
3264#    addu $s3,$s3,4
3268#    bltu $t4,$t3,gCinnerCopy2
3269##...sltu $at,$t4,$t3
3273##...bne $at,$zero,3252

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3276#    sw $zero,-8($t1) # 0 in first header-slot
3280#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3284#    sw $t8,($t0)

3288:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3288#    addu $t0,$t0,4
3292#    bltu $t0,$s1,gcNextPointerInObject
3293##...sltu $at,$t0,$s1
3297##...bne $at,$zero,3136

3300:  gcTestIterDone2:
3300#    bltu $t0,$s3,gcHeapCopyLoop
3301##...sltu $at,$t0,$s3
3305##...bne $at,$zero,3020

    ###### end outer loop to copy all non-stack-direct objects ######

3308:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3308#    lw $s2,($sp)
3312#    addu $sp,$sp,4

    # mark us as having already GC'd
3316#    move $t5,$s5

    # go try and allocate again
3320#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3324:vm_init:

    # mark bottom of stack
3324#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3328#    move $s2,$zero

    #set up the "five" register
3332#    li $s5,5

3336#    la $s3,seg0_start
3340#    la $s4,seg0_end
3341##...lui $s4,16
3345##...addiu $s4,$s4,4232
    
    # return
3348#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3352:divide:
    # get parameters; abort if divisor zero
3352#    lw $t0,($sp)
3356#    lw $t1,8($sp)
3360#    beq $t0,$zero,divByZeroError
    
    # perform division
3364#    div $t1,$t0
3368#    mflo $t0

    # store result, adjust stack and return
3372#    addu $sp,$sp,8 # adjust stack
3376#    sw $t0,($sp) # store result
3380#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3384:remainder:
    # get parameters; abort if divisor zero
3384#    lw $t0,($sp)
3388#    lw $t1,8($sp)
3392#    beq $t0,$zero,divByZeroError
    
    # perform division
3396#    div $t1,$t0
3400#    mfhi $t0

    # store result, adjust stack and return
3404#    addu $sp,$sp,8 # adjust stack
3408#    sw $t0,($sp) # store result
3412#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3416:checkCast:
    # get object, allow immediately if null
3416#    lw $t2,($sp)
3420#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3424#    lw $t2,-12($t2) # vtable address
3428#    bge $t2,$t1,castException
3429##...slt $at,$t2,$t1
3433##...beq $at,$zero,3604
3436#    blt $t2,$t0,castException
3437##...slt $at,$t2,$t0
3441##...bne $at,$zero,3604
    
    # return, leaving object unchanged on stack
3444:  checkCastReturn:    
3444#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3448:instanceOf:
    # get object, go return false if null
3448#    lw $t2,($sp)
3452#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3456#    lw $t2,-12($t2) # vtable address
3460#    sge $t0,$t2,$t0 # are we >= the lower limit?
3461##...slt $t0,$t2,$t0
3465##...xori $t0,$t0,0x1
3468#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3472#    and $t2,$t0,$t1
3476#    sw $t2,($sp)
3480:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3480#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3484:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3484#    li $v0,4 # syscall-code for print-string
3488#    syscall
3492:exit:
    # print termination message
3492#    li $v0,4 # syscall-code for print-string
3496#    la $a0,exitString
3500#    syscall

    # print number of garbage collections
3504#    li $v0,4 # syscall-code for print-string
3508#    la $a0,gcCountMsg
3512#    syscall # print "Number of ..."
3516#    lw $a0,gcCount # number of GC's
3520#    li $v0,1 # code for print-int
3524#    syscall
3528#    li $v0,4 # syscall-code for print-string
3532#    la $a0,newlineString
3536#    syscall # print newline

    # terminate execution
3540#    li $v0,10 #syscall-code for "exit"
3544#    syscall

3548:outOfHeapMemory:
3548#    la $a0,heapMemoryMsg
3552#    j exitError
3556:divByZeroError:
3556#    la $a0,divByZeroMsg
3560#    j exitError
3564:strIndexOutOfBounds:
3564#    la $a0,strIndexOutOfBoundsMsg
3568#    j exitError
3572:arrayIndexOutOfBounds:
3572#    la $a0,arrayIndexOutOfBoundsMsg
3576#    j exitError
3580:arraySizeOutOfBounds:
3580#    la $a0,arraySizeOutOfBoundsMsg
3584#    j exitError
3588:nullPtrException:
3588#    la $a0,nullPtrExceptionMsg
3592#    j exitError
3596:badIntegerFormat:
3596#    la $a0,badIntegerFormatMsg
3600#    j exitError
3604:castException:
3604#    la $a0,castExceptionMsg
3608#    j exitError
############## data section ################
    .data
3860:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3876:cur_seg:
    .word 0

3880:lastCharRead:
    .word -2 # -2 => no buffered character
3884:prevHashConstant:
    .word 0x2785173c
3888:gcCount:
    .word 0

3892:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3919:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3942:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3977:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
4011:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
4044:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
4075:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
4123:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
4144:gcMsg: 
      .asciiz "\nGC!\n"
4150:gcCountMsg: 
      .asciiz "Number of garbage collections: "

4182:nullStr:
    .asciiz "null"
4187:trueString:
    .asciiz "true"
4192:falseString:
    .asciiz "false"
4198:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
4226:newlineString:
    .asciiz "\n"
    
    .align 2
4228:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

4232:seg0_start:
    .space 0x100000
1052808:seg0_end:
1052808:seg1_start:
    .space 0x100000
2101384:seg1_end:
