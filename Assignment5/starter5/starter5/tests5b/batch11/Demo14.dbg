 .data # Program at 2.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3804:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3824:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3856:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3860:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 2.7
  .byte 205 # 'M' with high bit set # ClassDecl at 2.7
  .byte 97 # 'a' # ClassDecl at 2.7
  .byte 105 # 'i' # ClassDecl at 2.7
  .byte 110 # 'n' # ClassDecl at 2.7
3908:CLASS_Main: # ClassDecl at 2.7
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 3.14
  .word mth_name_Main # MethodDeclNonVoid at 9.16
# ****** class YourClass ****** # ClassDecl at 14.7
  .byte 0 # ClassDecl at 14.7
  .byte 0 # ClassDecl at 14.7
  .byte 0 # ClassDecl at 14.7
  .byte 217 # 'Y' with high bit set # ClassDecl at 14.7
  .byte 111 # 'o' # ClassDecl at 14.7
  .byte 117 # 'u' # ClassDecl at 14.7
  .byte 114 # 'r' # ClassDecl at 14.7
  .byte 67 # 'C' # ClassDecl at 14.7
  .byte 108 # 'l' # ClassDecl at 14.7
  .byte 97 # 'a' # ClassDecl at 14.7
  .byte 115 # 's' # ClassDecl at 14.7
  .byte 115 # 's' # ClassDecl at 14.7
3972:CLASS_YourClass: # ClassDecl at 14.7
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 3.14
  .word mth_name_YourClass # MethodDeclNonVoid at 15.16
4024:END_CLASS_YourClass: # ClassDecl at 14.7
4024:END_CLASS_Main: # ClassDecl at 2.7
4024:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
4032:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
4044:END_CLASS_RunMain: # ClassDecl at 0.0
4044:dataArrayVTableStart: # IntType at 0.0
4044:END_CLASS_Object: # ClassDecl at 0.0
  .byte 66 # StringLit at 10.19
  .byte 117 # StringLit at 10.19
  .byte 100 # StringLit at 10.19
  .byte 100 # StringLit at 10.19
  .byte 121 # StringLit at 10.19
  .byte 0 # StringLit at 10.19
  .byte 0 # StringLit at 10.19
  .byte 0 # StringLit at 10.19
  .word CLASS_String # StringLit at 10.19
  .word 3 # StringLit at 10.19
  .word -5 # StringLit at 10.19
4064:strLit_23: # StringLit at 10.19
  .byte 77 # StringLit at 11.23
  .byte 117 # StringLit at 11.23
  .byte 100 # StringLit at 11.23
  .byte 100 # StringLit at 11.23
  .byte 121 # StringLit at 11.23
  .byte 0 # StringLit at 11.23
  .byte 0 # StringLit at 11.23
  .byte 0 # StringLit at 11.23
  .word CLASS_String # StringLit at 11.23
  .word 3 # StringLit at 11.23
  .word -5 # StringLit at 11.23
4084:strLit_30: # StringLit at 11.23
  .byte 65 # StringLit at 16.10
  .byte 0 # StringLit at 16.10
  .byte 0 # StringLit at 16.10
  .byte 0 # StringLit at 16.10
  .word CLASS_String # StringLit at 16.10
  .word 2 # StringLit at 16.10
  .word -1 # StringLit at 16.10
4100:strLit_40: # StringLit at 16.10
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  lw $t0, 0($sp)
28#  sw $s2, 0($sp)
32#  move $s2, $t0
36#  beq $s2, $zero, nullPtrException
40#  lw $t0, -12($s2)
44#  lw $t0, 44($t0)
48#  jalr $t0
52#  lw $s2, ($sp)
56#  addu $sp, $sp, 4
60#  subu $sp, $sp, 4
64#  sw $t0, ($sp)
68#  lw $t0, ($sp)
72#  addu $sp, $sp, 4
76#  li $v0, 10
80#  syscall
.globl mth_main_Main
84:mth_main_Main:
84#  subu $sp, $sp, 4
88#  sw $ra, ($sp)
92#  li $t0, 0
96#  subu $sp, $sp, 4
100#  sw $t0, ($sp)
104#  li $s6, 1
108#  li $s7, 0
112#  jal newObject
116#  la $t0, CLASS_YourClass
120#  sw $t0, -12($s7)
124#  lw $t0, ($sp)
128#  addu $sp, $sp, 4
132#  sw $t0, 0($sp)
136#  lw $t0, 0($sp)
140#  subu $sp, $sp, 4
144#  sw $t0, ($sp)
148#  li $t0, 44
152#  subu $sp, $sp, 8
156#  sw $zero, 4($sp)
160#  sw $t0, ($sp)
164#  lw $t0, 8($sp)
168#  sw $s2, 8($sp)
172#  move $s2, $t0
176#  beq $s2, $zero, nullPtrException
180#  lw $t0, -12($s2)
184#  lw $t0, 48($t0)
188#  jalr $t0
192#  addu $sp, $sp, 8
196#  lw $s2, ($sp)
200#  addu $sp, $sp, 4
204#  subu $sp, $sp, 4
208#  sw $t0, ($sp)
212#  subu $sp, $sp, 4
216#  sw $s2, ($sp)
220#  lw $t0, 4($sp)
224#  subu $sp, $sp, 4
228#  sw $t0, ($sp)
232#  lw $t0, 4($sp)
236#  sw $s2, 4($sp)
240#  move $s2, $t0
244#  beq $s2, $zero, nullPtrException
248#  lw $t0, -12($s2)
252#  lw $t0, 24($t0)
256#  jalr $t0
260#  addu $sp, $sp, 4
264#  lw $s2, ($sp)
268#  addu $sp, $sp, 4
272#  subu $sp, $sp, 4
276#  sw $t0, ($sp)
280#  lw $t0, ($sp)
284#  addu $sp, $sp, 4
288#  addu $sp, $sp, 8
292#  lw $ra, ($sp)
296#  addu $sp, $sp, 4
300#  jr $ra
.globl mth_name_Main
304:mth_name_Main:
304#  subu $sp, $sp, 4
308#  sw $ra, ($sp)
312#  la $t0, strLit_23
316#  subu $sp, $sp, 4
320#  sw $t0, ($sp)
324#  lw $t0, 8($sp)
328#  subu $sp, $sp, 8
332#  sw $zero, 4($sp)
336#  sw $t0, ($sp)
340#  li $t0, 0
344#  subu $sp, $sp, 8
348#  sw $zero, 4($sp)
352#  sw $t0, ($sp)
356#  lw $t2, ($sp)
360#  addu $sp, $sp, 8
364#  lw $t1, ($sp)
368#  addu $sp, $sp, 8
372#  sgt $t0, $t1, $t2
376#  subu $sp, $sp, 4
380#  sw $t0, ($sp)
384#  lw $t0, ($sp)
388#  addu $sp, $sp, 4
392#  beq $t0, $zero, if_else_33
396#  la $t0, strLit_30
400#  subu $sp, $sp, 4
404#  sw $t0, ($sp)
408#  lw $t0, ($sp)
412#  addu $sp, $sp, 4
416#  sw $t0, 0($sp)
420#  j if_done_33
424:if_else_33:
424:if_done_33:
424#  lw $t0, 0($sp)
428#  subu $sp, $sp, 4
432#  sw $t0, ($sp)
436#  lw $t0, ($sp)
440#  addu $sp, $sp, 4
444#  addu $sp, $sp, 4
448#  lw $ra, ($sp)
452#  addu $sp, $sp, 4
456#  jr $ra
.globl mth_name_YourClass
460:mth_name_YourClass:
460#  subu $sp, $sp, 4
464#  sw $ra, ($sp)
468#  la $t0, strLit_40
472#  subu $sp, $sp, 4
476#  sw $t0, ($sp)
480#  subu $sp, $sp, 4
484#  sw $s2, ($sp)
488#  lw $t0, 12($sp)
492#  subu $sp, $sp, 8
496#  sw $zero, 4($sp)
500#  sw $t0, ($sp)
504#  li $t0, 20
508#  subu $sp, $sp, 8
512#  sw $zero, 4($sp)
516#  sw $t0, ($sp)
520#  lw $t2, ($sp)
524#  addu $sp, $sp, 8
528#  lw $t1, ($sp)
532#  addu $sp, $sp, 8
536#  subu $t0, $t1, $t2
540#  subu $sp, $sp, 8
544#  sw $zero, 4($sp)
548#  sw $t0, ($sp)
552#  lw $t0, 8($sp)
556#  sw $s2, 8($sp)
560#  move $s2, $t0
564#  jal mth_name_Main
568#  addu $sp, $sp, 8
572#  lw $s2, ($sp)
576#  addu $sp, $sp, 4
580#  subu $sp, $sp, 4
584#  sw $t0, ($sp)
588#  lw $t0, 4($sp)
592#  sw $s2, 4($sp)
596#  move $s2, $t0
600#  beq $s2, $zero, nullPtrException
604#  lw $t0, -12($s2)
608#  lw $t0, 12($t0)
612#  jalr $t0
616#  addu $sp, $sp, 4
620#  lw $s2, ($sp)
624#  addu $sp, $sp, 4
628#  subu $sp, $sp, 4
632#  sw $t0, ($sp)
636#  lw $t0, ($sp)
640#  addu $sp, $sp, 4
644#  lw $ra, ($sp)
648#  addu $sp, $sp, 4
652#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
656:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
656#    lw $t0,-8($s2) # get word with hash bits from this
660#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
664#    addu $t0,$t1

668#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
672:mth_equals_Object:
672#    move $t0,$s2 # first pointer (this)
676#    lw $t1,($sp) # second pointer
680#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
681##...subu $at,$t0,$t1
685##...sltiu $t0,$at,1

688#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
692:mth_hashCode_String:
692#    move $t0,$s2 # put this pointer in t0

696#    li $t2,0 # current hash value
700#    lw $t1,-4($t0) # negative of number of characters in string
704#    beq $t1,$zero,done_hashCode_String # if zero length => done
708#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
712#    and $t0,0xfffffffc # adjust for padding
713##...lui $at,65535
717##...ori $at,$at,0xfffc
721##...and $t0,$t0,$at
724#    subu $t1,$t0,$t1 # address at end of string +12
728:loop_hashCode_String:    
728#    sll $t4,$t2,5 # multiply by 32
732#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
736#    lbu $t3,-12($t0) # current character
740#    addu $t2,$t3 # add in current character
744#    addu $t0,1 # go to next character's address
748#    bne $t0,$t1,loop_hashCode_String # loop back if more
752:done_hashCode_String:    

    # push int on stack
752#    move $t0, $t2 # put $t2 in return slot
756#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
760:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
760#    subu $sp,4
764#    sw $ra,($sp)
    
    # get hash value
768#    lw $t2,-12($s2) # vtable address
772#    lw $t0,($t2) # method address
776#    jalr $t0
780#    subu $sp,8 # push result (int in $t0) on stack
784#    sw $s5,4($sp)
788#    sw $t0,($sp)

    # compute print-size of hash value
792#    li $t0, 1
796#    lw $t1, ($sp)
800#    srl $t1,$t1,4
804#    beq $t1,$zero,toString_skip_loop
808:toString_loop_top:
808#    addu $t0,1
812#    srl $t1,$t1,4
816#    bne $t1,$zero,toString_loop_top
820:toString_skip_loop:
820#    subu $sp,8
824#    sw $s5,4($sp)
828#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
832#    lw $t2,-12($s2) # vtable address
836#    move $t0,$t2
840:toString_checkTagBit:
840#    subu $t0,1
844#    lb $t1,($t0)
848#    and $t1,0x80
849##...lui $at,0
853##...ori $at,$at,0x80
857##...and $t1,$t1,$at
860#    beq $t1,$zero,toString_checkTagBit
864#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
868#    subu $sp,8
872#    sw $t1,($sp)
876#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
880#    move $s7,$zero # -4 offset header word
884#    lw $t4,8($sp) # bytes for hash value
888#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
892#    addu $t4,8
896#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
900#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

904#    li $t0,CLASS_String # store string vtable address into ...
908#    sw $t0,-12($s7) # ... -12 offset in object header
912#    lw $t2,-12($s2) # our object's vtable address
916#    lw $t5,4($sp) # number characters in prefix
920#    lw $t1,12($sp) # number of characters in hashcode
924#    addu $t3,$t1,$t5 # number of characters in string ...
928#    addu $t3,1 # ... including the '@' character
932#    subu $t0,$zero,$t3 # store negated length into ...
936#    sw $t0,-4($s7) # ... -4 offset header word
940#    lw $t0,-8($s7) # get -8 offset header-word
944#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
945##...slt $at,$t0,$zero
949##...beq $at,$zero,968
952#    and $t0,0xff # mask off object's hash value ...
953##...lui $at,0
957##...ori $at,$at,0xff
961##...and $t0,$t0,$at
964#    sw $t0,-8($s7) # ... and store back into -8 header-word
968:skipMask_toString:
968#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
972#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
976#    subu $t4,$t2,$t5
980:toString_copyLoop:    
980#    lb $t3,($t4)
984#    and $t3,0x7f # mask off sign bit (in case first word)
985##...lui $at,0
989##...ori $at,$at,0x7f
993##...and $t3,$t3,$at
996#    sb $t3,-8($t0)
1000#    addu $t4,1
1004#    addu $t0,1
1008#    blt $t4,$t2,toString_copyLoop
1009##...slt $at,$t4,$t2
1013##...bne $at,$zero,980

    # copy the '@' sign
1016#    li $t3,'@'
1020#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
1024#    addu $t2,$t0,$t1 # address of last byte in string to store +8
1028#    lw $t3,20($sp) # hash code to print
1032:toString_hashCopyLoop:
1032#    and $t4,$t3,0xf
1036#    srl $t3,4
1040#    lb $t4,hexChars($t4)
1044#    sb $t4,-8($t2)
1048#    subu $t2,$t2,1
1052#    bgt $t2,$t0,toString_hashCopyLoop
1053##...slt $at,$t0,$t2
1057##...bne $at,$zero,1032

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
1060#    addu $sp,28
1064#    lw $ra,($sp)
1068#    addu $sp,4
1072#    move $t0, $s7 # return string we created
1076#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
1080:mth_equals_String:
    
    # quick test for obvious false
1080#    lw $t0,($sp) # second parameter
1084#    beq $t0,$zero,goEsFalse # go return false if null
1088#    lw $t1,-12($t0) # vtable pointer
1092#    la $t2,CLASS_String # string vtable pointer
1096#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
1100#    subu $sp,12
1104#    sw $ra,8($sp)
1108#    sw $s2,4($sp)
1112#    sw $t0,($sp)
1116#    jal mth_compareTo_String
1120#    lw $s2, 4($sp) #restore this pointer and pop parameters
1124#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
1128#    seq $t0,$t0,$zero
1129##...subu $at,$t0,$zero
1133##...sltiu $t0,$at,1

    # pop return address and return $t0
1136#    lw $ra,($sp)
1140#    addu $sp,4

1144#    jr $ra # return
    
1148:goEsFalse:
    # put 0 in $t0 and return
1148#    li $t0, 0
1152#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
1156:mth_toString_String:
    #we are a string, so return this.
1156#    move $t0, $s2
1160#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
1164:mth_readLine_Lib:
    
1164#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
1168#    sw $ra, 4($sp) # save $ra
1172#    move $t1,$sp # save "original" sp

1176:  doRead:

    # read the character
1176#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
1180#    subu $t2,$v0,13
1184#    bne $t2,$zero,notReturnRL
1188#    jal readLogicalChar
1192#    subu $t2,$v0,10 # check for newline
1196#    beq $t2,$zero,foundNewLine
1200#    sw $v0,lastCharRead # push back character into queue
1204#    j foundNewLine # go process the line

1208:  notReturnRL:

    # at this point, $v0 has our character

1208#    subu $t0,$v0,10
1212#    beq $t0,$zero,foundNewLine
1216#    blt $v0,$zero,foundEof
1217##...slt $at,$v0,$zero
1221##...bne $at,$zero,1236

    # we have a character, so push it onto stack
1224#    subu $sp,$sp,4
1228#    sw $v0,($sp)

    # loop back up to get next character
1232#    j doRead

1236:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
1236#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
1240#    li $s7, 0 # return-value: null
1244#    j rlReturn # return

1248:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
1248#    subu $t0,$t1,$sp # number of chars we read (times 4)
1252#    srl $s6,$t0,2 # number of words on stack with chars
1256#    subu $sp,$sp,4 # push char-count ...
1260#    sw $s6,($sp) # ... onto stack
1264#    addu $t2,$t0,5 # GC tag, incl. for count-word
1268#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
1272#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
1276#    srl $s6,$s6,2  # data words in object
1280#    move $s7,$zero # object words in object
1284#    jal newObject  # allocate space
    
    # store header words
1288#    la $t0,CLASS_String
1292#    sw $t0,-12($s7)# store class tag (String) into object
1296#    lw $t2,4($sp) # char-count
1300#    sll $t1,$t2,2 # 4 times number of chars
1304#    subu $t2,$zero,$t2 # negative of char-count
1308#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
1312#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
1316#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
1317##...slt $at,$t0,$zero
1321##...beq $at,$zero,1340
1324#    and $t0,0xff # mask off object's hash value ...
1325##...lui $at,0
1329##...ori $at,$at,0xff
1333##...and $t0,$t0,$at
1336#    sw $t0,-8($s7) # ... and store back into -8 header-word
1340:  skipMask_readLine:

1340#    sll $t0,$t0,2 # data bytes in object
1344#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
1348#    subu $t0,$t0,$t2 # place to store last character (plus 9)
1352#    addu $sp,$sp,8 # pop redundant object-pointer and count
1356#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

1360#    beq $sp,$t1,rlReturn
1364:  charCopyLoop:
1364#    lw $t2,($sp)
1368#    sb $t2,-9($t0)
1372#    addu $sp,$sp,4
1376#    subu $t0,$t0,1
1380#    bne $sp,$t1,charCopyLoop
1384:  rlReturn:

1384#    lw $ra, 4($sp)
1388#    addu $sp, 8   # pop $ra and pointless word off stack
1392#    move $t0, $s7 # return string we allocated in $s7
1396#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1400:mth_readInt_Lib:

    # save $ra by pushing onto stack
1400#    subu $sp,$sp,4
1404#    sw $ra,($sp)

1408:  riSkipWhiteLoop:
    # read a character
1408#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
1412#    subu $t0,$v0,32
1416#    bgt $t0,$zero,nonWhite
1417##...slt $at,$zero,$t0
1421##...bne $at,$zero,1464
1424#    beq $t0,$zero,riSkipWhiteLoop # start over if space
1428#    subu $t0,$v0,10
1432#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
1436#    subu $t0,$v0,9
1440#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
1444#    subu $t0,$v0,13
1448#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1452#    subu $t0,$v0,12
1456#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1460#    j badIntegerFormat # illegal integer char: abort program

1464:  nonWhite:
1464#    subu $t0,$v0,'-'
1468#    li $t4,1 # final multiplier
1472#    bne $t0,$zero,helpReadInt # go read
1476#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1480#    jal readLogicalChar

1484:  helpReadInt:
1484#    li $t2,10
1488#    subu $t1,$v0,'0' # convert digit to 0-9 value
1492#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1493##...sltu $at,$t1,$t2
1497##...beq $at,$zero,3780
1500#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1504:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1504#    jal readLogicalChar # read next character
1508#    subu $t1,$v0,'0' # convert digit to 0-9 value
1512#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1513##...sltu $at,$t1,$t2
1517##...beq $at,$zero,1536

1520#    mul $t3,$t3,$t2 # multiply old value by 10
1521##...mult $t3,$t2
1525##...mflo $t3
1528#    addu $t3,$t3,$t1 # add in value of new digit
1532#    j digitLoop

1536:  doneDigitLoop:

    # "push back" unused character into queue
1536#    sw $v0,lastCharRead

    # restore return address
1540#    lw $ra,($sp)
1544#    addu $sp, 4

    # multiply by sign and put return value in $t0
1548#    mult $t3,$t4 # multiply to account for poss. minus sign
1552#    mflo $t3
1556#    move $t0, $t3
    
    # return
1560#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1564:mth_readChar_Lib:

    # save $ra by pushing onto stack
1564#    subu $sp,$sp,4
1568#    sw $ra,($sp)

      # read the character
1572#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1576#    lw $ra,($sp)
1580#    addu $sp, 4
1584#    move $t0, $v0

    # return
1588#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1592:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1592#    lw $t0,($sp)
1596#    bne $t0,$zero,psNotNull

    # print "(null)"
1600#    la $a0,nullStr
1604#    li $v0,4
1608#    syscall
1612#    j donePrintStr
    
1616:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1616#    subu $t0,$t0,8
1620#    lw $t1,($t0) # number of data words, but needs modification
1624#    blt $t1,$zero,skipPrintStr1
1625##...slt $at,$t1,$zero
1629##...bne $at,$zero,1648
1632#    and $t1,0xff # mask out all but low byte
1633##...lui $at,0
1637##...ori $at,$at,0xff
1641##...and $t1,$t1,$at
1644#    b skipPrintStr2
1648:skipPrintStr1:
1648#    and $t1,0x7ffffff
1649##...lui $at,2047
1653##...ori $at,$at,0xffff
1657##...and $t1,$t1,$at
1660:skipPrintStr2:    
1660#    sll $t1,$t1,2
1664#    subu $t1,$t0,$t1 # first word in string
1668#    lw $t0,4($t0) # negative of string-length
1672#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1676#    beq $t0,$t1,donePrintStr
1680:  psLoop:
1680#    lb $a0,($t1) # next byte
1684#    li $v0,11 # code for printing char
1688#        syscall # print the char
1692#    addu $t1,$t1,1 # go to next char
1696#    blt $t1,$t0,psLoop
1697##...slt $at,$t1,$t0
1701##...bne $at,$zero,1680

1704:  donePrintStr:
1704#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1708:mth_printInt_Lib:
1708#    lw $a0,($sp)

    # print it
1712#    li $v0,1 # code for print-int
1716#    syscall

    # return
1720#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1724:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1724#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1728#    la $a0,falseString
1732#    beq $t0,$zero,skipPB
1736#    la $a0,trueString
1740:  skipPB:
1740#    li $v0,4 # code for print-string
1744#    syscall

    # return
1748#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1752:mth_substring_String:

    # save $ra by pushing onto stack
1752#    subu $sp,$sp,4
1756#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1760#    move $t0,$s2
1764#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1768#    lw $t0,-4($t0) # negative of string length
1772#    subu $t0,$zero,$t0 # string length
1776#    lw $t1,4($sp) # k
1780#    lw $t2,12($sp) # n
1784#    bgt $zero,$t2,strIndexOutOfBounds
1785##...slt $at,$t2,$zero
1789##...bne $at,$zero,3748
1792#    bgt $t2,$t1,strIndexOutOfBounds
1793##...slt $at,$t1,$t2
1797##...bne $at,$zero,3748
1800#    bgt $t1,$t0,strIndexOutOfBounds
1801##...slt $at,$t0,$t1
1805##...bne $at,$zero,3748

    # allocate memory
1808#    subu $s6,$t1,$t2 # # chars in target-string
1812#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1816#    srl $s6,$s6,2 # convert bytes-count to word-count
1820#    move $s7,$zero # (no object-bytes in string)
1824#    jal newObject

    # store "String" tag in object-type field
1828#    la $t0,CLASS_String
1832#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1836#    lw $t4,-8($s7) # data words in source string
1840#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1841##...slt $at,$t4,$zero
1845##...beq $at,$zero,1864
1848#    and $t4,0xff # mask off object's hash value ...
1849##...lui $at,0
1853##...ori $at,$at,0xff
1857##...and $t4,$t4,$at
1860#    sw $t4,-8($s7) # ... and store back into -8 header-word
1864:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1864#    lw $t1,8($sp) # k
1868#    lw $t2,16($sp) # n
1872#    subu $t0,$t2,$t1 # value to store
1876#    sw $t0,-4($s7) # store value
    
1880#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1884#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1888#    lw $t4,-8($t3) # data words in source string
1892#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1896#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1900#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1904#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1908#    lw $t2,-8($s7)   # data words in target string
1912#    sll $t2,$t2,2    # data bytes in target string
1916#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1920:  subCopyLoopzz:
1920#    lb $t4,-8($t3)
1924#    sb $t4,-8($t2)
1928#    addu $t2,$t2,1
1932#    addu $t3,$t3,1
1936#    blt $t3,$t1,subCopyLoopzz
1937##...slt $at,$t3,$t1
1941##...bne $at,$zero,1920
    
1944:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1944#    lw $ra,4($sp) # restore $ra
1948#    addu $sp,$sp,8 # pop ra, and allocated newObject
1952#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1956#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1960:mth_length_String:

    # the length is the negative of 4($s2)
1960#    lw $t0,-4($s2) # -length
1964#    subu $t0,$zero,$t0

    # return
1968#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1972:mth_concat_String:
    # save $ra by pushing onto stack
1972#    subu $sp,$sp,4
1976#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1980#    lw $t0,4($sp)
1984#    beq $t0,$zero,nullPtrException
1988#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1992#    lw $t0,-4($t0) # negative size of second object
1996#    lw $t1,-4($t1) # negative size of first object
2000#    addu $s6,$t0,$t1 # sum of negative sizes
2004#    sra $s6,$s6,2 # negative word-size of char part
2008#    subu $s6,$zero,$s6 # word size of char part
2012#    addu $s6,$s6,1 # data word size, including v-table word
2016#    move $s7,$zero
2020#    jal newObject

    # store String vtable address in vtable address field
2024#    la $t0,CLASS_String
2028#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2032#    lw $t4,-8($s7) # # data words in source string
2036#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
2037##...slt $at,$t4,$zero
2041##...beq $at,$zero,2060
2044#    and $t4,0xff # mask off object's hash value ...
2045##...lui $at,0
2049##...ori $at,$at,0xff
2053##...and $t4,$t4,$at
2056#    sw $t4,-8($s7) # ... and store back into -8 header-word
2060:skipMask_concat:

2060#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
2064#    move $t1,$s2   # first object
2068#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
2072#    lw $t2,-4($t0) # negative length of second object
2076#    lw $t3,-4($t1) # negative length of first object
2080#    addu $t4,$t2,$t3 # sum of negative lengths
2084#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
2088#    lw $t4,-8($t1) # # data words in first string
2092#    sll $t4,$t4,2 # # data bytes in first string
2096#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
2100#    lw $t4,-8($s7) # # data words in new string
2104#    sll $t4,$t4,2 # # data bytes in new string
2108#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
2112#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
2116#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
2120:  concatLoop1zz:
2120#    lb $t4,-8($t1)
2124#    sb $t4,-8($s7)
2128#    addu $s7,$s7,1
2132#    addu $t1,$t1,1
2136#    blt $t1,$t3,concatLoop1zz
2137##...slt $at,$t1,$t3
2141##...bne $at,$zero,2120
2144:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
2144#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
2148#    lw $t4,-8($t0) # # data words in second string
2152#    sll $t4,$t4,2 # # data bytes in second string
2156#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
2160#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
2164:  concatLoop2zz:
2164#    lb $t4,-8($t1)
2168#    sb $t4,-8($s7)
2172#    addu $s7,$s7,1
2176#    addu $t1,$t1,1
2180#    blt $t1,$t3,concatLoop2zz
2181##...slt $at,$t1,$t3
2185##...bne $at,$zero,2164
2188:  doneConcatLoop2zz:
 
2188:  concatRtnzz:
    # return
2188#    move $t0, $t9 # put saved return string in $t0
2192#    lw $ra, 4($sp)
2196#    addu $sp, 8 # pop ra and allocated newObject
2200#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
2204:mth_charAt_String:
    # get string
2204#    move $t0,$s2

    # check that index is in bounds
2208#    lw $t1,-4($t0) # negative of # data words in string
2212#    subu $t3,$zero,$t1 # # chars in string
2216#    lw $t2,($sp) # index
2220#    bgeu $t2,$t3,strIndexOutOfBounds
2221##...sltu $at,$t2,$t3
2225##...beq $at,$zero,3748

    # access element
2228#    lw $t1,-8($t0) # # data words in object
2232#    sll $t1,$t1,2 # - byte-offset from end of chars
2236#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
2240#    addu $t0,$t0,$t1 # - address of our char, offset by 8
2244#    lb $t0,-8($t0) # our char

    # return
2248#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
2252:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
2252#    subu $sp,$sp,8
2256#    sw $ra,4($sp)

    # save current sp
2260#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
2264#    li $t3,10

    # get argument, negate if negative
2268#    lw $t1,8($sp)
2272#    bge $t1,$zero,itsNonNegLoop
2273##...slt $at,$t1,$zero
2277##...beq $at,$zero,2284
2280#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
2284:  itsNonNegLoop:
2284#    divu $t1,$t3
2288#    mflo $t1 # quotient
2292#    mfhi $t4 # remainder
2296#    addu $t4,$t4,'0' # turn remainder into digit
2300#    subu $sp,$sp,4
2304#    sw $t4,($sp) # push digit onto stack
2308#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
2312#    lw $t4,8($t0)
2316#    bge $t4,$zero,itsSkipNeg
2317##...slt $at,$t4,$zero
2321##...beq $at,$zero,2336
2324#    li $t4,'-'
2328#    subu $sp,$sp,4
2332#    sw $t4,($sp)
2336:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
2336#    subu $s6,$t0,$sp
2340#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
2344#    sw $t3,($t0)
2348#    srl $s6,$s6,2
2352#    subu $sp,$sp,4
2356#    sw $s6,($sp)
    
    # allocate memory
2360#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
2364#    srl $s6,$s6,2
2368#    move $s7,$zero # no "object" words in object
2372#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
2376#    lw $t0,4($sp)
2380#    addu $sp,$sp,8

    # store "String" tag into class field
2384#    subu $s7,$s7,8 # address of header-1 word
2388#    la $t1,CLASS_String
2392#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
2396#    lw $t4,($s7) # # data words in source string
2400#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
2401##...slt $at,$t4,$zero
2405##...beq $at,$zero,2424
2408#    and $t4,0xff # mask off object's hash value ...
2409##...lui $at,0
2413##...ori $at,$at,0xff
2417##...and $t4,$t4,$at
2420#    sw $t4,($s7) # ... and store back into -8 header-word
2424:skipMask_intToString:
    
    # store negative of char-count into header-2 word
2424#    subu $t0,$zero,$t0
2428#    sw $t0,4($s7)

2432#    lw $t1,($s7) # number of data words in string
2436#    sll $t1,$t1,2 # number data bytes in string
2440#    subu $t1,$s7,$t1 # first location to store chars in string
2444#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2448:  itsLoop:
2448#    lw $t2,($sp)
2452#    addu $sp,$sp,4
2456#    sb $t2,($t1)
2460#    addu $t1,$t1,1
2464#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2468#    lw $ra,4($sp)
2472#    addu $sp,$sp,8
2476#    addu $t0,$t0,15
2480#    and $t0,$t0,0xfffffffc
2481##...lui $at,65535
2485##...ori $at,$at,0xfffc
2489##...and $t0,$t0,$at
2492#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2496:mth_intToChar_Lib:
    # save return address
2496#    subu $sp,$sp,4
2500#    sw $ra,($sp)

    # allocate object
2504#    li $s6,2
2508#    move $s7,$zero
2512#    jal newObject

    # get n from string.
2516#    lw $t1,8($sp)

    # store "String" tag into class field
2520#    la $t0,CLASS_String
2524#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2528#    lw $t4,-8($s7) # # data words in source string
2532#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2533##...slt $at,$t4,$zero
2537##...beq $at,$zero,2556
2540#    and $t4,0xff # mask off object's hash value ...
2541##...lui $at,0
2545##...ori $at,$at,0xff
2549##...and $t4,$t4,$at
2552#    sw $t4,-8($s7) # ... and store back into -8 header-word
2556:  skipMask_intToChar:

    # store data in string
2556#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2560#    li $t0,-1
2564#    sw $t0,-4($s7)

    # we're returning $s7
2568#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2572#    lw $ra,4($sp)
2576#    addu $sp,$sp,8

    # return
2580#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2584:mth_compareTo_String:
    # get string pointers and check parameter for null
2584#    lw $t0,($sp) # second string
2588#    beq $t0,$zero,nullPtrException
2592#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2596#    lw $t2,-4($t0) # negative length of second string
2600#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2604#    slt $t4,$t3,$t2
2608#    slt $t5,$t2,$t3
2612#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2616#    lw $t2,-8($t0) # # data words in second string
2620#    lw $t3,-8($t1) # # data words in first string
2624#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2628#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2632#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2636#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2640#    beq $zero,$t5,skipStrCmpLenzz
2644#    move $t2,$t3
2648:  skipStrCmpLenzz:
2648#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2652:  cmpLoopzz:
2652#    lw $t3,-8($t1) # word from str2
2656#    lw $t4,-8($t0) # word from str1
2660#    bne $t3,$t4,cmpNotEqualzz
2664#    addu $t1,$t1,4
2668#    addu $t0,$t0,4
2672#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2676#    j cmpHitLimitzz

2680:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2680#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2684#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2688#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2692:  cmpHitLimitzz:
2692#    move $t0, $t5 # store return value
2696#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2700:readLogicalChar:

    # check if we already have a character
2700#    lw $v0,lastCharRead
2704#    li $t0,-2
2708#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2712#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2716#    jr $ra # return
    
2720:  doReadCh:
    # we need to actually read a character: read and return
2720#    li $v0,12 # use system call to read a character
2724#    syscall
2728#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2732:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2732#    blt $s7,$zero arraySizeOutOfBounds
2733##...slt $at,$s7,$zero
2737##...bne $at,$zero,3764

    # mark the fact that we have not yet GC'd for this allocation
2740#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2744:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2744#    addu $t0,$s6,$s7
2748#    sll $t0,$t0,2
2752#    addu $t0,$t0,8 # amount of memory needed
2756#    addu $t1,$t0,$s3
2760#    bgtu $t1,$s4,doGC
2761##...sltu $at,$s4,$t1
2765##...bne $at,$zero,2888

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2768#    move $t2,$s3
2772:  zeroObjLoop:
2772#    sw $zero,($s3)
2776#    lw $zero,($s3)

2780#    addu $s3,4
2784#    bltu $s3,$t1,zeroObjLoop
2785##...sltu $at,$s3,$t1
2789##...bne $at,$zero,2772

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2792#    sll $t0,$s7,2    # number of post-header bytes
2796#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2800#    li $t2,255
2804#    bgeu $s6,$t2 firstHeaderLarge
2805##...sltu $at,$s6,$t2
2809##...beq $at,$zero,2856
2812#    lw $t1,prevHashConstant
2816#    addu $t1,0x12b68fd2
2817##...lui $at,4791
2821##...addiu $at,$at,-28718
2825##...addu $t1,$at,$t1
2828#    sw $t1,prevHashConstant # update hash constant
2832#    or $t1,0x80000000 # set top bit
2833##...lui $at,32768
2837##...or $t1,$at,$t1
2840#    and $t1,0xffffff00 # clear low bit
2841##...lui $at,65535
2845##...ori $at,$at,0xff00
2849##...and $t1,$t1,$at
2852#    or $s6,$t1
2856:firstHeaderLarge:
2856#    sw $s6,-8($t0) # store header-1 value
2860#    lw $zero,-8($t0)

    # store second header-value
2864#    sw $s7,-4($t0)   # second header-word
2868#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2872#    move $s7,$t0
2876#    subu $sp,$sp,4
2880#    sw $t0,($sp)

2884#    jr $ra
    
2888:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2888#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2892#    lw $t5,gcCount
2896#    addu $t5,1
2900#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2904#    subu $sp,$sp,4
2908#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2912#    lw $t0,cur_seg
2916#    move $t7,$s4
2920#    la $t6,seg1_start
2921##...lui $t6,16
2925##...addiu $t6,$t6,4472
2928#    la $s3,seg0_start
2932#    la $s4,seg0_end
2933##...lui $s4,16
2937##...addiu $s4,$s4,4472
2940#    sw $zero,cur_seg
2944#    bne $t0,$zero,skipGc1
2948#    la $t6,seg0_start
2952#    la $s3,seg1_start
2953##...lui $s3,16
2957##...addiu $s3,$s3,4472
2960#    la $s4,seg1_end
2961##...lui $s4,32
2965##...addiu $s4,$s4,4472
2968#    sw $s5,cur_seg
2972:  skipGc1:
2972#    li $t5,0x80000000
2976#    lw $t0,stack_bottom
2980#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2984:  gcStackCopyLoop:
2984#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2988#    sll $t2,$t1,31 # zero iff low bit was zero
2992#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2996#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
3000#    bleu $t1,$t6,gcTestIterDone1
3001##...sltu $at,$t6,$t1
3005##...beq $at,$zero,3168
3008#    bgtu $t1,$t7,gcTestIterDone1
3009##...sltu $at,$t7,$t1
3013##...bne $at,$zero,3168

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3016#    lw $t2,-8($t1) # possible forwarding tag
3020#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
3024#    lw $t2,-4($t1) # forwarding pointer: object's new address
3028#    sw $t2,4($t0) # update stack value
3032#    j gcTestIterDone1 # do next iteration

3036:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
3036#    bge $t2,$zero,gcSkip2
3037##...slt $at,$t2,$zero
3041##...beq $at,$zero,3056
3044#    and $t2,0xff #### mask off hash value
3045##...lui $at,0
3049##...ori $at,$at,0xff
3053##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

3056:  gcSkip2:
3056#    sll $t2,$t2,2
3060#    addu $t2,$t2,8
3064#    subu $t4,$t1,$t2 # address of first word of source
3068#    lw $t3,-4($t1) # number of object words (negative means zero)
3072#    bge $t3,$zero,gcH2Neg1
3073##...slt $at,$t3,$zero
3077##...beq $at,$zero,3084
3080#    move $t3,$zero
3084:  gcH2Neg1:
3084#    sll $t3,$t3,2
3088#    addu $t3,$t3,$t1 # address one word past last word of source

3092#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
3096#    lw $s0,($t4)
3100#    lw $s1,-8($t1)
3104#    sw $s1,($t4)
3108#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
3112:  gCinnerCopy1:
3112#    lw $s0,($t4)
3116#    sw $s0,($s3)
3120#    addu $t4,$t4,4
3124#    addu $s3,$s3,4
3128#    bltu $t4,$t3,gCinnerCopy1
3129##...sltu $at,$t4,$t3
3133##...bne $at,$zero,3112

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3136#    sw $zero,-8($t1) # 0 in first header-slot
3140#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
3144#    sw $t2,4($t0)
3148#    lw $zero,-8($t2)
3152#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
3156#    j gcTestIterDone1

3160:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
3160#    subu $t0,$t0,$t1
3164#    subu $t0,$t0,3

3168:  gcTestIterDone1:
3168#    bgeu $t0,$sp,gcStackCopyLoop
3169##...sltu $at,$t0,$sp
3173##...beq $at,$zero,2984

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
3176#    lw $t1,cur_seg
3180#    la $t0,seg0_start
3184#    beq $t1,$zero,gcSkip4
3188#    la $t0,seg1_start
3189##...lui $t0,16
3193##...addiu $t0,$t0,4472


3196:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
3196#    bgeu $t0,$s3,gcFinishUp
3197##...sltu $at,$t0,$s3
3201##...beq $at,$zero,3492

    ###### begin outer loop to copy all non-stack-direct objects ######

3204:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
3204#    lw $t1,($t0) # (what will be) first header word for current object
3208#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
3212#    bge $t2,$zero,gcSkip4a # are there hash bits?
3213##...slt $at,$t2,$zero
3217##...beq $at,$zero,3232
3220#    and $t2,0xff # mask off hash bits
3221##...lui $at,0
3225##...ori $at,$at,0xff
3229##...and $t2,$t2,$at
3232:gcSkip4a:    
3232#    sll $t2,$t2,2 # # data bytes
3236#    addu $t2,$t2,8 # to skip header words
3240#    addu $t2,$t0,$t2 # pointer to new object
3244#    lw $t3,-8($t2) # word to swap
3248#    sw $t1,-8($t2) # store header word
3252#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
3256#    lw $t1,-12($t2) # vtable pointer
3260#    li $t0,dataArrayVTableStart # address of vtable for first data array
3264#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
3265##...slt $at,$t1,$t0
3269##...bne $at,$zero,3288

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
3272:  skipToNextObj:
3272#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
3276#    sll $t1,$t1,2 # convert to byte-count
3280#    addu $t0,$t2,$t1 # skip over object
3284#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
3288:  gcNotDataArray:
3288#    lw $t3,-4($t2) # # object words (negative treated as zero)
3292#    bge $t3,$zero,gcH2Neg2
3293##...slt $at,$t3,$zero
3297##...beq $at,$zero,3304
3300#    move $t3,$zero
3304:  gcH2Neg2:
3304#    sll $t3,$t3,2 # # object bytes
3308#    move $t0,$t2 # address of first pointer in object
3312#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
3316#    beq $t0,$s1,gcTestIterDone2
    
3320:  gcNextPointerInObject:
    # get pointer from object
3320#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
3324#    bleu $t1,$t6,gcGoNextPointerSlot
3325##...sltu $at,$t6,$t1
3329##...beq $at,$zero,3472
3332#    bgtu $t1,$t7,gcGoNextPointerSlot
3333##...sltu $at,$t7,$t1
3337##...bne $at,$zero,3472

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3340#    lw $t8,-8($t1) # possible forwarding tag
3344#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
3348#    lw $t8,-4($t1) # forwarding pointer: object's new address
3352#    sw $t8,($t0) # update pointer in object
3356#    j gcGoNextPointerSlot # do next iteration

3360:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
3360#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
3361##...slt $at,$t8,$zero
3365##...beq $at,$zero,3380
3368#    and $t8,0xff # mask off hash bits
3369##...lui $at,0
3373##...ori $at,$at,0xff
3377##...and $t8,$t8,$at

3380:  gcSkip5:
3380#    sll $t8,$t8,2 # bytes in negative-offset portion ...
3384#    addu $t8,$t8,8 # ... add in bytes for header-words
3388#    subu $t4,$t1,$t8 # address of first word of source object
3392#    lw $t3,-4($t1) # # words in positive-offset portion
3396#    bge $t3,$zero,gcNoNeg
3397##...slt $at,$t3,$zero
3401##...beq $at,$zero,3408
3404#    move $t3,$zero # negative treated as zero
3408:  gcNoNeg:
3408#    sll $t3,$t3,2 # bytes in negative-offset portion
3412#    addu $t3,$t3,$t1 # address one word past last word of source

3416#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
3420#    lw $s0,($t4)
3424#    lw $t9,-8($t1)
3428#    sw $t9,($t4)
3432#    sw $s0,-8($t1)

    # copy all source bytes to the target
3436:  gCinnerCopy2:
3436#    lw $s0,($t4)
3440#    sw $s0,($s3)
3444#    addu $t4,$t4,4
3448#    addu $s3,$s3,4
3452#    bltu $t4,$t3,gCinnerCopy2
3453##...sltu $at,$t4,$t3
3457##...bne $at,$zero,3436

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3460#    sw $zero,-8($t1) # 0 in first header-slot
3464#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3468#    sw $t8,($t0)

3472:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3472#    addu $t0,$t0,4
3476#    bltu $t0,$s1,gcNextPointerInObject
3477##...sltu $at,$t0,$s1
3481##...bne $at,$zero,3320

3484:  gcTestIterDone2:
3484#    bltu $t0,$s3,gcHeapCopyLoop
3485##...sltu $at,$t0,$s3
3489##...bne $at,$zero,3204

    ###### end outer loop to copy all non-stack-direct objects ######

3492:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3492#    lw $s2,($sp)
3496#    addu $sp,$sp,4

    # mark us as having already GC'd
3500#    move $t5,$s5

    # go try and allocate again
3504#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3508:vm_init:

    # mark bottom of stack
3508#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3512#    move $s2,$zero

    #set up the "five" register
3516#    li $s5,5

3520#    la $s3,seg0_start
3524#    la $s4,seg0_end
3525##...lui $s4,16
3529##...addiu $s4,$s4,4472
    
    # return
3532#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3536:divide:
    # get parameters; abort if divisor zero
3536#    lw $t0,($sp)
3540#    lw $t1,8($sp)
3544#    beq $t0,$zero,divByZeroError
    
    # perform division
3548#    div $t1,$t0
3552#    mflo $t0

    # store result, adjust stack and return
3556#    addu $sp,$sp,8 # adjust stack
3560#    sw $t0,($sp) # store result
3564#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3568:remainder:
    # get parameters; abort if divisor zero
3568#    lw $t0,($sp)
3572#    lw $t1,8($sp)
3576#    beq $t0,$zero,divByZeroError
    
    # perform division
3580#    div $t1,$t0
3584#    mfhi $t0

    # store result, adjust stack and return
3588#    addu $sp,$sp,8 # adjust stack
3592#    sw $t0,($sp) # store result
3596#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3600:checkCast:
    # get object, allow immediately if null
3600#    lw $t2,($sp)
3604#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3608#    lw $t2,-12($t2) # vtable address
3612#    bge $t2,$t1,castException
3613##...slt $at,$t2,$t1
3617##...beq $at,$zero,3788
3620#    blt $t2,$t0,castException
3621##...slt $at,$t2,$t0
3625##...bne $at,$zero,3788
    
    # return, leaving object unchanged on stack
3628:  checkCastReturn:    
3628#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3632:instanceOf:
    # get object, go return false if null
3632#    lw $t2,($sp)
3636#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3640#    lw $t2,-12($t2) # vtable address
3644#    sge $t0,$t2,$t0 # are we >= the lower limit?
3645##...slt $t0,$t2,$t0
3649##...xori $t0,$t0,0x1
3652#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3656#    and $t2,$t0,$t1
3660#    sw $t2,($sp)
3664:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3664#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3668:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3668#    li $v0,4 # syscall-code for print-string
3672#    syscall
3676:exit:
    # print termination message
3676#    li $v0,4 # syscall-code for print-string
3680#    la $a0,exitString
3684#    syscall

    # print number of garbage collections
3688#    li $v0,4 # syscall-code for print-string
3692#    la $a0,gcCountMsg
3696#    syscall # print "Number of ..."
3700#    lw $a0,gcCount # number of GC's
3704#    li $v0,1 # code for print-int
3708#    syscall
3712#    li $v0,4 # syscall-code for print-string
3716#    la $a0,newlineString
3720#    syscall # print newline

    # terminate execution
3724#    li $v0,10 #syscall-code for "exit"
3728#    syscall

3732:outOfHeapMemory:
3732#    la $a0,heapMemoryMsg
3736#    j exitError
3740:divByZeroError:
3740#    la $a0,divByZeroMsg
3744#    j exitError
3748:strIndexOutOfBounds:
3748#    la $a0,strIndexOutOfBoundsMsg
3752#    j exitError
3756:arrayIndexOutOfBounds:
3756#    la $a0,arrayIndexOutOfBoundsMsg
3760#    j exitError
3764:arraySizeOutOfBounds:
3764#    la $a0,arraySizeOutOfBoundsMsg
3768#    j exitError
3772:nullPtrException:
3772#    la $a0,nullPtrExceptionMsg
3776#    j exitError
3780:badIntegerFormat:
3780#    la $a0,badIntegerFormatMsg
3784#    j exitError
3788:castException:
3788#    la $a0,castExceptionMsg
3792#    j exitError
############## data section ################
    .data
4100:hexChars:
    .ascii "0123456789abcdef"
    .align 2
4116:cur_seg:
    .word 0

4120:lastCharRead:
    .word -2 # -2 => no buffered character
4124:prevHashConstant:
    .word 0x2785173c
4128:gcCount:
    .word 0

4132:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
4159:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
4182:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
4217:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
4251:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
4284:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
4315:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
4363:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
4384:gcMsg: 
      .asciiz "\nGC!\n"
4390:gcCountMsg: 
      .asciiz "Number of garbage collections: "

4422:nullStr:
    .asciiz "null"
4427:trueString:
    .asciiz "true"
4432:falseString:
    .asciiz "false"
4438:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
4466:newlineString:
    .asciiz "\n"
    
    .align 2
4468:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

4472:seg0_start:
    .space 0x100000
1053048:seg0_end:
1053048:seg1_start:
    .space 0x100000
2101624:seg1_end:
