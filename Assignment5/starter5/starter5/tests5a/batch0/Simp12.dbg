 .data # Program at 1.0
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3476:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3496:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3528:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3532:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 1.6
  .byte 205 # 'M' with high bit set # ClassDecl at 1.6
  .byte 97 # 'a' # ClassDecl at 1.6
  .byte 105 # 'i' # ClassDecl at 1.6
  .byte 110 # 'n' # ClassDecl at 1.6
3580:CLASS_Main: # ClassDecl at 1.6
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 2.15
3628:END_CLASS_Main: # ClassDecl at 1.6
3628:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3636:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3648:END_CLASS_RunMain: # ClassDecl at 0.0
3648:dataArrayVTableStart: # IntType at 0.0
3648:END_CLASS_Object: # ClassDecl at 0.0
  .byte 10 # StringLit at 6.26
  .byte 0 # StringLit at 6.26
  .byte 0 # StringLit at 6.26
  .byte 0 # StringLit at 6.26
  .word CLASS_String # StringLit at 6.26
  .word 2 # StringLit at 6.26
  .word -1 # StringLit at 6.26
3664:strLit_16: # StringLit at 6.26
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp, $sp, 4
28#  move $s2, $s7
32#  jal mth_main_Main
36#  li $v0, 10
40#  syscall
.globl mth_main_Main
44:mth_main_Main:
44#  subu $sp, $sp, 4
48#  sw $ra, ($sp)
52#  li $t0, 321
56#  subu $sp, $sp, 8
60#  sw $zero, 4($sp)
64#  sw $t0, ($sp)
68#  lw $t0, 0($sp)
72#  subu $sp, $sp, 8
76#  sw $zero, 4($sp)
80#  sw $t0, ($sp)
84#  li $t0, 18
88#  subu $sp, $sp, 8
92#  sw $zero, 4($sp)
96#  sw $t0, ($sp)
100#  lw $t2, ($sp)
104#  addu $sp, $sp, 8
108#  lw $t1, ($sp)
112#  addu $sp, $sp, 8
116#  addu $t0, $t1, $t2
120#  subu $sp, $sp, 8
124#  sw $zero, 4($sp)
128#  sw $t0, ($sp)
132#  subu $sp, $sp, 4
136#  sw $s2, ($sp)
140#  lw $t0, 4($sp)
144#  subu $sp, $sp, 8
148#  sw $zero, 4($sp)
152#  sw $t0, ($sp)
156#  li $t0, 17
160#  subu $sp, $sp, 8
164#  sw $zero, 4($sp)
168#  sw $t0, ($sp)
172#  lw $t2, ($sp)
176#  addu $sp, $sp, 8
180#  lw $t1, ($sp)
184#  addu $sp, $sp, 8
188#  addu $t0, $t1, $t2
192#  subu $sp, $sp, 8
196#  sw $zero, 4($sp)
200#  sw $t0, ($sp)
204#  lw $t0, 8($sp)
208#  sw $s2, 8($sp)
212#  move $s2, $t0
216#  jal mth_printInt_Lib
220#  addu $sp, $sp, 8
224#  lw $s2, ($sp)
228#  addu $sp, $sp, 4
232#  subu $sp, $sp, 4
236#  sw $t0, ($sp)
240#  lw $t0, ($sp)
244#  addu $sp, $sp, 4
248#  subu $sp, $sp, 4
252#  sw $s2, ($sp)
256#  la $t0, strLit_16
260#  subu $sp, $sp, 4
264#  sw $t0, ($sp)
268#  lw $t0, 4($sp)
272#  sw $s2, 4($sp)
276#  move $s2, $t0
280#  jal mth_printStr_Lib
284#  addu $sp, $sp, 4
288#  lw $s2, ($sp)
292#  addu $sp, $sp, 4
296#  subu $sp, $sp, 4
300#  sw $t0, ($sp)
304#  lw $t0, ($sp)
308#  addu $sp, $sp, 4
312#  addu $sp, $sp, 16
316#  lw $ra, ($sp)
320#  addu $sp, $sp, 4
324#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
328:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
328#    lw $t0,-8($s2) # get word with hash bits from this
332#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
336#    addu $t0,$t1

340#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
344:mth_equals_Object:
344#    move $t0,$s2 # first pointer (this)
348#    lw $t1,($sp) # second pointer
352#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
353##...subu $at,$t0,$t1
357##...sltiu $t0,$at,1

360#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
364:mth_hashCode_String:
364#    move $t0,$s2 # put this pointer in t0

368#    li $t2,0 # current hash value
372#    lw $t1,-4($t0) # negative of number of characters in string
376#    beq $t1,$zero,done_hashCode_String # if zero length => done
380#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
384#    and $t0,0xfffffffc # adjust for padding
385##...lui $at,65535
389##...ori $at,$at,0xfffc
393##...and $t0,$t0,$at
396#    subu $t1,$t0,$t1 # address at end of string +12
400:loop_hashCode_String:    
400#    sll $t4,$t2,5 # multiply by 32
404#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
408#    lbu $t3,-12($t0) # current character
412#    addu $t2,$t3 # add in current character
416#    addu $t0,1 # go to next character's address
420#    bne $t0,$t1,loop_hashCode_String # loop back if more
424:done_hashCode_String:    

    # push int on stack
424#    move $t0, $t2 # put $t2 in return slot
428#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
432:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
432#    subu $sp,4
436#    sw $ra,($sp)
    
    # get hash value
440#    lw $t2,-12($s2) # vtable address
444#    lw $t0,($t2) # method address
448#    jalr $t0
452#    subu $sp,8 # push result (int in $t0) on stack
456#    sw $s5,4($sp)
460#    sw $t0,($sp)

    # compute print-size of hash value
464#    li $t0, 1
468#    lw $t1, ($sp)
472#    srl $t1,$t1,4
476#    beq $t1,$zero,toString_skip_loop
480:toString_loop_top:
480#    addu $t0,1
484#    srl $t1,$t1,4
488#    bne $t1,$zero,toString_loop_top
492:toString_skip_loop:
492#    subu $sp,8
496#    sw $s5,4($sp)
500#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
504#    lw $t2,-12($s2) # vtable address
508#    move $t0,$t2
512:toString_checkTagBit:
512#    subu $t0,1
516#    lb $t1,($t0)
520#    and $t1,0x80
521##...lui $at,0
525##...ori $at,$at,0x80
529##...and $t1,$t1,$at
532#    beq $t1,$zero,toString_checkTagBit
536#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
540#    subu $sp,8
544#    sw $t1,($sp)
548#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
552#    move $s7,$zero # -4 offset header word
556#    lw $t4,8($sp) # bytes for hash value
560#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
564#    addu $t4,8
568#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
572#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

576#    li $t0,CLASS_String # store string vtable address into ...
580#    sw $t0,-12($s7) # ... -12 offset in object header
584#    lw $t2,-12($s2) # our object's vtable address
588#    lw $t5,4($sp) # number characters in prefix
592#    lw $t1,12($sp) # number of characters in hashcode
596#    addu $t3,$t1,$t5 # number of characters in string ...
600#    addu $t3,1 # ... including the '@' character
604#    subu $t0,$zero,$t3 # store negated length into ...
608#    sw $t0,-4($s7) # ... -4 offset header word
612#    lw $t0,-8($s7) # get -8 offset header-word
616#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
617##...slt $at,$t0,$zero
621##...beq $at,$zero,640
624#    and $t0,0xff # mask off object's hash value ...
625##...lui $at,0
629##...ori $at,$at,0xff
633##...and $t0,$t0,$at
636#    sw $t0,-8($s7) # ... and store back into -8 header-word
640:skipMask_toString:
640#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
644#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
648#    subu $t4,$t2,$t5
652:toString_copyLoop:    
652#    lb $t3,($t4)
656#    and $t3,0x7f # mask off sign bit (in case first word)
657##...lui $at,0
661##...ori $at,$at,0x7f
665##...and $t3,$t3,$at
668#    sb $t3,-8($t0)
672#    addu $t4,1
676#    addu $t0,1
680#    blt $t4,$t2,toString_copyLoop
681##...slt $at,$t4,$t2
685##...bne $at,$zero,652

    # copy the '@' sign
688#    li $t3,'@'
692#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
696#    addu $t2,$t0,$t1 # address of last byte in string to store +8
700#    lw $t3,20($sp) # hash code to print
704:toString_hashCopyLoop:
704#    and $t4,$t3,0xf
708#    srl $t3,4
712#    lb $t4,hexChars($t4)
716#    sb $t4,-8($t2)
720#    subu $t2,$t2,1
724#    bgt $t2,$t0,toString_hashCopyLoop
725##...slt $at,$t0,$t2
729##...bne $at,$zero,704

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
732#    addu $sp,28
736#    lw $ra,($sp)
740#    addu $sp,4
744#    move $t0, $s7 # return string we created
748#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
752:mth_equals_String:
    
    # quick test for obvious false
752#    lw $t0,($sp) # second parameter
756#    beq $t0,$zero,goEsFalse # go return false if null
760#    lw $t1,-12($t0) # vtable pointer
764#    la $t2,CLASS_String # string vtable pointer
768#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
772#    subu $sp,12
776#    sw $ra,8($sp)
780#    sw $s2,4($sp)
784#    sw $t0,($sp)
788#    jal mth_compareTo_String
792#    lw $s2, 4($sp) #restore this pointer and pop parameters
796#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
800#    seq $t0,$t0,$zero
801##...subu $at,$t0,$zero
805##...sltiu $t0,$at,1

    # pop return address and return $t0
808#    lw $ra,($sp)
812#    addu $sp,4

816#    jr $ra # return
    
820:goEsFalse:
    # put 0 in $t0 and return
820#    li $t0, 0
824#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
828:mth_toString_String:
    #we are a string, so return this.
828#    move $t0, $s2
832#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
836:mth_readLine_Lib:
    
836#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
840#    sw $ra, 4($sp) # save $ra
844#    move $t1,$sp # save "original" sp

848:  doRead:

    # read the character
848#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
852#    subu $t2,$v0,13
856#    bne $t2,$zero,notReturnRL
860#    jal readLogicalChar
864#    subu $t2,$v0,10 # check for newline
868#    beq $t2,$zero,foundNewLine
872#    sw $v0,lastCharRead # push back character into queue
876#    j foundNewLine # go process the line

880:  notReturnRL:

    # at this point, $v0 has our character

880#    subu $t0,$v0,10
884#    beq $t0,$zero,foundNewLine
888#    blt $v0,$zero,foundEof
889##...slt $at,$v0,$zero
893##...bne $at,$zero,908

    # we have a character, so push it onto stack
896#    subu $sp,$sp,4
900#    sw $v0,($sp)

    # loop back up to get next character
904#    j doRead

908:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
908#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
912#    li $s7, 0 # return-value: null
916#    j rlReturn # return

920:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
920#    subu $t0,$t1,$sp # number of chars we read (times 4)
924#    srl $s6,$t0,2 # number of words on stack with chars
928#    subu $sp,$sp,4 # push char-count ...
932#    sw $s6,($sp) # ... onto stack
936#    addu $t2,$t0,5 # GC tag, incl. for count-word
940#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
944#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
948#    srl $s6,$s6,2  # data words in object
952#    move $s7,$zero # object words in object
956#    jal newObject  # allocate space
    
    # store header words
960#    la $t0,CLASS_String
964#    sw $t0,-12($s7)# store class tag (String) into object
968#    lw $t2,4($sp) # char-count
972#    sll $t1,$t2,2 # 4 times number of chars
976#    subu $t2,$zero,$t2 # negative of char-count
980#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
984#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
988#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
989##...slt $at,$t0,$zero
993##...beq $at,$zero,1012
996#    and $t0,0xff # mask off object's hash value ...
997##...lui $at,0
1001##...ori $at,$at,0xff
1005##...and $t0,$t0,$at
1008#    sw $t0,-8($s7) # ... and store back into -8 header-word
1012:  skipMask_readLine:

1012#    sll $t0,$t0,2 # data bytes in object
1016#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
1020#    subu $t0,$t0,$t2 # place to store last character (plus 9)
1024#    addu $sp,$sp,8 # pop redundant object-pointer and count
1028#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

1032#    beq $sp,$t1,rlReturn
1036:  charCopyLoop:
1036#    lw $t2,($sp)
1040#    sb $t2,-9($t0)
1044#    addu $sp,$sp,4
1048#    subu $t0,$t0,1
1052#    bne $sp,$t1,charCopyLoop
1056:  rlReturn:

1056#    lw $ra, 4($sp)
1060#    addu $sp, 8   # pop $ra and pointless word off stack
1064#    move $t0, $s7 # return string we allocated in $s7
1068#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1072:mth_readInt_Lib:

    # save $ra by pushing onto stack
1072#    subu $sp,$sp,4
1076#    sw $ra,($sp)

1080:  riSkipWhiteLoop:
    # read a character
1080#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
1084#    subu $t0,$v0,32
1088#    bgt $t0,$zero,nonWhite
1089##...slt $at,$zero,$t0
1093##...bne $at,$zero,1136
1096#    beq $t0,$zero,riSkipWhiteLoop # start over if space
1100#    subu $t0,$v0,10
1104#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
1108#    subu $t0,$v0,9
1112#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
1116#    subu $t0,$v0,13
1120#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1124#    subu $t0,$v0,12
1128#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1132#    j badIntegerFormat # illegal integer char: abort program

1136:  nonWhite:
1136#    subu $t0,$v0,'-'
1140#    li $t4,1 # final multiplier
1144#    bne $t0,$zero,helpReadInt # go read
1148#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1152#    jal readLogicalChar

1156:  helpReadInt:
1156#    li $t2,10
1160#    subu $t1,$v0,'0' # convert digit to 0-9 value
1164#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1165##...sltu $at,$t1,$t2
1169##...beq $at,$zero,3452
1172#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1176:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1176#    jal readLogicalChar # read next character
1180#    subu $t1,$v0,'0' # convert digit to 0-9 value
1184#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1185##...sltu $at,$t1,$t2
1189##...beq $at,$zero,1208

1192#    mul $t3,$t3,$t2 # multiply old value by 10
1193##...mult $t3,$t2
1197##...mflo $t3
1200#    addu $t3,$t3,$t1 # add in value of new digit
1204#    j digitLoop

1208:  doneDigitLoop:

    # "push back" unused character into queue
1208#    sw $v0,lastCharRead

    # restore return address
1212#    lw $ra,($sp)
1216#    addu $sp, 4

    # multiply by sign and put return value in $t0
1220#    mult $t3,$t4 # multiply to account for poss. minus sign
1224#    mflo $t3
1228#    move $t0, $t3
    
    # return
1232#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1236:mth_readChar_Lib:

    # save $ra by pushing onto stack
1236#    subu $sp,$sp,4
1240#    sw $ra,($sp)

      # read the character
1244#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1248#    lw $ra,($sp)
1252#    addu $sp, 4
1256#    move $t0, $v0

    # return
1260#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1264:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1264#    lw $t0,($sp)
1268#    bne $t0,$zero,psNotNull

    # print "(null)"
1272#    la $a0,nullStr
1276#    li $v0,4
1280#    syscall
1284#    j donePrintStr
    
1288:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1288#    subu $t0,$t0,8
1292#    lw $t1,($t0) # number of data words, but needs modification
1296#    blt $t1,$zero,skipPrintStr1
1297##...slt $at,$t1,$zero
1301##...bne $at,$zero,1320
1304#    and $t1,0xff # mask out all but low byte
1305##...lui $at,0
1309##...ori $at,$at,0xff
1313##...and $t1,$t1,$at
1316#    b skipPrintStr2
1320:skipPrintStr1:
1320#    and $t1,0x7ffffff
1321##...lui $at,2047
1325##...ori $at,$at,0xffff
1329##...and $t1,$t1,$at
1332:skipPrintStr2:    
1332#    sll $t1,$t1,2
1336#    subu $t1,$t0,$t1 # first word in string
1340#    lw $t0,4($t0) # negative of string-length
1344#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1348#    beq $t0,$t1,donePrintStr
1352:  psLoop:
1352#    lb $a0,($t1) # next byte
1356#    li $v0,11 # code for printing char
1360#        syscall # print the char
1364#    addu $t1,$t1,1 # go to next char
1368#    blt $t1,$t0,psLoop
1369##...slt $at,$t1,$t0
1373##...bne $at,$zero,1352

1376:  donePrintStr:
1376#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1380:mth_printInt_Lib:
1380#    lw $a0,($sp)

    # print it
1384#    li $v0,1 # code for print-int
1388#    syscall

    # return
1392#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1396:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1396#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1400#    la $a0,falseString
1404#    beq $t0,$zero,skipPB
1408#    la $a0,trueString
1412:  skipPB:
1412#    li $v0,4 # code for print-string
1416#    syscall

    # return
1420#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1424:mth_substring_String:

    # save $ra by pushing onto stack
1424#    subu $sp,$sp,4
1428#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1432#    move $t0,$s2
1436#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1440#    lw $t0,-4($t0) # negative of string length
1444#    subu $t0,$zero,$t0 # string length
1448#    lw $t1,4($sp) # k
1452#    lw $t2,12($sp) # n
1456#    bgt $zero,$t2,strIndexOutOfBounds
1457##...slt $at,$t2,$zero
1461##...bne $at,$zero,3420
1464#    bgt $t2,$t1,strIndexOutOfBounds
1465##...slt $at,$t1,$t2
1469##...bne $at,$zero,3420
1472#    bgt $t1,$t0,strIndexOutOfBounds
1473##...slt $at,$t0,$t1
1477##...bne $at,$zero,3420

    # allocate memory
1480#    subu $s6,$t1,$t2 # # chars in target-string
1484#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1488#    srl $s6,$s6,2 # convert bytes-count to word-count
1492#    move $s7,$zero # (no object-bytes in string)
1496#    jal newObject

    # store "String" tag in object-type field
1500#    la $t0,CLASS_String
1504#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1508#    lw $t4,-8($s7) # data words in source string
1512#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1513##...slt $at,$t4,$zero
1517##...beq $at,$zero,1536
1520#    and $t4,0xff # mask off object's hash value ...
1521##...lui $at,0
1525##...ori $at,$at,0xff
1529##...and $t4,$t4,$at
1532#    sw $t4,-8($s7) # ... and store back into -8 header-word
1536:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1536#    lw $t1,8($sp) # k
1540#    lw $t2,16($sp) # n
1544#    subu $t0,$t2,$t1 # value to store
1548#    sw $t0,-4($s7) # store value
    
1552#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1556#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1560#    lw $t4,-8($t3) # data words in source string
1564#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1568#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1572#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1576#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1580#    lw $t2,-8($s7)   # data words in target string
1584#    sll $t2,$t2,2    # data bytes in target string
1588#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1592:  subCopyLoopzz:
1592#    lb $t4,-8($t3)
1596#    sb $t4,-8($t2)
1600#    addu $t2,$t2,1
1604#    addu $t3,$t3,1
1608#    blt $t3,$t1,subCopyLoopzz
1609##...slt $at,$t3,$t1
1613##...bne $at,$zero,1592
    
1616:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1616#    lw $ra,4($sp) # restore $ra
1620#    addu $sp,$sp,8 # pop ra, and allocated newObject
1624#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1628#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1632:mth_length_String:

    # the length is the negative of 4($s2)
1632#    lw $t0,-4($s2) # -length
1636#    subu $t0,$zero,$t0

    # return
1640#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1644:mth_concat_String:
    # save $ra by pushing onto stack
1644#    subu $sp,$sp,4
1648#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1652#    lw $t0,4($sp)
1656#    beq $t0,$zero,nullPtrException
1660#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1664#    lw $t0,-4($t0) # negative size of second object
1668#    lw $t1,-4($t1) # negative size of first object
1672#    addu $s6,$t0,$t1 # sum of negative sizes
1676#    sra $s6,$s6,2 # negative word-size of char part
1680#    subu $s6,$zero,$s6 # word size of char part
1684#    addu $s6,$s6,1 # data word size, including v-table word
1688#    move $s7,$zero
1692#    jal newObject

    # store String vtable address in vtable address field
1696#    la $t0,CLASS_String
1700#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1704#    lw $t4,-8($s7) # # data words in source string
1708#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1709##...slt $at,$t4,$zero
1713##...beq $at,$zero,1732
1716#    and $t4,0xff # mask off object's hash value ...
1717##...lui $at,0
1721##...ori $at,$at,0xff
1725##...and $t4,$t4,$at
1728#    sw $t4,-8($s7) # ... and store back into -8 header-word
1732:skipMask_concat:

1732#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1736#    move $t1,$s2   # first object
1740#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1744#    lw $t2,-4($t0) # negative length of second object
1748#    lw $t3,-4($t1) # negative length of first object
1752#    addu $t4,$t2,$t3 # sum of negative lengths
1756#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1760#    lw $t4,-8($t1) # # data words in first string
1764#    sll $t4,$t4,2 # # data bytes in first string
1768#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1772#    lw $t4,-8($s7) # # data words in new string
1776#    sll $t4,$t4,2 # # data bytes in new string
1780#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1784#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1788#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1792:  concatLoop1zz:
1792#    lb $t4,-8($t1)
1796#    sb $t4,-8($s7)
1800#    addu $s7,$s7,1
1804#    addu $t1,$t1,1
1808#    blt $t1,$t3,concatLoop1zz
1809##...slt $at,$t1,$t3
1813##...bne $at,$zero,1792
1816:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1816#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1820#    lw $t4,-8($t0) # # data words in second string
1824#    sll $t4,$t4,2 # # data bytes in second string
1828#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1832#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1836:  concatLoop2zz:
1836#    lb $t4,-8($t1)
1840#    sb $t4,-8($s7)
1844#    addu $s7,$s7,1
1848#    addu $t1,$t1,1
1852#    blt $t1,$t3,concatLoop2zz
1853##...slt $at,$t1,$t3
1857##...bne $at,$zero,1836
1860:  doneConcatLoop2zz:
 
1860:  concatRtnzz:
    # return
1860#    move $t0, $t9 # put saved return string in $t0
1864#    lw $ra, 4($sp)
1868#    addu $sp, 8 # pop ra and allocated newObject
1872#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1876:mth_charAt_String:
    # get string
1876#    move $t0,$s2

    # check that index is in bounds
1880#    lw $t1,-4($t0) # negative of # data words in string
1884#    subu $t3,$zero,$t1 # # chars in string
1888#    lw $t2,($sp) # index
1892#    bgeu $t2,$t3,strIndexOutOfBounds
1893##...sltu $at,$t2,$t3
1897##...beq $at,$zero,3420

    # access element
1900#    lw $t1,-8($t0) # # data words in object
1904#    sll $t1,$t1,2 # - byte-offset from end of chars
1908#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1912#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1916#    lb $t0,-8($t0) # our char

    # return
1920#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1924:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1924#    subu $sp,$sp,8
1928#    sw $ra,4($sp)

    # save current sp
1932#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1936#    li $t3,10

    # get argument, negate if negative
1940#    lw $t1,8($sp)
1944#    bge $t1,$zero,itsNonNegLoop
1945##...slt $at,$t1,$zero
1949##...beq $at,$zero,1956
1952#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1956:  itsNonNegLoop:
1956#    divu $t1,$t3
1960#    mflo $t1 # quotient
1964#    mfhi $t4 # remainder
1968#    addu $t4,$t4,'0' # turn remainder into digit
1972#    subu $sp,$sp,4
1976#    sw $t4,($sp) # push digit onto stack
1980#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1984#    lw $t4,8($t0)
1988#    bge $t4,$zero,itsSkipNeg
1989##...slt $at,$t4,$zero
1993##...beq $at,$zero,2008
1996#    li $t4,'-'
2000#    subu $sp,$sp,4
2004#    sw $t4,($sp)
2008:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
2008#    subu $s6,$t0,$sp
2012#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
2016#    sw $t3,($t0)
2020#    srl $s6,$s6,2
2024#    subu $sp,$sp,4
2028#    sw $s6,($sp)
    
    # allocate memory
2032#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
2036#    srl $s6,$s6,2
2040#    move $s7,$zero # no "object" words in object
2044#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
2048#    lw $t0,4($sp)
2052#    addu $sp,$sp,8

    # store "String" tag into class field
2056#    subu $s7,$s7,8 # address of header-1 word
2060#    la $t1,CLASS_String
2064#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
2068#    lw $t4,($s7) # # data words in source string
2072#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
2073##...slt $at,$t4,$zero
2077##...beq $at,$zero,2096
2080#    and $t4,0xff # mask off object's hash value ...
2081##...lui $at,0
2085##...ori $at,$at,0xff
2089##...and $t4,$t4,$at
2092#    sw $t4,($s7) # ... and store back into -8 header-word
2096:skipMask_intToString:
    
    # store negative of char-count into header-2 word
2096#    subu $t0,$zero,$t0
2100#    sw $t0,4($s7)

2104#    lw $t1,($s7) # number of data words in string
2108#    sll $t1,$t1,2 # number data bytes in string
2112#    subu $t1,$s7,$t1 # first location to store chars in string
2116#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2120:  itsLoop:
2120#    lw $t2,($sp)
2124#    addu $sp,$sp,4
2128#    sb $t2,($t1)
2132#    addu $t1,$t1,1
2136#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2140#    lw $ra,4($sp)
2144#    addu $sp,$sp,8
2148#    addu $t0,$t0,15
2152#    and $t0,$t0,0xfffffffc
2153##...lui $at,65535
2157##...ori $at,$at,0xfffc
2161##...and $t0,$t0,$at
2164#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2168:mth_intToChar_Lib:
    # save return address
2168#    subu $sp,$sp,4
2172#    sw $ra,($sp)

    # allocate object
2176#    li $s6,2
2180#    move $s7,$zero
2184#    jal newObject

    # get n from string.
2188#    lw $t1,8($sp)

    # store "String" tag into class field
2192#    la $t0,CLASS_String
2196#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2200#    lw $t4,-8($s7) # # data words in source string
2204#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2205##...slt $at,$t4,$zero
2209##...beq $at,$zero,2228
2212#    and $t4,0xff # mask off object's hash value ...
2213##...lui $at,0
2217##...ori $at,$at,0xff
2221##...and $t4,$t4,$at
2224#    sw $t4,-8($s7) # ... and store back into -8 header-word
2228:  skipMask_intToChar:

    # store data in string
2228#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2232#    li $t0,-1
2236#    sw $t0,-4($s7)

    # we're returning $s7
2240#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2244#    lw $ra,4($sp)
2248#    addu $sp,$sp,8

    # return
2252#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2256:mth_compareTo_String:
    # get string pointers and check parameter for null
2256#    lw $t0,($sp) # second string
2260#    beq $t0,$zero,nullPtrException
2264#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2268#    lw $t2,-4($t0) # negative length of second string
2272#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2276#    slt $t4,$t3,$t2
2280#    slt $t5,$t2,$t3
2284#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2288#    lw $t2,-8($t0) # # data words in second string
2292#    lw $t3,-8($t1) # # data words in first string
2296#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2300#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2304#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2308#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2312#    beq $zero,$t5,skipStrCmpLenzz
2316#    move $t2,$t3
2320:  skipStrCmpLenzz:
2320#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2324:  cmpLoopzz:
2324#    lw $t3,-8($t1) # word from str2
2328#    lw $t4,-8($t0) # word from str1
2332#    bne $t3,$t4,cmpNotEqualzz
2336#    addu $t1,$t1,4
2340#    addu $t0,$t0,4
2344#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2348#    j cmpHitLimitzz

2352:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2352#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2356#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2360#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2364:  cmpHitLimitzz:
2364#    move $t0, $t5 # store return value
2368#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2372:readLogicalChar:

    # check if we already have a character
2372#    lw $v0,lastCharRead
2376#    li $t0,-2
2380#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2384#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2388#    jr $ra # return
    
2392:  doReadCh:
    # we need to actually read a character: read and return
2392#    li $v0,12 # use system call to read a character
2396#    syscall
2400#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2404:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2404#    blt $s7,$zero arraySizeOutOfBounds
2405##...slt $at,$s7,$zero
2409##...bne $at,$zero,3436

    # mark the fact that we have not yet GC'd for this allocation
2412#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2416:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2416#    addu $t0,$s6,$s7
2420#    sll $t0,$t0,2
2424#    addu $t0,$t0,8 # amount of memory needed
2428#    addu $t1,$t0,$s3
2432#    bgtu $t1,$s4,doGC
2433##...sltu $at,$s4,$t1
2437##...bne $at,$zero,2560

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2440#    move $t2,$s3
2444:  zeroObjLoop:
2444#    sw $zero,($s3)
2448#    lw $zero,($s3)

2452#    addu $s3,4
2456#    bltu $s3,$t1,zeroObjLoop
2457##...sltu $at,$s3,$t1
2461##...bne $at,$zero,2444

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2464#    sll $t0,$s7,2    # number of post-header bytes
2468#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2472#    li $t2,255
2476#    bgeu $s6,$t2 firstHeaderLarge
2477##...sltu $at,$s6,$t2
2481##...beq $at,$zero,2528
2484#    lw $t1,prevHashConstant
2488#    addu $t1,0x12b68fd2
2489##...lui $at,4791
2493##...addiu $at,$at,-28718
2497##...addu $t1,$at,$t1
2500#    sw $t1,prevHashConstant # update hash constant
2504#    or $t1,0x80000000 # set top bit
2505##...lui $at,32768
2509##...or $t1,$at,$t1
2512#    and $t1,0xffffff00 # clear low bit
2513##...lui $at,65535
2517##...ori $at,$at,0xff00
2521##...and $t1,$t1,$at
2524#    or $s6,$t1
2528:firstHeaderLarge:
2528#    sw $s6,-8($t0) # store header-1 value
2532#    lw $zero,-8($t0)

    # store second header-value
2536#    sw $s7,-4($t0)   # second header-word
2540#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2544#    move $s7,$t0
2548#    subu $sp,$sp,4
2552#    sw $t0,($sp)

2556#    jr $ra
    
2560:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2560#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2564#    lw $t5,gcCount
2568#    addu $t5,1
2572#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2576#    subu $sp,$sp,4
2580#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2584#    lw $t0,cur_seg
2588#    move $t7,$s4
2592#    la $t6,seg1_start
2593##...lui $t6,16
2597##...addiu $t6,$t6,4036
2600#    la $s3,seg0_start
2604#    la $s4,seg0_end
2605##...lui $s4,16
2609##...addiu $s4,$s4,4036
2612#    sw $zero,cur_seg
2616#    bne $t0,$zero,skipGc1
2620#    la $t6,seg0_start
2624#    la $s3,seg1_start
2625##...lui $s3,16
2629##...addiu $s3,$s3,4036
2632#    la $s4,seg1_end
2633##...lui $s4,32
2637##...addiu $s4,$s4,4036
2640#    sw $s5,cur_seg
2644:  skipGc1:
2644#    li $t5,0x80000000
2648#    lw $t0,stack_bottom
2652#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2656:  gcStackCopyLoop:
2656#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2660#    sll $t2,$t1,31 # zero iff low bit was zero
2664#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2668#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2672#    bleu $t1,$t6,gcTestIterDone1
2673##...sltu $at,$t6,$t1
2677##...beq $at,$zero,2840
2680#    bgtu $t1,$t7,gcTestIterDone1
2681##...sltu $at,$t7,$t1
2685##...bne $at,$zero,2840

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2688#    lw $t2,-8($t1) # possible forwarding tag
2692#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2696#    lw $t2,-4($t1) # forwarding pointer: object's new address
2700#    sw $t2,4($t0) # update stack value
2704#    j gcTestIterDone1 # do next iteration

2708:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2708#    bge $t2,$zero,gcSkip2
2709##...slt $at,$t2,$zero
2713##...beq $at,$zero,2728
2716#    and $t2,0xff #### mask off hash value
2717##...lui $at,0
2721##...ori $at,$at,0xff
2725##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2728:  gcSkip2:
2728#    sll $t2,$t2,2
2732#    addu $t2,$t2,8
2736#    subu $t4,$t1,$t2 # address of first word of source
2740#    lw $t3,-4($t1) # number of object words (negative means zero)
2744#    bge $t3,$zero,gcH2Neg1
2745##...slt $at,$t3,$zero
2749##...beq $at,$zero,2756
2752#    move $t3,$zero
2756:  gcH2Neg1:
2756#    sll $t3,$t3,2
2760#    addu $t3,$t3,$t1 # address one word past last word of source

2764#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2768#    lw $s0,($t4)
2772#    lw $s1,-8($t1)
2776#    sw $s1,($t4)
2780#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2784:  gCinnerCopy1:
2784#    lw $s0,($t4)
2788#    sw $s0,($s3)
2792#    addu $t4,$t4,4
2796#    addu $s3,$s3,4
2800#    bltu $t4,$t3,gCinnerCopy1
2801##...sltu $at,$t4,$t3
2805##...bne $at,$zero,2784

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2808#    sw $zero,-8($t1) # 0 in first header-slot
2812#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2816#    sw $t2,4($t0)
2820#    lw $zero,-8($t2)
2824#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2828#    j gcTestIterDone1

2832:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2832#    subu $t0,$t0,$t1
2836#    subu $t0,$t0,3

2840:  gcTestIterDone1:
2840#    bgeu $t0,$sp,gcStackCopyLoop
2841##...sltu $at,$t0,$sp
2845##...beq $at,$zero,2656

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2848#    lw $t1,cur_seg
2852#    la $t0,seg0_start
2856#    beq $t1,$zero,gcSkip4
2860#    la $t0,seg1_start
2861##...lui $t0,16
2865##...addiu $t0,$t0,4036


2868:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2868#    bgeu $t0,$s3,gcFinishUp
2869##...sltu $at,$t0,$s3
2873##...beq $at,$zero,3164

    ###### begin outer loop to copy all non-stack-direct objects ######

2876:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2876#    lw $t1,($t0) # (what will be) first header word for current object
2880#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2884#    bge $t2,$zero,gcSkip4a # are there hash bits?
2885##...slt $at,$t2,$zero
2889##...beq $at,$zero,2904
2892#    and $t2,0xff # mask off hash bits
2893##...lui $at,0
2897##...ori $at,$at,0xff
2901##...and $t2,$t2,$at
2904:gcSkip4a:    
2904#    sll $t2,$t2,2 # # data bytes
2908#    addu $t2,$t2,8 # to skip header words
2912#    addu $t2,$t0,$t2 # pointer to new object
2916#    lw $t3,-8($t2) # word to swap
2920#    sw $t1,-8($t2) # store header word
2924#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2928#    lw $t1,-12($t2) # vtable pointer
2932#    li $t0,dataArrayVTableStart # address of vtable for first data array
2936#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2937##...slt $at,$t1,$t0
2941##...bne $at,$zero,2960

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2944:  skipToNextObj:
2944#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2948#    sll $t1,$t1,2 # convert to byte-count
2952#    addu $t0,$t2,$t1 # skip over object
2956#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2960:  gcNotDataArray:
2960#    lw $t3,-4($t2) # # object words (negative treated as zero)
2964#    bge $t3,$zero,gcH2Neg2
2965##...slt $at,$t3,$zero
2969##...beq $at,$zero,2976
2972#    move $t3,$zero
2976:  gcH2Neg2:
2976#    sll $t3,$t3,2 # # object bytes
2980#    move $t0,$t2 # address of first pointer in object
2984#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2988#    beq $t0,$s1,gcTestIterDone2
    
2992:  gcNextPointerInObject:
    # get pointer from object
2992#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2996#    bleu $t1,$t6,gcGoNextPointerSlot
2997##...sltu $at,$t6,$t1
3001##...beq $at,$zero,3144
3004#    bgtu $t1,$t7,gcGoNextPointerSlot
3005##...sltu $at,$t7,$t1
3009##...bne $at,$zero,3144

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3012#    lw $t8,-8($t1) # possible forwarding tag
3016#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
3020#    lw $t8,-4($t1) # forwarding pointer: object's new address
3024#    sw $t8,($t0) # update pointer in object
3028#    j gcGoNextPointerSlot # do next iteration

3032:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
3032#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
3033##...slt $at,$t8,$zero
3037##...beq $at,$zero,3052
3040#    and $t8,0xff # mask off hash bits
3041##...lui $at,0
3045##...ori $at,$at,0xff
3049##...and $t8,$t8,$at

3052:  gcSkip5:
3052#    sll $t8,$t8,2 # bytes in negative-offset portion ...
3056#    addu $t8,$t8,8 # ... add in bytes for header-words
3060#    subu $t4,$t1,$t8 # address of first word of source object
3064#    lw $t3,-4($t1) # # words in positive-offset portion
3068#    bge $t3,$zero,gcNoNeg
3069##...slt $at,$t3,$zero
3073##...beq $at,$zero,3080
3076#    move $t3,$zero # negative treated as zero
3080:  gcNoNeg:
3080#    sll $t3,$t3,2 # bytes in negative-offset portion
3084#    addu $t3,$t3,$t1 # address one word past last word of source

3088#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
3092#    lw $s0,($t4)
3096#    lw $t9,-8($t1)
3100#    sw $t9,($t4)
3104#    sw $s0,-8($t1)

    # copy all source bytes to the target
3108:  gCinnerCopy2:
3108#    lw $s0,($t4)
3112#    sw $s0,($s3)
3116#    addu $t4,$t4,4
3120#    addu $s3,$s3,4
3124#    bltu $t4,$t3,gCinnerCopy2
3125##...sltu $at,$t4,$t3
3129##...bne $at,$zero,3108

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3132#    sw $zero,-8($t1) # 0 in first header-slot
3136#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3140#    sw $t8,($t0)

3144:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3144#    addu $t0,$t0,4
3148#    bltu $t0,$s1,gcNextPointerInObject
3149##...sltu $at,$t0,$s1
3153##...bne $at,$zero,2992

3156:  gcTestIterDone2:
3156#    bltu $t0,$s3,gcHeapCopyLoop
3157##...sltu $at,$t0,$s3
3161##...bne $at,$zero,2876

    ###### end outer loop to copy all non-stack-direct objects ######

3164:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3164#    lw $s2,($sp)
3168#    addu $sp,$sp,4

    # mark us as having already GC'd
3172#    move $t5,$s5

    # go try and allocate again
3176#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3180:vm_init:

    # mark bottom of stack
3180#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3184#    move $s2,$zero

    #set up the "five" register
3188#    li $s5,5

3192#    la $s3,seg0_start
3196#    la $s4,seg0_end
3197##...lui $s4,16
3201##...addiu $s4,$s4,4036
    
    # return
3204#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3208:divide:
    # get parameters; abort if divisor zero
3208#    lw $t0,($sp)
3212#    lw $t1,8($sp)
3216#    beq $t0,$zero,divByZeroError
    
    # perform division
3220#    div $t1,$t0
3224#    mflo $t0

    # store result, adjust stack and return
3228#    addu $sp,$sp,8 # adjust stack
3232#    sw $t0,($sp) # store result
3236#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3240:remainder:
    # get parameters; abort if divisor zero
3240#    lw $t0,($sp)
3244#    lw $t1,8($sp)
3248#    beq $t0,$zero,divByZeroError
    
    # perform division
3252#    div $t1,$t0
3256#    mfhi $t0

    # store result, adjust stack and return
3260#    addu $sp,$sp,8 # adjust stack
3264#    sw $t0,($sp) # store result
3268#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3272:checkCast:
    # get object, allow immediately if null
3272#    lw $t2,($sp)
3276#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3280#    lw $t2,-12($t2) # vtable address
3284#    bge $t2,$t1,castException
3285##...slt $at,$t2,$t1
3289##...beq $at,$zero,3460
3292#    blt $t2,$t0,castException
3293##...slt $at,$t2,$t0
3297##...bne $at,$zero,3460
    
    # return, leaving object unchanged on stack
3300:  checkCastReturn:    
3300#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3304:instanceOf:
    # get object, go return false if null
3304#    lw $t2,($sp)
3308#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3312#    lw $t2,-12($t2) # vtable address
3316#    sge $t0,$t2,$t0 # are we >= the lower limit?
3317##...slt $t0,$t2,$t0
3321##...xori $t0,$t0,0x1
3324#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3328#    and $t2,$t0,$t1
3332#    sw $t2,($sp)
3336:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3336#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3340:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3340#    li $v0,4 # syscall-code for print-string
3344#    syscall
3348:exit:
    # print termination message
3348#    li $v0,4 # syscall-code for print-string
3352#    la $a0,exitString
3356#    syscall

    # print number of garbage collections
3360#    li $v0,4 # syscall-code for print-string
3364#    la $a0,gcCountMsg
3368#    syscall # print "Number of ..."
3372#    lw $a0,gcCount # number of GC's
3376#    li $v0,1 # code for print-int
3380#    syscall
3384#    li $v0,4 # syscall-code for print-string
3388#    la $a0,newlineString
3392#    syscall # print newline

    # terminate execution
3396#    li $v0,10 #syscall-code for "exit"
3400#    syscall

3404:outOfHeapMemory:
3404#    la $a0,heapMemoryMsg
3408#    j exitError
3412:divByZeroError:
3412#    la $a0,divByZeroMsg
3416#    j exitError
3420:strIndexOutOfBounds:
3420#    la $a0,strIndexOutOfBoundsMsg
3424#    j exitError
3428:arrayIndexOutOfBounds:
3428#    la $a0,arrayIndexOutOfBoundsMsg
3432#    j exitError
3436:arraySizeOutOfBounds:
3436#    la $a0,arraySizeOutOfBoundsMsg
3440#    j exitError
3444:nullPtrException:
3444#    la $a0,nullPtrExceptionMsg
3448#    j exitError
3452:badIntegerFormat:
3452#    la $a0,badIntegerFormatMsg
3456#    j exitError
3460:castException:
3460#    la $a0,castExceptionMsg
3464#    j exitError
############## data section ################
    .data
3664:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3680:cur_seg:
    .word 0

3684:lastCharRead:
    .word -2 # -2 => no buffered character
3688:prevHashConstant:
    .word 0x2785173c
3692:gcCount:
    .word 0

3696:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3723:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3746:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3781:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3815:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3848:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3879:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3927:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3948:gcMsg: 
      .asciiz "\nGC!\n"
3954:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3986:nullStr:
    .asciiz "null"
3991:trueString:
    .asciiz "true"
3996:falseString:
    .asciiz "false"
4002:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
4030:newlineString:
    .asciiz "\n"
    
    .align 2
4032:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

4036:seg0_start:
    .space 0x100000
1052612:seg0_end:
1052612:seg1_start:
    .space 0x100000
2101188:seg1_end:
