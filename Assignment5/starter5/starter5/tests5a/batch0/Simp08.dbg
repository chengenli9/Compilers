 .data # Program at 1.0
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3348:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3368:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3400:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3404:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 1.6
  .byte 205 # 'M' with high bit set # ClassDecl at 1.6
  .byte 97 # 'a' # ClassDecl at 1.6
  .byte 105 # 'i' # ClassDecl at 1.6
  .byte 110 # 'n' # ClassDecl at 1.6
3452:CLASS_Main: # ClassDecl at 1.6
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 2.15
3500:END_CLASS_Main: # ClassDecl at 1.6
3500:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3508:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3520:END_CLASS_RunMain: # ClassDecl at 0.0
3520:dataArrayVTableStart: # IntType at 0.0
3520:END_CLASS_Object: # ClassDecl at 0.0
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp, $sp, 4
28#  move $s2, $s7
32#  jal mth_main_Main
36#  li $v0, 10
40#  syscall
.globl mth_main_Main
44:mth_main_Main:
44#  subu $sp, $sp, 4
48#  sw $ra, ($sp)
52#  li $t0, 27
56#  subu $sp, $sp, 8
60#  sw $zero, 4($sp)
64#  sw $t0, ($sp)
68#  subu $sp, $sp, 4
72#  sw $s2, ($sp)
76#  lw $t0, 4($sp)
80#  subu $sp, $sp, 8
84#  sw $zero, 4($sp)
88#  sw $t0, ($sp)
92#  li $t0, 263
96#  subu $sp, $sp, 8
100#  sw $zero, 4($sp)
104#  sw $t0, ($sp)
108#  lw $t2, ($sp)
112#  addu $sp, $sp, 8
116#  lw $t1, ($sp)
120#  addu $sp, $sp, 8
124#  addu $t0, $t1, $t2
128#  subu $sp, $sp, 8
132#  sw $zero, 4($sp)
136#  sw $t0, ($sp)
140#  lw $t0, 8($sp)
144#  sw $s2, 8($sp)
148#  move $s2, $t0
152#  jal mth_printInt_Lib
156#  addu $sp, $sp, 8
160#  lw $s2, ($sp)
164#  addu $sp, $sp, 4
168#  subu $sp, $sp, 4
172#  sw $t0, ($sp)
176#  lw $t0, ($sp)
180#  addu $sp, $sp, 4
184#  addu $sp, $sp, 8
188#  lw $ra, ($sp)
192#  addu $sp, $sp, 4
196#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
200:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
200#    lw $t0,-8($s2) # get word with hash bits from this
204#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
208#    addu $t0,$t1

212#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
216:mth_equals_Object:
216#    move $t0,$s2 # first pointer (this)
220#    lw $t1,($sp) # second pointer
224#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
225##...subu $at,$t0,$t1
229##...sltiu $t0,$at,1

232#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
236:mth_hashCode_String:
236#    move $t0,$s2 # put this pointer in t0

240#    li $t2,0 # current hash value
244#    lw $t1,-4($t0) # negative of number of characters in string
248#    beq $t1,$zero,done_hashCode_String # if zero length => done
252#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
256#    and $t0,0xfffffffc # adjust for padding
257##...lui $at,65535
261##...ori $at,$at,0xfffc
265##...and $t0,$t0,$at
268#    subu $t1,$t0,$t1 # address at end of string +12
272:loop_hashCode_String:    
272#    sll $t4,$t2,5 # multiply by 32
276#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
280#    lbu $t3,-12($t0) # current character
284#    addu $t2,$t3 # add in current character
288#    addu $t0,1 # go to next character's address
292#    bne $t0,$t1,loop_hashCode_String # loop back if more
296:done_hashCode_String:    

    # push int on stack
296#    move $t0, $t2 # put $t2 in return slot
300#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
304:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
304#    subu $sp,4
308#    sw $ra,($sp)
    
    # get hash value
312#    lw $t2,-12($s2) # vtable address
316#    lw $t0,($t2) # method address
320#    jalr $t0
324#    subu $sp,8 # push result (int in $t0) on stack
328#    sw $s5,4($sp)
332#    sw $t0,($sp)

    # compute print-size of hash value
336#    li $t0, 1
340#    lw $t1, ($sp)
344#    srl $t1,$t1,4
348#    beq $t1,$zero,toString_skip_loop
352:toString_loop_top:
352#    addu $t0,1
356#    srl $t1,$t1,4
360#    bne $t1,$zero,toString_loop_top
364:toString_skip_loop:
364#    subu $sp,8
368#    sw $s5,4($sp)
372#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
376#    lw $t2,-12($s2) # vtable address
380#    move $t0,$t2
384:toString_checkTagBit:
384#    subu $t0,1
388#    lb $t1,($t0)
392#    and $t1,0x80
393##...lui $at,0
397##...ori $at,$at,0x80
401##...and $t1,$t1,$at
404#    beq $t1,$zero,toString_checkTagBit
408#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
412#    subu $sp,8
416#    sw $t1,($sp)
420#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
424#    move $s7,$zero # -4 offset header word
428#    lw $t4,8($sp) # bytes for hash value
432#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
436#    addu $t4,8
440#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
444#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

448#    li $t0,CLASS_String # store string vtable address into ...
452#    sw $t0,-12($s7) # ... -12 offset in object header
456#    lw $t2,-12($s2) # our object's vtable address
460#    lw $t5,4($sp) # number characters in prefix
464#    lw $t1,12($sp) # number of characters in hashcode
468#    addu $t3,$t1,$t5 # number of characters in string ...
472#    addu $t3,1 # ... including the '@' character
476#    subu $t0,$zero,$t3 # store negated length into ...
480#    sw $t0,-4($s7) # ... -4 offset header word
484#    lw $t0,-8($s7) # get -8 offset header-word
488#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
489##...slt $at,$t0,$zero
493##...beq $at,$zero,512
496#    and $t0,0xff # mask off object's hash value ...
497##...lui $at,0
501##...ori $at,$at,0xff
505##...and $t0,$t0,$at
508#    sw $t0,-8($s7) # ... and store back into -8 header-word
512:skipMask_toString:
512#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
516#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
520#    subu $t4,$t2,$t5
524:toString_copyLoop:    
524#    lb $t3,($t4)
528#    and $t3,0x7f # mask off sign bit (in case first word)
529##...lui $at,0
533##...ori $at,$at,0x7f
537##...and $t3,$t3,$at
540#    sb $t3,-8($t0)
544#    addu $t4,1
548#    addu $t0,1
552#    blt $t4,$t2,toString_copyLoop
553##...slt $at,$t4,$t2
557##...bne $at,$zero,524

    # copy the '@' sign
560#    li $t3,'@'
564#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
568#    addu $t2,$t0,$t1 # address of last byte in string to store +8
572#    lw $t3,20($sp) # hash code to print
576:toString_hashCopyLoop:
576#    and $t4,$t3,0xf
580#    srl $t3,4
584#    lb $t4,hexChars($t4)
588#    sb $t4,-8($t2)
592#    subu $t2,$t2,1
596#    bgt $t2,$t0,toString_hashCopyLoop
597##...slt $at,$t0,$t2
601##...bne $at,$zero,576

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
604#    addu $sp,28
608#    lw $ra,($sp)
612#    addu $sp,4
616#    move $t0, $s7 # return string we created
620#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
624:mth_equals_String:
    
    # quick test for obvious false
624#    lw $t0,($sp) # second parameter
628#    beq $t0,$zero,goEsFalse # go return false if null
632#    lw $t1,-12($t0) # vtable pointer
636#    la $t2,CLASS_String # string vtable pointer
640#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
644#    subu $sp,12
648#    sw $ra,8($sp)
652#    sw $s2,4($sp)
656#    sw $t0,($sp)
660#    jal mth_compareTo_String
664#    lw $s2, 4($sp) #restore this pointer and pop parameters
668#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
672#    seq $t0,$t0,$zero
673##...subu $at,$t0,$zero
677##...sltiu $t0,$at,1

    # pop return address and return $t0
680#    lw $ra,($sp)
684#    addu $sp,4

688#    jr $ra # return
    
692:goEsFalse:
    # put 0 in $t0 and return
692#    li $t0, 0
696#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
700:mth_toString_String:
    #we are a string, so return this.
700#    move $t0, $s2
704#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
708:mth_readLine_Lib:
    
708#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
712#    sw $ra, 4($sp) # save $ra
716#    move $t1,$sp # save "original" sp

720:  doRead:

    # read the character
720#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
724#    subu $t2,$v0,13
728#    bne $t2,$zero,notReturnRL
732#    jal readLogicalChar
736#    subu $t2,$v0,10 # check for newline
740#    beq $t2,$zero,foundNewLine
744#    sw $v0,lastCharRead # push back character into queue
748#    j foundNewLine # go process the line

752:  notReturnRL:

    # at this point, $v0 has our character

752#    subu $t0,$v0,10
756#    beq $t0,$zero,foundNewLine
760#    blt $v0,$zero,foundEof
761##...slt $at,$v0,$zero
765##...bne $at,$zero,780

    # we have a character, so push it onto stack
768#    subu $sp,$sp,4
772#    sw $v0,($sp)

    # loop back up to get next character
776#    j doRead

780:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
780#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
784#    li $s7, 0 # return-value: null
788#    j rlReturn # return

792:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
792#    subu $t0,$t1,$sp # number of chars we read (times 4)
796#    srl $s6,$t0,2 # number of words on stack with chars
800#    subu $sp,$sp,4 # push char-count ...
804#    sw $s6,($sp) # ... onto stack
808#    addu $t2,$t0,5 # GC tag, incl. for count-word
812#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
816#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
820#    srl $s6,$s6,2  # data words in object
824#    move $s7,$zero # object words in object
828#    jal newObject  # allocate space
    
    # store header words
832#    la $t0,CLASS_String
836#    sw $t0,-12($s7)# store class tag (String) into object
840#    lw $t2,4($sp) # char-count
844#    sll $t1,$t2,2 # 4 times number of chars
848#    subu $t2,$zero,$t2 # negative of char-count
852#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
856#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
860#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
861##...slt $at,$t0,$zero
865##...beq $at,$zero,884
868#    and $t0,0xff # mask off object's hash value ...
869##...lui $at,0
873##...ori $at,$at,0xff
877##...and $t0,$t0,$at
880#    sw $t0,-8($s7) # ... and store back into -8 header-word
884:  skipMask_readLine:

884#    sll $t0,$t0,2 # data bytes in object
888#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
892#    subu $t0,$t0,$t2 # place to store last character (plus 9)
896#    addu $sp,$sp,8 # pop redundant object-pointer and count
900#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

904#    beq $sp,$t1,rlReturn
908:  charCopyLoop:
908#    lw $t2,($sp)
912#    sb $t2,-9($t0)
916#    addu $sp,$sp,4
920#    subu $t0,$t0,1
924#    bne $sp,$t1,charCopyLoop
928:  rlReturn:

928#    lw $ra, 4($sp)
932#    addu $sp, 8   # pop $ra and pointless word off stack
936#    move $t0, $s7 # return string we allocated in $s7
940#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
944:mth_readInt_Lib:

    # save $ra by pushing onto stack
944#    subu $sp,$sp,4
948#    sw $ra,($sp)

952:  riSkipWhiteLoop:
    # read a character
952#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
956#    subu $t0,$v0,32
960#    bgt $t0,$zero,nonWhite
961##...slt $at,$zero,$t0
965##...bne $at,$zero,1008
968#    beq $t0,$zero,riSkipWhiteLoop # start over if space
972#    subu $t0,$v0,10
976#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
980#    subu $t0,$v0,9
984#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
988#    subu $t0,$v0,13
992#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
996#    subu $t0,$v0,12
1000#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1004#    j badIntegerFormat # illegal integer char: abort program

1008:  nonWhite:
1008#    subu $t0,$v0,'-'
1012#    li $t4,1 # final multiplier
1016#    bne $t0,$zero,helpReadInt # go read
1020#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1024#    jal readLogicalChar

1028:  helpReadInt:
1028#    li $t2,10
1032#    subu $t1,$v0,'0' # convert digit to 0-9 value
1036#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1037##...sltu $at,$t1,$t2
1041##...beq $at,$zero,3324
1044#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1048:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1048#    jal readLogicalChar # read next character
1052#    subu $t1,$v0,'0' # convert digit to 0-9 value
1056#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1057##...sltu $at,$t1,$t2
1061##...beq $at,$zero,1080

1064#    mul $t3,$t3,$t2 # multiply old value by 10
1065##...mult $t3,$t2
1069##...mflo $t3
1072#    addu $t3,$t3,$t1 # add in value of new digit
1076#    j digitLoop

1080:  doneDigitLoop:

    # "push back" unused character into queue
1080#    sw $v0,lastCharRead

    # restore return address
1084#    lw $ra,($sp)
1088#    addu $sp, 4

    # multiply by sign and put return value in $t0
1092#    mult $t3,$t4 # multiply to account for poss. minus sign
1096#    mflo $t3
1100#    move $t0, $t3
    
    # return
1104#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1108:mth_readChar_Lib:

    # save $ra by pushing onto stack
1108#    subu $sp,$sp,4
1112#    sw $ra,($sp)

      # read the character
1116#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1120#    lw $ra,($sp)
1124#    addu $sp, 4
1128#    move $t0, $v0

    # return
1132#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1136:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1136#    lw $t0,($sp)
1140#    bne $t0,$zero,psNotNull

    # print "(null)"
1144#    la $a0,nullStr
1148#    li $v0,4
1152#    syscall
1156#    j donePrintStr
    
1160:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1160#    subu $t0,$t0,8
1164#    lw $t1,($t0) # number of data words, but needs modification
1168#    blt $t1,$zero,skipPrintStr1
1169##...slt $at,$t1,$zero
1173##...bne $at,$zero,1192
1176#    and $t1,0xff # mask out all but low byte
1177##...lui $at,0
1181##...ori $at,$at,0xff
1185##...and $t1,$t1,$at
1188#    b skipPrintStr2
1192:skipPrintStr1:
1192#    and $t1,0x7ffffff
1193##...lui $at,2047
1197##...ori $at,$at,0xffff
1201##...and $t1,$t1,$at
1204:skipPrintStr2:    
1204#    sll $t1,$t1,2
1208#    subu $t1,$t0,$t1 # first word in string
1212#    lw $t0,4($t0) # negative of string-length
1216#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1220#    beq $t0,$t1,donePrintStr
1224:  psLoop:
1224#    lb $a0,($t1) # next byte
1228#    li $v0,11 # code for printing char
1232#        syscall # print the char
1236#    addu $t1,$t1,1 # go to next char
1240#    blt $t1,$t0,psLoop
1241##...slt $at,$t1,$t0
1245##...bne $at,$zero,1224

1248:  donePrintStr:
1248#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1252:mth_printInt_Lib:
1252#    lw $a0,($sp)

    # print it
1256#    li $v0,1 # code for print-int
1260#    syscall

    # return
1264#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1268:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1268#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1272#    la $a0,falseString
1276#    beq $t0,$zero,skipPB
1280#    la $a0,trueString
1284:  skipPB:
1284#    li $v0,4 # code for print-string
1288#    syscall

    # return
1292#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1296:mth_substring_String:

    # save $ra by pushing onto stack
1296#    subu $sp,$sp,4
1300#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1304#    move $t0,$s2
1308#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1312#    lw $t0,-4($t0) # negative of string length
1316#    subu $t0,$zero,$t0 # string length
1320#    lw $t1,4($sp) # k
1324#    lw $t2,12($sp) # n
1328#    bgt $zero,$t2,strIndexOutOfBounds
1329##...slt $at,$t2,$zero
1333##...bne $at,$zero,3292
1336#    bgt $t2,$t1,strIndexOutOfBounds
1337##...slt $at,$t1,$t2
1341##...bne $at,$zero,3292
1344#    bgt $t1,$t0,strIndexOutOfBounds
1345##...slt $at,$t0,$t1
1349##...bne $at,$zero,3292

    # allocate memory
1352#    subu $s6,$t1,$t2 # # chars in target-string
1356#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1360#    srl $s6,$s6,2 # convert bytes-count to word-count
1364#    move $s7,$zero # (no object-bytes in string)
1368#    jal newObject

    # store "String" tag in object-type field
1372#    la $t0,CLASS_String
1376#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1380#    lw $t4,-8($s7) # data words in source string
1384#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1385##...slt $at,$t4,$zero
1389##...beq $at,$zero,1408
1392#    and $t4,0xff # mask off object's hash value ...
1393##...lui $at,0
1397##...ori $at,$at,0xff
1401##...and $t4,$t4,$at
1404#    sw $t4,-8($s7) # ... and store back into -8 header-word
1408:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1408#    lw $t1,8($sp) # k
1412#    lw $t2,16($sp) # n
1416#    subu $t0,$t2,$t1 # value to store
1420#    sw $t0,-4($s7) # store value
    
1424#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1428#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1432#    lw $t4,-8($t3) # data words in source string
1436#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1440#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1444#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1448#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1452#    lw $t2,-8($s7)   # data words in target string
1456#    sll $t2,$t2,2    # data bytes in target string
1460#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1464:  subCopyLoopzz:
1464#    lb $t4,-8($t3)
1468#    sb $t4,-8($t2)
1472#    addu $t2,$t2,1
1476#    addu $t3,$t3,1
1480#    blt $t3,$t1,subCopyLoopzz
1481##...slt $at,$t3,$t1
1485##...bne $at,$zero,1464
    
1488:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1488#    lw $ra,4($sp) # restore $ra
1492#    addu $sp,$sp,8 # pop ra, and allocated newObject
1496#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1500#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1504:mth_length_String:

    # the length is the negative of 4($s2)
1504#    lw $t0,-4($s2) # -length
1508#    subu $t0,$zero,$t0

    # return
1512#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1516:mth_concat_String:
    # save $ra by pushing onto stack
1516#    subu $sp,$sp,4
1520#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1524#    lw $t0,4($sp)
1528#    beq $t0,$zero,nullPtrException
1532#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1536#    lw $t0,-4($t0) # negative size of second object
1540#    lw $t1,-4($t1) # negative size of first object
1544#    addu $s6,$t0,$t1 # sum of negative sizes
1548#    sra $s6,$s6,2 # negative word-size of char part
1552#    subu $s6,$zero,$s6 # word size of char part
1556#    addu $s6,$s6,1 # data word size, including v-table word
1560#    move $s7,$zero
1564#    jal newObject

    # store String vtable address in vtable address field
1568#    la $t0,CLASS_String
1572#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1576#    lw $t4,-8($s7) # # data words in source string
1580#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1581##...slt $at,$t4,$zero
1585##...beq $at,$zero,1604
1588#    and $t4,0xff # mask off object's hash value ...
1589##...lui $at,0
1593##...ori $at,$at,0xff
1597##...and $t4,$t4,$at
1600#    sw $t4,-8($s7) # ... and store back into -8 header-word
1604:skipMask_concat:

1604#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1608#    move $t1,$s2   # first object
1612#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1616#    lw $t2,-4($t0) # negative length of second object
1620#    lw $t3,-4($t1) # negative length of first object
1624#    addu $t4,$t2,$t3 # sum of negative lengths
1628#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1632#    lw $t4,-8($t1) # # data words in first string
1636#    sll $t4,$t4,2 # # data bytes in first string
1640#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1644#    lw $t4,-8($s7) # # data words in new string
1648#    sll $t4,$t4,2 # # data bytes in new string
1652#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1656#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1660#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1664:  concatLoop1zz:
1664#    lb $t4,-8($t1)
1668#    sb $t4,-8($s7)
1672#    addu $s7,$s7,1
1676#    addu $t1,$t1,1
1680#    blt $t1,$t3,concatLoop1zz
1681##...slt $at,$t1,$t3
1685##...bne $at,$zero,1664
1688:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1688#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1692#    lw $t4,-8($t0) # # data words in second string
1696#    sll $t4,$t4,2 # # data bytes in second string
1700#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1704#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1708:  concatLoop2zz:
1708#    lb $t4,-8($t1)
1712#    sb $t4,-8($s7)
1716#    addu $s7,$s7,1
1720#    addu $t1,$t1,1
1724#    blt $t1,$t3,concatLoop2zz
1725##...slt $at,$t1,$t3
1729##...bne $at,$zero,1708
1732:  doneConcatLoop2zz:
 
1732:  concatRtnzz:
    # return
1732#    move $t0, $t9 # put saved return string in $t0
1736#    lw $ra, 4($sp)
1740#    addu $sp, 8 # pop ra and allocated newObject
1744#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1748:mth_charAt_String:
    # get string
1748#    move $t0,$s2

    # check that index is in bounds
1752#    lw $t1,-4($t0) # negative of # data words in string
1756#    subu $t3,$zero,$t1 # # chars in string
1760#    lw $t2,($sp) # index
1764#    bgeu $t2,$t3,strIndexOutOfBounds
1765##...sltu $at,$t2,$t3
1769##...beq $at,$zero,3292

    # access element
1772#    lw $t1,-8($t0) # # data words in object
1776#    sll $t1,$t1,2 # - byte-offset from end of chars
1780#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1784#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1788#    lb $t0,-8($t0) # our char

    # return
1792#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1796:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1796#    subu $sp,$sp,8
1800#    sw $ra,4($sp)

    # save current sp
1804#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1808#    li $t3,10

    # get argument, negate if negative
1812#    lw $t1,8($sp)
1816#    bge $t1,$zero,itsNonNegLoop
1817##...slt $at,$t1,$zero
1821##...beq $at,$zero,1828
1824#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1828:  itsNonNegLoop:
1828#    divu $t1,$t3
1832#    mflo $t1 # quotient
1836#    mfhi $t4 # remainder
1840#    addu $t4,$t4,'0' # turn remainder into digit
1844#    subu $sp,$sp,4
1848#    sw $t4,($sp) # push digit onto stack
1852#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1856#    lw $t4,8($t0)
1860#    bge $t4,$zero,itsSkipNeg
1861##...slt $at,$t4,$zero
1865##...beq $at,$zero,1880
1868#    li $t4,'-'
1872#    subu $sp,$sp,4
1876#    sw $t4,($sp)
1880:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
1880#    subu $s6,$t0,$sp
1884#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
1888#    sw $t3,($t0)
1892#    srl $s6,$s6,2
1896#    subu $sp,$sp,4
1900#    sw $s6,($sp)
    
    # allocate memory
1904#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1908#    srl $s6,$s6,2
1912#    move $s7,$zero # no "object" words in object
1916#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
1920#    lw $t0,4($sp)
1924#    addu $sp,$sp,8

    # store "String" tag into class field
1928#    subu $s7,$s7,8 # address of header-1 word
1932#    la $t1,CLASS_String
1936#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
1940#    lw $t4,($s7) # # data words in source string
1944#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
1945##...slt $at,$t4,$zero
1949##...beq $at,$zero,1968
1952#    and $t4,0xff # mask off object's hash value ...
1953##...lui $at,0
1957##...ori $at,$at,0xff
1961##...and $t4,$t4,$at
1964#    sw $t4,($s7) # ... and store back into -8 header-word
1968:skipMask_intToString:
    
    # store negative of char-count into header-2 word
1968#    subu $t0,$zero,$t0
1972#    sw $t0,4($s7)

1976#    lw $t1,($s7) # number of data words in string
1980#    sll $t1,$t1,2 # number data bytes in string
1984#    subu $t1,$s7,$t1 # first location to store chars in string
1988#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
1992:  itsLoop:
1992#    lw $t2,($sp)
1996#    addu $sp,$sp,4
2000#    sb $t2,($t1)
2004#    addu $t1,$t1,1
2008#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2012#    lw $ra,4($sp)
2016#    addu $sp,$sp,8
2020#    addu $t0,$t0,15
2024#    and $t0,$t0,0xfffffffc
2025##...lui $at,65535
2029##...ori $at,$at,0xfffc
2033##...and $t0,$t0,$at
2036#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2040:mth_intToChar_Lib:
    # save return address
2040#    subu $sp,$sp,4
2044#    sw $ra,($sp)

    # allocate object
2048#    li $s6,2
2052#    move $s7,$zero
2056#    jal newObject

    # get n from string.
2060#    lw $t1,8($sp)

    # store "String" tag into class field
2064#    la $t0,CLASS_String
2068#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2072#    lw $t4,-8($s7) # # data words in source string
2076#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2077##...slt $at,$t4,$zero
2081##...beq $at,$zero,2100
2084#    and $t4,0xff # mask off object's hash value ...
2085##...lui $at,0
2089##...ori $at,$at,0xff
2093##...and $t4,$t4,$at
2096#    sw $t4,-8($s7) # ... and store back into -8 header-word
2100:  skipMask_intToChar:

    # store data in string
2100#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2104#    li $t0,-1
2108#    sw $t0,-4($s7)

    # we're returning $s7
2112#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2116#    lw $ra,4($sp)
2120#    addu $sp,$sp,8

    # return
2124#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2128:mth_compareTo_String:
    # get string pointers and check parameter for null
2128#    lw $t0,($sp) # second string
2132#    beq $t0,$zero,nullPtrException
2136#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2140#    lw $t2,-4($t0) # negative length of second string
2144#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2148#    slt $t4,$t3,$t2
2152#    slt $t5,$t2,$t3
2156#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2160#    lw $t2,-8($t0) # # data words in second string
2164#    lw $t3,-8($t1) # # data words in first string
2168#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2172#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2176#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2180#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2184#    beq $zero,$t5,skipStrCmpLenzz
2188#    move $t2,$t3
2192:  skipStrCmpLenzz:
2192#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2196:  cmpLoopzz:
2196#    lw $t3,-8($t1) # word from str2
2200#    lw $t4,-8($t0) # word from str1
2204#    bne $t3,$t4,cmpNotEqualzz
2208#    addu $t1,$t1,4
2212#    addu $t0,$t0,4
2216#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2220#    j cmpHitLimitzz

2224:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2224#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2228#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2232#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2236:  cmpHitLimitzz:
2236#    move $t0, $t5 # store return value
2240#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2244:readLogicalChar:

    # check if we already have a character
2244#    lw $v0,lastCharRead
2248#    li $t0,-2
2252#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2256#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2260#    jr $ra # return
    
2264:  doReadCh:
    # we need to actually read a character: read and return
2264#    li $v0,12 # use system call to read a character
2268#    syscall
2272#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2276:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2276#    blt $s7,$zero arraySizeOutOfBounds
2277##...slt $at,$s7,$zero
2281##...bne $at,$zero,3308

    # mark the fact that we have not yet GC'd for this allocation
2284#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2288:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2288#    addu $t0,$s6,$s7
2292#    sll $t0,$t0,2
2296#    addu $t0,$t0,8 # amount of memory needed
2300#    addu $t1,$t0,$s3
2304#    bgtu $t1,$s4,doGC
2305##...sltu $at,$s4,$t1
2309##...bne $at,$zero,2432

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2312#    move $t2,$s3
2316:  zeroObjLoop:
2316#    sw $zero,($s3)
2320#    lw $zero,($s3)

2324#    addu $s3,4
2328#    bltu $s3,$t1,zeroObjLoop
2329##...sltu $at,$s3,$t1
2333##...bne $at,$zero,2316

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2336#    sll $t0,$s7,2    # number of post-header bytes
2340#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2344#    li $t2,255
2348#    bgeu $s6,$t2 firstHeaderLarge
2349##...sltu $at,$s6,$t2
2353##...beq $at,$zero,2400
2356#    lw $t1,prevHashConstant
2360#    addu $t1,0x12b68fd2
2361##...lui $at,4791
2365##...addiu $at,$at,-28718
2369##...addu $t1,$at,$t1
2372#    sw $t1,prevHashConstant # update hash constant
2376#    or $t1,0x80000000 # set top bit
2377##...lui $at,32768
2381##...or $t1,$at,$t1
2384#    and $t1,0xffffff00 # clear low bit
2385##...lui $at,65535
2389##...ori $at,$at,0xff00
2393##...and $t1,$t1,$at
2396#    or $s6,$t1
2400:firstHeaderLarge:
2400#    sw $s6,-8($t0) # store header-1 value
2404#    lw $zero,-8($t0)

    # store second header-value
2408#    sw $s7,-4($t0)   # second header-word
2412#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2416#    move $s7,$t0
2420#    subu $sp,$sp,4
2424#    sw $t0,($sp)

2428#    jr $ra
    
2432:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2432#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2436#    lw $t5,gcCount
2440#    addu $t5,1
2444#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2448#    subu $sp,$sp,4
2452#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2456#    lw $t0,cur_seg
2460#    move $t7,$s4
2464#    la $t6,seg1_start
2465##...lui $t6,16
2469##...addiu $t6,$t6,3892
2472#    la $s3,seg0_start
2476#    la $s4,seg0_end
2477##...lui $s4,16
2481##...addiu $s4,$s4,3892
2484#    sw $zero,cur_seg
2488#    bne $t0,$zero,skipGc1
2492#    la $t6,seg0_start
2496#    la $s3,seg1_start
2497##...lui $s3,16
2501##...addiu $s3,$s3,3892
2504#    la $s4,seg1_end
2505##...lui $s4,32
2509##...addiu $s4,$s4,3892
2512#    sw $s5,cur_seg
2516:  skipGc1:
2516#    li $t5,0x80000000
2520#    lw $t0,stack_bottom
2524#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2528:  gcStackCopyLoop:
2528#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2532#    sll $t2,$t1,31 # zero iff low bit was zero
2536#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2540#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2544#    bleu $t1,$t6,gcTestIterDone1
2545##...sltu $at,$t6,$t1
2549##...beq $at,$zero,2712
2552#    bgtu $t1,$t7,gcTestIterDone1
2553##...sltu $at,$t7,$t1
2557##...bne $at,$zero,2712

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2560#    lw $t2,-8($t1) # possible forwarding tag
2564#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2568#    lw $t2,-4($t1) # forwarding pointer: object's new address
2572#    sw $t2,4($t0) # update stack value
2576#    j gcTestIterDone1 # do next iteration

2580:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2580#    bge $t2,$zero,gcSkip2
2581##...slt $at,$t2,$zero
2585##...beq $at,$zero,2600
2588#    and $t2,0xff #### mask off hash value
2589##...lui $at,0
2593##...ori $at,$at,0xff
2597##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2600:  gcSkip2:
2600#    sll $t2,$t2,2
2604#    addu $t2,$t2,8
2608#    subu $t4,$t1,$t2 # address of first word of source
2612#    lw $t3,-4($t1) # number of object words (negative means zero)
2616#    bge $t3,$zero,gcH2Neg1
2617##...slt $at,$t3,$zero
2621##...beq $at,$zero,2628
2624#    move $t3,$zero
2628:  gcH2Neg1:
2628#    sll $t3,$t3,2
2632#    addu $t3,$t3,$t1 # address one word past last word of source

2636#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2640#    lw $s0,($t4)
2644#    lw $s1,-8($t1)
2648#    sw $s1,($t4)
2652#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2656:  gCinnerCopy1:
2656#    lw $s0,($t4)
2660#    sw $s0,($s3)
2664#    addu $t4,$t4,4
2668#    addu $s3,$s3,4
2672#    bltu $t4,$t3,gCinnerCopy1
2673##...sltu $at,$t4,$t3
2677##...bne $at,$zero,2656

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2680#    sw $zero,-8($t1) # 0 in first header-slot
2684#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2688#    sw $t2,4($t0)
2692#    lw $zero,-8($t2)
2696#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2700#    j gcTestIterDone1

2704:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2704#    subu $t0,$t0,$t1
2708#    subu $t0,$t0,3

2712:  gcTestIterDone1:
2712#    bgeu $t0,$sp,gcStackCopyLoop
2713##...sltu $at,$t0,$sp
2717##...beq $at,$zero,2528

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2720#    lw $t1,cur_seg
2724#    la $t0,seg0_start
2728#    beq $t1,$zero,gcSkip4
2732#    la $t0,seg1_start
2733##...lui $t0,16
2737##...addiu $t0,$t0,3892


2740:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2740#    bgeu $t0,$s3,gcFinishUp
2741##...sltu $at,$t0,$s3
2745##...beq $at,$zero,3036

    ###### begin outer loop to copy all non-stack-direct objects ######

2748:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2748#    lw $t1,($t0) # (what will be) first header word for current object
2752#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2756#    bge $t2,$zero,gcSkip4a # are there hash bits?
2757##...slt $at,$t2,$zero
2761##...beq $at,$zero,2776
2764#    and $t2,0xff # mask off hash bits
2765##...lui $at,0
2769##...ori $at,$at,0xff
2773##...and $t2,$t2,$at
2776:gcSkip4a:    
2776#    sll $t2,$t2,2 # # data bytes
2780#    addu $t2,$t2,8 # to skip header words
2784#    addu $t2,$t0,$t2 # pointer to new object
2788#    lw $t3,-8($t2) # word to swap
2792#    sw $t1,-8($t2) # store header word
2796#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2800#    lw $t1,-12($t2) # vtable pointer
2804#    li $t0,dataArrayVTableStart # address of vtable for first data array
2808#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2809##...slt $at,$t1,$t0
2813##...bne $at,$zero,2832

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2816:  skipToNextObj:
2816#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2820#    sll $t1,$t1,2 # convert to byte-count
2824#    addu $t0,$t2,$t1 # skip over object
2828#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2832:  gcNotDataArray:
2832#    lw $t3,-4($t2) # # object words (negative treated as zero)
2836#    bge $t3,$zero,gcH2Neg2
2837##...slt $at,$t3,$zero
2841##...beq $at,$zero,2848
2844#    move $t3,$zero
2848:  gcH2Neg2:
2848#    sll $t3,$t3,2 # # object bytes
2852#    move $t0,$t2 # address of first pointer in object
2856#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2860#    beq $t0,$s1,gcTestIterDone2
    
2864:  gcNextPointerInObject:
    # get pointer from object
2864#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2868#    bleu $t1,$t6,gcGoNextPointerSlot
2869##...sltu $at,$t6,$t1
2873##...beq $at,$zero,3016
2876#    bgtu $t1,$t7,gcGoNextPointerSlot
2877##...sltu $at,$t7,$t1
2881##...bne $at,$zero,3016

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2884#    lw $t8,-8($t1) # possible forwarding tag
2888#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
2892#    lw $t8,-4($t1) # forwarding pointer: object's new address
2896#    sw $t8,($t0) # update pointer in object
2900#    j gcGoNextPointerSlot # do next iteration

2904:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
2904#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
2905##...slt $at,$t8,$zero
2909##...beq $at,$zero,2924
2912#    and $t8,0xff # mask off hash bits
2913##...lui $at,0
2917##...ori $at,$at,0xff
2921##...and $t8,$t8,$at

2924:  gcSkip5:
2924#    sll $t8,$t8,2 # bytes in negative-offset portion ...
2928#    addu $t8,$t8,8 # ... add in bytes for header-words
2932#    subu $t4,$t1,$t8 # address of first word of source object
2936#    lw $t3,-4($t1) # # words in positive-offset portion
2940#    bge $t3,$zero,gcNoNeg
2941##...slt $at,$t3,$zero
2945##...beq $at,$zero,2952
2948#    move $t3,$zero # negative treated as zero
2952:  gcNoNeg:
2952#    sll $t3,$t3,2 # bytes in negative-offset portion
2956#    addu $t3,$t3,$t1 # address one word past last word of source

2960#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
2964#    lw $s0,($t4)
2968#    lw $t9,-8($t1)
2972#    sw $t9,($t4)
2976#    sw $s0,-8($t1)

    # copy all source bytes to the target
2980:  gCinnerCopy2:
2980#    lw $s0,($t4)
2984#    sw $s0,($s3)
2988#    addu $t4,$t4,4
2992#    addu $s3,$s3,4
2996#    bltu $t4,$t3,gCinnerCopy2
2997##...sltu $at,$t4,$t3
3001##...bne $at,$zero,2980

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3004#    sw $zero,-8($t1) # 0 in first header-slot
3008#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3012#    sw $t8,($t0)

3016:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3016#    addu $t0,$t0,4
3020#    bltu $t0,$s1,gcNextPointerInObject
3021##...sltu $at,$t0,$s1
3025##...bne $at,$zero,2864

3028:  gcTestIterDone2:
3028#    bltu $t0,$s3,gcHeapCopyLoop
3029##...sltu $at,$t0,$s3
3033##...bne $at,$zero,2748

    ###### end outer loop to copy all non-stack-direct objects ######

3036:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3036#    lw $s2,($sp)
3040#    addu $sp,$sp,4

    # mark us as having already GC'd
3044#    move $t5,$s5

    # go try and allocate again
3048#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3052:vm_init:

    # mark bottom of stack
3052#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3056#    move $s2,$zero

    #set up the "five" register
3060#    li $s5,5

3064#    la $s3,seg0_start
3068#    la $s4,seg0_end
3069##...lui $s4,16
3073##...addiu $s4,$s4,3892
    
    # return
3076#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3080:divide:
    # get parameters; abort if divisor zero
3080#    lw $t0,($sp)
3084#    lw $t1,8($sp)
3088#    beq $t0,$zero,divByZeroError
    
    # perform division
3092#    div $t1,$t0
3096#    mflo $t0

    # store result, adjust stack and return
3100#    addu $sp,$sp,8 # adjust stack
3104#    sw $t0,($sp) # store result
3108#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3112:remainder:
    # get parameters; abort if divisor zero
3112#    lw $t0,($sp)
3116#    lw $t1,8($sp)
3120#    beq $t0,$zero,divByZeroError
    
    # perform division
3124#    div $t1,$t0
3128#    mfhi $t0

    # store result, adjust stack and return
3132#    addu $sp,$sp,8 # adjust stack
3136#    sw $t0,($sp) # store result
3140#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3144:checkCast:
    # get object, allow immediately if null
3144#    lw $t2,($sp)
3148#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3152#    lw $t2,-12($t2) # vtable address
3156#    bge $t2,$t1,castException
3157##...slt $at,$t2,$t1
3161##...beq $at,$zero,3332
3164#    blt $t2,$t0,castException
3165##...slt $at,$t2,$t0
3169##...bne $at,$zero,3332
    
    # return, leaving object unchanged on stack
3172:  checkCastReturn:    
3172#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3176:instanceOf:
    # get object, go return false if null
3176#    lw $t2,($sp)
3180#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3184#    lw $t2,-12($t2) # vtable address
3188#    sge $t0,$t2,$t0 # are we >= the lower limit?
3189##...slt $t0,$t2,$t0
3193##...xori $t0,$t0,0x1
3196#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3200#    and $t2,$t0,$t1
3204#    sw $t2,($sp)
3208:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3208#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3212:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3212#    li $v0,4 # syscall-code for print-string
3216#    syscall
3220:exit:
    # print termination message
3220#    li $v0,4 # syscall-code for print-string
3224#    la $a0,exitString
3228#    syscall

    # print number of garbage collections
3232#    li $v0,4 # syscall-code for print-string
3236#    la $a0,gcCountMsg
3240#    syscall # print "Number of ..."
3244#    lw $a0,gcCount # number of GC's
3248#    li $v0,1 # code for print-int
3252#    syscall
3256#    li $v0,4 # syscall-code for print-string
3260#    la $a0,newlineString
3264#    syscall # print newline

    # terminate execution
3268#    li $v0,10 #syscall-code for "exit"
3272#    syscall

3276:outOfHeapMemory:
3276#    la $a0,heapMemoryMsg
3280#    j exitError
3284:divByZeroError:
3284#    la $a0,divByZeroMsg
3288#    j exitError
3292:strIndexOutOfBounds:
3292#    la $a0,strIndexOutOfBoundsMsg
3296#    j exitError
3300:arrayIndexOutOfBounds:
3300#    la $a0,arrayIndexOutOfBoundsMsg
3304#    j exitError
3308:arraySizeOutOfBounds:
3308#    la $a0,arraySizeOutOfBoundsMsg
3312#    j exitError
3316:nullPtrException:
3316#    la $a0,nullPtrExceptionMsg
3320#    j exitError
3324:badIntegerFormat:
3324#    la $a0,badIntegerFormatMsg
3328#    j exitError
3332:castException:
3332#    la $a0,castExceptionMsg
3336#    j exitError
############## data section ################
    .data
3520:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3536:cur_seg:
    .word 0

3540:lastCharRead:
    .word -2 # -2 => no buffered character
3544:prevHashConstant:
    .word 0x2785173c
3548:gcCount:
    .word 0

3552:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3579:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3602:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3637:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3671:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3704:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3735:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3783:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3804:gcMsg: 
      .asciiz "\nGC!\n"
3810:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3842:nullStr:
    .asciiz "null"
3847:trueString:
    .asciiz "true"
3852:falseString:
    .asciiz "false"
3858:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
3886:newlineString:
    .asciiz "\n"
    
    .align 2
3888:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

3892:seg0_start:
    .space 0x100000
1052468:seg0_end:
1052468:seg1_start:
    .space 0x100000
2101044:seg1_end:
