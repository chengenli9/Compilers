 .data # Program at 4.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3340:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3360:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3392:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3396:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 4.7
  .byte 205 # 'M' with high bit set # ClassDecl at 4.7
  .byte 97 # 'a' # ClassDecl at 4.7
  .byte 105 # 'i' # ClassDecl at 4.7
  .byte 110 # 'n' # ClassDecl at 4.7
3444:CLASS_Main: # ClassDecl at 4.7
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 5.15
3492:END_CLASS_Main: # ClassDecl at 4.7
3492:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3500:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3512:END_CLASS_RunMain: # ClassDecl at 0.0
3512:dataArrayVTableStart: # IntType at 0.0
3512:END_CLASS_Object: # ClassDecl at 0.0
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  lw $t0, 0($sp)
28#  sw $s2, 0($sp)
32#  move $s2, $t0
36#  beq $s2, $zero, nullPtrException
40#  lw $t0, -12($s2)
44#  lw $t0, 44($t0)
48#  jalr $t0
52#  lw $s2, ($sp)
56#  addu $sp, $sp, 4
60#  subu $sp, $sp, 4
64#  sw $t0, ($sp)
68#  lw $t0, ($sp)
72#  addu $sp, $sp, 4
76#  li $v0, 10
80#  syscall
.globl mth_main_Main
84:mth_main_Main:
84#  subu $sp, $sp, 4
88#  sw $ra, ($sp)
92#  subu $sp, $sp, 4
96#  sw $s2, ($sp)
100#  li $t0, 52
104#  subu $sp, $sp, 8
108#  sw $zero, 4($sp)
112#  sw $t0, ($sp)
116#  li $t0, 5
120#  subu $sp, $sp, 8
124#  sw $zero, 4($sp)
128#  sw $t0, ($sp)
132#  jal divide
136#  lw $t0, 8($sp)
140#  sw $s2, 8($sp)
144#  move $s2, $t0
148#  jal mth_printInt_Lib
152#  addu $sp, $sp, 8
156#  lw $s2, ($sp)
160#  addu $sp, $sp, 4
164#  subu $sp, $sp, 4
168#  sw $t0, ($sp)
172#  lw $t0, ($sp)
176#  addu $sp, $sp, 4
180#  lw $ra, ($sp)
184#  addu $sp, $sp, 4
188#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
192:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
192#    lw $t0,-8($s2) # get word with hash bits from this
196#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
200#    addu $t0,$t1

204#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
208:mth_equals_Object:
208#    move $t0,$s2 # first pointer (this)
212#    lw $t1,($sp) # second pointer
216#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
217##...subu $at,$t0,$t1
221##...sltiu $t0,$at,1

224#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
228:mth_hashCode_String:
228#    move $t0,$s2 # put this pointer in t0

232#    li $t2,0 # current hash value
236#    lw $t1,-4($t0) # negative of number of characters in string
240#    beq $t1,$zero,done_hashCode_String # if zero length => done
244#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
248#    and $t0,0xfffffffc # adjust for padding
249##...lui $at,65535
253##...ori $at,$at,0xfffc
257##...and $t0,$t0,$at
260#    subu $t1,$t0,$t1 # address at end of string +12
264:loop_hashCode_String:    
264#    sll $t4,$t2,5 # multiply by 32
268#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
272#    lbu $t3,-12($t0) # current character
276#    addu $t2,$t3 # add in current character
280#    addu $t0,1 # go to next character's address
284#    bne $t0,$t1,loop_hashCode_String # loop back if more
288:done_hashCode_String:    

    # push int on stack
288#    move $t0, $t2 # put $t2 in return slot
292#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
296:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
296#    subu $sp,4
300#    sw $ra,($sp)
    
    # get hash value
304#    lw $t2,-12($s2) # vtable address
308#    lw $t0,($t2) # method address
312#    jalr $t0
316#    subu $sp,8 # push result (int in $t0) on stack
320#    sw $s5,4($sp)
324#    sw $t0,($sp)

    # compute print-size of hash value
328#    li $t0, 1
332#    lw $t1, ($sp)
336#    srl $t1,$t1,4
340#    beq $t1,$zero,toString_skip_loop
344:toString_loop_top:
344#    addu $t0,1
348#    srl $t1,$t1,4
352#    bne $t1,$zero,toString_loop_top
356:toString_skip_loop:
356#    subu $sp,8
360#    sw $s5,4($sp)
364#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
368#    lw $t2,-12($s2) # vtable address
372#    move $t0,$t2
376:toString_checkTagBit:
376#    subu $t0,1
380#    lb $t1,($t0)
384#    and $t1,0x80
385##...lui $at,0
389##...ori $at,$at,0x80
393##...and $t1,$t1,$at
396#    beq $t1,$zero,toString_checkTagBit
400#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
404#    subu $sp,8
408#    sw $t1,($sp)
412#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
416#    move $s7,$zero # -4 offset header word
420#    lw $t4,8($sp) # bytes for hash value
424#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
428#    addu $t4,8
432#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
436#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

440#    li $t0,CLASS_String # store string vtable address into ...
444#    sw $t0,-12($s7) # ... -12 offset in object header
448#    lw $t2,-12($s2) # our object's vtable address
452#    lw $t5,4($sp) # number characters in prefix
456#    lw $t1,12($sp) # number of characters in hashcode
460#    addu $t3,$t1,$t5 # number of characters in string ...
464#    addu $t3,1 # ... including the '@' character
468#    subu $t0,$zero,$t3 # store negated length into ...
472#    sw $t0,-4($s7) # ... -4 offset header word
476#    lw $t0,-8($s7) # get -8 offset header-word
480#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
481##...slt $at,$t0,$zero
485##...beq $at,$zero,504
488#    and $t0,0xff # mask off object's hash value ...
489##...lui $at,0
493##...ori $at,$at,0xff
497##...and $t0,$t0,$at
500#    sw $t0,-8($s7) # ... and store back into -8 header-word
504:skipMask_toString:
504#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
508#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
512#    subu $t4,$t2,$t5
516:toString_copyLoop:    
516#    lb $t3,($t4)
520#    and $t3,0x7f # mask off sign bit (in case first word)
521##...lui $at,0
525##...ori $at,$at,0x7f
529##...and $t3,$t3,$at
532#    sb $t3,-8($t0)
536#    addu $t4,1
540#    addu $t0,1
544#    blt $t4,$t2,toString_copyLoop
545##...slt $at,$t4,$t2
549##...bne $at,$zero,516

    # copy the '@' sign
552#    li $t3,'@'
556#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
560#    addu $t2,$t0,$t1 # address of last byte in string to store +8
564#    lw $t3,20($sp) # hash code to print
568:toString_hashCopyLoop:
568#    and $t4,$t3,0xf
572#    srl $t3,4
576#    lb $t4,hexChars($t4)
580#    sb $t4,-8($t2)
584#    subu $t2,$t2,1
588#    bgt $t2,$t0,toString_hashCopyLoop
589##...slt $at,$t0,$t2
593##...bne $at,$zero,568

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
596#    addu $sp,28
600#    lw $ra,($sp)
604#    addu $sp,4
608#    move $t0, $s7 # return string we created
612#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
616:mth_equals_String:
    
    # quick test for obvious false
616#    lw $t0,($sp) # second parameter
620#    beq $t0,$zero,goEsFalse # go return false if null
624#    lw $t1,-12($t0) # vtable pointer
628#    la $t2,CLASS_String # string vtable pointer
632#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
636#    subu $sp,12
640#    sw $ra,8($sp)
644#    sw $s2,4($sp)
648#    sw $t0,($sp)
652#    jal mth_compareTo_String
656#    lw $s2, 4($sp) #restore this pointer and pop parameters
660#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
664#    seq $t0,$t0,$zero
665##...subu $at,$t0,$zero
669##...sltiu $t0,$at,1

    # pop return address and return $t0
672#    lw $ra,($sp)
676#    addu $sp,4

680#    jr $ra # return
    
684:goEsFalse:
    # put 0 in $t0 and return
684#    li $t0, 0
688#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
692:mth_toString_String:
    #we are a string, so return this.
692#    move $t0, $s2
696#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
700:mth_readLine_Lib:
    
700#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
704#    sw $ra, 4($sp) # save $ra
708#    move $t1,$sp # save "original" sp

712:  doRead:

    # read the character
712#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
716#    subu $t2,$v0,13
720#    bne $t2,$zero,notReturnRL
724#    jal readLogicalChar
728#    subu $t2,$v0,10 # check for newline
732#    beq $t2,$zero,foundNewLine
736#    sw $v0,lastCharRead # push back character into queue
740#    j foundNewLine # go process the line

744:  notReturnRL:

    # at this point, $v0 has our character

744#    subu $t0,$v0,10
748#    beq $t0,$zero,foundNewLine
752#    blt $v0,$zero,foundEof
753##...slt $at,$v0,$zero
757##...bne $at,$zero,772

    # we have a character, so push it onto stack
760#    subu $sp,$sp,4
764#    sw $v0,($sp)

    # loop back up to get next character
768#    j doRead

772:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
772#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
776#    li $s7, 0 # return-value: null
780#    j rlReturn # return

784:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
784#    subu $t0,$t1,$sp # number of chars we read (times 4)
788#    srl $s6,$t0,2 # number of words on stack with chars
792#    subu $sp,$sp,4 # push char-count ...
796#    sw $s6,($sp) # ... onto stack
800#    addu $t2,$t0,5 # GC tag, incl. for count-word
804#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
808#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
812#    srl $s6,$s6,2  # data words in object
816#    move $s7,$zero # object words in object
820#    jal newObject  # allocate space
    
    # store header words
824#    la $t0,CLASS_String
828#    sw $t0,-12($s7)# store class tag (String) into object
832#    lw $t2,4($sp) # char-count
836#    sll $t1,$t2,2 # 4 times number of chars
840#    subu $t2,$zero,$t2 # negative of char-count
844#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
848#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
852#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
853##...slt $at,$t0,$zero
857##...beq $at,$zero,876
860#    and $t0,0xff # mask off object's hash value ...
861##...lui $at,0
865##...ori $at,$at,0xff
869##...and $t0,$t0,$at
872#    sw $t0,-8($s7) # ... and store back into -8 header-word
876:  skipMask_readLine:

876#    sll $t0,$t0,2 # data bytes in object
880#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
884#    subu $t0,$t0,$t2 # place to store last character (plus 9)
888#    addu $sp,$sp,8 # pop redundant object-pointer and count
892#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

896#    beq $sp,$t1,rlReturn
900:  charCopyLoop:
900#    lw $t2,($sp)
904#    sb $t2,-9($t0)
908#    addu $sp,$sp,4
912#    subu $t0,$t0,1
916#    bne $sp,$t1,charCopyLoop
920:  rlReturn:

920#    lw $ra, 4($sp)
924#    addu $sp, 8   # pop $ra and pointless word off stack
928#    move $t0, $s7 # return string we allocated in $s7
932#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
936:mth_readInt_Lib:

    # save $ra by pushing onto stack
936#    subu $sp,$sp,4
940#    sw $ra,($sp)

944:  riSkipWhiteLoop:
    # read a character
944#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
948#    subu $t0,$v0,32
952#    bgt $t0,$zero,nonWhite
953##...slt $at,$zero,$t0
957##...bne $at,$zero,1000
960#    beq $t0,$zero,riSkipWhiteLoop # start over if space
964#    subu $t0,$v0,10
968#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
972#    subu $t0,$v0,9
976#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
980#    subu $t0,$v0,13
984#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
988#    subu $t0,$v0,12
992#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
996#    j badIntegerFormat # illegal integer char: abort program

1000:  nonWhite:
1000#    subu $t0,$v0,'-'
1004#    li $t4,1 # final multiplier
1008#    bne $t0,$zero,helpReadInt # go read
1012#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1016#    jal readLogicalChar

1020:  helpReadInt:
1020#    li $t2,10
1024#    subu $t1,$v0,'0' # convert digit to 0-9 value
1028#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1029##...sltu $at,$t1,$t2
1033##...beq $at,$zero,3316
1036#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1040:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1040#    jal readLogicalChar # read next character
1044#    subu $t1,$v0,'0' # convert digit to 0-9 value
1048#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1049##...sltu $at,$t1,$t2
1053##...beq $at,$zero,1072

1056#    mul $t3,$t3,$t2 # multiply old value by 10
1057##...mult $t3,$t2
1061##...mflo $t3
1064#    addu $t3,$t3,$t1 # add in value of new digit
1068#    j digitLoop

1072:  doneDigitLoop:

    # "push back" unused character into queue
1072#    sw $v0,lastCharRead

    # restore return address
1076#    lw $ra,($sp)
1080#    addu $sp, 4

    # multiply by sign and put return value in $t0
1084#    mult $t3,$t4 # multiply to account for poss. minus sign
1088#    mflo $t3
1092#    move $t0, $t3
    
    # return
1096#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1100:mth_readChar_Lib:

    # save $ra by pushing onto stack
1100#    subu $sp,$sp,4
1104#    sw $ra,($sp)

      # read the character
1108#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1112#    lw $ra,($sp)
1116#    addu $sp, 4
1120#    move $t0, $v0

    # return
1124#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1128:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1128#    lw $t0,($sp)
1132#    bne $t0,$zero,psNotNull

    # print "(null)"
1136#    la $a0,nullStr
1140#    li $v0,4
1144#    syscall
1148#    j donePrintStr
    
1152:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1152#    subu $t0,$t0,8
1156#    lw $t1,($t0) # number of data words, but needs modification
1160#    blt $t1,$zero,skipPrintStr1
1161##...slt $at,$t1,$zero
1165##...bne $at,$zero,1184
1168#    and $t1,0xff # mask out all but low byte
1169##...lui $at,0
1173##...ori $at,$at,0xff
1177##...and $t1,$t1,$at
1180#    b skipPrintStr2
1184:skipPrintStr1:
1184#    and $t1,0x7ffffff
1185##...lui $at,2047
1189##...ori $at,$at,0xffff
1193##...and $t1,$t1,$at
1196:skipPrintStr2:    
1196#    sll $t1,$t1,2
1200#    subu $t1,$t0,$t1 # first word in string
1204#    lw $t0,4($t0) # negative of string-length
1208#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1212#    beq $t0,$t1,donePrintStr
1216:  psLoop:
1216#    lb $a0,($t1) # next byte
1220#    li $v0,11 # code for printing char
1224#        syscall # print the char
1228#    addu $t1,$t1,1 # go to next char
1232#    blt $t1,$t0,psLoop
1233##...slt $at,$t1,$t0
1237##...bne $at,$zero,1216

1240:  donePrintStr:
1240#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1244:mth_printInt_Lib:
1244#    lw $a0,($sp)

    # print it
1248#    li $v0,1 # code for print-int
1252#    syscall

    # return
1256#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1260:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1260#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1264#    la $a0,falseString
1268#    beq $t0,$zero,skipPB
1272#    la $a0,trueString
1276:  skipPB:
1276#    li $v0,4 # code for print-string
1280#    syscall

    # return
1284#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1288:mth_substring_String:

    # save $ra by pushing onto stack
1288#    subu $sp,$sp,4
1292#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1296#    move $t0,$s2
1300#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1304#    lw $t0,-4($t0) # negative of string length
1308#    subu $t0,$zero,$t0 # string length
1312#    lw $t1,4($sp) # k
1316#    lw $t2,12($sp) # n
1320#    bgt $zero,$t2,strIndexOutOfBounds
1321##...slt $at,$t2,$zero
1325##...bne $at,$zero,3284
1328#    bgt $t2,$t1,strIndexOutOfBounds
1329##...slt $at,$t1,$t2
1333##...bne $at,$zero,3284
1336#    bgt $t1,$t0,strIndexOutOfBounds
1337##...slt $at,$t0,$t1
1341##...bne $at,$zero,3284

    # allocate memory
1344#    subu $s6,$t1,$t2 # # chars in target-string
1348#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1352#    srl $s6,$s6,2 # convert bytes-count to word-count
1356#    move $s7,$zero # (no object-bytes in string)
1360#    jal newObject

    # store "String" tag in object-type field
1364#    la $t0,CLASS_String
1368#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1372#    lw $t4,-8($s7) # data words in source string
1376#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1377##...slt $at,$t4,$zero
1381##...beq $at,$zero,1400
1384#    and $t4,0xff # mask off object's hash value ...
1385##...lui $at,0
1389##...ori $at,$at,0xff
1393##...and $t4,$t4,$at
1396#    sw $t4,-8($s7) # ... and store back into -8 header-word
1400:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1400#    lw $t1,8($sp) # k
1404#    lw $t2,16($sp) # n
1408#    subu $t0,$t2,$t1 # value to store
1412#    sw $t0,-4($s7) # store value
    
1416#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1420#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1424#    lw $t4,-8($t3) # data words in source string
1428#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1432#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1436#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1440#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1444#    lw $t2,-8($s7)   # data words in target string
1448#    sll $t2,$t2,2    # data bytes in target string
1452#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1456:  subCopyLoopzz:
1456#    lb $t4,-8($t3)
1460#    sb $t4,-8($t2)
1464#    addu $t2,$t2,1
1468#    addu $t3,$t3,1
1472#    blt $t3,$t1,subCopyLoopzz
1473##...slt $at,$t3,$t1
1477##...bne $at,$zero,1456
    
1480:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1480#    lw $ra,4($sp) # restore $ra
1484#    addu $sp,$sp,8 # pop ra, and allocated newObject
1488#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1492#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1496:mth_length_String:

    # the length is the negative of 4($s2)
1496#    lw $t0,-4($s2) # -length
1500#    subu $t0,$zero,$t0

    # return
1504#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1508:mth_concat_String:
    # save $ra by pushing onto stack
1508#    subu $sp,$sp,4
1512#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1516#    lw $t0,4($sp)
1520#    beq $t0,$zero,nullPtrException
1524#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1528#    lw $t0,-4($t0) # negative size of second object
1532#    lw $t1,-4($t1) # negative size of first object
1536#    addu $s6,$t0,$t1 # sum of negative sizes
1540#    sra $s6,$s6,2 # negative word-size of char part
1544#    subu $s6,$zero,$s6 # word size of char part
1548#    addu $s6,$s6,1 # data word size, including v-table word
1552#    move $s7,$zero
1556#    jal newObject

    # store String vtable address in vtable address field
1560#    la $t0,CLASS_String
1564#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1568#    lw $t4,-8($s7) # # data words in source string
1572#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1573##...slt $at,$t4,$zero
1577##...beq $at,$zero,1596
1580#    and $t4,0xff # mask off object's hash value ...
1581##...lui $at,0
1585##...ori $at,$at,0xff
1589##...and $t4,$t4,$at
1592#    sw $t4,-8($s7) # ... and store back into -8 header-word
1596:skipMask_concat:

1596#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1600#    move $t1,$s2   # first object
1604#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1608#    lw $t2,-4($t0) # negative length of second object
1612#    lw $t3,-4($t1) # negative length of first object
1616#    addu $t4,$t2,$t3 # sum of negative lengths
1620#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1624#    lw $t4,-8($t1) # # data words in first string
1628#    sll $t4,$t4,2 # # data bytes in first string
1632#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1636#    lw $t4,-8($s7) # # data words in new string
1640#    sll $t4,$t4,2 # # data bytes in new string
1644#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1648#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1652#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1656:  concatLoop1zz:
1656#    lb $t4,-8($t1)
1660#    sb $t4,-8($s7)
1664#    addu $s7,$s7,1
1668#    addu $t1,$t1,1
1672#    blt $t1,$t3,concatLoop1zz
1673##...slt $at,$t1,$t3
1677##...bne $at,$zero,1656
1680:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1680#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1684#    lw $t4,-8($t0) # # data words in second string
1688#    sll $t4,$t4,2 # # data bytes in second string
1692#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1696#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1700:  concatLoop2zz:
1700#    lb $t4,-8($t1)
1704#    sb $t4,-8($s7)
1708#    addu $s7,$s7,1
1712#    addu $t1,$t1,1
1716#    blt $t1,$t3,concatLoop2zz
1717##...slt $at,$t1,$t3
1721##...bne $at,$zero,1700
1724:  doneConcatLoop2zz:
 
1724:  concatRtnzz:
    # return
1724#    move $t0, $t9 # put saved return string in $t0
1728#    lw $ra, 4($sp)
1732#    addu $sp, 8 # pop ra and allocated newObject
1736#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1740:mth_charAt_String:
    # get string
1740#    move $t0,$s2

    # check that index is in bounds
1744#    lw $t1,-4($t0) # negative of # data words in string
1748#    subu $t3,$zero,$t1 # # chars in string
1752#    lw $t2,($sp) # index
1756#    bgeu $t2,$t3,strIndexOutOfBounds
1757##...sltu $at,$t2,$t3
1761##...beq $at,$zero,3284

    # access element
1764#    lw $t1,-8($t0) # # data words in object
1768#    sll $t1,$t1,2 # - byte-offset from end of chars
1772#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1776#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1780#    lb $t0,-8($t0) # our char

    # return
1784#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1788:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1788#    subu $sp,$sp,8
1792#    sw $ra,4($sp)

    # save current sp
1796#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1800#    li $t3,10

    # get argument, negate if negative
1804#    lw $t1,8($sp)
1808#    bge $t1,$zero,itsNonNegLoop
1809##...slt $at,$t1,$zero
1813##...beq $at,$zero,1820
1816#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1820:  itsNonNegLoop:
1820#    divu $t1,$t3
1824#    mflo $t1 # quotient
1828#    mfhi $t4 # remainder
1832#    addu $t4,$t4,'0' # turn remainder into digit
1836#    subu $sp,$sp,4
1840#    sw $t4,($sp) # push digit onto stack
1844#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1848#    lw $t4,8($t0)
1852#    bge $t4,$zero,itsSkipNeg
1853##...slt $at,$t4,$zero
1857##...beq $at,$zero,1872
1860#    li $t4,'-'
1864#    subu $sp,$sp,4
1868#    sw $t4,($sp)
1872:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
1872#    subu $s6,$t0,$sp
1876#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
1880#    sw $t3,($t0)
1884#    srl $s6,$s6,2
1888#    subu $sp,$sp,4
1892#    sw $s6,($sp)
    
    # allocate memory
1896#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1900#    srl $s6,$s6,2
1904#    move $s7,$zero # no "object" words in object
1908#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
1912#    lw $t0,4($sp)
1916#    addu $sp,$sp,8

    # store "String" tag into class field
1920#    subu $s7,$s7,8 # address of header-1 word
1924#    la $t1,CLASS_String
1928#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
1932#    lw $t4,($s7) # # data words in source string
1936#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
1937##...slt $at,$t4,$zero
1941##...beq $at,$zero,1960
1944#    and $t4,0xff # mask off object's hash value ...
1945##...lui $at,0
1949##...ori $at,$at,0xff
1953##...and $t4,$t4,$at
1956#    sw $t4,($s7) # ... and store back into -8 header-word
1960:skipMask_intToString:
    
    # store negative of char-count into header-2 word
1960#    subu $t0,$zero,$t0
1964#    sw $t0,4($s7)

1968#    lw $t1,($s7) # number of data words in string
1972#    sll $t1,$t1,2 # number data bytes in string
1976#    subu $t1,$s7,$t1 # first location to store chars in string
1980#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
1984:  itsLoop:
1984#    lw $t2,($sp)
1988#    addu $sp,$sp,4
1992#    sb $t2,($t1)
1996#    addu $t1,$t1,1
2000#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2004#    lw $ra,4($sp)
2008#    addu $sp,$sp,8
2012#    addu $t0,$t0,15
2016#    and $t0,$t0,0xfffffffc
2017##...lui $at,65535
2021##...ori $at,$at,0xfffc
2025##...and $t0,$t0,$at
2028#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2032:mth_intToChar_Lib:
    # save return address
2032#    subu $sp,$sp,4
2036#    sw $ra,($sp)

    # allocate object
2040#    li $s6,2
2044#    move $s7,$zero
2048#    jal newObject

    # get n from string.
2052#    lw $t1,8($sp)

    # store "String" tag into class field
2056#    la $t0,CLASS_String
2060#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2064#    lw $t4,-8($s7) # # data words in source string
2068#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2069##...slt $at,$t4,$zero
2073##...beq $at,$zero,2092
2076#    and $t4,0xff # mask off object's hash value ...
2077##...lui $at,0
2081##...ori $at,$at,0xff
2085##...and $t4,$t4,$at
2088#    sw $t4,-8($s7) # ... and store back into -8 header-word
2092:  skipMask_intToChar:

    # store data in string
2092#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2096#    li $t0,-1
2100#    sw $t0,-4($s7)

    # we're returning $s7
2104#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2108#    lw $ra,4($sp)
2112#    addu $sp,$sp,8

    # return
2116#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2120:mth_compareTo_String:
    # get string pointers and check parameter for null
2120#    lw $t0,($sp) # second string
2124#    beq $t0,$zero,nullPtrException
2128#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2132#    lw $t2,-4($t0) # negative length of second string
2136#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2140#    slt $t4,$t3,$t2
2144#    slt $t5,$t2,$t3
2148#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2152#    lw $t2,-8($t0) # # data words in second string
2156#    lw $t3,-8($t1) # # data words in first string
2160#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2164#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2168#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2172#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2176#    beq $zero,$t5,skipStrCmpLenzz
2180#    move $t2,$t3
2184:  skipStrCmpLenzz:
2184#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2188:  cmpLoopzz:
2188#    lw $t3,-8($t1) # word from str2
2192#    lw $t4,-8($t0) # word from str1
2196#    bne $t3,$t4,cmpNotEqualzz
2200#    addu $t1,$t1,4
2204#    addu $t0,$t0,4
2208#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2212#    j cmpHitLimitzz

2216:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2216#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2220#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2224#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2228:  cmpHitLimitzz:
2228#    move $t0, $t5 # store return value
2232#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2236:readLogicalChar:

    # check if we already have a character
2236#    lw $v0,lastCharRead
2240#    li $t0,-2
2244#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2248#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2252#    jr $ra # return
    
2256:  doReadCh:
    # we need to actually read a character: read and return
2256#    li $v0,12 # use system call to read a character
2260#    syscall
2264#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2268:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2268#    blt $s7,$zero arraySizeOutOfBounds
2269##...slt $at,$s7,$zero
2273##...bne $at,$zero,3300

    # mark the fact that we have not yet GC'd for this allocation
2276#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2280:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2280#    addu $t0,$s6,$s7
2284#    sll $t0,$t0,2
2288#    addu $t0,$t0,8 # amount of memory needed
2292#    addu $t1,$t0,$s3
2296#    bgtu $t1,$s4,doGC
2297##...sltu $at,$s4,$t1
2301##...bne $at,$zero,2424

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2304#    move $t2,$s3
2308:  zeroObjLoop:
2308#    sw $zero,($s3)
2312#    lw $zero,($s3)

2316#    addu $s3,4
2320#    bltu $s3,$t1,zeroObjLoop
2321##...sltu $at,$s3,$t1
2325##...bne $at,$zero,2308

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2328#    sll $t0,$s7,2    # number of post-header bytes
2332#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2336#    li $t2,255
2340#    bgeu $s6,$t2 firstHeaderLarge
2341##...sltu $at,$s6,$t2
2345##...beq $at,$zero,2392
2348#    lw $t1,prevHashConstant
2352#    addu $t1,0x12b68fd2
2353##...lui $at,4791
2357##...addiu $at,$at,-28718
2361##...addu $t1,$at,$t1
2364#    sw $t1,prevHashConstant # update hash constant
2368#    or $t1,0x80000000 # set top bit
2369##...lui $at,32768
2373##...or $t1,$at,$t1
2376#    and $t1,0xffffff00 # clear low bit
2377##...lui $at,65535
2381##...ori $at,$at,0xff00
2385##...and $t1,$t1,$at
2388#    or $s6,$t1
2392:firstHeaderLarge:
2392#    sw $s6,-8($t0) # store header-1 value
2396#    lw $zero,-8($t0)

    # store second header-value
2400#    sw $s7,-4($t0)   # second header-word
2404#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2408#    move $s7,$t0
2412#    subu $sp,$sp,4
2416#    sw $t0,($sp)

2420#    jr $ra
    
2424:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2424#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2428#    lw $t5,gcCount
2432#    addu $t5,1
2436#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2440#    subu $sp,$sp,4
2444#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2448#    lw $t0,cur_seg
2452#    move $t7,$s4
2456#    la $t6,seg1_start
2457##...lui $t6,16
2461##...addiu $t6,$t6,3884
2464#    la $s3,seg0_start
2468#    la $s4,seg0_end
2469##...lui $s4,16
2473##...addiu $s4,$s4,3884
2476#    sw $zero,cur_seg
2480#    bne $t0,$zero,skipGc1
2484#    la $t6,seg0_start
2488#    la $s3,seg1_start
2489##...lui $s3,16
2493##...addiu $s3,$s3,3884
2496#    la $s4,seg1_end
2497##...lui $s4,32
2501##...addiu $s4,$s4,3884
2504#    sw $s5,cur_seg
2508:  skipGc1:
2508#    li $t5,0x80000000
2512#    lw $t0,stack_bottom
2516#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2520:  gcStackCopyLoop:
2520#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2524#    sll $t2,$t1,31 # zero iff low bit was zero
2528#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2532#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2536#    bleu $t1,$t6,gcTestIterDone1
2537##...sltu $at,$t6,$t1
2541##...beq $at,$zero,2704
2544#    bgtu $t1,$t7,gcTestIterDone1
2545##...sltu $at,$t7,$t1
2549##...bne $at,$zero,2704

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2552#    lw $t2,-8($t1) # possible forwarding tag
2556#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2560#    lw $t2,-4($t1) # forwarding pointer: object's new address
2564#    sw $t2,4($t0) # update stack value
2568#    j gcTestIterDone1 # do next iteration

2572:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2572#    bge $t2,$zero,gcSkip2
2573##...slt $at,$t2,$zero
2577##...beq $at,$zero,2592
2580#    and $t2,0xff #### mask off hash value
2581##...lui $at,0
2585##...ori $at,$at,0xff
2589##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2592:  gcSkip2:
2592#    sll $t2,$t2,2
2596#    addu $t2,$t2,8
2600#    subu $t4,$t1,$t2 # address of first word of source
2604#    lw $t3,-4($t1) # number of object words (negative means zero)
2608#    bge $t3,$zero,gcH2Neg1
2609##...slt $at,$t3,$zero
2613##...beq $at,$zero,2620
2616#    move $t3,$zero
2620:  gcH2Neg1:
2620#    sll $t3,$t3,2
2624#    addu $t3,$t3,$t1 # address one word past last word of source

2628#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2632#    lw $s0,($t4)
2636#    lw $s1,-8($t1)
2640#    sw $s1,($t4)
2644#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2648:  gCinnerCopy1:
2648#    lw $s0,($t4)
2652#    sw $s0,($s3)
2656#    addu $t4,$t4,4
2660#    addu $s3,$s3,4
2664#    bltu $t4,$t3,gCinnerCopy1
2665##...sltu $at,$t4,$t3
2669##...bne $at,$zero,2648

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2672#    sw $zero,-8($t1) # 0 in first header-slot
2676#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2680#    sw $t2,4($t0)
2684#    lw $zero,-8($t2)
2688#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2692#    j gcTestIterDone1

2696:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2696#    subu $t0,$t0,$t1
2700#    subu $t0,$t0,3

2704:  gcTestIterDone1:
2704#    bgeu $t0,$sp,gcStackCopyLoop
2705##...sltu $at,$t0,$sp
2709##...beq $at,$zero,2520

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2712#    lw $t1,cur_seg
2716#    la $t0,seg0_start
2720#    beq $t1,$zero,gcSkip4
2724#    la $t0,seg1_start
2725##...lui $t0,16
2729##...addiu $t0,$t0,3884


2732:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2732#    bgeu $t0,$s3,gcFinishUp
2733##...sltu $at,$t0,$s3
2737##...beq $at,$zero,3028

    ###### begin outer loop to copy all non-stack-direct objects ######

2740:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2740#    lw $t1,($t0) # (what will be) first header word for current object
2744#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2748#    bge $t2,$zero,gcSkip4a # are there hash bits?
2749##...slt $at,$t2,$zero
2753##...beq $at,$zero,2768
2756#    and $t2,0xff # mask off hash bits
2757##...lui $at,0
2761##...ori $at,$at,0xff
2765##...and $t2,$t2,$at
2768:gcSkip4a:    
2768#    sll $t2,$t2,2 # # data bytes
2772#    addu $t2,$t2,8 # to skip header words
2776#    addu $t2,$t0,$t2 # pointer to new object
2780#    lw $t3,-8($t2) # word to swap
2784#    sw $t1,-8($t2) # store header word
2788#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2792#    lw $t1,-12($t2) # vtable pointer
2796#    li $t0,dataArrayVTableStart # address of vtable for first data array
2800#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2801##...slt $at,$t1,$t0
2805##...bne $at,$zero,2824

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2808:  skipToNextObj:
2808#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2812#    sll $t1,$t1,2 # convert to byte-count
2816#    addu $t0,$t2,$t1 # skip over object
2820#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2824:  gcNotDataArray:
2824#    lw $t3,-4($t2) # # object words (negative treated as zero)
2828#    bge $t3,$zero,gcH2Neg2
2829##...slt $at,$t3,$zero
2833##...beq $at,$zero,2840
2836#    move $t3,$zero
2840:  gcH2Neg2:
2840#    sll $t3,$t3,2 # # object bytes
2844#    move $t0,$t2 # address of first pointer in object
2848#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2852#    beq $t0,$s1,gcTestIterDone2
    
2856:  gcNextPointerInObject:
    # get pointer from object
2856#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2860#    bleu $t1,$t6,gcGoNextPointerSlot
2861##...sltu $at,$t6,$t1
2865##...beq $at,$zero,3008
2868#    bgtu $t1,$t7,gcGoNextPointerSlot
2869##...sltu $at,$t7,$t1
2873##...bne $at,$zero,3008

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2876#    lw $t8,-8($t1) # possible forwarding tag
2880#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
2884#    lw $t8,-4($t1) # forwarding pointer: object's new address
2888#    sw $t8,($t0) # update pointer in object
2892#    j gcGoNextPointerSlot # do next iteration

2896:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
2896#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
2897##...slt $at,$t8,$zero
2901##...beq $at,$zero,2916
2904#    and $t8,0xff # mask off hash bits
2905##...lui $at,0
2909##...ori $at,$at,0xff
2913##...and $t8,$t8,$at

2916:  gcSkip5:
2916#    sll $t8,$t8,2 # bytes in negative-offset portion ...
2920#    addu $t8,$t8,8 # ... add in bytes for header-words
2924#    subu $t4,$t1,$t8 # address of first word of source object
2928#    lw $t3,-4($t1) # # words in positive-offset portion
2932#    bge $t3,$zero,gcNoNeg
2933##...slt $at,$t3,$zero
2937##...beq $at,$zero,2944
2940#    move $t3,$zero # negative treated as zero
2944:  gcNoNeg:
2944#    sll $t3,$t3,2 # bytes in negative-offset portion
2948#    addu $t3,$t3,$t1 # address one word past last word of source

2952#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
2956#    lw $s0,($t4)
2960#    lw $t9,-8($t1)
2964#    sw $t9,($t4)
2968#    sw $s0,-8($t1)

    # copy all source bytes to the target
2972:  gCinnerCopy2:
2972#    lw $s0,($t4)
2976#    sw $s0,($s3)
2980#    addu $t4,$t4,4
2984#    addu $s3,$s3,4
2988#    bltu $t4,$t3,gCinnerCopy2
2989##...sltu $at,$t4,$t3
2993##...bne $at,$zero,2972

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2996#    sw $zero,-8($t1) # 0 in first header-slot
3000#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3004#    sw $t8,($t0)

3008:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3008#    addu $t0,$t0,4
3012#    bltu $t0,$s1,gcNextPointerInObject
3013##...sltu $at,$t0,$s1
3017##...bne $at,$zero,2856

3020:  gcTestIterDone2:
3020#    bltu $t0,$s3,gcHeapCopyLoop
3021##...sltu $at,$t0,$s3
3025##...bne $at,$zero,2740

    ###### end outer loop to copy all non-stack-direct objects ######

3028:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3028#    lw $s2,($sp)
3032#    addu $sp,$sp,4

    # mark us as having already GC'd
3036#    move $t5,$s5

    # go try and allocate again
3040#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3044:vm_init:

    # mark bottom of stack
3044#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3048#    move $s2,$zero

    #set up the "five" register
3052#    li $s5,5

3056#    la $s3,seg0_start
3060#    la $s4,seg0_end
3061##...lui $s4,16
3065##...addiu $s4,$s4,3884
    
    # return
3068#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3072:divide:
    # get parameters; abort if divisor zero
3072#    lw $t0,($sp)
3076#    lw $t1,8($sp)
3080#    beq $t0,$zero,divByZeroError
    
    # perform division
3084#    div $t1,$t0
3088#    mflo $t0

    # store result, adjust stack and return
3092#    addu $sp,$sp,8 # adjust stack
3096#    sw $t0,($sp) # store result
3100#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3104:remainder:
    # get parameters; abort if divisor zero
3104#    lw $t0,($sp)
3108#    lw $t1,8($sp)
3112#    beq $t0,$zero,divByZeroError
    
    # perform division
3116#    div $t1,$t0
3120#    mfhi $t0

    # store result, adjust stack and return
3124#    addu $sp,$sp,8 # adjust stack
3128#    sw $t0,($sp) # store result
3132#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3136:checkCast:
    # get object, allow immediately if null
3136#    lw $t2,($sp)
3140#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3144#    lw $t2,-12($t2) # vtable address
3148#    bge $t2,$t1,castException
3149##...slt $at,$t2,$t1
3153##...beq $at,$zero,3324
3156#    blt $t2,$t0,castException
3157##...slt $at,$t2,$t0
3161##...bne $at,$zero,3324
    
    # return, leaving object unchanged on stack
3164:  checkCastReturn:    
3164#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3168:instanceOf:
    # get object, go return false if null
3168#    lw $t2,($sp)
3172#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3176#    lw $t2,-12($t2) # vtable address
3180#    sge $t0,$t2,$t0 # are we >= the lower limit?
3181##...slt $t0,$t2,$t0
3185##...xori $t0,$t0,0x1
3188#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3192#    and $t2,$t0,$t1
3196#    sw $t2,($sp)
3200:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3200#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3204:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3204#    li $v0,4 # syscall-code for print-string
3208#    syscall
3212:exit:
    # print termination message
3212#    li $v0,4 # syscall-code for print-string
3216#    la $a0,exitString
3220#    syscall

    # print number of garbage collections
3224#    li $v0,4 # syscall-code for print-string
3228#    la $a0,gcCountMsg
3232#    syscall # print "Number of ..."
3236#    lw $a0,gcCount # number of GC's
3240#    li $v0,1 # code for print-int
3244#    syscall
3248#    li $v0,4 # syscall-code for print-string
3252#    la $a0,newlineString
3256#    syscall # print newline

    # terminate execution
3260#    li $v0,10 #syscall-code for "exit"
3264#    syscall

3268:outOfHeapMemory:
3268#    la $a0,heapMemoryMsg
3272#    j exitError
3276:divByZeroError:
3276#    la $a0,divByZeroMsg
3280#    j exitError
3284:strIndexOutOfBounds:
3284#    la $a0,strIndexOutOfBoundsMsg
3288#    j exitError
3292:arrayIndexOutOfBounds:
3292#    la $a0,arrayIndexOutOfBoundsMsg
3296#    j exitError
3300:arraySizeOutOfBounds:
3300#    la $a0,arraySizeOutOfBoundsMsg
3304#    j exitError
3308:nullPtrException:
3308#    la $a0,nullPtrExceptionMsg
3312#    j exitError
3316:badIntegerFormat:
3316#    la $a0,badIntegerFormatMsg
3320#    j exitError
3324:castException:
3324#    la $a0,castExceptionMsg
3328#    j exitError
############## data section ################
    .data
3512:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3528:cur_seg:
    .word 0

3532:lastCharRead:
    .word -2 # -2 => no buffered character
3536:prevHashConstant:
    .word 0x2785173c
3540:gcCount:
    .word 0

3544:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3571:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3594:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3629:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3663:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3696:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3727:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3775:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3796:gcMsg: 
      .asciiz "\nGC!\n"
3802:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3834:nullStr:
    .asciiz "null"
3839:trueString:
    .asciiz "true"
3844:falseString:
    .asciiz "false"
3850:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
3878:newlineString:
    .asciiz "\n"
    
    .align 2
3880:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

3884:seg0_start:
    .space 0x100000
1052460:seg0_end:
1052460:seg1_start:
    .space 0x100000
2101036:seg1_end:
