 .data # Program at 3.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3356:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3376:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3408:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3412:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
3456:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3464:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3476:END_CLASS_RunMain: # ClassDecl at 0.0
# ****** class Main ****** # ClassDecl at 3.7
  .byte 205 # 'M' with high bit set # ClassDecl at 3.7
  .byte 97 # 'a' # ClassDecl at 3.7
  .byte 105 # 'i' # ClassDecl at 3.7
  .byte 110 # 'n' # ClassDecl at 3.7
3480:CLASS_Main: # ClassDecl at 3.7
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 4.15
3496:END_CLASS_Main: # ClassDecl at 3.7
3496:dataArrayVTableStart: # IntType at 0.0
3496:END_CLASS_Object: # ClassDecl at 0.0
  .byte 97 # StringLit at 5.25
  .byte 98 # StringLit at 5.25
  .byte 99 # StringLit at 5.25
  .byte 0 # StringLit at 5.25
  .word CLASS_String # StringLit at 5.25
  .word 2 # StringLit at 5.25
  .word -3 # StringLit at 5.25
3512:strLit_2: # StringLit at 5.25
  .byte 97 # StringLit at 5.34
  .byte 98 # StringLit at 5.34
  .byte 99 # StringLit at 5.34
  .byte 0 # StringLit at 5.34
  .word CLASS_String # StringLit at 5.34
  .word 2 # StringLit at 5.34
  .word -3 # StringLit at 5.34
3528:strLit_3: # StringLit at 5.34
.text
.globl main
0:main:
0#  jal vm_init
4#  li $s6, 1
8#  li $s7, 0
12#  jal newObject
16#  la $t0, CLASS_Main
20#  sw $t0, -12($s7)
24#  addu $sp, $sp, 4
28#  move $s2, $s7
32#  jal mth_main_Main
36#  li $v0, 10
40#  syscall
.globl mth_main_Main
44:mth_main_Main:
44#  subu $sp, $sp, 4
48#  sw $ra, ($sp)
52#  li $s6, 1
56#  li $s7, 0
60#  jal newObject
64#  la $t0, CLASS_Lib
68#  sw $t0, -12($s7)
72#  la $t0, strLit_2
76#  subu $sp, $sp, 4
80#  sw $t0, ($sp)
84#  la $t0, strLit_3
88#  subu $sp, $sp, 4
92#  sw $t0, ($sp)
96#  lw $t2, ($sp)
100#  addu $sp, $sp, 4
104#  lw $t1, ($sp)
108#  addu $sp, $sp, 4
112#  seq $t0, $t1, $t2
113##...subu $at,$t1,$t2
117##...sltiu $t0,$at,1
120#  subu $sp, $sp, 4
124#  sw $t0, ($sp)
128#  lw $t0, ($sp)
132#  xor $t0, $t0, 1
136#  sw $t0, ($sp)
140#  lw $t0, 4($sp)
144#  sw $s2, 4($sp)
148#  move $s2, $t0
152#  beq $s2, $zero, nullPtrException
156#  lw $t0, -12($s2)
160#  lw $t0, 28($t0)
164#  jalr $t0
168#  addu $sp, $sp, 4
172#  lw $s2, ($sp)
176#  addu $sp, $sp, 4
180#  subu $sp, $sp, 4
184#  sw $t0, ($sp)
188#  lw $t0, ($sp)
192#  addu $sp, $sp, 4
196#  lw $ra, ($sp)
200#  addu $sp, $sp, 4
204#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
208:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
208#    lw $t0,-8($s2) # get word with hash bits from this
212#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
216#    addu $t0,$t1

220#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
224:mth_equals_Object:
224#    move $t0,$s2 # first pointer (this)
228#    lw $t1,($sp) # second pointer
232#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
233##...subu $at,$t0,$t1
237##...sltiu $t0,$at,1

240#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
244:mth_hashCode_String:
244#    move $t0,$s2 # put this pointer in t0

248#    li $t2,0 # current hash value
252#    lw $t1,-4($t0) # negative of number of characters in string
256#    beq $t1,$zero,done_hashCode_String # if zero length => done
260#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
264#    and $t0,0xfffffffc # adjust for padding
265##...lui $at,65535
269##...ori $at,$at,0xfffc
273##...and $t0,$t0,$at
276#    subu $t1,$t0,$t1 # address at end of string +12
280:loop_hashCode_String:    
280#    sll $t4,$t2,5 # multiply by 32
284#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
288#    lbu $t3,-12($t0) # current character
292#    addu $t2,$t3 # add in current character
296#    addu $t0,1 # go to next character's address
300#    bne $t0,$t1,loop_hashCode_String # loop back if more
304:done_hashCode_String:    

    # push int on stack
304#    move $t0, $t2 # put $t2 in return slot
308#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
312:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
312#    subu $sp,4
316#    sw $ra,($sp)
    
    # get hash value
320#    lw $t2,-12($s2) # vtable address
324#    lw $t0,($t2) # method address
328#    jalr $t0
332#    subu $sp,8 # push result (int in $t0) on stack
336#    sw $s5,4($sp)
340#    sw $t0,($sp)

    # compute print-size of hash value
344#    li $t0, 1
348#    lw $t1, ($sp)
352#    srl $t1,$t1,4
356#    beq $t1,$zero,toString_skip_loop
360:toString_loop_top:
360#    addu $t0,1
364#    srl $t1,$t1,4
368#    bne $t1,$zero,toString_loop_top
372:toString_skip_loop:
372#    subu $sp,8
376#    sw $s5,4($sp)
380#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
384#    lw $t2,-12($s2) # vtable address
388#    move $t0,$t2
392:toString_checkTagBit:
392#    subu $t0,1
396#    lb $t1,($t0)
400#    and $t1,0x80
401##...lui $at,0
405##...ori $at,$at,0x80
409##...and $t1,$t1,$at
412#    beq $t1,$zero,toString_checkTagBit
416#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
420#    subu $sp,8
424#    sw $t1,($sp)
428#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
432#    move $s7,$zero # -4 offset header word
436#    lw $t4,8($sp) # bytes for hash value
440#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
444#    addu $t4,8
448#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
452#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

456#    li $t0,CLASS_String # store string vtable address into ...
460#    sw $t0,-12($s7) # ... -12 offset in object header
464#    lw $t2,-12($s2) # our object's vtable address
468#    lw $t5,4($sp) # number characters in prefix
472#    lw $t1,12($sp) # number of characters in hashcode
476#    addu $t3,$t1,$t5 # number of characters in string ...
480#    addu $t3,1 # ... including the '@' character
484#    subu $t0,$zero,$t3 # store negated length into ...
488#    sw $t0,-4($s7) # ... -4 offset header word
492#    lw $t0,-8($s7) # get -8 offset header-word
496#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
497##...slt $at,$t0,$zero
501##...beq $at,$zero,520
504#    and $t0,0xff # mask off object's hash value ...
505##...lui $at,0
509##...ori $at,$at,0xff
513##...and $t0,$t0,$at
516#    sw $t0,-8($s7) # ... and store back into -8 header-word
520:skipMask_toString:
520#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
524#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
528#    subu $t4,$t2,$t5
532:toString_copyLoop:    
532#    lb $t3,($t4)
536#    and $t3,0x7f # mask off sign bit (in case first word)
537##...lui $at,0
541##...ori $at,$at,0x7f
545##...and $t3,$t3,$at
548#    sb $t3,-8($t0)
552#    addu $t4,1
556#    addu $t0,1
560#    blt $t4,$t2,toString_copyLoop
561##...slt $at,$t4,$t2
565##...bne $at,$zero,532

    # copy the '@' sign
568#    li $t3,'@'
572#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
576#    addu $t2,$t0,$t1 # address of last byte in string to store +8
580#    lw $t3,20($sp) # hash code to print
584:toString_hashCopyLoop:
584#    and $t4,$t3,0xf
588#    srl $t3,4
592#    lb $t4,hexChars($t4)
596#    sb $t4,-8($t2)
600#    subu $t2,$t2,1
604#    bgt $t2,$t0,toString_hashCopyLoop
605##...slt $at,$t0,$t2
609##...bne $at,$zero,584

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
612#    addu $sp,28
616#    lw $ra,($sp)
620#    addu $sp,4
624#    move $t0, $s7 # return string we created
628#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
632:mth_equals_String:
    
    # quick test for obvious false
632#    lw $t0,($sp) # second parameter
636#    beq $t0,$zero,goEsFalse # go return false if null
640#    lw $t1,-12($t0) # vtable pointer
644#    la $t2,CLASS_String # string vtable pointer
648#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
652#    subu $sp,12
656#    sw $ra,8($sp)
660#    sw $s2,4($sp)
664#    sw $t0,($sp)
668#    jal mth_compareTo_String
672#    lw $s2, 4($sp) #restore this pointer and pop parameters
676#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
680#    seq $t0,$t0,$zero
681##...subu $at,$t0,$zero
685##...sltiu $t0,$at,1

    # pop return address and return $t0
688#    lw $ra,($sp)
692#    addu $sp,4

696#    jr $ra # return
    
700:goEsFalse:
    # put 0 in $t0 and return
700#    li $t0, 0
704#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
708:mth_toString_String:
    #we are a string, so return this.
708#    move $t0, $s2
712#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
716:mth_readLine_Lib:
    
716#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
720#    sw $ra, 4($sp) # save $ra
724#    move $t1,$sp # save "original" sp

728:  doRead:

    # read the character
728#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
732#    subu $t2,$v0,13
736#    bne $t2,$zero,notReturnRL
740#    jal readLogicalChar
744#    subu $t2,$v0,10 # check for newline
748#    beq $t2,$zero,foundNewLine
752#    sw $v0,lastCharRead # push back character into queue
756#    j foundNewLine # go process the line

760:  notReturnRL:

    # at this point, $v0 has our character

760#    subu $t0,$v0,10
764#    beq $t0,$zero,foundNewLine
768#    blt $v0,$zero,foundEof
769##...slt $at,$v0,$zero
773##...bne $at,$zero,788

    # we have a character, so push it onto stack
776#    subu $sp,$sp,4
780#    sw $v0,($sp)

    # loop back up to get next character
784#    j doRead

788:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
788#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
792#    li $s7, 0 # return-value: null
796#    j rlReturn # return

800:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
800#    subu $t0,$t1,$sp # number of chars we read (times 4)
804#    srl $s6,$t0,2 # number of words on stack with chars
808#    subu $sp,$sp,4 # push char-count ...
812#    sw $s6,($sp) # ... onto stack
816#    addu $t2,$t0,5 # GC tag, incl. for count-word
820#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
824#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
828#    srl $s6,$s6,2  # data words in object
832#    move $s7,$zero # object words in object
836#    jal newObject  # allocate space
    
    # store header words
840#    la $t0,CLASS_String
844#    sw $t0,-12($s7)# store class tag (String) into object
848#    lw $t2,4($sp) # char-count
852#    sll $t1,$t2,2 # 4 times number of chars
856#    subu $t2,$zero,$t2 # negative of char-count
860#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
864#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
868#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
869##...slt $at,$t0,$zero
873##...beq $at,$zero,892
876#    and $t0,0xff # mask off object's hash value ...
877##...lui $at,0
881##...ori $at,$at,0xff
885##...and $t0,$t0,$at
888#    sw $t0,-8($s7) # ... and store back into -8 header-word
892:  skipMask_readLine:

892#    sll $t0,$t0,2 # data bytes in object
896#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
900#    subu $t0,$t0,$t2 # place to store last character (plus 9)
904#    addu $sp,$sp,8 # pop redundant object-pointer and count
908#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

912#    beq $sp,$t1,rlReturn
916:  charCopyLoop:
916#    lw $t2,($sp)
920#    sb $t2,-9($t0)
924#    addu $sp,$sp,4
928#    subu $t0,$t0,1
932#    bne $sp,$t1,charCopyLoop
936:  rlReturn:

936#    lw $ra, 4($sp)
940#    addu $sp, 8   # pop $ra and pointless word off stack
944#    move $t0, $s7 # return string we allocated in $s7
948#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
952:mth_readInt_Lib:

    # save $ra by pushing onto stack
952#    subu $sp,$sp,4
956#    sw $ra,($sp)

960:  riSkipWhiteLoop:
    # read a character
960#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
964#    subu $t0,$v0,32
968#    bgt $t0,$zero,nonWhite
969##...slt $at,$zero,$t0
973##...bne $at,$zero,1016
976#    beq $t0,$zero,riSkipWhiteLoop # start over if space
980#    subu $t0,$v0,10
984#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
988#    subu $t0,$v0,9
992#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
996#    subu $t0,$v0,13
1000#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1004#    subu $t0,$v0,12
1008#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1012#    j badIntegerFormat # illegal integer char: abort program

1016:  nonWhite:
1016#    subu $t0,$v0,'-'
1020#    li $t4,1 # final multiplier
1024#    bne $t0,$zero,helpReadInt # go read
1028#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1032#    jal readLogicalChar

1036:  helpReadInt:
1036#    li $t2,10
1040#    subu $t1,$v0,'0' # convert digit to 0-9 value
1044#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1045##...sltu $at,$t1,$t2
1049##...beq $at,$zero,3332
1052#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1056:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1056#    jal readLogicalChar # read next character
1060#    subu $t1,$v0,'0' # convert digit to 0-9 value
1064#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1065##...sltu $at,$t1,$t2
1069##...beq $at,$zero,1088

1072#    mul $t3,$t3,$t2 # multiply old value by 10
1073##...mult $t3,$t2
1077##...mflo $t3
1080#    addu $t3,$t3,$t1 # add in value of new digit
1084#    j digitLoop

1088:  doneDigitLoop:

    # "push back" unused character into queue
1088#    sw $v0,lastCharRead

    # restore return address
1092#    lw $ra,($sp)
1096#    addu $sp, 4

    # multiply by sign and put return value in $t0
1100#    mult $t3,$t4 # multiply to account for poss. minus sign
1104#    mflo $t3
1108#    move $t0, $t3
    
    # return
1112#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1116:mth_readChar_Lib:

    # save $ra by pushing onto stack
1116#    subu $sp,$sp,4
1120#    sw $ra,($sp)

      # read the character
1124#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1128#    lw $ra,($sp)
1132#    addu $sp, 4
1136#    move $t0, $v0

    # return
1140#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1144:mth_printStr_Lib:
    # check for null, printing "(null)", if so
1144#    lw $t0,($sp)
1148#    bne $t0,$zero,psNotNull

    # print "(null)"
1152#    la $a0,nullStr
1156#    li $v0,4
1160#    syscall
1164#    j donePrintStr
    
1168:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1168#    subu $t0,$t0,8
1172#    lw $t1,($t0) # number of data words, but needs modification
1176#    blt $t1,$zero,skipPrintStr1
1177##...slt $at,$t1,$zero
1181##...bne $at,$zero,1200
1184#    and $t1,0xff # mask out all but low byte
1185##...lui $at,0
1189##...ori $at,$at,0xff
1193##...and $t1,$t1,$at
1196#    b skipPrintStr2
1200:skipPrintStr1:
1200#    and $t1,0x7ffffff
1201##...lui $at,2047
1205##...ori $at,$at,0xffff
1209##...and $t1,$t1,$at
1212:skipPrintStr2:    
1212#    sll $t1,$t1,2
1216#    subu $t1,$t0,$t1 # first word in string
1220#    lw $t0,4($t0) # negative of string-length
1224#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1228#    beq $t0,$t1,donePrintStr
1232:  psLoop:
1232#    lb $a0,($t1) # next byte
1236#    li $v0,11 # code for printing char
1240#        syscall # print the char
1244#    addu $t1,$t1,1 # go to next char
1248#    blt $t1,$t0,psLoop
1249##...slt $at,$t1,$t0
1253##...bne $at,$zero,1232

1256:  donePrintStr:
1256#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1260:mth_printInt_Lib:
1260#    lw $a0,($sp)

    # print it
1264#    li $v0,1 # code for print-int
1268#    syscall

    # return
1272#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1276:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1276#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1280#    la $a0,falseString
1284#    beq $t0,$zero,skipPB
1288#    la $a0,trueString
1292:  skipPB:
1292#    li $v0,4 # code for print-string
1296#    syscall

    # return
1300#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1304:mth_substring_String:

    # save $ra by pushing onto stack
1304#    subu $sp,$sp,4
1308#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1312#    move $t0,$s2
1316#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1320#    lw $t0,-4($t0) # negative of string length
1324#    subu $t0,$zero,$t0 # string length
1328#    lw $t1,4($sp) # k
1332#    lw $t2,12($sp) # n
1336#    bgt $zero,$t2,strIndexOutOfBounds
1337##...slt $at,$t2,$zero
1341##...bne $at,$zero,3300
1344#    bgt $t2,$t1,strIndexOutOfBounds
1345##...slt $at,$t1,$t2
1349##...bne $at,$zero,3300
1352#    bgt $t1,$t0,strIndexOutOfBounds
1353##...slt $at,$t0,$t1
1357##...bne $at,$zero,3300

    # allocate memory
1360#    subu $s6,$t1,$t2 # # chars in target-string
1364#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1368#    srl $s6,$s6,2 # convert bytes-count to word-count
1372#    move $s7,$zero # (no object-bytes in string)
1376#    jal newObject

    # store "String" tag in object-type field
1380#    la $t0,CLASS_String
1384#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1388#    lw $t4,-8($s7) # data words in source string
1392#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1393##...slt $at,$t4,$zero
1397##...beq $at,$zero,1416
1400#    and $t4,0xff # mask off object's hash value ...
1401##...lui $at,0
1405##...ori $at,$at,0xff
1409##...and $t4,$t4,$at
1412#    sw $t4,-8($s7) # ... and store back into -8 header-word
1416:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1416#    lw $t1,8($sp) # k
1420#    lw $t2,16($sp) # n
1424#    subu $t0,$t2,$t1 # value to store
1428#    sw $t0,-4($s7) # store value
    
1432#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1436#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1440#    lw $t4,-8($t3) # data words in source string
1444#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1448#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1452#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1456#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1460#    lw $t2,-8($s7)   # data words in target string
1464#    sll $t2,$t2,2    # data bytes in target string
1468#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1472:  subCopyLoopzz:
1472#    lb $t4,-8($t3)
1476#    sb $t4,-8($t2)
1480#    addu $t2,$t2,1
1484#    addu $t3,$t3,1
1488#    blt $t3,$t1,subCopyLoopzz
1489##...slt $at,$t3,$t1
1493##...bne $at,$zero,1472
    
1496:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1496#    lw $ra,4($sp) # restore $ra
1500#    addu $sp,$sp,8 # pop ra, and allocated newObject
1504#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1508#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1512:mth_length_String:

    # the length is the negative of 4($s2)
1512#    lw $t0,-4($s2) # -length
1516#    subu $t0,$zero,$t0

    # return
1520#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1524:mth_concat_String:
    # save $ra by pushing onto stack
1524#    subu $sp,$sp,4
1528#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1532#    lw $t0,4($sp)
1536#    beq $t0,$zero,nullPtrException
1540#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1544#    lw $t0,-4($t0) # negative size of second object
1548#    lw $t1,-4($t1) # negative size of first object
1552#    addu $s6,$t0,$t1 # sum of negative sizes
1556#    sra $s6,$s6,2 # negative word-size of char part
1560#    subu $s6,$zero,$s6 # word size of char part
1564#    addu $s6,$s6,1 # data word size, including v-table word
1568#    move $s7,$zero
1572#    jal newObject

    # store String vtable address in vtable address field
1576#    la $t0,CLASS_String
1580#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1584#    lw $t4,-8($s7) # # data words in source string
1588#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1589##...slt $at,$t4,$zero
1593##...beq $at,$zero,1612
1596#    and $t4,0xff # mask off object's hash value ...
1597##...lui $at,0
1601##...ori $at,$at,0xff
1605##...and $t4,$t4,$at
1608#    sw $t4,-8($s7) # ... and store back into -8 header-word
1612:skipMask_concat:

1612#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1616#    move $t1,$s2   # first object
1620#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1624#    lw $t2,-4($t0) # negative length of second object
1628#    lw $t3,-4($t1) # negative length of first object
1632#    addu $t4,$t2,$t3 # sum of negative lengths
1636#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1640#    lw $t4,-8($t1) # # data words in first string
1644#    sll $t4,$t4,2 # # data bytes in first string
1648#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1652#    lw $t4,-8($s7) # # data words in new string
1656#    sll $t4,$t4,2 # # data bytes in new string
1660#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1664#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1668#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1672:  concatLoop1zz:
1672#    lb $t4,-8($t1)
1676#    sb $t4,-8($s7)
1680#    addu $s7,$s7,1
1684#    addu $t1,$t1,1
1688#    blt $t1,$t3,concatLoop1zz
1689##...slt $at,$t1,$t3
1693##...bne $at,$zero,1672
1696:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1696#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1700#    lw $t4,-8($t0) # # data words in second string
1704#    sll $t4,$t4,2 # # data bytes in second string
1708#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1712#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1716:  concatLoop2zz:
1716#    lb $t4,-8($t1)
1720#    sb $t4,-8($s7)
1724#    addu $s7,$s7,1
1728#    addu $t1,$t1,1
1732#    blt $t1,$t3,concatLoop2zz
1733##...slt $at,$t1,$t3
1737##...bne $at,$zero,1716
1740:  doneConcatLoop2zz:
 
1740:  concatRtnzz:
    # return
1740#    move $t0, $t9 # put saved return string in $t0
1744#    lw $ra, 4($sp)
1748#    addu $sp, 8 # pop ra and allocated newObject
1752#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1756:mth_charAt_String:
    # get string
1756#    move $t0,$s2

    # check that index is in bounds
1760#    lw $t1,-4($t0) # negative of # data words in string
1764#    subu $t3,$zero,$t1 # # chars in string
1768#    lw $t2,($sp) # index
1772#    bgeu $t2,$t3,strIndexOutOfBounds
1773##...sltu $at,$t2,$t3
1777##...beq $at,$zero,3300

    # access element
1780#    lw $t1,-8($t0) # # data words in object
1784#    sll $t1,$t1,2 # - byte-offset from end of chars
1788#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1792#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1796#    lb $t0,-8($t0) # our char

    # return
1800#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1804:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1804#    subu $sp,$sp,8
1808#    sw $ra,4($sp)

    # save current sp
1812#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1816#    li $t3,10

    # get argument, negate if negative
1820#    lw $t1,8($sp)
1824#    bge $t1,$zero,itsNonNegLoop
1825##...slt $at,$t1,$zero
1829##...beq $at,$zero,1836
1832#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1836:  itsNonNegLoop:
1836#    divu $t1,$t3
1840#    mflo $t1 # quotient
1844#    mfhi $t4 # remainder
1848#    addu $t4,$t4,'0' # turn remainder into digit
1852#    subu $sp,$sp,4
1856#    sw $t4,($sp) # push digit onto stack
1860#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1864#    lw $t4,8($t0)
1868#    bge $t4,$zero,itsSkipNeg
1869##...slt $at,$t4,$zero
1873##...beq $at,$zero,1888
1876#    li $t4,'-'
1880#    subu $sp,$sp,4
1884#    sw $t4,($sp)
1888:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
1888#    subu $s6,$t0,$sp
1892#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
1896#    sw $t3,($t0)
1900#    srl $s6,$s6,2
1904#    subu $sp,$sp,4
1908#    sw $s6,($sp)
    
    # allocate memory
1912#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1916#    srl $s6,$s6,2
1920#    move $s7,$zero # no "object" words in object
1924#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
1928#    lw $t0,4($sp)
1932#    addu $sp,$sp,8

    # store "String" tag into class field
1936#    subu $s7,$s7,8 # address of header-1 word
1940#    la $t1,CLASS_String
1944#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
1948#    lw $t4,($s7) # # data words in source string
1952#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
1953##...slt $at,$t4,$zero
1957##...beq $at,$zero,1976
1960#    and $t4,0xff # mask off object's hash value ...
1961##...lui $at,0
1965##...ori $at,$at,0xff
1969##...and $t4,$t4,$at
1972#    sw $t4,($s7) # ... and store back into -8 header-word
1976:skipMask_intToString:
    
    # store negative of char-count into header-2 word
1976#    subu $t0,$zero,$t0
1980#    sw $t0,4($s7)

1984#    lw $t1,($s7) # number of data words in string
1988#    sll $t1,$t1,2 # number data bytes in string
1992#    subu $t1,$s7,$t1 # first location to store chars in string
1996#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2000:  itsLoop:
2000#    lw $t2,($sp)
2004#    addu $sp,$sp,4
2008#    sb $t2,($t1)
2012#    addu $t1,$t1,1
2016#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2020#    lw $ra,4($sp)
2024#    addu $sp,$sp,8
2028#    addu $t0,$t0,15
2032#    and $t0,$t0,0xfffffffc
2033##...lui $at,65535
2037##...ori $at,$at,0xfffc
2041##...and $t0,$t0,$at
2044#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2048:mth_intToChar_Lib:
    # save return address
2048#    subu $sp,$sp,4
2052#    sw $ra,($sp)

    # allocate object
2056#    li $s6,2
2060#    move $s7,$zero
2064#    jal newObject

    # get n from string.
2068#    lw $t1,8($sp)

    # store "String" tag into class field
2072#    la $t0,CLASS_String
2076#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2080#    lw $t4,-8($s7) # # data words in source string
2084#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2085##...slt $at,$t4,$zero
2089##...beq $at,$zero,2108
2092#    and $t4,0xff # mask off object's hash value ...
2093##...lui $at,0
2097##...ori $at,$at,0xff
2101##...and $t4,$t4,$at
2104#    sw $t4,-8($s7) # ... and store back into -8 header-word
2108:  skipMask_intToChar:

    # store data in string
2108#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2112#    li $t0,-1
2116#    sw $t0,-4($s7)

    # we're returning $s7
2120#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2124#    lw $ra,4($sp)
2128#    addu $sp,$sp,8

    # return
2132#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2136:mth_compareTo_String:
    # get string pointers and check parameter for null
2136#    lw $t0,($sp) # second string
2140#    beq $t0,$zero,nullPtrException
2144#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2148#    lw $t2,-4($t0) # negative length of second string
2152#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2156#    slt $t4,$t3,$t2
2160#    slt $t5,$t2,$t3
2164#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2168#    lw $t2,-8($t0) # # data words in second string
2172#    lw $t3,-8($t1) # # data words in first string
2176#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2180#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2184#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2188#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2192#    beq $zero,$t5,skipStrCmpLenzz
2196#    move $t2,$t3
2200:  skipStrCmpLenzz:
2200#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2204:  cmpLoopzz:
2204#    lw $t3,-8($t1) # word from str2
2208#    lw $t4,-8($t0) # word from str1
2212#    bne $t3,$t4,cmpNotEqualzz
2216#    addu $t1,$t1,4
2220#    addu $t0,$t0,4
2224#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2228#    j cmpHitLimitzz

2232:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2232#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2236#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2240#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2244:  cmpHitLimitzz:
2244#    move $t0, $t5 # store return value
2248#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2252:readLogicalChar:

    # check if we already have a character
2252#    lw $v0,lastCharRead
2256#    li $t0,-2
2260#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2264#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2268#    jr $ra # return
    
2272:  doReadCh:
    # we need to actually read a character: read and return
2272#    li $v0,12 # use system call to read a character
2276#    syscall
2280#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2284:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2284#    blt $s7,$zero arraySizeOutOfBounds
2285##...slt $at,$s7,$zero
2289##...bne $at,$zero,3316

    # mark the fact that we have not yet GC'd for this allocation
2292#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2296:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2296#    addu $t0,$s6,$s7
2300#    sll $t0,$t0,2
2304#    addu $t0,$t0,8 # amount of memory needed
2308#    addu $t1,$t0,$s3
2312#    bgtu $t1,$s4,doGC
2313##...sltu $at,$s4,$t1
2317##...bne $at,$zero,2440

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2320#    move $t2,$s3
2324:  zeroObjLoop:
2324#    sw $zero,($s3)
2328#    lw $zero,($s3)

2332#    addu $s3,4
2336#    bltu $s3,$t1,zeroObjLoop
2337##...sltu $at,$s3,$t1
2341##...bne $at,$zero,2324

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2344#    sll $t0,$s7,2    # number of post-header bytes
2348#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2352#    li $t2,255
2356#    bgeu $s6,$t2 firstHeaderLarge
2357##...sltu $at,$s6,$t2
2361##...beq $at,$zero,2408
2364#    lw $t1,prevHashConstant
2368#    addu $t1,0x12b68fd2
2369##...lui $at,4791
2373##...addiu $at,$at,-28718
2377##...addu $t1,$at,$t1
2380#    sw $t1,prevHashConstant # update hash constant
2384#    or $t1,0x80000000 # set top bit
2385##...lui $at,32768
2389##...or $t1,$at,$t1
2392#    and $t1,0xffffff00 # clear low bit
2393##...lui $at,65535
2397##...ori $at,$at,0xff00
2401##...and $t1,$t1,$at
2404#    or $s6,$t1
2408:firstHeaderLarge:
2408#    sw $s6,-8($t0) # store header-1 value
2412#    lw $zero,-8($t0)

    # store second header-value
2416#    sw $s7,-4($t0)   # second header-word
2420#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2424#    move $s7,$t0
2428#    subu $sp,$sp,4
2432#    sw $t0,($sp)

2436#    jr $ra
    
2440:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2440#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2444#    lw $t5,gcCount
2448#    addu $t5,1
2452#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2456#    subu $sp,$sp,4
2460#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2464#    lw $t0,cur_seg
2468#    move $t7,$s4
2472#    la $t6,seg1_start
2473##...lui $t6,16
2477##...addiu $t6,$t6,3900
2480#    la $s3,seg0_start
2484#    la $s4,seg0_end
2485##...lui $s4,16
2489##...addiu $s4,$s4,3900
2492#    sw $zero,cur_seg
2496#    bne $t0,$zero,skipGc1
2500#    la $t6,seg0_start
2504#    la $s3,seg1_start
2505##...lui $s3,16
2509##...addiu $s3,$s3,3900
2512#    la $s4,seg1_end
2513##...lui $s4,32
2517##...addiu $s4,$s4,3900
2520#    sw $s5,cur_seg
2524:  skipGc1:
2524#    li $t5,0x80000000
2528#    lw $t0,stack_bottom
2532#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2536:  gcStackCopyLoop:
2536#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2540#    sll $t2,$t1,31 # zero iff low bit was zero
2544#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2548#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2552#    bleu $t1,$t6,gcTestIterDone1
2553##...sltu $at,$t6,$t1
2557##...beq $at,$zero,2720
2560#    bgtu $t1,$t7,gcTestIterDone1
2561##...sltu $at,$t7,$t1
2565##...bne $at,$zero,2720

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2568#    lw $t2,-8($t1) # possible forwarding tag
2572#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2576#    lw $t2,-4($t1) # forwarding pointer: object's new address
2580#    sw $t2,4($t0) # update stack value
2584#    j gcTestIterDone1 # do next iteration

2588:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2588#    bge $t2,$zero,gcSkip2
2589##...slt $at,$t2,$zero
2593##...beq $at,$zero,2608
2596#    and $t2,0xff #### mask off hash value
2597##...lui $at,0
2601##...ori $at,$at,0xff
2605##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2608:  gcSkip2:
2608#    sll $t2,$t2,2
2612#    addu $t2,$t2,8
2616#    subu $t4,$t1,$t2 # address of first word of source
2620#    lw $t3,-4($t1) # number of object words (negative means zero)
2624#    bge $t3,$zero,gcH2Neg1
2625##...slt $at,$t3,$zero
2629##...beq $at,$zero,2636
2632#    move $t3,$zero
2636:  gcH2Neg1:
2636#    sll $t3,$t3,2
2640#    addu $t3,$t3,$t1 # address one word past last word of source

2644#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2648#    lw $s0,($t4)
2652#    lw $s1,-8($t1)
2656#    sw $s1,($t4)
2660#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2664:  gCinnerCopy1:
2664#    lw $s0,($t4)
2668#    sw $s0,($s3)
2672#    addu $t4,$t4,4
2676#    addu $s3,$s3,4
2680#    bltu $t4,$t3,gCinnerCopy1
2681##...sltu $at,$t4,$t3
2685##...bne $at,$zero,2664

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2688#    sw $zero,-8($t1) # 0 in first header-slot
2692#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2696#    sw $t2,4($t0)
2700#    lw $zero,-8($t2)
2704#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2708#    j gcTestIterDone1

2712:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2712#    subu $t0,$t0,$t1
2716#    subu $t0,$t0,3

2720:  gcTestIterDone1:
2720#    bgeu $t0,$sp,gcStackCopyLoop
2721##...sltu $at,$t0,$sp
2725##...beq $at,$zero,2536

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2728#    lw $t1,cur_seg
2732#    la $t0,seg0_start
2736#    beq $t1,$zero,gcSkip4
2740#    la $t0,seg1_start
2741##...lui $t0,16
2745##...addiu $t0,$t0,3900


2748:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2748#    bgeu $t0,$s3,gcFinishUp
2749##...sltu $at,$t0,$s3
2753##...beq $at,$zero,3044

    ###### begin outer loop to copy all non-stack-direct objects ######

2756:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2756#    lw $t1,($t0) # (what will be) first header word for current object
2760#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2764#    bge $t2,$zero,gcSkip4a # are there hash bits?
2765##...slt $at,$t2,$zero
2769##...beq $at,$zero,2784
2772#    and $t2,0xff # mask off hash bits
2773##...lui $at,0
2777##...ori $at,$at,0xff
2781##...and $t2,$t2,$at
2784:gcSkip4a:    
2784#    sll $t2,$t2,2 # # data bytes
2788#    addu $t2,$t2,8 # to skip header words
2792#    addu $t2,$t0,$t2 # pointer to new object
2796#    lw $t3,-8($t2) # word to swap
2800#    sw $t1,-8($t2) # store header word
2804#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2808#    lw $t1,-12($t2) # vtable pointer
2812#    li $t0,dataArrayVTableStart # address of vtable for first data array
2816#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2817##...slt $at,$t1,$t0
2821##...bne $at,$zero,2840

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2824:  skipToNextObj:
2824#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2828#    sll $t1,$t1,2 # convert to byte-count
2832#    addu $t0,$t2,$t1 # skip over object
2836#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2840:  gcNotDataArray:
2840#    lw $t3,-4($t2) # # object words (negative treated as zero)
2844#    bge $t3,$zero,gcH2Neg2
2845##...slt $at,$t3,$zero
2849##...beq $at,$zero,2856
2852#    move $t3,$zero
2856:  gcH2Neg2:
2856#    sll $t3,$t3,2 # # object bytes
2860#    move $t0,$t2 # address of first pointer in object
2864#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2868#    beq $t0,$s1,gcTestIterDone2
    
2872:  gcNextPointerInObject:
    # get pointer from object
2872#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2876#    bleu $t1,$t6,gcGoNextPointerSlot
2877##...sltu $at,$t6,$t1
2881##...beq $at,$zero,3024
2884#    bgtu $t1,$t7,gcGoNextPointerSlot
2885##...sltu $at,$t7,$t1
2889##...bne $at,$zero,3024

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2892#    lw $t8,-8($t1) # possible forwarding tag
2896#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
2900#    lw $t8,-4($t1) # forwarding pointer: object's new address
2904#    sw $t8,($t0) # update pointer in object
2908#    j gcGoNextPointerSlot # do next iteration

2912:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
2912#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
2913##...slt $at,$t8,$zero
2917##...beq $at,$zero,2932
2920#    and $t8,0xff # mask off hash bits
2921##...lui $at,0
2925##...ori $at,$at,0xff
2929##...and $t8,$t8,$at

2932:  gcSkip5:
2932#    sll $t8,$t8,2 # bytes in negative-offset portion ...
2936#    addu $t8,$t8,8 # ... add in bytes for header-words
2940#    subu $t4,$t1,$t8 # address of first word of source object
2944#    lw $t3,-4($t1) # # words in positive-offset portion
2948#    bge $t3,$zero,gcNoNeg
2949##...slt $at,$t3,$zero
2953##...beq $at,$zero,2960
2956#    move $t3,$zero # negative treated as zero
2960:  gcNoNeg:
2960#    sll $t3,$t3,2 # bytes in negative-offset portion
2964#    addu $t3,$t3,$t1 # address one word past last word of source

2968#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
2972#    lw $s0,($t4)
2976#    lw $t9,-8($t1)
2980#    sw $t9,($t4)
2984#    sw $s0,-8($t1)

    # copy all source bytes to the target
2988:  gCinnerCopy2:
2988#    lw $s0,($t4)
2992#    sw $s0,($s3)
2996#    addu $t4,$t4,4
3000#    addu $s3,$s3,4
3004#    bltu $t4,$t3,gCinnerCopy2
3005##...sltu $at,$t4,$t3
3009##...bne $at,$zero,2988

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3012#    sw $zero,-8($t1) # 0 in first header-slot
3016#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3020#    sw $t8,($t0)

3024:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3024#    addu $t0,$t0,4
3028#    bltu $t0,$s1,gcNextPointerInObject
3029##...sltu $at,$t0,$s1
3033##...bne $at,$zero,2872

3036:  gcTestIterDone2:
3036#    bltu $t0,$s3,gcHeapCopyLoop
3037##...sltu $at,$t0,$s3
3041##...bne $at,$zero,2756

    ###### end outer loop to copy all non-stack-direct objects ######

3044:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3044#    lw $s2,($sp)
3048#    addu $sp,$sp,4

    # mark us as having already GC'd
3052#    move $t5,$s5

    # go try and allocate again
3056#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3060:vm_init:

    # mark bottom of stack
3060#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3064#    move $s2,$zero

    #set up the "five" register
3068#    li $s5,5

3072#    la $s3,seg0_start
3076#    la $s4,seg0_end
3077##...lui $s4,16
3081##...addiu $s4,$s4,3900
    
    # return
3084#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3088:divide:
    # get parameters; abort if divisor zero
3088#    lw $t0,($sp)
3092#    lw $t1,8($sp)
3096#    beq $t0,$zero,divByZeroError
    
    # perform division
3100#    div $t1,$t0
3104#    mflo $t0

    # store result, adjust stack and return
3108#    addu $sp,$sp,8 # adjust stack
3112#    sw $t0,($sp) # store result
3116#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3120:remainder:
    # get parameters; abort if divisor zero
3120#    lw $t0,($sp)
3124#    lw $t1,8($sp)
3128#    beq $t0,$zero,divByZeroError
    
    # perform division
3132#    div $t1,$t0
3136#    mfhi $t0

    # store result, adjust stack and return
3140#    addu $sp,$sp,8 # adjust stack
3144#    sw $t0,($sp) # store result
3148#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3152:checkCast:
    # get object, allow immediately if null
3152#    lw $t2,($sp)
3156#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3160#    lw $t2,-12($t2) # vtable address
3164#    bge $t2,$t1,castException
3165##...slt $at,$t2,$t1
3169##...beq $at,$zero,3340
3172#    blt $t2,$t0,castException
3173##...slt $at,$t2,$t0
3177##...bne $at,$zero,3340
    
    # return, leaving object unchanged on stack
3180:  checkCastReturn:    
3180#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3184:instanceOf:
    # get object, go return false if null
3184#    lw $t2,($sp)
3188#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3192#    lw $t2,-12($t2) # vtable address
3196#    sge $t0,$t2,$t0 # are we >= the lower limit?
3197##...slt $t0,$t2,$t0
3201##...xori $t0,$t0,0x1
3204#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3208#    and $t2,$t0,$t1
3212#    sw $t2,($sp)
3216:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3216#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3220:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3220#    li $v0,4 # syscall-code for print-string
3224#    syscall
3228:exit:
    # print termination message
3228#    li $v0,4 # syscall-code for print-string
3232#    la $a0,exitString
3236#    syscall

    # print number of garbage collections
3240#    li $v0,4 # syscall-code for print-string
3244#    la $a0,gcCountMsg
3248#    syscall # print "Number of ..."
3252#    lw $a0,gcCount # number of GC's
3256#    li $v0,1 # code for print-int
3260#    syscall
3264#    li $v0,4 # syscall-code for print-string
3268#    la $a0,newlineString
3272#    syscall # print newline

    # terminate execution
3276#    li $v0,10 #syscall-code for "exit"
3280#    syscall

3284:outOfHeapMemory:
3284#    la $a0,heapMemoryMsg
3288#    j exitError
3292:divByZeroError:
3292#    la $a0,divByZeroMsg
3296#    j exitError
3300:strIndexOutOfBounds:
3300#    la $a0,strIndexOutOfBoundsMsg
3304#    j exitError
3308:arrayIndexOutOfBounds:
3308#    la $a0,arrayIndexOutOfBoundsMsg
3312#    j exitError
3316:arraySizeOutOfBounds:
3316#    la $a0,arraySizeOutOfBoundsMsg
3320#    j exitError
3324:nullPtrException:
3324#    la $a0,nullPtrExceptionMsg
3328#    j exitError
3332:badIntegerFormat:
3332#    la $a0,badIntegerFormatMsg
3336#    j exitError
3340:castException:
3340#    la $a0,castExceptionMsg
3344#    j exitError
############## data section ################
    .data
3528:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3544:cur_seg:
    .word 0

3548:lastCharRead:
    .word -2 # -2 => no buffered character
3552:prevHashConstant:
    .word 0x2785173c
3556:gcCount:
    .word 0

3560:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3587:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3610:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3645:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3679:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3712:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3743:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3791:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3812:gcMsg: 
      .asciiz "\nGC!\n"
3818:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3850:nullStr:
    .asciiz "null"
3855:trueString:
    .asciiz "true"
3860:falseString:
    .asciiz "false"
3866:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
3894:newlineString:
    .asciiz "\n"
    
    .align 2
3896:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

3900:seg0_start:
    .space 0x100000
1052476:seg0_end:
1052476:seg1_start:
    .space 0x100000
2101052:seg1_end:
