 .data # Program at 1.0
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3184:CLASS_Object: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3204:CLASS_String: # ClassDecl at 0.0
  .word mth_hashCode_String # MethodDeclNonVoid at 0.0
  .word mth_equals_String # MethodDeclNonVoid at 0.0
  .word mth_toString_String # MethodDeclNonVoid at 0.0
  .word mth_concat_String # MethodDeclNonVoid at 0.0
  .word mth_substring_String # MethodDeclNonVoid at 0.0
  .word mth_length_String # MethodDeclNonVoid at 0.0
  .word mth_charAt_String # MethodDeclNonVoid at 0.0
  .word mth_compareTo_String # MethodDeclNonVoid at 0.0
3236:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3240:CLASS_Lib: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_readLine_Lib # MethodDeclNonVoid at 0.0
  .word mth_readInt_Lib # MethodDeclNonVoid at 0.0
  .word mth_readChar_Lib # MethodDeclNonVoid at 0.0
  .word mth_printStr_Lib # MethodDeclVoid at 0.0
  .word mth_printBool_Lib # MethodDeclVoid at 0.0
  .word mth_printInt_Lib # MethodDeclVoid at 0.0
  .word mth_intToString_Lib # MethodDeclNonVoid at 0.0
  .word mth_intToChar_Lib # MethodDeclNonVoid at 0.0
3284:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3292:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
3304:END_CLASS_RunMain: # ClassDecl at 0.0
# ****** class Main ****** # ClassDecl at 1.6
  .byte 205 # 'M' with high bit set # ClassDecl at 1.6
  .byte 97 # 'a' # ClassDecl at 1.6
  .byte 105 # 'i' # ClassDecl at 1.6
  .byte 110 # 'n' # ClassDecl at 1.6
3308:CLASS_Main: # ClassDecl at 1.6
  .word mth_hashCode_Object # MethodDeclNonVoid at 0.0
  .word mth_equals_Object # MethodDeclNonVoid at 0.0
  .word mth_toString_Object # MethodDeclNonVoid at 0.0
  .word mth_main_Main # MethodDeclVoid at 2.15
3324:END_CLASS_Main: # ClassDecl at 1.6
3324:dataArrayVTableStart: # IntType at 0.0
3324:END_CLASS_Object: # ClassDecl at 0.0
.text
.globl main
0:main:
0#  jal vm_init
# begin: Call at 0.0; stackHeight = 0
4#  li $v0, 10
8#  syscall
.globl mth_main_Main
12:mth_main_Main:
12# subu $sp, $sp, 4
16# sw $ra, ($sp)
20# addu $sp, $sp, 0
24# lw $ra, ($sp)
28# addu $sp, $sp, 4
32# jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
36:mth_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
36#    lw $t0,-8($s2) # get word with hash bits from this
40#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
44#    addu $t0,$t1

48#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
52:mth_equals_Object:
52#    move $t0,$s2 # first pointer (this)
56#    lw $t1,($sp) # second pointer
60#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
61##...subu $at,$t0,$t1
65##...sltiu $t0,$at,1

68#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
72:mth_hashCode_String:
72#    move $t0,$s2 # put this pointer in t0

76#    li $t2,0 # current hash value
80#    lw $t1,-4($t0) # negative of number of characters in string
84#    beq $t1,$zero,done_hashCode_String # if zero length => done
88#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
92#    and $t0,0xfffffffc # adjust for padding
93##...lui $at,65535
97##...ori $at,$at,0xfffc
101##...and $t0,$t0,$at
104#    subu $t1,$t0,$t1 # address at end of string +12
108:loop_hashCode_String:    
108#    sll $t4,$t2,5 # multiply by 32
112#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
116#    lbu $t3,-12($t0) # current character
120#    addu $t2,$t3 # add in current character
124#    addu $t0,1 # go to next character's address
128#    bne $t0,$t1,loop_hashCode_String # loop back if more
132:done_hashCode_String:    

    # push int on stack
132#    move $t0, $t2 # put $t2 in return slot
136#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
140:mth_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
140#    subu $sp,4
144#    sw $ra,($sp)
    
    # get hash value
148#    lw $t2,-12($s2) # vtable address
152#    lw $t0,($t2) # method address
156#    jalr $t0
160#    subu $sp,8 # push result (int in $t0) on stack
164#    sw $s5,4($sp)
168#    sw $t0,($sp)

    # compute print-size of hash value
172#    li $t0, 1
176#    lw $t1, ($sp)
180#    srl $t1,$t1,4
184#    beq $t1,$zero,toString_skip_loop
188:toString_loop_top:
188#    addu $t0,1
192#    srl $t1,$t1,4
196#    bne $t1,$zero,toString_loop_top
200:toString_skip_loop:
200#    subu $sp,8
204#    sw $s5,4($sp)
208#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
212#    lw $t2,-12($s2) # vtable address
216#    move $t0,$t2
220:toString_checkTagBit:
220#    subu $t0,1
224#    lb $t1,($t0)
228#    and $t1,0x80
229##...lui $at,0
233##...ori $at,$at,0x80
237##...and $t1,$t1,$at
240#    beq $t1,$zero,toString_checkTagBit
244#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
248#    subu $sp,8
252#    sw $t1,($sp)
256#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
260#    move $s7,$zero # -4 offset header word
264#    lw $t4,8($sp) # bytes for hash value
268#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
272#    addu $t4,8
276#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
280#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

284#    li $t0,CLASS_String # store string vtable address into ...
288#    sw $t0,-12($s7) # ... -12 offset in object header
292#    lw $t2,-12($s2) # our object's vtable address
296#    lw $t5,4($sp) # number characters in prefix
300#    lw $t1,12($sp) # number of characters in hashcode
304#    addu $t3,$t1,$t5 # number of characters in string ...
308#    addu $t3,1 # ... including the '@' character
312#    subu $t0,$zero,$t3 # store negated length into ...
316#    sw $t0,-4($s7) # ... -4 offset header word
320#    lw $t0,-8($s7) # get -8 offset header-word
324#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
325##...slt $at,$t0,$zero
329##...beq $at,$zero,348
332#    and $t0,0xff # mask off object's hash value ...
333##...lui $at,0
337##...ori $at,$at,0xff
341##...and $t0,$t0,$at
344#    sw $t0,-8($s7) # ... and store back into -8 header-word
348:skipMask_toString:
348#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
352#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
356#    subu $t4,$t2,$t5
360:toString_copyLoop:    
360#    lb $t3,($t4)
364#    and $t3,0x7f # mask off sign bit (in case first word)
365##...lui $at,0
369##...ori $at,$at,0x7f
373##...and $t3,$t3,$at
376#    sb $t3,-8($t0)
380#    addu $t4,1
384#    addu $t0,1
388#    blt $t4,$t2,toString_copyLoop
389##...slt $at,$t4,$t2
393##...bne $at,$zero,360

    # copy the '@' sign
396#    li $t3,'@'
400#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
404#    addu $t2,$t0,$t1 # address of last byte in string to store +8
408#    lw $t3,20($sp) # hash code to print
412:toString_hashCopyLoop:
412#    and $t4,$t3,0xf
416#    srl $t3,4
420#    lb $t4,hexChars($t4)
424#    sb $t4,-8($t2)
428#    subu $t2,$t2,1
432#    bgt $t2,$t0,toString_hashCopyLoop
433##...slt $at,$t0,$t2
437##...bne $at,$zero,412

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
440#    addu $sp,28
444#    lw $ra,($sp)
448#    addu $sp,4
452#    move $t0, $s7 # return string we created
456#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
460:mth_equals_String:
    
    # quick test for obvious false
460#    lw $t0,($sp) # second parameter
464#    beq $t0,$zero,goEsFalse # go return false if null
468#    lw $t1,-12($t0) # vtable pointer
472#    la $t2,CLASS_String # string vtable pointer
476#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
480#    subu $sp,12
484#    sw $ra,8($sp)
488#    sw $s2,4($sp)
492#    sw $t0,($sp)
496#    jal mth_compareTo_String
500#    lw $s2, 4($sp) #restore this pointer and pop parameters
504#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
508#    seq $t0,$t0,$zero
509##...subu $at,$t0,$zero
513##...sltiu $t0,$at,1

    # pop return address and return $t0
516#    lw $ra,($sp)
520#    addu $sp,4

524#    jr $ra # return
    
528:goEsFalse:
    # put 0 in $t0 and return
528#    li $t0, 0
532#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
536:mth_toString_String:
    #we are a string, so return this.
536#    move $t0, $s2
540#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
544:mth_readLine_Lib:
    
544#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
548#    sw $ra, 4($sp) # save $ra
552#    move $t1,$sp # save "original" sp

556:  doRead:

    # read the character
556#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
560#    subu $t2,$v0,13
564#    bne $t2,$zero,notReturnRL
568#    jal readLogicalChar
572#    subu $t2,$v0,10 # check for newline
576#    beq $t2,$zero,foundNewLine
580#    sw $v0,lastCharRead # push back character into queue
584#    j foundNewLine # go process the line

588:  notReturnRL:

    # at this point, $v0 has our character

588#    subu $t0,$v0,10
592#    beq $t0,$zero,foundNewLine
596#    blt $v0,$zero,foundEof
597##...slt $at,$v0,$zero
601##...bne $at,$zero,616

    # we have a character, so push it onto stack
604#    subu $sp,$sp,4
608#    sw $v0,($sp)

    # loop back up to get next character
612#    j doRead

616:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
616#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
620#    li $s7, 0 # return-value: null
624#    j rlReturn # return

628:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
628#    subu $t0,$t1,$sp # number of chars we read (times 4)
632#    srl $s6,$t0,2 # number of words on stack with chars
636#    subu $sp,$sp,4 # push char-count ...
640#    sw $s6,($sp) # ... onto stack
644#    addu $t2,$t0,5 # GC tag, incl. for count-word
648#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
652#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
656#    srl $s6,$s6,2  # data words in object
660#    move $s7,$zero # object words in object
664#    jal newObject  # allocate space
    
    # store header words
668#    la $t0,CLASS_String
672#    sw $t0,-12($s7)# store class tag (String) into object
676#    lw $t2,4($sp) # char-count
680#    sll $t1,$t2,2 # 4 times number of chars
684#    subu $t2,$zero,$t2 # negative of char-count
688#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
692#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
696#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
697##...slt $at,$t0,$zero
701##...beq $at,$zero,720
704#    and $t0,0xff # mask off object's hash value ...
705##...lui $at,0
709##...ori $at,$at,0xff
713##...and $t0,$t0,$at
716#    sw $t0,-8($s7) # ... and store back into -8 header-word
720:  skipMask_readLine:

720#    sll $t0,$t0,2 # data bytes in object
724#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
728#    subu $t0,$t0,$t2 # place to store last character (plus 9)
732#    addu $sp,$sp,8 # pop redundant object-pointer and count
736#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

740#    beq $sp,$t1,rlReturn
744:  charCopyLoop:
744#    lw $t2,($sp)
748#    sb $t2,-9($t0)
752#    addu $sp,$sp,4
756#    subu $t0,$t0,1
760#    bne $sp,$t1,charCopyLoop
764:  rlReturn:

764#    lw $ra, 4($sp)
768#    addu $sp, 8   # pop $ra and pointless word off stack
772#    move $t0, $s7 # return string we allocated in $s7
776#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
780:mth_readInt_Lib:

    # save $ra by pushing onto stack
780#    subu $sp,$sp,4
784#    sw $ra,($sp)

788:  riSkipWhiteLoop:
    # read a character
788#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
792#    subu $t0,$v0,32
796#    bgt $t0,$zero,nonWhite
797##...slt $at,$zero,$t0
801##...bne $at,$zero,844
804#    beq $t0,$zero,riSkipWhiteLoop # start over if space
808#    subu $t0,$v0,10
812#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
816#    subu $t0,$v0,9
820#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
824#    subu $t0,$v0,13
828#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
832#    subu $t0,$v0,12
836#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
840#    j badIntegerFormat # illegal integer char: abort program

844:  nonWhite:
844#    subu $t0,$v0,'-'
848#    li $t4,1 # final multiplier
852#    bne $t0,$zero,helpReadInt # go read
856#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
860#    jal readLogicalChar

864:  helpReadInt:
864#    li $t2,10
868#    subu $t1,$v0,'0' # convert digit to 0-9 value
872#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
873##...sltu $at,$t1,$t2
877##...beq $at,$zero,3160
880#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

884:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

884#    jal readLogicalChar # read next character
888#    subu $t1,$v0,'0' # convert digit to 0-9 value
892#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
893##...sltu $at,$t1,$t2
897##...beq $at,$zero,916

900#    mul $t3,$t3,$t2 # multiply old value by 10
901##...mult $t3,$t2
905##...mflo $t3
908#    addu $t3,$t3,$t1 # add in value of new digit
912#    j digitLoop

916:  doneDigitLoop:

    # "push back" unused character into queue
916#    sw $v0,lastCharRead

    # restore return address
920#    lw $ra,($sp)
924#    addu $sp, 4

    # multiply by sign and put return value in $t0
928#    mult $t3,$t4 # multiply to account for poss. minus sign
932#    mflo $t3
936#    move $t0, $t3
    
    # return
940#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
944:mth_readChar_Lib:

    # save $ra by pushing onto stack
944#    subu $sp,$sp,4
948#    sw $ra,($sp)

      # read the character
952#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
956#    lw $ra,($sp)
960#    addu $sp, 4
964#    move $t0, $v0

    # return
968#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
972:mth_printStr_Lib:
    # check for null, printing "(null)", if so
972#    lw $t0,($sp)
976#    bne $t0,$zero,psNotNull

    # print "(null)"
980#    la $a0,nullStr
984#    li $v0,4
988#    syscall
992#    j donePrintStr
    
996:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
996#    subu $t0,$t0,8
1000#    lw $t1,($t0) # number of data words, but needs modification
1004#    blt $t1,$zero,skipPrintStr1
1005##...slt $at,$t1,$zero
1009##...bne $at,$zero,1028
1012#    and $t1,0xff # mask out all but low byte
1013##...lui $at,0
1017##...ori $at,$at,0xff
1021##...and $t1,$t1,$at
1024#    b skipPrintStr2
1028:skipPrintStr1:
1028#    and $t1,0x7ffffff
1029##...lui $at,2047
1033##...ori $at,$at,0xffff
1037##...and $t1,$t1,$at
1040:skipPrintStr2:    
1040#    sll $t1,$t1,2
1044#    subu $t1,$t0,$t1 # first word in string
1048#    lw $t0,4($t0) # negative of string-length
1052#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1056#    beq $t0,$t1,donePrintStr
1060:  psLoop:
1060#    lb $a0,($t1) # next byte
1064#    li $v0,11 # code for printing char
1068#        syscall # print the char
1072#    addu $t1,$t1,1 # go to next char
1076#    blt $t1,$t0,psLoop
1077##...slt $at,$t1,$t0
1081##...bne $at,$zero,1060

1084:  donePrintStr:
1084#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1088:mth_printInt_Lib:
1088#    lw $a0,($sp)

    # print it
1092#    li $v0,1 # code for print-int
1096#    syscall

    # return
1100#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1104:mth_printBool_Lib:
    # pop value off stack, along with 'this'
1104#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1108#    la $a0,falseString
1112#    beq $t0,$zero,skipPB
1116#    la $a0,trueString
1120:  skipPB:
1120#    li $v0,4 # code for print-string
1124#    syscall

    # return
1128#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1132:mth_substring_String:

    # save $ra by pushing onto stack
1132#    subu $sp,$sp,4
1136#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1140#    move $t0,$s2
1144#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1148#    lw $t0,-4($t0) # negative of string length
1152#    subu $t0,$zero,$t0 # string length
1156#    lw $t1,4($sp) # k
1160#    lw $t2,12($sp) # n
1164#    bgt $zero,$t2,strIndexOutOfBounds
1165##...slt $at,$t2,$zero
1169##...bne $at,$zero,3128
1172#    bgt $t2,$t1,strIndexOutOfBounds
1173##...slt $at,$t1,$t2
1177##...bne $at,$zero,3128
1180#    bgt $t1,$t0,strIndexOutOfBounds
1181##...slt $at,$t0,$t1
1185##...bne $at,$zero,3128

    # allocate memory
1188#    subu $s6,$t1,$t2 # # chars in target-string
1192#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1196#    srl $s6,$s6,2 # convert bytes-count to word-count
1200#    move $s7,$zero # (no object-bytes in string)
1204#    jal newObject

    # store "String" tag in object-type field
1208#    la $t0,CLASS_String
1212#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1216#    lw $t4,-8($s7) # data words in source string
1220#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1221##...slt $at,$t4,$zero
1225##...beq $at,$zero,1244
1228#    and $t4,0xff # mask off object's hash value ...
1229##...lui $at,0
1233##...ori $at,$at,0xff
1237##...and $t4,$t4,$at
1240#    sw $t4,-8($s7) # ... and store back into -8 header-word
1244:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1244#    lw $t1,8($sp) # k
1248#    lw $t2,16($sp) # n
1252#    subu $t0,$t2,$t1 # value to store
1256#    sw $t0,-4($s7) # store value
    
1260#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1264#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1268#    lw $t4,-8($t3) # data words in source string
1272#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1276#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1280#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1284#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1288#    lw $t2,-8($s7)   # data words in target string
1292#    sll $t2,$t2,2    # data bytes in target string
1296#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1300:  subCopyLoopzz:
1300#    lb $t4,-8($t3)
1304#    sb $t4,-8($t2)
1308#    addu $t2,$t2,1
1312#    addu $t3,$t3,1
1316#    blt $t3,$t1,subCopyLoopzz
1317##...slt $at,$t3,$t1
1321##...bne $at,$zero,1300
    
1324:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1324#    lw $ra,4($sp) # restore $ra
1328#    addu $sp,$sp,8 # pop ra, and allocated newObject
1332#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1336#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1340:mth_length_String:

    # the length is the negative of 4($s2)
1340#    lw $t0,-4($s2) # -length
1344#    subu $t0,$zero,$t0

    # return
1348#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1352:mth_concat_String:
    # save $ra by pushing onto stack
1352#    subu $sp,$sp,4
1356#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1360#    lw $t0,4($sp)
1364#    beq $t0,$zero,nullPtrException
1368#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1372#    lw $t0,-4($t0) # negative size of second object
1376#    lw $t1,-4($t1) # negative size of first object
1380#    addu $s6,$t0,$t1 # sum of negative sizes
1384#    sra $s6,$s6,2 # negative word-size of char part
1388#    subu $s6,$zero,$s6 # word size of char part
1392#    addu $s6,$s6,1 # data word size, including v-table word
1396#    move $s7,$zero
1400#    jal newObject

    # store String vtable address in vtable address field
1404#    la $t0,CLASS_String
1408#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1412#    lw $t4,-8($s7) # # data words in source string
1416#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1417##...slt $at,$t4,$zero
1421##...beq $at,$zero,1440
1424#    and $t4,0xff # mask off object's hash value ...
1425##...lui $at,0
1429##...ori $at,$at,0xff
1433##...and $t4,$t4,$at
1436#    sw $t4,-8($s7) # ... and store back into -8 header-word
1440:skipMask_concat:

1440#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1444#    move $t1,$s2   # first object
1448#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1452#    lw $t2,-4($t0) # negative length of second object
1456#    lw $t3,-4($t1) # negative length of first object
1460#    addu $t4,$t2,$t3 # sum of negative lengths
1464#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1468#    lw $t4,-8($t1) # # data words in first string
1472#    sll $t4,$t4,2 # # data bytes in first string
1476#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1480#    lw $t4,-8($s7) # # data words in new string
1484#    sll $t4,$t4,2 # # data bytes in new string
1488#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1492#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1496#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1500:  concatLoop1zz:
1500#    lb $t4,-8($t1)
1504#    sb $t4,-8($s7)
1508#    addu $s7,$s7,1
1512#    addu $t1,$t1,1
1516#    blt $t1,$t3,concatLoop1zz
1517##...slt $at,$t1,$t3
1521##...bne $at,$zero,1500
1524:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1524#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1528#    lw $t4,-8($t0) # # data words in second string
1532#    sll $t4,$t4,2 # # data bytes in second string
1536#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1540#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
1544:  concatLoop2zz:
1544#    lb $t4,-8($t1)
1548#    sb $t4,-8($s7)
1552#    addu $s7,$s7,1
1556#    addu $t1,$t1,1
1560#    blt $t1,$t3,concatLoop2zz
1561##...slt $at,$t1,$t3
1565##...bne $at,$zero,1544
1568:  doneConcatLoop2zz:
 
1568:  concatRtnzz:
    # return
1568#    move $t0, $t9 # put saved return string in $t0
1572#    lw $ra, 4($sp)
1576#    addu $sp, 8 # pop ra and allocated newObject
1580#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
1584:mth_charAt_String:
    # get string
1584#    move $t0,$s2

    # check that index is in bounds
1588#    lw $t1,-4($t0) # negative of # data words in string
1592#    subu $t3,$zero,$t1 # # chars in string
1596#    lw $t2,($sp) # index
1600#    bgeu $t2,$t3,strIndexOutOfBounds
1601##...sltu $at,$t2,$t3
1605##...beq $at,$zero,3128

    # access element
1608#    lw $t1,-8($t0) # # data words in object
1612#    sll $t1,$t1,2 # - byte-offset from end of chars
1616#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1620#    addu $t0,$t0,$t1 # - address of our char, offset by 8
1624#    lb $t0,-8($t0) # our char

    # return
1628#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
1632:mth_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
1632#    subu $sp,$sp,8
1636#    sw $ra,4($sp)

    # save current sp
1640#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
1644#    li $t3,10

    # get argument, negate if negative
1648#    lw $t1,8($sp)
1652#    bge $t1,$zero,itsNonNegLoop
1653##...slt $at,$t1,$zero
1657##...beq $at,$zero,1664
1660#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
1664:  itsNonNegLoop:
1664#    divu $t1,$t3
1668#    mflo $t1 # quotient
1672#    mfhi $t4 # remainder
1676#    addu $t4,$t4,'0' # turn remainder into digit
1680#    subu $sp,$sp,4
1684#    sw $t4,($sp) # push digit onto stack
1688#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
1692#    lw $t4,8($t0)
1696#    bge $t4,$zero,itsSkipNeg
1697##...slt $at,$t4,$zero
1701##...beq $at,$zero,1716
1704#    li $t4,'-'
1708#    subu $sp,$sp,4
1712#    sw $t4,($sp)
1716:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
1716#    subu $s6,$t0,$sp
1720#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
1724#    sw $t3,($t0)
1728#    srl $s6,$s6,2
1732#    subu $sp,$sp,4
1736#    sw $s6,($sp)
    
    # allocate memory
1740#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1744#    srl $s6,$s6,2
1748#    move $s7,$zero # no "object" words in object
1752#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
1756#    lw $t0,4($sp)
1760#    addu $sp,$sp,8

    # store "String" tag into class field
1764#    subu $s7,$s7,8 # address of header-1 word
1768#    la $t1,CLASS_String
1772#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
1776#    lw $t4,($s7) # # data words in source string
1780#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
1781##...slt $at,$t4,$zero
1785##...beq $at,$zero,1804
1788#    and $t4,0xff # mask off object's hash value ...
1789##...lui $at,0
1793##...ori $at,$at,0xff
1797##...and $t4,$t4,$at
1800#    sw $t4,($s7) # ... and store back into -8 header-word
1804:skipMask_intToString:
    
    # store negative of char-count into header-2 word
1804#    subu $t0,$zero,$t0
1808#    sw $t0,4($s7)

1812#    lw $t1,($s7) # number of data words in string
1816#    sll $t1,$t1,2 # number data bytes in string
1820#    subu $t1,$s7,$t1 # first location to store chars in string
1824#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
1828:  itsLoop:
1828#    lw $t2,($sp)
1832#    addu $sp,$sp,4
1836#    sb $t2,($t1)
1840#    addu $t1,$t1,1
1844#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
1848#    lw $ra,4($sp)
1852#    addu $sp,$sp,8
1856#    addu $t0,$t0,15
1860#    and $t0,$t0,0xfffffffc
1861##...lui $at,65535
1865##...ori $at,$at,0xfffc
1869##...and $t0,$t0,$at
1872#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
1876:mth_intToChar_Lib:
    # save return address
1876#    subu $sp,$sp,4
1880#    sw $ra,($sp)

    # allocate object
1884#    li $s6,2
1888#    move $s7,$zero
1892#    jal newObject

    # get n from string.
1896#    lw $t1,8($sp)

    # store "String" tag into class field
1900#    la $t0,CLASS_String
1904#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1908#    lw $t4,-8($s7) # # data words in source string
1912#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
1913##...slt $at,$t4,$zero
1917##...beq $at,$zero,1936
1920#    and $t4,0xff # mask off object's hash value ...
1921##...lui $at,0
1925##...ori $at,$at,0xff
1929##...and $t4,$t4,$at
1932#    sw $t4,-8($s7) # ... and store back into -8 header-word
1936:  skipMask_intToChar:

    # store data in string
1936#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
1940#    li $t0,-1
1944#    sw $t0,-4($s7)

    # we're returning $s7
1948#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
1952#    lw $ra,4($sp)
1956#    addu $sp,$sp,8

    # return
1960#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
1964:mth_compareTo_String:
    # get string pointers and check parameter for null
1964#    lw $t0,($sp) # second string
1968#    beq $t0,$zero,nullPtrException
1972#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
1976#    lw $t2,-4($t0) # negative length of second string
1980#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
1984#    slt $t4,$t3,$t2
1988#    slt $t5,$t2,$t3
1992#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
1996#    lw $t2,-8($t0) # # data words in second string
2000#    lw $t3,-8($t1) # # data words in first string
2004#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2008#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2012#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2016#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2020#    beq $zero,$t5,skipStrCmpLenzz
2024#    move $t2,$t3
2028:  skipStrCmpLenzz:
2028#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2032:  cmpLoopzz:
2032#    lw $t3,-8($t1) # word from str2
2036#    lw $t4,-8($t0) # word from str1
2040#    bne $t3,$t4,cmpNotEqualzz
2044#    addu $t1,$t1,4
2048#    addu $t0,$t0,4
2052#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2056#    j cmpHitLimitzz

2060:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2060#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2064#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2068#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2072:  cmpHitLimitzz:
2072#    move $t0, $t5 # store return value
2076#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2080:readLogicalChar:

    # check if we already have a character
2080#    lw $v0,lastCharRead
2084#    li $t0,-2
2088#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2092#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2096#    jr $ra # return
    
2100:  doReadCh:
    # we need to actually read a character: read and return
2100#    li $v0,12 # use system call to read a character
2104#    syscall
2108#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2112:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2112#    blt $s7,$zero arraySizeOutOfBounds
2113##...slt $at,$s7,$zero
2117##...bne $at,$zero,3144

    # mark the fact that we have not yet GC'd for this allocation
2120#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2124:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2124#    addu $t0,$s6,$s7
2128#    sll $t0,$t0,2
2132#    addu $t0,$t0,8 # amount of memory needed
2136#    addu $t1,$t0,$s3
2140#    bgtu $t1,$s4,doGC
2141##...sltu $at,$s4,$t1
2145##...bne $at,$zero,2268

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2148#    move $t2,$s3
2152:  zeroObjLoop:
2152#    sw $zero,($s3)
2156#    lw $zero,($s3)

2160#    addu $s3,4
2164#    bltu $s3,$t1,zeroObjLoop
2165##...sltu $at,$s3,$t1
2169##...bne $at,$zero,2152

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2172#    sll $t0,$s7,2    # number of post-header bytes
2176#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2180#    li $t2,255
2184#    bgeu $s6,$t2 firstHeaderLarge
2185##...sltu $at,$s6,$t2
2189##...beq $at,$zero,2236
2192#    lw $t1,prevHashConstant
2196#    addu $t1,0x12b68fd2
2197##...lui $at,4791
2201##...addiu $at,$at,-28718
2205##...addu $t1,$at,$t1
2208#    sw $t1,prevHashConstant # update hash constant
2212#    or $t1,0x80000000 # set top bit
2213##...lui $at,32768
2217##...or $t1,$at,$t1
2220#    and $t1,0xffffff00 # clear low bit
2221##...lui $at,65535
2225##...ori $at,$at,0xff00
2229##...and $t1,$t1,$at
2232#    or $s6,$t1
2236:firstHeaderLarge:
2236#    sw $s6,-8($t0) # store header-1 value
2240#    lw $zero,-8($t0)

    # store second header-value
2244#    sw $s7,-4($t0)   # second header-word
2248#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2252#    move $s7,$t0
2256#    subu $sp,$sp,4
2260#    sw $t0,($sp)

2264#    jr $ra
    
2268:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2268#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2272#    lw $t5,gcCount
2276#    addu $t5,1
2280#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2284#    subu $sp,$sp,4
2288#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2292#    lw $t0,cur_seg
2296#    move $t7,$s4
2300#    la $t6,seg1_start
2301##...lui $t6,16
2305##...addiu $t6,$t6,3696
2308#    la $s3,seg0_start
2312#    la $s4,seg0_end
2313##...lui $s4,16
2317##...addiu $s4,$s4,3696
2320#    sw $zero,cur_seg
2324#    bne $t0,$zero,skipGc1
2328#    la $t6,seg0_start
2332#    la $s3,seg1_start
2333##...lui $s3,16
2337##...addiu $s3,$s3,3696
2340#    la $s4,seg1_end
2341##...lui $s4,32
2345##...addiu $s4,$s4,3696
2348#    sw $s5,cur_seg
2352:  skipGc1:
2352#    li $t5,0x80000000
2356#    lw $t0,stack_bottom
2360#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2364:  gcStackCopyLoop:
2364#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2368#    sll $t2,$t1,31 # zero iff low bit was zero
2372#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2376#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2380#    bleu $t1,$t6,gcTestIterDone1
2381##...sltu $at,$t6,$t1
2385##...beq $at,$zero,2548
2388#    bgtu $t1,$t7,gcTestIterDone1
2389##...sltu $at,$t7,$t1
2393##...bne $at,$zero,2548

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2396#    lw $t2,-8($t1) # possible forwarding tag
2400#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2404#    lw $t2,-4($t1) # forwarding pointer: object's new address
2408#    sw $t2,4($t0) # update stack value
2412#    j gcTestIterDone1 # do next iteration

2416:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2416#    bge $t2,$zero,gcSkip2
2417##...slt $at,$t2,$zero
2421##...beq $at,$zero,2436
2424#    and $t2,0xff #### mask off hash value
2425##...lui $at,0
2429##...ori $at,$at,0xff
2433##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2436:  gcSkip2:
2436#    sll $t2,$t2,2
2440#    addu $t2,$t2,8
2444#    subu $t4,$t1,$t2 # address of first word of source
2448#    lw $t3,-4($t1) # number of object words (negative means zero)
2452#    bge $t3,$zero,gcH2Neg1
2453##...slt $at,$t3,$zero
2457##...beq $at,$zero,2464
2460#    move $t3,$zero
2464:  gcH2Neg1:
2464#    sll $t3,$t3,2
2468#    addu $t3,$t3,$t1 # address one word past last word of source

2472#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2476#    lw $s0,($t4)
2480#    lw $s1,-8($t1)
2484#    sw $s1,($t4)
2488#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2492:  gCinnerCopy1:
2492#    lw $s0,($t4)
2496#    sw $s0,($s3)
2500#    addu $t4,$t4,4
2504#    addu $s3,$s3,4
2508#    bltu $t4,$t3,gCinnerCopy1
2509##...sltu $at,$t4,$t3
2513##...bne $at,$zero,2492

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2516#    sw $zero,-8($t1) # 0 in first header-slot
2520#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2524#    sw $t2,4($t0)
2528#    lw $zero,-8($t2)
2532#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
2536#    j gcTestIterDone1

2540:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
2540#    subu $t0,$t0,$t1
2544#    subu $t0,$t0,3

2548:  gcTestIterDone1:
2548#    bgeu $t0,$sp,gcStackCopyLoop
2549##...sltu $at,$t0,$sp
2553##...beq $at,$zero,2364

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
2556#    lw $t1,cur_seg
2560#    la $t0,seg0_start
2564#    beq $t1,$zero,gcSkip4
2568#    la $t0,seg1_start
2569##...lui $t0,16
2573##...addiu $t0,$t0,3696


2576:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
2576#    bgeu $t0,$s3,gcFinishUp
2577##...sltu $at,$t0,$s3
2581##...beq $at,$zero,2872

    ###### begin outer loop to copy all non-stack-direct objects ######

2584:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
2584#    lw $t1,($t0) # (what will be) first header word for current object
2588#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
2592#    bge $t2,$zero,gcSkip4a # are there hash bits?
2593##...slt $at,$t2,$zero
2597##...beq $at,$zero,2612
2600#    and $t2,0xff # mask off hash bits
2601##...lui $at,0
2605##...ori $at,$at,0xff
2609##...and $t2,$t2,$at
2612:gcSkip4a:    
2612#    sll $t2,$t2,2 # # data bytes
2616#    addu $t2,$t2,8 # to skip header words
2620#    addu $t2,$t0,$t2 # pointer to new object
2624#    lw $t3,-8($t2) # word to swap
2628#    sw $t1,-8($t2) # store header word
2632#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
2636#    lw $t1,-12($t2) # vtable pointer
2640#    li $t0,dataArrayVTableStart # address of vtable for first data array
2644#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
2645##...slt $at,$t1,$t0
2649##...bne $at,$zero,2668

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
2652:  skipToNextObj:
2652#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
2656#    sll $t1,$t1,2 # convert to byte-count
2660#    addu $t0,$t2,$t1 # skip over object
2664#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
2668:  gcNotDataArray:
2668#    lw $t3,-4($t2) # # object words (negative treated as zero)
2672#    bge $t3,$zero,gcH2Neg2
2673##...slt $at,$t3,$zero
2677##...beq $at,$zero,2684
2680#    move $t3,$zero
2684:  gcH2Neg2:
2684#    sll $t3,$t3,2 # # object bytes
2688#    move $t0,$t2 # address of first pointer in object
2692#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
2696#    beq $t0,$s1,gcTestIterDone2
    
2700:  gcNextPointerInObject:
    # get pointer from object
2700#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
2704#    bleu $t1,$t6,gcGoNextPointerSlot
2705##...sltu $at,$t6,$t1
2709##...beq $at,$zero,2852
2712#    bgtu $t1,$t7,gcGoNextPointerSlot
2713##...sltu $at,$t7,$t1
2717##...bne $at,$zero,2852

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2720#    lw $t8,-8($t1) # possible forwarding tag
2724#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
2728#    lw $t8,-4($t1) # forwarding pointer: object's new address
2732#    sw $t8,($t0) # update pointer in object
2736#    j gcGoNextPointerSlot # do next iteration

2740:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
2740#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
2741##...slt $at,$t8,$zero
2745##...beq $at,$zero,2760
2748#    and $t8,0xff # mask off hash bits
2749##...lui $at,0
2753##...ori $at,$at,0xff
2757##...and $t8,$t8,$at

2760:  gcSkip5:
2760#    sll $t8,$t8,2 # bytes in negative-offset portion ...
2764#    addu $t8,$t8,8 # ... add in bytes for header-words
2768#    subu $t4,$t1,$t8 # address of first word of source object
2772#    lw $t3,-4($t1) # # words in positive-offset portion
2776#    bge $t3,$zero,gcNoNeg
2777##...slt $at,$t3,$zero
2781##...beq $at,$zero,2788
2784#    move $t3,$zero # negative treated as zero
2788:  gcNoNeg:
2788#    sll $t3,$t3,2 # bytes in negative-offset portion
2792#    addu $t3,$t3,$t1 # address one word past last word of source

2796#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
2800#    lw $s0,($t4)
2804#    lw $t9,-8($t1)
2808#    sw $t9,($t4)
2812#    sw $s0,-8($t1)

    # copy all source bytes to the target
2816:  gCinnerCopy2:
2816#    lw $s0,($t4)
2820#    sw $s0,($s3)
2824#    addu $t4,$t4,4
2828#    addu $s3,$s3,4
2832#    bltu $t4,$t3,gCinnerCopy2
2833##...sltu $at,$t4,$t3
2837##...bne $at,$zero,2816

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2840#    sw $zero,-8($t1) # 0 in first header-slot
2844#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
2848#    sw $t8,($t0)

2852:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
2852#    addu $t0,$t0,4
2856#    bltu $t0,$s1,gcNextPointerInObject
2857##...sltu $at,$t0,$s1
2861##...bne $at,$zero,2700

2864:  gcTestIterDone2:
2864#    bltu $t0,$s3,gcHeapCopyLoop
2865##...sltu $at,$t0,$s3
2869##...bne $at,$zero,2584

    ###### end outer loop to copy all non-stack-direct objects ######

2872:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
2872#    lw $s2,($sp)
2876#    addu $sp,$sp,4

    # mark us as having already GC'd
2880#    move $t5,$s5

    # go try and allocate again
2884#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
2888:vm_init:

    # mark bottom of stack
2888#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
2892#    move $s2,$zero

    #set up the "five" register
2896#    li $s5,5

2900#    la $s3,seg0_start
2904#    la $s4,seg0_end
2905##...lui $s4,16
2909##...addiu $s4,$s4,3696
    
    # return
2912#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2916:divide:
    # get parameters; abort if divisor zero
2916#    lw $t0,($sp)
2920#    lw $t1,8($sp)
2924#    beq $t0,$zero,divByZeroError
    
    # perform division
2928#    div $t1,$t0
2932#    mflo $t0

    # store result, adjust stack and return
2936#    addu $sp,$sp,8 # adjust stack
2940#    sw $t0,($sp) # store result
2944#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2948:remainder:
    # get parameters; abort if divisor zero
2948#    lw $t0,($sp)
2952#    lw $t1,8($sp)
2956#    beq $t0,$zero,divByZeroError
    
    # perform division
2960#    div $t1,$t0
2964#    mfhi $t0

    # store result, adjust stack and return
2968#    addu $sp,$sp,8 # adjust stack
2972#    sw $t0,($sp) # store result
2976#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
2980:checkCast:
    # get object, allow immediately if null
2980#    lw $t2,($sp)
2984#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
2988#    lw $t2,-12($t2) # vtable address
2992#    bge $t2,$t1,castException
2993##...slt $at,$t2,$t1
2997##...beq $at,$zero,3168
3000#    blt $t2,$t0,castException
3001##...slt $at,$t2,$t0
3005##...bne $at,$zero,3168
    
    # return, leaving object unchanged on stack
3008:  checkCastReturn:    
3008#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3012:instanceOf:
    # get object, go return false if null
3012#    lw $t2,($sp)
3016#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3020#    lw $t2,-12($t2) # vtable address
3024#    sge $t0,$t2,$t0 # are we >= the lower limit?
3025##...slt $t0,$t2,$t0
3029##...xori $t0,$t0,0x1
3032#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3036#    and $t2,$t0,$t1
3040#    sw $t2,($sp)
3044:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3044#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3048:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3048#    li $v0,4 # syscall-code for print-string
3052#    syscall
3056:exit:
    # print termination message
3056#    li $v0,4 # syscall-code for print-string
3060#    la $a0,exitString
3064#    syscall

    # print number of garbage collections
3068#    li $v0,4 # syscall-code for print-string
3072#    la $a0,gcCountMsg
3076#    syscall # print "Number of ..."
3080#    lw $a0,gcCount # number of GC's
3084#    li $v0,1 # code for print-int
3088#    syscall
3092#    li $v0,4 # syscall-code for print-string
3096#    la $a0,newlineString
3100#    syscall # print newline

    # terminate execution
3104#    li $v0,10 #syscall-code for "exit"
3108#    syscall

3112:outOfHeapMemory:
3112#    la $a0,heapMemoryMsg
3116#    j exitError
3120:divByZeroError:
3120#    la $a0,divByZeroMsg
3124#    j exitError
3128:strIndexOutOfBounds:
3128#    la $a0,strIndexOutOfBoundsMsg
3132#    j exitError
3136:arrayIndexOutOfBounds:
3136#    la $a0,arrayIndexOutOfBoundsMsg
3140#    j exitError
3144:arraySizeOutOfBounds:
3144#    la $a0,arraySizeOutOfBoundsMsg
3148#    j exitError
3152:nullPtrException:
3152#    la $a0,nullPtrExceptionMsg
3156#    j exitError
3160:badIntegerFormat:
3160#    la $a0,badIntegerFormatMsg
3164#    j exitError
3168:castException:
3168#    la $a0,castExceptionMsg
3172#    j exitError
############## data section ################
    .data
3324:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3340:cur_seg:
    .word 0

3344:lastCharRead:
    .word -2 # -2 => no buffered character
3348:prevHashConstant:
    .word 0x2785173c
3352:gcCount:
    .word 0

3356:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3383:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3406:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3441:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3475:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
3508:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
3539:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
3587:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
3608:gcMsg: 
      .asciiz "\nGC!\n"
3614:gcCountMsg: 
      .asciiz "Number of garbage collections: "

3646:nullStr:
    .asciiz "null"
3651:trueString:
    .asciiz "true"
3656:falseString:
    .asciiz "false"
3662:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
3690:newlineString:
    .asciiz "\n"
    
    .align 2
3692:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

3696:seg0_start:
    .space 0x100000
1052272:seg0_end:
1052272:seg1_start:
    .space 0x100000
2100848:seg1_end:
